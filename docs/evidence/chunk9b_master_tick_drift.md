# Q-20 evidence -- the instrument master's tick is not stable day to day

Generated by `docs/evidence/chunk9b_master_tick_drift.py`, committed under CLAUDE.md's
evidence rule. Offline, read-only, decides nothing (QUESTIONS.md Q-20 is the question).

- older snapshot : `OpenAPIScripMaster_2026-07-31.json` -- 2,433 NSE-EQ instruments (runbook step 3, the master the Q-18 rebuild ran under)
- newer snapshot : `OpenAPIScripMaster_2026-08-02.json` -- 2,438 NSE-EQ instruments (pulled by the T4 re-fetch two days later; this is the one `latest_cached_master` hands the run)
- compared over  : the SEALED universe, 210 symbols (`docs/recovery/sealed_universe_210.json`)

## 11 sealed symbols carry a DIFFERENT tick in the two snapshots

`rows` is the number of NSE `-EQ` rows the raw master carries for that symbol in each
snapshot. Both are 1 everywhere below, so this is **not** a selection artefact: the
vendor publishes one row per symbol and simply states a different `tick_size` in it.

| Symbol | older tick | newer tick | ratio | walked by the run? | rows older / newer |
|---|---|---|---|---|---|
| BAJAJ-AUTO | 50p | 100p | x2 | **yes, settled** | 1 / 1 |
| BANKBARODA | 5p | 1p | x0.2 | **yes, settled** | 1 / 1 |
| HEROMOTOCO | 10p | 50p | x5 | **yes, settled** | 1 / 1 |
| INDUSINDBK | 5p | 10p | x2 | **yes, settled** | 1 / 1 |
| JIOFIN | 1p | 5p | x5 | **yes, settled** | 1 / 1 |
| KEI | 50p | 10p | x0.2 | **yes, settled** | 1 / 1 |
| LODHA | 5p | 10p | x2 | **yes, settled** | 1 / 1 |
| NAUKRI | 5p | 10p | x2 | **yes, settled** | 1 / 1 |
| PERSISTENT | 10p | 50p | x5 | **yes, settled** | 1 / 1 |
| SWIGGY | 1p | 5p | x5 | **yes, settled** | 1 / 1 |
| TORNTPHARM | 10p | 50p | x5 | **yes, settled** | 1 / 1 |

**11 of the 11 are SETTLED**, i.e. inside the 204-symbol universe the
chunk-9B run walks.

- tokens changed over the sealed universe: **none**
- symbols absent from either snapshot: **none**

## What it means, and what it does not

It does NOT mean a store is damaged: both snapshots are intact vendor files and the
parser reads each correctly. It means CONTEXT 3.3's tick INPUT is not a fixed quantity
on this machine -- re-running the same backtest two days apart would build a different
row grid for these symbols, and CONTEXT 3.3's POC would move with it, silently.

The run manifest does NOT record which master a run used: `build_manifest` in
`src/acumen/backtest.py` has no master field, and `execute` prints the filename to
stdout and drops it. So the tick input is neither PINNED nor RECORDED -- a finished
ledger cannot be traced back to the ticks that shaped its POCs. Which snapshot a
historical backtest must use is unanswered by CONTEXT.md, so the chunk-9B RESUME-1
session refused to choose and raised **QUESTIONS.md Q-20**.

