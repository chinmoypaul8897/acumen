# chunk 9B RESUME-1 -- store migration verified through the NEW roots

Generated by `docs/evidence/chunk9b_store_migration.py`, committed under CLAUDE.md's
evidence rule. Offline and read-only on both stores.

The operator moved the two mutable stores out of the repository tree on 02-Aug-2026
(CLAUDE.md, "Data-store safety", Q-18 layer 1). A move is not a copy, so every check
below re-measures a figure this repo already published and compares it digit for digit:
a loss, or a code path still pointing at the old in-repo location, would show up as a
smaller number rather than as an error.

- `data_root`  : `C:\Users\chinm\acumen-data`
- `cache_root` : `C:\Users\chinm\acumen-data\cache`
- network      : 8 entry point(s) patched to raise before the CA check

| # | Check | Verdict | Measured |
|---|---|---|---|
| 1 | both store roots live OUTSIDE the repository tree; old locations gone | **PASS** | data_root=C:\Users\chinm\acumen-data cache_root=C:\Users\chinm\acumen-data\cache; neither is under C:\Users\chinm\acumen; the old in-repo locations are GONE (data/ and cache/ absent) |
| 2 | the loader REFUSES a store root inside the repo (layer 1 is structural) | **PASS** | refused: paths.data_root in C:\Users\chinm\AppData\Local\Temp\tmpnm7t399k\config.yaml points INSIDE the repository tree (C:\Users\chinm\acumen\data). CLAUDE.md's Q-18 layer 1 requires the mutable stores to live outside it, so that no git command can reach them -- an in-repo store was destroyed by `git worktree remove --force` on 31-Jul-2026 (QUESTIONS.md Q-18). Move the store and point data_root at its new home; do not point it back inside. |
| 3 | both trees are populated at the new location | **PASS** | data_root 21,837 files / 250 dirs / 2.82 GiB (excluding the nested cache_root); cache_root 2 files / 1 dirs / 0.06 GiB |
| 4 | daily ledger summary through data_root matches CONTEXT 4.6 (v1.5) | **PASS** | 2000-01-01 .. 2026-07-30: file-present 6,610 (CONTEXT 4.6 v1.5 says 6,610), confirmed-404 3,098, error 0, never-attempted 0, pending 0, weekend sessions 32 |
| 5 | minute-store day count = CONTEXT 4.6 (v1.5)'s 435,641 | **PASS** | 435,641 stored symbol-days re-counted from the parquet files under C:\Users\chinm\acumen-data\minute_store\minute, over 210 symbols to the rebuilt scope end 2026-07-31 -- CONTEXT 4.6 (v1.5) seals 435,641 |
| 6 | disclosed-residual register loads: 204 settled / 6 quarantined | **PASS** | 204 settled / 6 quarantined (APLAPOLLO, ASTRAL, IEX, NTPC, UPL, VBL) from universe_backfill/ledger.json under data_root |
| 7 | CA day-cache loads from cache_root/data_root, OFFLINE, at any age | **PASS** | 41,372 corporate-action rows parsed from the day-cache under data_root for 2005..2026, with 8 network entry point(s) patched to raise (acumen.nse_http.cached_json_fetch, acumen.nse_http.fetch_json, acumen.nse_http.fetch_binary, acumen.nse_http.new_session, requests.get, requests.request, Session.get, Session.request) and no request escaped -- docs/recovery/q18_reconciliation.md section 0 records 41,372 (NOT the 41,371 the earlier Part-0 sweep saw: T4's sanctioned EXIDEIND/NUVAMA re-fetch rewrote 11 day-cache year file(s), 2016..2026, at 01:16:35.76..01:16:55.60, between the two readings; the single differing row cannot be localised because the pre-T4 cache was overwritten in place) |
| 8 | instrument master resolves token AND tick through cache_root | **PASS** | OpenAPIScripMaster_2026-08-02.json (2,438 NSE-EQ instruments) read through cache_root; RELIANCE token=2885 tick=10p; TCS token=11536 tick=10p; HDFCBANK token=1333 tick=5p; APLAPOLLO token=25780 tick=10p; NESTLEIND token=17963 tick=10p |

**VERDICT: ALL CHECKS PASS** (8 of 8).

