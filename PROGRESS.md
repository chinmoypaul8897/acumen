# PROGRESS.md — session log

**Newest entry on TOP.** Every session appends one entry using the exact template from
plan.md §6 (reproduced below). An entry is the only place a Class-B implementation decision
is recorded — a deviation recorded nowhere is a defect, even if the code is right (plan.md §5).

<!-- TEMPLATE — copy exactly (plan.md §6)

## [YYYY-MM-DD HH:MM] chunk <N> · <build|review|fix> · <done|blocked|handed-off>
scope: <one line — what this session did>
files: <created/changed paths>
tests: <pass count / fail count; fixtures touched>
decisions: <Class-B items with 1-line rationale; "none">
questions: <QUESTIONS.md items raised; "none">
gate: <n/a | pending | closed — evidence path>
status-ledger: <the STATUS.md line this session set>
state-for-next-session: <exact current situation + the single next action>

-->

## [2026-08-02 16:05] chunk 9B · build (RESUME-2: the tick pin, the pilot, the projection) · done
scope: Record and EXECUTE the architect's Q-20 ruling (one pinned instrument master governs the whole backtest), verify the POC calibration reproduces under the pin, re-run the chunk-9A pilot on the rebuilt stores and regenerate its pack, measure the throughput and project the full run, and STAGE the run command. The run itself does NOT start -- the re-seal review gates it.
files: created `docs/evidence/chunk9b_tick_pin.{py,md}`, `docs/evidence/chunk9b_throughput.{py,md}`, `docs/evidence/chunk9b_smoke.log`, `docs/evidence/chunk9b_preflight.md`, `docs/evidence/chunk9b_out_of_session.md`, `tests/test_q20_master_pin.py`; changed `config.yaml` (the `instrument_master` pin), `src/acumen/config.py`, `src/acumen/backtest.py`, `src/acumen/run_backtest.py`, `src/acumen/pilot_evidence.py`, `src/acumen/trade_evidence.py`, `src/acumen/instrument_master.py`, `docs/evidence/chunk9a_pilot.md` (regenerated), `docs/recovery/q18_runbook.md` (step 6, the staged run), `QUESTIONS.md`, `README.md`, `STATUS.md`, `PROGRESS.md`, tests `test_config`, `test_backtest`, `test_run_backtest`, `test_pilot_evidence`, `test_review9a2_probes`, and the retired-path-key repair in `docs/evidence/chunk9b_out_of_session.py`, `q18_part0_verification.py`, `q18_smoke_bhavcopy.py`, `docs/recovery/q18_t3_forensics.py`, `q18_t5_improvements.py`. NOT changed: `CONTEXT.md`, `CLAUDE.md`, `plan.md`, any fixture, `poc/`, and the pure engine modules `bias`, `poc`, `signals`, `simulate`, `signal_engine`, `bias_engine`, `quality_gates`, `aggregate` -- byte-identical to their committed blobs.
tests: **1927 passed / 0 failed / 0 SKIPPED from clean** (`.pytest_cache` + every `__pycache__` deleted first). **The 7 skips are GONE**: they were the `test_review9a2_probes.py` probes waiting on the chunk-9A pilot ledger the Q-18 incident destroyed, and the pilot re-run restored it, so all 19 now RUN. Collection 1900 -> 1927; test FUNCTIONS 1,351 -> 1,370 (+19), **no file losing a single function**, one new file (`test_q20_master_pin.py`). No fixture byte changed (`git diff 8e67081..HEAD -- tests/fixtures poc` is empty). No test weakened or deleted: two annotated pins were FLIPPED to assert their rules, three pack-claim pins were RE-POINTED at the regenerated artefact (each now pinning a corrected number, a narrower true claim, or strictly more content), and one synthetic manifest gained the field the invariant reads.
decisions:
  - B234 The Q-20 pin is a REQUIRED `config.yaml` key, not a literal in `src/`. The filename IS a date and CLAUDE.md's code standards forbid a hardcoded date; the loader validates it as a bare FILENAME (no separator, no `..`, not absolute) so the pin can never resolve outside the cache the operator snapshots. No default and no "newest wins" fallback -- that fallback is precisely what the ruling retired.
  - B235 `backtest.latest_cached_master` is DELETED rather than deprecated, and newest-by-filename selection moves to `instrument_master.newest_cached_master`, beside `cached_masters`, where it is cache inspection for the operator CLI and the migration evidence. A selector that still exists on the run path is not retired, only unused today.
  - B236 `trade_evidence` (chunk-8's sweep) is pinned too, though it is NOT the run path. It computes POCs, and the repo should have ONE tick source rather than two that agree until the day they do not. Its own docstring's premise -- "the tick sizes it reads are per-symbol constants, not a daily quantity" -- is what Q-20 measured false.
  - B237 The pin rides on `RunSpec` as `master_file` + `master_sha256`, inside the spec digest, rather than as a manifest-only field. That is what makes the resume refusal free: `_resume_state` already compares the digest, so the ruling's "a resume under any other master REFUSES" needed no new branch. Both halves are carried because a filename alone is not enough -- a vendor file can be overwritten in place.
  - B238 `build_runner` REFUSES a `master_path` whose name is not the configured pin. An explicit path is the preflight CONFIRMING what it resolved and printed to the operator, never an override; without the guard, threading the path would have re-opened by argument the door `config.yaml` closed.
  - B239 The manifest gains `caveat_basis` -- the register entries the CAVEAT quotes -- kept separate from `per_symbol`, which is the universe the run WALKED. Without it the "caveat quotes its own register" invariant could never pass on any run smaller than the full universe, because IOC and TATASTEEL are not in a five-symbol pilot. CONTEXT 4.6 (v1.5) requires the caveat's figures be quoted from the register's current figures; this makes that checkable from the manifest alone.
  - B240 The calibration pack classifies a mismatch as MOVED (the pin changed a figure -> STOP) or PUBLISHED-SLIP (a published figure disagrees with arithmetic the pin does not touch), decided by RECOMPUTING the day under the OTHER cached snapshot. Collapsing the two would either hide a real tick move inside a known typo or halt a run over one.
  - B241 The throughput projection has THREE terms, not one, and its samples are spread EVENLY across the universe. A blended smoke figure x102 would have projected both wiring terms at 1/102 of their cost; and an alphabetically-first sample gave 10.4 s/symbol at n=2 against 13.4 at n=10, which reads as superlinearity and is really the early alphabet carrying less history. The session scan is projected by MONTH-FILE count (20,460 vs 1,261 sampled), which is the work it actually does.
  - B242 Five committed evidence/recovery generators still called `config.path("data_dir")`, a key RESUME-1's rename retired; each would have raised on its first store access, including the `chunk9b_out_of_session.py` this session was required to run. Repaired in place, one word each; no output value can move and none was regenerated -- their measurements belong to their own sessions.
  - B243 The card's chunk-6 oracle citation (BHARTIARTL 2026-07-24, POC 1913.9, 25 rows) disagrees with this repo's record (2026-07-17; 1913.9/25 rows are the TRADER's chart reading, not an engine output). BOTH dates were computed and both reported; neither was quietly preferred. 2026-07-17 reproduces the engine's 1914.60 in 26 rows exactly.
questions: **Q-20 RESOLVED** -- the ruling recorded VERBATIM with its execution beneath, and the architect's approvals of B220, B221 and B232 recorded beside the questions they answer. **NO new question raised.** **Q-17 gains a MEASURED ADDENDUM**: its open follow-up (are the MARKET-WIDE mixed dates special/shortened sessions, to be excluded at the DATE level?) has **FIVE** candidates on the rebuilt store, not the three the original record named -- 2020-12-08 (143 of 167 symbols) and **2021-02-24 (168 of 169)** are new. Nothing is decided or blocked; the ruling stands and those dates trade normally. Two findings for the re-seal reviewer, neither a spec hole: REVIEW_7's ICICIBANK 2026-05-21 walk publishes "23 rows" where CONTEXT 3.3's own arithmetic gives ceil(82/4) = 21 (a transcription slip -- the HDFCBANK walk above it is genuinely 23; no POC, entry, stop, target or money figure depends on a printed row count), and the pilot pack's universe-wide bias-materiality count reads 45 where the sealed era read 47.
gate: n/a (this is the run's staging session, not a chunk gate)
status-ledger: `chunk 9B: resume-2 done -- staged for re-seal review (unreviewed)`
state-for-next-session: **EVERYTHING THE RUN NEEDS IS DONE AND VERIFIED; THE RUN IS STAGED, NOT HANDED OVER** -- `docs/recovery/q18_runbook.md` step 6 carries the exact command (`python scripts/run_backtest.py`, no flags), the four-item pre-run checklist and both manifest stamps, and its status line says the re-seal review gates it. **Q-20 IS EXECUTED**: `config.yaml` pins `OpenAPIScripMaster_2026-07-31.json` (sha256 `ce198be4...b29f5ab`), latest-by-filename is gone from the run path and tripwired, the manifest records the pin by filename AND digest, and a resume under any other master refuses exactly as a moved code SHA does. **THE PIN IS VALIDATED**: 38 of 39 calibration figures reproduce EXACTLY under it -- every F7 and sealed-walk tick, all 25 frozen prorata POCs, the chunk-6 gate day (1914.60 in 26 rows) and two of three REVIEW_7 walks -- so the card's STOP condition does not fire. **THE PILOT LEDGER CAME BACK BYTE-IDENTICAL**, sha256 `c3363f6f...c1e318`, the same bytes REVIEW_9A verified at four kill points before the incident; 290/146/88/56, 53,750 shares, net -Rs 1,934.95, and the pack regenerates byte-identically across two consecutive runs. **THE PROJECTION IS 6h 04m** (measured 25.56 symbol-days/s over the full era, plus two separately-measured wiring terms), far under the 36-hour STOP threshold. **THE SPAN NOW REACHES 2026-07-30**, not 2026-07-24: step 1's re-ingest cleared CONTEXT 4.6's 178-day store lag and the oracle clamp costs exactly one day. **WHAT THE REVIEWER SHOULD ATTACK FIRST**: (1) the pin -- is `pinned_master` really the only resolver, and does the resume refusal fire on BOTH the filename and the digest; (2) the calibration pack's MOVED-vs-PUBLISHED-SLIP classification, which is this session's own judgement and is what kept the run from stopping over REVIEW_7's row-count slip; (3) the 47 -> 45 materiality delta -- the pin is refuted structurally (the scan takes no master), but the two individual pairs CANNOT be named, because the sealed run published only its first 15 changed pairs and the store that produced the rest is gone; (4) the three RE-POINTED pack pins, which were green for two days against a stale artefact exactly as the two annotated ones were, but without a note saying so -- look for that pattern elsewhere. **OPERATOR OWES A SNAPSHOT**: this session wrote the pilot runs and the smoke under `<data_root>/backtests/`, so both stores have moved; the preflight prints their last-changed stamps.

## [2026-08-02 14:10] chunk 9B · build (RESUME-1: law + migration + guard) · done
scope: Commit the architect's CONTEXT v1.5 and the three CLAUDE.md data-safety layers, move the mutable stores outside the repository tree and route every path through the loader, and build the Q-19 structural 404-sealing guard. No store fetch, no pilot re-run, no run command (all RESUME-2).
files: created `docs/evidence/chunk9b_store_migration.{py,md}`, `docs/evidence/chunk9b_master_tick_drift.{py,md}`, `tests/test_q19_seal_guard.py`; changed `CONTEXT.md` (architect-authored, committed ALONE), `CLAUDE.md` (architect-authored, committed alone), `config.yaml`, `.gitignore`, `README.md`, `QUESTIONS.md` (new Q-20), `src/acumen/config.py`, `src/acumen/bhavcopy.py`, `src/acumen/daily_store.py`, `src/acumen/calendar.py`, `src/acumen/backfill_daily.py`, `src/acumen/run_backtest.py`, `src/acumen/backtest.py`, and the path-key rename across `build_adjustment_map`, `corp_actions`, `instrument_master`, `minute_backfill`, `pilot_evidence`, `poc_evidence`, `recovery_reconcile`, `trade_evidence`, `universe`, `universe_backfill`, `vendor_adjustment`; `docs/recovery/q18_runbook.md` (step-1 operator rule now records that the guard exists); `src/acumen/pilot_evidence.py`; tests `test_config`, `test_universe`, `test_review9a2_probes`, `test_simulate`, `test_backtest`, `test_bhavcopy`, `test_review2_probes`, `test_pilot_evidence`, `test_run_backtest`. NOT changed: `plan.md`, any fixture, `poc/`, and the pure engine modules `bias`, `poc`, `signals`, `simulate`, `signal_engine`, `bias_engine`, `quality_gates`, `aggregate` — byte-identical to their committed blobs.
tests: **1893 passed / 0 failed / 7 skipped from clean** (`.pytest_cache` + every `__pycache__` deleted first). Collection 1863 -> 1900 = exactly the +37 this session added (22 parametrised cases from `test_q19_seal_guard.py`'s 11 functions, 9 from parametrised additions to `test_config.py`, 6 from `test_run_backtest.py`); test FUNCTIONS 1,331 -> 1,351 (+20) with **no file losing a single function**. The 7 skips are the same pre-existing `test_review9a2_probes.py` probes waiting on the chunk-9A pilot ledger the Q-18 incident destroyed; no test moved into or out of a skip. No fixture byte changed (`git diff 601f3bf..HEAD -- tests/fixtures poc` is empty). No test weakened, deleted or made to skip.
decisions:
  - B220 CONTEXT.md's header DATE moved with the version (29 July -> 2 August 2026). The architect's instruction named only `"Version 1.4" -> "Version 1.5"`, but the header line is `Version X · <that version's date>` and every prior bump moved both. Disclosed in the commit body and the report so it is one keystroke to revert if the architect meant the date to stand.
  - B221 The §10 version-log row was supplied wrapped across six lines; it is committed as ONE line. A markdown table row cannot span lines — wrapped, it would not render as a row at all. Not one word changed.
  - B222 `data_root`/`cache_root` REPLACE `data_dir`/`cache_dir` rather than sitting beside them. Two config names for one store is exactly how a store drifts back inside the tree while both names look plausible.
  - B223 Layer 1 is STRUCTURAL, not conventional: the loader REFUSES a store root that is missing, relative, or inside the repository tree, rather than warning. A silently in-repo store is invisible until the day it is fatal — which is precisely the 31-Jul-2026 incident.
  - B224 `Preflight.data_dir`/`run_dir` became `Path | None`. On the one branch where `config.yaml` did not load, the store roots are genuinely UNKNOWN and nothing invents `data/`; that branch is always NO-GO and the renderer reads neither field unless the verdict is GO.
  - B225 Layer 3's store-freshness stamps are RENDERED but never enter `notes`. `notes` flow into `disclosures` and thence into the run manifest, which must stay byte-identical across runs — a clock read there would move its digest every run.
  - B226 Q-19's PENDING is a FOURTH PERSISTED OUTCOME, not an omitted ledger row. The architect's words are "recorded PENDING": a row records that we ASKED and got a 404 we refuse to seal, keeping the http_status and the reason, which is strictly more than absence. `pending_dates()` already returns every non-terminal outcome, so the retry needed no change.
  - B227 `MIN_SEAL_AGE_DAYS = 7` is a cited module constant, not a config knob. CONTEXT 4.6 (v1.5) states the seven days as LAW, and this repo already carries spec constants that way (`UDIFF_FIRST_DATE`, `MIN_SECONDS_BETWEEN_REQUESTS`). Q-19 option (c) offered a config knob; the architect ruled the law instead.
  - B228 The seal horizon counts CALENDAR days with NO weekend exception. A weekday test would re-introduce the very assumption — that we already know which dates traded — that this ledger exists to establish. A weekend 404 simply seals a week later, at no cost.
  - B229 `SPEC_VERSION` bumped `v1.4` -> `v1.5` in `src/acumen/backtest.py`, beyond the four assigned parts. Left alone, RESUME-2's run manifest would stamp a ledger `spec v1.4` while running under v1.5 law. No artefact was regenerated: chunk-9A's runs were destroyed by the incident and chunk-9B's run is RESUME-2's.
  - B230 The migration evidence's CA-cache expectation cites `docs/recovery/q18_reconciliation.md` §0 (41,372), not the earlier Part-0 sweep (41,371). Both were right at their own moment; T4's sanctioned EXIDEIND/NUVAMA re-fetch rewrote ELEVEN day-cache year files (2016..2026, 01:16:35.76..01:16:55.61) between the two readings, and the file mtimes still order the three events on disk. The sweep MEASURES that rewritten set rather than asserting it, and says plainly what it does not establish: the single differing row cannot be localised, because the pre-T4 cache was overwritten in place.
  - B231 `--no-retry-errors` opts out of ERRORS only, never out of a Q-19 `pending`. Filtering the resume list on ledger MEMBERSHIP was correct while `error` was the only non-terminal outcome; adding a second one silently made the guard able to defer a date and then drop it. Mirrors `MinuteStore.pending_windows`, which keys on terminality.
  - B232 `RESIDUAL_CAVEAT` (a frozen string) became `residual_caveat(register)`, computed in `Fraction` and rounded half-even. CONTEXT 4.6 (v1.5) requires the figures be "quoted from the register's own current figures"; the rebuilt register puts IOC at 42.0%, so the frozen 41.9% was WRONG, not merely stale, on the qualification record of a money-bearing ledger. "a minority of stored history" became "only part of" in the same sentence because TATASTEEL at 65.8% is a majority — the old wording was false for one of its two subjects.
  - B233 This session audited its OWN diff adversarially before handing it over (21 candidates, 16 refuted, 5 confirmed and fixed). That is diligence, NOT the review CLAUDE.md requires: no `docs/reviews/REVIEW_<N>.md` was written and no PASS was claimed. The QC review still runs in a FRESH session over the whole span.
questions: **NEW Q-20 (class A, OPEN, STOP)** — the instrument master's `tick_size` is not stable day to day. The 2026-07-31 snapshot (the master the Q-18 rebuild ran and gated under) and the 2026-08-02 snapshot disagree for **11 of the sealed 210, ALL of them SETTLED and therefore walked by the run**: BAJAJ-AUTO 50->100p, BANKBARODA 5->1p, HEROMOTOCO 10->50p, INDUSINDBK 5->10p, JIOFIN 1->5p, KEI 50->10p, LODHA 5->10p, NAUKRI 5->10p, PERSISTENT 10->50p, SWIGGY 1->5p, TORNTPHARM 10->50p. Each master carries exactly ONE NSE `-EQ` row per symbol, so it is the vendor's own value moving, not our parsing. CONTEXT 3.3 sizes the profile row grid by tick, so this moves POCs — and entries, stops and targets — silently, and makes a re-run non-reproducible. `latest_cached_master` resolves newest-by-filename, which is an inherited implementation choice, not a ruling. Nothing was decided and no code changed; four options are put to the architect. Blocks RESUME-2's tick input only.
gate: n/a (law + infrastructure session, not a chunk gate)
status-ledger: `chunk 9B: resume-1 done — law + migration + guard (unreviewed)`
state-for-next-session: **CONTEXT v1.5 IS LAW and is committed alone**, so the Q-18 re-seal is closed and the block on the chunk-9B run is lifted from the spec side. **THE STORES NOW LIVE OUTSIDE THE REPO** at `C:/Users/chinm/acumen-data` (+ `/cache`), moved by the operator and verified from the machine: 21,837 files / 2.82 GiB, and every headline figure re-measured through the new roots and matching CONTEXT 4.6 (v1.5) digit for digit — daily ledger 6,610 file-present / 0 error / 0 never-attempted, minute lake **435,641 stored symbol-days**, register 204 settled / 6 quarantined, CA day-cache 41,372 rows offline with zero requests fired, instrument master resolving token AND tick (`docs/evidence/chunk9b_store_migration.{py,md}`, 8 of 8 PASS). Layer 1 is enforced by the loader, not by convention. **THE Q-19 GUARD IS IN**: a 404 seals only past 7 calendar days, younger ones record PENDING and are retried, an ERROR is still never a holiday, and the exact 2026-07-31 phantom shape is pinned as a regression. **RESUME-2 STILL OWES**, unchanged: the pilot-pack regeneration and its two annotated probe flips, the smoke re-run for a MEASURED throughput and full-run projection, `docs/evidence/chunk9b_preflight.md` and `chunk9b_out_of_session.md`, and only then the run itself. **TWO THINGS RESUME-2 MUST SETTLE FIRST.** (1) **Q-20 is a STOP on its tick input** — the run must not start until the architect rules which master snapshot it reads, because 11 walked symbols' POC grids depend on the answer. (2) **The machine is memory-starved**: 0.4 GB free of 7.7 GB, 19.3 GB committed against a 31.7 GB limit, which is why a 21,000-file read took 25 minutes here and why one verification pass was reaped mid-scan. A multi-hour full-history run on that headroom will stall repeatedly; it is resumable by construction so nothing would corrupt, but the operator should free memory before starting. **OPERATOR OWES A SNAPSHOT** of the two stores to an offline drive, in TWO generations (CLAUDE.md layer 3) — the preflight now prints both roots' last-changed timestamps so "is my snapshot newer?" is answerable without guessing.

## [2026-08-02 02:15] chunk 9B · build (Q-18 VERIFICATION + TRIAGE) · done
scope: Certify the operator-executed Q-18 rebuild from the machine itself, then execute the architect's five triage rulings on the 354 unexplained divergences the operator's reconciliation reported. Verdict moves to ZERO unexplained.
files: created `docs/evidence/q18_part0_verification.{py,md}`, `docs/recovery/q18_t3_forensics.{py,md,json}`, `docs/recovery/q18_t5_improvements.{py,md}`, `docs/recovery/q18_sealed_universe.py`, `docs/recovery/sealed_universe_210.json`; changed `CLAUDE.md` (architect-authored, alone), `QUESTIONS.md` (the T1-T5 rulings verbatim + a measured Q-19 addendum), `src/acumen/recovery_reconcile.py` (T1/T2 + two new classes), `tests/test_recovery_reconcile.py`, `docs/recovery/backfill_minute_report_rebuild.md` (regenerated on the SEALED universe), `docs/recovery/q18_reconciliation.md`, `STATUS.md`, `PROGRESS.md`. NOT changed: CONTEXT.md, plan.md, any fixture, `poc/`, `docs/backfill_minute_report.md` (the SEALED baseline, verified byte-identical to HEAD), and every reviewed engine module -- `bias`, `poc`, `signals`, `simulate`, `signal_engine`, `bias_engine`, `corp_actions`, `aggregate`, `bhavcopy`, `quality_gates`, `minute_backfill`, `universe_backfill`, `backtest`, `portfolio` are byte-identical to their committed blobs. The gates were RE-RUN, never edited.
tests: **1856 passed / 0 failed / 7 skipped from clean** (`.pytest_cache` + every `__pycache__` deleted first). Collection 1838 -> 1863 = exactly the +25 this session added, all to `tests/test_recovery_reconcile.py` (38 -> 63). Skips went 8 -> 7: the minute-store-backed probes RUN again now the lake is rebuilt, and the remaining 7 are all `test_review9a2_probes.py` waiting on `data/backtests/`, which the Q-18 incident destroyed and this runbook does not rebuild (chunk 9A's pilot re-run owns it). No fixture touched, no test weakened or deleted; the only edits to an existing test file are appended functions plus three rewritten assertions that follow a METHOD change I made mid-session (the T1 gate leg), each still asserting strictly more than before.
decisions:
  - B214 T1's boundary B is defined as the rebuilt store's `sealed_days`-th STORED date, not as an interval of equal-count dates. The running count equals the sealed count across a whole range of dates, so pinning B to a day the store actually holds is what makes it the "unique boundary date" the ruling asks for.
  - B215 T1's gate leg is a TWO-SIDED INTERVAL BOUND, not an equality. My first implementation demanded that re-gating the days after B reproduce the gate-1 delta exactly; that is wrong, because the sealed era's daily store stopped earlier than the rebuild's (CONTEXT 4.6's disclosed store-lag), so days that were ungated then are gated now. The bound instead brackets what the sealed tail COULD have contributed using the sealed report's own totals. It proves attribution to the fetch window, NOT the identity of individual days, and the report says so in those words.
  - B216 `vendor-snapshot-drift` is READ from a committed forensics file, never inferred by the reconciler. A symbol the forensics ESCALATED keeps `unexplained`, and the six-clause drift test was written down before the data was read so the verdict could not be fitted to it.
  - B217 The two MEASURED classes (T1's arithmetic, T3's forensics) are evaluated BEFORE `new-CA-explained`, which rests only on a nearby event existing. T2's own ruling ("reclassify, letting T1 catch them") points the same way.
  - B218 T3's forensics names a symbol's STATUS divergence alongside its gate divergence when the status actually moved, because a quarantine follows mechanically from the gate-1 rate crossing 80% -- explaining the cause and leaving the consequence dangling would be a bookkeeping artefact.
  - B219 The Part-0 step-5 check runs the reconciler AS COMMITTED AT HEAD from a throwaway COPY of `src/` inside the repo root (never a junction -- the Q-18 incident rule), because this session amends that module and a check against the amended code would certify the operator's artefact against something that did not exist when it was produced.
questions: none raised. Q-19 gains a MEASURED addendum (its workaround costs exactly 208 gate-1P no-oracle days, all 2026-07-31, 0.048 pp) and stays OPEN for the architect; nothing was decided on it here.
gate: n/a (this is a recovery/triage session, not a chunk gate)
status-ledger: `chunk 9B: Q-18 rebuild RECONCILED -- zero unexplained; awaiting CONTEXT 4.6 v1.5 (unreviewed)`
state-for-next-session: The rebuilt era reconciles with **ZERO unexplained divergences** -- 414 sealed-fetch-horizon, 5 vendor-snapshot-drift, 3 vendor-repair-explained, 0 new-CA-explained. Final era: **435,641 stored symbol-days, 409,252 passing all three gates = 93.9425%, 204 settled / 6 quarantined** (APLAPOLLO in, NESTLEIND out, vs the sealed six). The deltas are the amendment payload; **writing CONTEXT 4.6 v1.5 is the ARCHITECT's**, and chunk 9B's run stays blocked until it exists. TWO things the architect must see before believing a number: (1) BSE's +84 is VOLUME-ONLY -- all five hand-verified days pass gate 1 but FAIL gate 1P at exactly 3.0x the exchange's own prices, so its usable days moved 2,115 -> 2,117, not +84, and no coverage is recovered; (2) APLAPOLLO's quarantine STANDS -- the forensics explains the -467 but does not clear it, gate 1 really is 77.9%. OPERATOR OWES A SNAPSHOT of `data/` and `cache/` to an offline drive: this session's T4 fetch changed the store. Still owed by chunk 9B before the run: the pilot-pack regeneration and its two annotated probe flips, the smoke re-run for a MEASURED throughput, `docs/evidence/chunk9b_preflight.md` and `chunk9b_out_of_session.md`.

## [2026-07-31 10:55] chunk 9B · build (Q-18 DATA RECOVERY) · handed-off
scope: Record the architect's Q-18 + Q-17 rulings, add the two data-store safety rules to CLAUDE.md, build the RECONCILIATION the Q-18 ruling requires, and hand the operator an ordered, resumable rebuild runbook that uses only the existing reviewed pipeline. Built no ingestion logic and fetched nothing heavy: the one network action was the runbook's own step-1 smoke, one month of bhavcopy.
files: created `docs/recovery/q18_runbook.md`, `docs/recovery/q18_reconcile.py`, `src/acumen/recovery_reconcile.py`, `tests/test_recovery_reconcile.py`, `scripts/fetch_instrument_master.py`, `scripts/ca_report.py`, `docs/evidence/q18_smoke_bhavcopy.py`, `docs/evidence/q18_smoke_bhavcopy.md`; changed `CLAUDE.md` (architect-authored, alone), `QUESTIONS.md` (the rulings verbatim + Q-19), `src/acumen/instrument_master.py` (a CLI on the existing reviewed module), `tests/test_instrument_master.py`, `pyproject.toml` (one console script), `STATUS.md`, `PROGRESS.md`. NOT changed: CONTEXT.md, plan.md, any fixture, `poc/`, `docs/backfill_minute_report.md` (the SEALED baseline), and every engine module under review -- `bias`, `poc`, `signals`, `simulate`, `signal_engine`, `bias_engine`, `corp_actions`, `aggregate`, `bhavcopy`, `quality_gates`, `minute_backfill`, `universe_backfill`, `backtest`, `portfolio` are all byte-identical to their committed blobs.
tests: **1830 passed / 0 failed / 8 skipped from clean** (`.pytest_cache` + every `__pycache__` deleted first). Collection went **1794 -> 1838** at `0ef821e..HEAD`, i.e. exactly the +44 this session added: 6 instrument-master CLI tests, 38 reconciliation tests. The 8 skips are the same pre-existing data-backed probes (6 pilot-ledger, 2 minute-store) -- no test moved into or out of a skip. No fixture touched (`git diff chunk8-pass..HEAD -- tests/fixtures poc` is empty). No existing test weakened, deleted or edited; the only edits to an existing test file are 6 appended functions.
decisions:
  - B209 The SEALED numbers are PARSED from the committed `docs/backfill_minute_report.md`, never typed as constants. It is the artefact CONTEXT 4.6 summarises and the one description of the era the incident could not touch, and both eras then go through ONE parser so neither is measured on its own terms. Pinned by a test that the section-3 `Days` column sums to 434,769 -- reading G's denominator exactly -- so the per-symbol quantity being diffed IS the quantity the sealed headline is built from.
  - B210 The ruling names the three classes but not the TEST for each, so the tests are this module's, they are PRINTED inside the report to be overruled by reading, and the default is `unexplained`. A divergence is called explained only when the evidence is on disk.
  - B211 `new-CA-explained` applies to gate outcomes ONLY. A corporate action re-scales a back-adjusted history (CONTEXT 4.6 / OPEN-8) but never adds or removes stored days, so a stored-day move can never take that class however many new events the symbol has.
  - B212 `vendor-repair-explained` requires an improvement AND a deficiency the SEALED era itself named (quarantined, gate-1P residual table, gate-3 register). A repair nobody ever recorded a defect for is not a repair anyone can point at; the alternative test ("gate-1 was below 100%") would have admitted nearly every symbol.
  - B213 The independent store leg BLOCKS only on a contradiction (the report claims depth for a symbol whose parquet directory is empty). A plain count difference is printed with its caveat, because the report's `Days` column counts from each symbol's own clamp and this session will not fail a whole rebuild over a semantic it cannot verify against a store that no longer exists.
  - B214 Stored days after the sealed scope end are counted and reported SEPARATELY as growth, never as drift. Comparing the rebuild's span against the sealed span directly would report a year of ordinary new trading as a defect.
  - B215 The instrument master becomes its own operator step with its own CLI on the existing reviewed module, because its FILENAME is cited by every pack quoting a tick and checked by the chunk-9B preflight -- deciding it before the long run beats discovering it after.
  - B216 `scripts/ca_report.py` added: the package is not installed in this clone, so `python -m acumen.ca_report` does not resolve and chunk 3 was the one module without the bare-clone launcher (chunk-0 B2, REVIEW_2 F12) that step 2's per-year loop needs.
  - B217 Runbook step 1 ends at the last COMPLETED trading day, narrowing the architect's own "2000->today". Stated as a deviation in the runbook and in Q-19 rather than assumed, because today's unpublished bhavcopy records as `confirmed-404` and Q-3 makes that a holiday.
  - B218 CLAUDE.md's two architect bullets went under a NEW "Data-store safety" heading rather than into the git rules: both are about mutable gitignored state, not about history, and diluting the git section would have buried them.
  - B219 The smoke's phantom-holiday row was un-recorded with the EXISTING documented recovery (`--rebuild-ledger`, REVIEW_2 F6) rather than with new code or a hand edit -- no chunk-2 semantic was touched, and the 404 ruling stays the architect's.
questions: **Q-18 and Q-17 RECORDED and CLOSED as rulings** (verbatim, at the end of QUESTIONS.md), with what was executed under each written beneath. Q-17 needed no code change -- the ruling confirms the fix already committed at `0b47d8e` clause by clause, and its two offered follow-up readings are NOT taken. **NEW Q-19 (class A, OPEN, NON-BLOCKING)**: a bhavcopy 404 for a date whose file is not published YET is indistinguishable from a 404 for a date with no session, so ingesting "up to today" writes a phantom holiday and shifts CONTEXT 3.2's next bias pair -- measured live by this session's own smoke (2026-07-31, market open, recorded `confirmed-404`). Workaround stated AND executed; three options recorded for the architect.
gate: n/a
status-ledger: `chunk 9B: BLOCKED on Q-18 rebuild -- runbook handed to operator (unreviewed)`
state-for-next-session: **The rebuild is the OPERATOR's next action, not a session's**: `docs/recovery/q18_runbook.md`, five ordered steps, each through an existing reviewed entry point, each safe to Ctrl-C and re-run. WHAT IS PROVEN: step 1 runs end to end on this machine -- 31 dates in 1m18s, 22 file-present, 0 errors, 75,419 rows, TCS 2026-07-30 reading back as an EQ row in integer paise (`docs/evidence/q18_smoke_bhavcopy.{py,md}`) -- and the step-5 reconciliation is built and tested, with its parser reproducing the sealed era from the committed report exactly: 210 symbols, 204 settled, the same six quarantined names, 411,690 / 434,769 = 94.6917%. WHAT IS DELIBERATELY NOT ESTIMATED: step 4's duration. It is UNKNOWN and says UNKNOWN; the only basis offered is the sealed report's own 23,138 windows at ~2 req/s = ~3.2 hours of REQUESTS ALONE, before the map measurement, the gate battery and the floor probes. Step 1's ~6.8 hours is labelled an EXTRAPOLATION from the measured 2.52 s/date, not a projection. TWO TRAPS ARE WRITTEN DOWN because both would be silent: step 4 must pass `--report-path`, since the default overwrites the committed sealed report that step 5 reconciles against; and step 1 must stop at the last COMPLETED trading day (Q-19). WHEN THE OPERATOR RETURNS: run step 5, send `docs/recovery/q18_reconciliation.md` to the architect, and the ruling then forks -- zero unexplained divergences means the deltas are the payload for a CONTEXT 4.6 **v1.5** amendment (the architect writes it; it also carries Q-17 into CONTEXT law), and any unexplained divergence is a defect to triage. Only after that does chunk 9B resume its own owed work: regenerate `docs/evidence/chunk9a_pilot.md` and flip the two annotated pins, re-run the smoke for a MEASURED throughput and full-run projection, write `docs/evidence/chunk9b_preflight.md` and `docs/evidence/chunk9b_out_of_session.md`, and only then hand over `python scripts/run_backtest.py`. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-31 09:55] chunk 9B · build (PREP) · blocked
scope: chunk-9B PREP -- record the architect's GO ruling, close the six REVIEW_9A_2 conditions, and build the preflight + operator run command for the full-history run. All of that is DONE and green. The smoke run then found a real defect (Q-17, fixed), and the session afterwards DESTROYED the local `data/` and `cache/` trees while cleaning up a verification worktree (Q-18), which blocks the run itself, the pack regeneration and every remaining store-backed claim.
files: created `src/acumen/run_backtest.py`, `scripts/run_backtest.py`, `docs/evidence/chunk9b_preflight.py`, `docs/evidence/chunk9b_out_of_session.py`, `tests/test_run_backtest.py`; changed `src/acumen/nse_http.py`, `src/acumen/pilot_evidence.py`, `src/acumen/backtest.py`, `src/acumen/signal_engine.py`, `src/acumen/aggregate.py`, `src/acumen/minute_backfill.py`, `src/acumen/universe.py`, `src/acumen/calendar.py`, `pyproject.toml`, `tests/test_aggregate.py`, `tests/test_backtest.py`, `tests/test_config.py`, `tests/test_nse_http.py`, `tests/test_universe.py`, `tests/test_pilot_evidence.py`, `tests/test_review9a2_probes.py`, `QUESTIONS.md`, `STATUS.md`, `PROGRESS.md`. NOT changed: CONTEXT.md, plan.md, any fixture, `docs/evidence/chunk9a_pilot.md` (the regeneration it is owed could not run -- see Q-18), `poc/`.
tests: **1794 passed / 0 failed from clean while the stores existed**; **1783 passed / 0 failed / 8 skipped from clean afterwards** -- the eight are the data-backed probes that already carried a "data/ is gitignored" skip guard and now take it. +43 new tests in `tests/test_run_backtest.py`, +3 in `tests/test_aggregate.py`, +3 in `tests/test_backtest.py`, +1 in `tests/test_pilot_evidence.py`. No fixture touched. Two build tests were REPLACED, both ruling-driven, both by same-scope tests asserting the retirement: `test_nse_http.py::test_cached_json_names_the_stale_date` and `test_universe.py::test_a_stale_cache_is_never_served_silently` pinned the behaviour the architect's C3 ruling retires. Two probes were FLIPPED (C1's refusal-order attribution, C3's cache refusal) and each FAILS on the pre-fix commit `6b0436d` for its own assertion. Two probes could NOT be flipped and are annotated instead (Q1, Q2 -- the renderer is fixed, the artefact is stale, Q-18 blocks the regeneration).
decisions: **B202** the GO ruling's condition (1) constant is NOT reworded -- `CAPITAL_FLAGS_PENDING_NOTE` keeps the sentence already recorded verbatim in QUESTIONS.md, printed in a committed pack and pinned by tests; rewording would move committed pack text for no gain in meaning (recorded on the record in the ruling block). **B203** `BacktestRunner.disclosures` is stamped on the manifest only when non-empty, so no chunk-9A manifest or published digest moves. **B204** the CONTEXT 7-E2 session scan is cached beside the ledger (decision B181's duty), keyed on universe + span + code SHA; a key mismatch is ignored, never trusted. **B205** the run's span is CLAMPED to the raw daily oracle's horizon and the clamp is printed as a DISCLOSED CONDITION and stamped on the manifest -- the calendar refuses to derive over dates the bhavcopy ledger never attempted (Q-3 safeguard 1) and gate 1P fails a day with no raw daily row, so the two minute-days beyond it could not have traded. **B206** Q-17's out-of-session drop is recorded as a ledger FLAG rather than a new column, so a clean row writes the bytes it always wrote and the chunk-9A pilot ledger's published sha256 does not move. **B207** the operator run command is the launcher path (`python scripts/run_backtest.py`), which works from a bare clone with no install step, matching every other operator command in this repo. **B208** the preflight's `Preflight.ok` is False on an empty check list, so an empty run can never read as GO.
questions: **Q-17 (class A, recorded, decided by the code's own existing ruling)** -- an out-of-session 1-minute stamp killed the run; gate 2 and `aggregate_15min` disagreed; measured (3,099 of ~493,900 symbol-days over 526 dates) and fixed by applying gate 2's written reading, with two follow-up rulings left to the architect. **Q-18 (class A, OPEN, STOP)** -- this session DESTROYED `data/` and `cache/`; the restore-or-rebuild decision is the architect's because CONTEXT 4.6 seals that era with exact counts a re-backfill may not reproduce.
gate: n/a
status-ledger: `chunk 9B: prep built -- run NOT handed to operator, BLOCKED on Q-18 (unreviewed)`
state-for-next-session: **Every code deliverable of chunk 9B PREP is committed and green; the RUN was not handed over, and cannot be until Q-18 is answered.** WHAT IS DONE AND VERIFIED: the architect's GO ruling recorded verbatim; all six REVIEW_9A_2 conditions closed with a test behind each; **C3 proved exactly as the card asked** -- the pre-fix tree `6b0436d` plus ONLY the one-line `cached_json` change regenerated the committed pilot pack byte-identically on 2026-07-31, sha256 `9e8a77c7...b724df41`, **with no harness**, which is the thing REVIEW_9A_2 could only achieve under one; the preflight PASSED all 10 checks on the intact stores (204 settled symbols from the register, span 2016-10-03 -> 2026-07-24 measured from the parquet files and clamped to the daily oracle with the clamp disclosed, 2,421 trading days, 41,351 corporate actions served offline at any age, instrument master `OpenAPIScripMaster_2026-07-28.json`); and the smoke found **Q-17**, a defect that would have killed the operator's run on its first symbol -- one vendor bar stamped 09:14 on RELIANCE 2017-04-28, on a day gate 2 deliberately admits. WHAT IS NOT DONE, and why: while cleaning up the C3 verification worktree, `git worktree remove --force` followed two NTFS junctions into `data/` and `cache/` and emptied both -- the chunk-5B minute lake, the daily store, the CA day-cache, the residual register, the adjustment maps and every chunk-9A run ledger, unrecoverable (no Recycle Bin entry, no shadow copy, no other copy on the machine). The tracked repo is undamaged and every committed artefact is intact. **THE NEXT SESSION'S SINGLE NEXT ACTION IS THE ARCHITECT'S RULING ON Q-18** (restore from an operator backup, or rebuild and re-seal, or rebuild and require the sealed numbers). Once a store exists again, the owed work is small and enumerated: regenerate `docs/evidence/chunk9a_pilot.md` and FLIP the two annotated pins (Q1, Q2); re-run the smoke to get the MEASURED throughput and the projected full-run duration, and STOP with options if that projection exceeds 36 hours; write `docs/evidence/chunk9b_preflight.md` and `docs/evidence/chunk9b_out_of_session.md`; then hand the operator `python scripts/run_backtest.py`. Chunk 9B's REPORT session still comes after the run, and Q-17's two open readings and Q44 both carry the GO ruling's escalation -- a ruling either way means a spec bump, a re-run, and the superseded ledger retained and labelled.

## [2026-07-30 19:05] chunk 9A · review · done
scope: RE-REVIEW (REVIEW_9A_2, both personas, fresh session) of the chunk-9A FIX span `4fa85fb..e83bc5e` -- did the fix execute the architect's Q-16(a), Q-16(b) and E13 PRESENTATION BASIS rulings, and did it break anything? Executed all nine directed checks: re-derived the 15-minute path by hand from the minute store and the ledger on a real four-position day, recomputed the Tukey fences under two independent estimators and numpy, recomputed both net-basis pots and the profit factor, ran every flipped probe against the PRE-FIX code in a scratch worktree, attacked the widened money tripwire with two injections, and judged B193-B201. **VERDICT PASS**, 8 findings, none blocking.
files: created docs/reviews/REVIEW_9A_2.md, tests/test_review9a2_probes.py (19 kept probes); edited STATUS.md, PROGRESS.md. NO file under review was modified -- `src/acumen/*.py` and every test file in the span are byte-identical to their committed blobs (the two modules deliberately mutated by the tripwire attack, `poc.py` and `universe.py`, were restored and sha256-verified). The evidence pack was regenerated to SCRATCH paths only, never over the committed file. No fixture touched; CONTEXT.md/plan.md/poc/ untouched. Whole session OFFLINE; every store read READ-ONLY; no credentialed call, no candle fetched, nothing written to either price store.
tests: **1747 passed / 0 failed** from a genuinely clean state (`.pytest_cache` + every `__pycache__` deleted first) -- the fix's own 1728/0 reproduced first, then +19 kept reviewer probes. No skips, no xfails. Fixtures touched: NONE (`git diff chunk8-pass..HEAD -- tests/fixtures poc` is empty; F9 byte-identical). No existing test weakened, deleted or edited by this session. Audit of the fix's own test changes: 106 -> 134 test functions (+28), 256 -> 387 assertions (+131), 0 skips/xfails; the four test names that disappear all pinned behaviour the ruling RETIRED and all four are replaced by tests asserting the retirement.
decisions: none -- a review session makes no Class-B decisions. **B193-B201 all 9 APPROVED**; B198's execution gap is raised as finding Q1 and B200's residual limit as finding C4.
questions: none raised. **Q-16 is RESOLVED and correctly closed** -- every clause of the recorded ruling is executed in code with a test behind it (a byte-comparison against the operator's own copy of the ruling text is available on request). C5 and Q8 remain open by deliberate scope and are recorded in the pack's section 9.
gate: n/a -- chunk 9A has no trader gate of its own. The chunk-4 and chunk-6 gates stay CLOSED with their evidence paths and were not touched.
status-ledger: chunk 9A: reviewed-PASS incl. fix (REVIEW_9A_2)
state-for-next-session: **Chunk 9A is reviewed-PASS including its Q-16 fix, tagged `chunk9A-fix-pass`.** Independently re-derived, importing nothing from src/acumen for the arithmetic: **2026-05-05's four concurrent positions walked observation by observation** from the raw minute parquet -- 16 stamps, every trade's first mark exactly -Rs 100.00 (B194's cost-at-entry), every last mark equal to the ledger's net, and the 15:15 observation Rs 102,705.35 == the day's closing equity; **all 146 paths / 903 marks reconcile, all 649 observations reproduce, the day-close identity holds on 58 of 58 days, zero marks outside the session**; path drawdown **Rs 14,231.00 (12.98%, 2026-06-11 12:15 -> 2026-07-23 11:45, 302 obs)** and run-up **Rs 17,515.10 (19.00%, 197 obs)** land to the paisa. **The exit-LEVEL rule BITES on real data** -- 107 of 146 exits fill at a level their own candle did not close at, worst single error Rs 2,843.75 -- so B196 is load-bearing, not decorative. Tukey: Q1 -Rs 1,099.00 / Q3 Rs 997.20 / IQR Rs 2,096.20 / fences [-Rs 4,243.30, Rs 4,141.50] / **0 outliers of 146**, reproduced under a from-scratch type-7 estimator AND `numpy.percentile`, with both hand-computed docstring fixtures verified digit by digit. Net basis: pots **Rs 96,489.60 / -Rs 98,424.55**, their sum IS the net PnL, **PF 0.9803**, and `winners x avg profit == gross profit` holds exactly on the real 146. **The seven flipped probes each FAIL on the pre-fix code for their own assertion and pass on HEAD** (three initially failed on a helper-signature artifact; the helper was patched in the pre-fix copy so each probe fails on its own defect), and the control passes on both. C1 closed: capital gone from all 35 files in `src/acumen` by the reviewer's own AST sweep, `initial_capital` REQUIRED on five refusal branches, the widened tripwire tripped by injections into `poc.py` and `universe.py` and both files restored byte-identically, and the `100*100` rewrite proved value-neutral over 79,800 (pass,total) pairs. NO COLLATERAL DAMAGE: run ledger still `c3363f6f...c1e318`, all 36 run artefacts byte-identical across three regenerations, the pack regenerates byte-identically THREE times to scratch (`9e8a77c7...`), both resume manifests hash equal at `b1390597...` while `code_sha` moved to HEAD (B179 confirmed independently), fixtures frozen, F9 untouched, every prior-reviewed engine module untouched. **8 FINDINGS, none blocking**, all about labels and untaken branches rather than wrong numbers: Q1 a third before-costs figure ("Gross PnL Rs 12,665.05") outside the one labelled line and the disclosed exception; Q2 the 15-min run-up prints its GIVEBACK as a "recovery" while the row above says "given back"; Q3 the MFE/MAE rows and section 8's excursion invariant are BEFORE-COST on a page declaring one NET basis (net PnL sits outside [MAE, MFE] on 20 of 146); Q4/Q5 INFO; C1 the C4 rewrite says "the first three are decided by walk_symbol" where exactly two are; **C2 MEDIUM, now CLOSED by probes** -- `_outlier_line` / `_path_line` / `_path_stamp` had ZERO test coverage and the pilot only ever exercises their empty branches, i.e. the branches 9B will actually take were untested; C3 the evidence pack **cannot be regenerated on any day after its CA day-cache's `fetched_on`** (this review verified byte-identity under a harness that only relabels the cache's age and changes no datum), which makes REVIEW_8 C2's rule unverifiable for every future session -- pre-existing, not this span's doing; C4 the tripwire is a literal scan and cannot see a computed magnitude. **NEXT ACTIONS:** chunk 9B remains blocked on the trader's Q43/Q44; before its report prints a number it owes the six items in REVIEW_9A_2 section 12 plus the two carried duties C5 (live path takes non-standard sessions from the published NSE calendar) and Q8 (a DISCRIMINATING E2 witness). This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-30 02:40] chunk 9A · fix · done
scope: Executed the architect's **Q-16(a) + Q-16(b) + E13 PRESENTATION BASIS** rulings (recorded VERBATIM in QUESTIONS.md, which closes Q-16) and the fixable REVIEW_9A conditions C1, Q1, Q2, Q3, Q4, Q5, C2 and C4; recorded C5 and Q8 as duties without coding them; regenerated the pilot evidence pack. Nothing else -- no full-history run, no report, no flag value.
files: edited config.yaml (+`initial_capital: 100000`, CONTEXT 3.5/R1-Q21a), src/acumen/config.py (the new REQUIRED key + `initial_capital_paise()` + its validator), src/acumen/portfolio.py (Tukey outliers, the 15-minute path metric, the NET-basis conversion, Q3/Q5/C2), src/acumen/backtest.py (`TradeMark`/`TradePath`/`assemble_trade_paths`/`minute_store_bars`, the C4 refusal-order comment, the residual percent scale written as 100*100), src/acumen/pilot_evidence.py (the definitions block, the outlier line, the two path lines, the two recorded duties, the capital threaded from config), tests/test_config.py (+11), tests/test_portfolio.py (+19, two hand-computed fixtures added to the docstring), tests/test_review9a_probes.py (SEVEN probe bodies FLIPPED, one kept with a re-pointed rationale; no probe added or removed), tests/test_pilot_evidence.py (+9), docs/evidence/chunk9a_pilot.md (regenerated by its own generator), QUESTIONS.md (the rulings verbatim + what was executed under each), STATUS.md, PROGRESS.md. NO change to CONTEXT.md, plan.md, poc/, any fixture, or any engine module under prior review (`bias`, `poc`, `signals`, `simulate`, `signal_engine`, `bias_engine`, `corp_actions`, `aggregate`, `atomic_io`, `minute_backfill` all untouched). Whole session OFFLINE (`allow_network=False`); every store read READ-ONLY; no credentialed call, no candle fetched, nothing written to either price store.
tests: **1728 passed / 0 failed** from a genuinely clean state (`.pytest_cache` + every `__pycache__` deleted first); was 1689 at chunk9A-pass -- +11 config, +19 portfolio, +9 pack (the seven flipped probes are re-pointed, not added). No skips, no xfails. Fixtures touched: NONE (`git diff chunk8-pass..HEAD -- tests/fixtures poc` is empty; F9 byte-identical). No existing test weakened or deleted: the flipped probes each keep their control and assert MORE than before (the rule plus, in three cases, what the old defect used to add). Beyond the suite: the pack regenerates BYTE-IDENTICALLY from its own committed generator (generated twice to scratch paths, `diff` empty both times, sha256 9e8a77c7...), and both hand-computed quartile pairs were cross-checked against `numpy.percentile` and match exactly.
decisions:
  - B193 The E13 pots keep their spec NAMES while their basis changes: `gross_profit_paise` / `gross_loss_paise` are now the NET sums over the NET populations, and the before-costs pair lives in two NEW explicit fields for the single labelled line the ruling allows. Renaming the fields would have drifted from E13's own vocabulary (and from TradingView's, which E13 mirrors); the basis is stated in the field docs, in the metric label and in the definitions block instead.
  - B194 On the 15-minute path the Rs 100 round-trip cost is charged at the ENTRY mark, not at the exit. The round trip is committed the moment the position opens, and charging it there makes each trade's LAST mark equal its realized net PnL exactly -- which is what lets each day's last observation equal that day's closing equity instead of stepping by Rs 100 per trade at the exits. Asserted as `path_reconciles` and as a pack invariant.
  - B195 The quartile estimator is the one thing Q-16(a) does not fix. Chosen: linear interpolation between order statistics (R's and numpy's default, type 7), computed exactly in Fractions -- because it is what a reviewer reproduces with `numpy.percentile(x, [25, 75])`, and both hand-computed fixtures were verified against it. The estimator is named IN the printed definition, so the architect can overrule it by reading the pack.
  - B196 The path's marks come from the SAME `aggregate_15min` bars the signal engine used (there is no second source of 15-minute candles in this repo), and the exit candle is marked at its EXIT LEVEL rather than its close, because a target or stop fills at its level even when the candle ran past it (CONTEXT 3.4-5); marking the close instead would have been wrong by Rs 1,000.00 on the fixture's own trade. If the aggregation has no bar on the entry or exit stamp, that observation is supplied from the LEDGER's own price -- never interpolated, and nothing between them is invented.
  - B197 Each All / Long / Short column and each per-symbol row builds its own 15-minute path from its own trades, so a side's intraday drawdown is that side's own and not a slice of the portfolio's -- the same discipline B184 applies to the daily series.
  - B198 Section 3's reconciliation table keeps chunk 8's own before-costs gross figures, because it is a cross-document check against a COMMITTED pack rather than a report metric. Both rows are relabelled "(chunk-8 basis, before Rs 100/trade costs)" and the exception is stated in the definitions block, so the ruling's "before-costs totals appear ONCE" holds for the E13 presentation and the one place it does not is disclosed rather than hidden.
  - B199 `initial_capital` is a REQUIRED config key, not an optional one like `capital_reference`: no open item stands behind it (CONTEXT 3.5 states it outright), so a config that loses it must fail at LOAD rather than at the point of use.
  - B200 The widened money tripwire derives its forbidden magnitudes FROM the committed config (so it cannot drift from the spec values it protects) and excludes the cost in RUPEES, because 100 is also the paise-per-rupee scale fourteen modules legitimately use -- an engine hardcoding a cost would hardcode it in the paise the engines work in, which IS checked. The one collision this created (`backtest.py`'s `ratio * 10000` percent scale) was rewritten as `ratio * 100 * 100`; Fraction arithmetic, so no value moved.
  - B201 `DailyPnL` drops its two excursion sums (they existed only to feed the retired band) and `metrics()` takes `TradePath`s and builds the path itself rather than taking prebuilt points -- so a subset's path and its daily series can never disagree about which trades they cover.
questions: none raised. **Q-16 is RESOLVED and closed** in QUESTIONS.md, with the rulings recorded verbatim and each clause's execution recorded under it. One thing the architect may want to overrule by return: B195's quartile estimator, which the ruling did not fix and which is printed in the pack's own definition.
gate: n/a -- chunk 9A has no trader gate of its own. The chunk-4 and chunk-6 gates stay CLOSED with their evidence paths and were not touched.
status-ledger: chunk 9A: fix built (unreviewed fix; REVIEW_9A_2 owed)
state-for-next-session: **The chunk-9A fix is BUILT and UNREVIEWED; a re-review (REVIEW_9A_2, fresh session, both personas) over `4fa85fb..HEAD` is the next scheduled session.** Eight commits, one per item, every one touching src/ or tests/ carrying `(unreviewed)`. WHAT CHANGED IN THE NUMBERS, all in the pilot pack: **profit factor 1.1432 -> 0.9803** (the old gross-basis factor read "profitable" directly above a net PnL of -Rs 1,934.95; on one basis it reads below 1); gross profit Rs 101,099.20 -> Rs 96,489.60 and gross loss -Rs 88,434.15 -> -Rs 98,424.55, whose difference IS the net PnL with no commission term left over; the largest-win/loss shares 2.97% -> 3.01% and 1.13% -> 1.12%; **outliers NOT COMPUTED -> computed**, Q1 -Rs 1,099.00 / Q3 Rs 997.20 / IQR Rs 2,096.20 / fences [-Rs 4,243.30, Rs 4,141.50] and NONE of the 146 trades outside them (a real property of a fixed-R strategy, whose every trade is bounded by construction between -Rs 1,100.00 and +Rs 2,900.00 -- worth stating to the trader rather than reading as an empty result); and the two PROVISIONAL rows replaced by the true path, **drawdown Rs 18,844.65 -> Rs 14,231.00 (12.98%)** and **run-up Rs 17,956.90 -> Rs 17,515.10 (19.00%)** -- so the retired construction overstated the drawdown by 32%, while the true path still shows 11.5% more than the close-to-close Rs 12,761.75, which is exactly what E13 asks the intra-trade form for. The run LEDGER is unchanged (sha256 still `c3363f6f...c1e318`), because the path is DERIVED from the candles and the ledger's own prices rather than stored in a row -- so REVIEW_9A's four resume proofs still stand as verified. THE REVIEWER SHOULD ATTACK, in order: (1) the 15-minute path's construction -- re-derive a day's observations by hand from the minute store and check the exit-level rule, the cost-at-entry choice (B194) and the day-close identity; (2) the Tukey estimator (B195) against `numpy.percentile` and the fences against the ledger's own net PnLs; (3) the net-basis conversion -- recompute both pots and the profit factor from the ledger and check that `winners x avg profit == gross profit` really is an identity now; (4) whether anything before-costs still appears outside the one labelled line and section 3's disclosed exception; (5) the six flipped probes -- each must FAIL against the pre-fix code and pass now; (6) the widened tripwire -- add a money literal to any module in src/acumen and confirm it trips. STILL OWED and NOT done here, by deliberate scope: **C5** (chunk 13 must take non-standard sessions from the published NSE calendar; the store scan stays the backtest-side cross-check) and **Q8** (9B's pack owes a DISCRIMINATING E2 witness) -- both recorded in the pack's "what chunk 9B still owes" section. 9B remains blocked on the trader's Q43/Q44. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-29 23:55] chunk 9A · review · done
scope: QC REVIEW (both personas, fresh session) of `dfa3ff8..998ac1d` -- the architect's plan amendment, the backtest runner + run ledger + portfolio layer, the pilot evidence pack and the ledger commit. Executed all nine directed checks, reimplemented CONTEXT 3.2 from the spec text, re-ran the 107-pair universe scan independently, re-derived every E13 metric from the ledger by hand, reproduced the resume at kill points the builder never used, and judged B178-B192. **VERDICT PASS**, 14 findings, six conditions carried to 9B.
files: created docs/reviews/REVIEW_9A.md, tests/test_review9a_probes.py (14 kept probes); edited STATUS.md, PROGRESS.md. NO file under review was modified -- `src/acumen/backtest.py`, `portfolio.py`, `pilot_evidence.py`, `config.py` and every test file in the span are byte-identical to their committed blobs. The evidence pack was regenerated to a SCRATCH path (`--out`), never over the committed file, so the working tree stayed clean throughout. No fixture touched; CONTEXT.md/plan.md/poc/ untouched. Whole session OFFLINE (`allow_network=False` everywhere) and every store read was READ-ONLY; no credentialed call, no candle fetched, nothing written to either price store.
tests: **1689 passed / 0 failed** from a genuinely clean state (`.pytest_cache` + every `__pycache__` deleted first) -- the build's own 1675/0 reproduced first, then +14 kept reviewer probes. No skips, no xfails. Fixtures touched: NONE (`git diff chunk8-pass..HEAD -- tests/fixtures poc` is empty). No existing test weakened, deleted or edited: `tests/` in the span is 1,793 insertions and **0 deletions**.
decisions: none -- a review session makes no Class-B decisions. **B178-B192 all 15 APPROVED**; B185's decision is approved but its implementation gap is raised as finding Q3.
questions: none raised. **Q-16 remains class A, OPEN, STOP** -- and the review records a MATERIAL DISCREPANCY (REVIEW_9A section 0): the architect's directed check 6 presumes a Q-16 fix (Tukey fences, a 15-min intraday path, PROVISIONAL removed) that **does not exist anywhere in this repository** -- `git log`, `reflog`, `branch -a` and `stash list` all confirm the span is four commits and no fix session ever ran. The builder correctly STOPPED instead of inventing a convention; if a Q-16 ruling was issued it has not reached this repo and a fix session is still owed.
gate: n/a -- chunk 9A has no trader gate of its own. The chunk-4 and chunk-6 gates stay CLOSED with their evidence paths and were not touched.
status-ledger: chunk 9A: reviewed-PASS
state-for-next-session: **Chunk 9A is reviewed-PASS and tagged `chunk9A-pass`.** Independently re-derived, importing nothing from src/acumen for the arithmetic: the pilot reconciles with the committed chunk-8 pack on **17 of 17 figures**; the real-vs-empty factor equality was re-verified against the raw CA cache (5 in-window events, all ordinary dividends, worst 1.6270%, zero share-count events); RELIANCE's 2024-10-28 bonus walk reproduces digit by digit with the **ten-paise** Rule-2 margin confirmed (at -11 paise the day becomes an outside bar) and HDFCBANK's 2019 split REVERSES the bias as claimed; the universe scan reproduces **204/112/107/47 exactly** under a from-scratch CONTEXT 3.2 reimplementation, with four changed pairs hand-walked including the Indian bonus conventions 1:4 -> 4/5 and 1:10 -> 10/11 and a non-terminating rounding; the resume is byte-identical at THREE kill points the builder never used plus a double interruption (ledger sha `c3363f6f...` = the published one, 0 duplicates), the moved-SHA resume is refused, no clock leaks, and the pack regenerates byte-identically; the refusal partition holds on 492 damaged IOC/TATASTEEL symbol-days where **75 rows fail two gates and each still carries exactly one reason**. **The E13 basis hunt found what the architect suspected**: the Rs 4,609.60 gap between 45 x avg profit and gross profit decomposes EXACTLY into Rs 4,500.00 of winners' commission (BASIS: avg profit is net, gross profit is gross) plus Rs 109.60 from four trades that are gross-positive and net-negative (MEMBERSHIP: 49 trades made money gross, 45 net) -- no number is wrong, the basis is undisclosed (Q1, Q2). Also found: the drawdown recovery date is silently lost whenever the peak is the opening capital, which B185 makes a normal case (Q3, not exercised by the pilot but live for 9B), and CONTEXT 3.5's Rs 1,00,000 capital is HARDCODED in `portfolio.py:44` with no config key while its two siblings are config-loaded (C1 -- the closest call in the review; carried as a blocking condition on 9B rather than a FAIL because the value is correct, cited, overridable, and cannot move a trade or any paise field). **NEXT ACTIONS: (a)** the architect rules **Q-16** (two E13 conventions) and relays the trader's **Q43/Q44**; **(b)** chunk 9B then runs the full history and must close all six conditions in REVIEW_9A section 12 before any report prints a number -- C1 (capital to config + widen the money tripwire beyond simulate.py), Q1+Q2 (print the basis and population beside every ratio), Q3 (fix `_first_recovery` and flip its pinning probe), C3 (print the PROVISIONAL construction beside its figure), C5 (live path must take non-standard sessions from the published calendar, not a store scan, before chunk 13's replay invariant), and Q-16 itself. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-29 22:10] chunk 9A · build · done
scope: Recorded the architect's PLAN AMENDMENT (chunk 9 splits into 9A/9B) and BUILT chunk 9A -- the backtest runner + run ledger + manifest (`src/acumen/backtest.py`), the PURE portfolio/metrics layer (`src/acumen/portfolio.py`), and the pilot evidence pack that proves the machine on real data. NOT built (9B): the full-history run, the report, and any capital-infeasibility flag VALUE.
files: created src/acumen/backtest.py, src/acumen/portfolio.py, src/acumen/pilot_evidence.py, docs/evidence/chunk9a_pilot.py, docs/evidence/chunk9a_pilot.md, tests/test_backtest.py (42), tests/test_portfolio.py (41), tests/test_pilot_evidence.py (28); edited config.yaml (+`capital_reference`, +`margin_basis`, both NULL), src/acumen/config.py (the two OPTIONAL keys + `capital_reference_paise()` / `margin_basis_text()` + their validators), tests/test_config.py (+12 tests; no existing test or assertion changed or removed), plan.md (ONE architect-authored line on the chunk-9 card recording the split, added on the architect's instruction and citing the ruling -- nothing else in the file touched), QUESTIONS.md (the amendment VERBATIM + NEW Q-16), STATUS.md, PROGRESS.md. NO change to CONTEXT.md, to any engine module under review (`bias.py`, `poc.py`, `signals.py`, `simulate.py`, `signal_engine.py`, `bias_engine.py` are untouched), to any fixture, or to `poc/`. NETWORK: one sanctioned NSE corporate-action pull (CONTEXT 4.2's own day-cached endpoint, one year per request, >= 2s apart, 2005..2026, 41,351 rows) because `data/nse/ca` was empty and chunk 9's own duty is to wire the REAL factor table; no credentialed call, no candle fetched, nothing written to either price store.
tests: **1675 passed / 0 failed** from a genuinely clean state (`.pytest_cache` + every `__pycache__` deleted first); was 1552 at chunk8-pass -- +42 runner, +41 portfolio, +28 evidence, +12 config. Fixtures touched: NONE (`git diff chunk8-pass..HEAD -- tests/fixtures poc` is empty). Beyond the suite: the committed pack regenerates BYTE-IDENTICALLY from its own committed generator (generated twice, `diff` empty -- REVIEW_8 finding C2's rule obeyed from the start rather than fixed later).
decisions:
  - B178 The run ledger is per-symbol SHARDS plus a `progress.json`, and the single `ledger.jsonl` is published only when every symbol is complete. A shard is written (atomic_io, fsync, atomic replace) only once its symbol is fully walked, so an interruption INSIDE a symbol leaves no shard for it and the resume re-walks exactly what it lost -- duplicate-freedom by construction rather than by de-duplication. Proved both ways in tests and in the pack.
  - B179 Nothing in a ledger row or a manifest is read from a clock; the only timestamps are CANDLE stamps. That is what makes a completed re-run byte-identical. The two commit-dependent manifest fields (`code_sha`, `config_digest`) stay ON DISK, where pinning a run to its commit is the point, but are excluded from `stable_manifest_digest`, which is what the evidence pack quotes -- otherwise the pack could not regenerate byte-identically after the next commit.
  - B180 `RunSpec.digest()` COVERS the code SHA, so resuming a run whose code has moved is REFUSED rather than silently mixed. A ledger is a function of one spec and one code state; the inconvenience is the point.
  - B181 CONTEXT 7-E2's own detection ("candle data ... outside 09:15-15:30") is executed literally: a date whose stored candles all fall outside the session is a NON-STANDARD session, is refused under its own reason, and is removed from the trading calendar BEFORE any bias pair is built -- so it is not the previous trading day of anything, exactly as the Q-5 weekend ruling treats a Saturday session. The detection is market-wide (a session is a property of the market, not of a symbol). NOTE FOR 9B: the scan currently runs at wiring time over the run's universe and span, which on a full-history run is one extra pass over the minute store; 9B should cache its result beside the ledger.
  - B182 `capital_reference` is a rupee amount and `margin_basis` is a MULTIPLE of it (CONTEXT 3.5's own example: 1,00,000 cash vs a 5,00,000 typical-MIS tier). Both keys are OPTIONAL and both are NULL, so no flag is computed and every output prints the pending sentence verbatim. The shape question rides with Q43 in QUESTIONS.md, non-blocking, because no flag value depends on it until the answer arrives.
  - B183 MFE/MAE per trade (CONTEXT 7-E13 names them and fixes no convention) are measured over the candles the position was actually HELD -- after the entry candle, up to and including the exit candle, which is exactly CONTEXT 3.4-5's monitoring window under E7 -- and are signed so MFE >= 0 and MAE <= 0. They are computed in the RUNNER (the layer that holds the bars) and stored in the ledger, which keeps `portfolio.py` pure over rows.
  - B184 The daily equity series covers every WALKED trading day, not only the days that traded: a flat day is a real observation for a daily Sharpe, and dropping it would annualize a different sample. E13's Long / Short columns and the per-symbol table are computed over the SAME day index for the same reason.
  - B185 The running extreme for drawdown and run-up starts at the run's OPENING capital, not at the first day's close. A fall measured from a close that came AFTER the low is not a fall anyone lived through; a `peak_day` / `trough_day` of `None` means "the opening capital", counted as the observation before the first day.
  - B186 Sharpe uses the SAMPLE standard deviation (n-1); Sortino's downside deviation is measured against E13's own zero target and averaged over ALL observations. Both return `None` -- never 0 -- when they are undefined (fewer than two returns, or a perfectly flat sample): an undefined ratio reported as zero is a lie a report would print in bold.
  - B187 CAGR is computed over the WALKED calendar span with a 365-day year, and is `None` when the final equity is at or below zero (a negative base has no real root).
  - B188 The buy and hold benchmark buys exact FRACTIONAL units -- rounding to whole shares would make an "equal-weight" benchmark depend on each stock's price level -- and a symbol with no close on the first trade date is EXCLUDED and NAMED in the note rather than back-filled.
  - B189 The "%" beside a largest win or loss is that trade's return on its OWN notional (qty x entry), the only percentage a single trade has; the "% of gross" figure beside it is its share of gross profit or gross loss.
  - B190 A bias that cannot be assembled refuses ONE DAY, not the symbol: the runner finds the first day of the span whose CONTEXT 3.2 pair the calendar can build and seeds the carry there, and every earlier day is refused under its own counted reason. Letting the engine's exception through would lose a whole symbol's history to its own first morning.
  - B191 The pack's universe-wide materiality scan compares two ENGINE answers (the orchestration run with the real table and with an empty one, each seeded 40 calendar days back) rather than two isolated pairs. Comparing isolated pairs counted "this pair fires no rule" as a change and inflated the result from 47 to 53; the run-level comparison is the one that means something.
  - B192 Rare-shape counts and the outcome partition are DERIVED from the ledger rows at manifest time rather than accumulated during the walk. A resumed run replays shards it did not walk, so a running counter would undercount and the manifest would stop being byte-identical -- and a derived count is one a reader can recompute from the committed ledger alone.
questions: **NEW Q-16 (class A, OPEN -- STOP, blocks exactly two CONTEXT 7-E13 metric entries).** (a) E13 lists "outliers" and fixes no definition at all -- no threshold, no rule, no reference -- so the metric is NOT COMPUTED and the pack prints the reason instead of a number (a test asserts no outlier count is ever printed). (b) E13's intra-trade / intrabar drawdown and run-up need an intraday equity PATH, and a take-all portfolio of concurrent intraday trades has none the ledger can observe (it holds each trade's MFE and MAE but not WHEN inside the day each occurred; five trades ran concurrently even in this pilot). It IS computed, under one explicitly stated worst-case construction, and LABELLED PROVISIONAL everywhere it appears -- on the pilot it reads Rs 18,844.65 against the close-to-close Rs 12,761.75, a 48% difference on the number a trader judges risk by, which is why it is the architect's to rule. Also RECORDED (not raised): the architect's plan amendment, verbatim, with the two Round-4 items (Q43, Q44) it leaves open and the one non-blocking shape question that rides with Q43.
gate: n/a -- chunk 9A has no trader gate of its own. The chunk-4 and chunk-6 gates stay CLOSED and were not touched; plan.md §2 required both before the chunk-9 run and both are closed with their evidence paths in STATUS.md.
status-ledger: chunk 9A: built  (and a new `chunk 9B: todo` line, blocked on Q43/Q44 per the split)
state-for-next-session: **Chunk 9A is BUILT and UNREVIEWED; the QC review (fresh session, both personas) is the next scheduled session, over `dfa3ff8..HEAD`.** What exists: `backtest.py` walks every settled symbol x trading day, recomputing CONTEXT 4.6's battery per day (there is no per-day exclusion file), with the REAL chunk-3 factor table wired into the chunk-4 bias engine and the chunk-3 suppression list honoured; every walked day yields exactly ONE ledger row -- a priced trade or a no-trade row carrying ONE reason in a fixed order (E2 -> no minutes -> gate 1 -> gate 2 -> gate 1P -> suppressed -> no bias -> no POC) -- and the ledger is resumable, idempotent and byte-identical on a re-run. `portfolio.py` is pure over those rows: daily PnL, the equity curve as a plain cumulative sum, the whole E13 list bar the two Q-16 entries, the Q40-d disclosures, and capital flags that compute nothing while Q43 is open. THE PILOT PROVES IT ON REAL DATA (`docs/evidence/chunk9a_pilot.md`): the chunk-8 window reconciles on **17 of 17 figures** through the runner with the real table (290 walked, 146/88/56, 53,750 shares, gross Rs 12,665.05, costs Rs 14,600.00, net -Rs 1,934.95, 45/101/0, 86 stop / 36 square-off / 24 target); RELIANCE's 1:1 bonus (ex 2024-10-28) is walked digit by digit and the adjusted Rule-2 verdict turns on TEN PAISE; HDFCBANK's 2019 split is a pair where the adjustment REVERSES the bias, and a scan of the settled universe finds **47 of 107** real share-count events change the bias -- the measured price of an empty table; the 2023-07-20 demerger window shows two suppressed counted no-trades and a clean resume; NSE's Muhurat 2024-11-01 is refused under E2 and removed from the calendar; and the resume proof kills the run after RELIANCE and comes back with an IDENTICAL ledger sha256 and zero duplicate keys. THE REVIEWER SHOULD ATTACK, in order: (1) the refusal ORDER and whether the counts really partition (a day that fails two ways must appear once); (2) the E2 decision -- it removes dates from the trading calendar, which moves bias PAIRS, and B181 is this session's reading of E2's own detection clause; (3) determinism under resume, including the manifest (kill at a different symbol, resume twice, diff the bytes); (4) the E13 metrics against the hand-computed fixture in `tests/test_portfolio.py`'s docstring, especially the drawdown/run-up initialization (B185) and the Long/Short day index (B184); (5) whether anything in `portfolio.py` can produce a float or divide by zero; (6) the pack's own claims -- regenerate it and diff. NEXT ACTIONS after the review: (a) the architect rules **Q-16** (two E13 conventions) and relays the trader's **Q43/Q44**; (b) chunk 9B then runs the full history -- it inherits the runner unchanged and needs only the universe list, the span, and (for the flags) the two config keys; 9B should also cache the E2 session scan beside the ledger (B181) and expect the run to be dominated by minute-store reads. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-29 18:40] chunk 8 · review · done
scope: QC REVIEW (both personas, fresh session) of `79c3c79..f079a2a` -- the architect's Part A (three git/evidence rules), the trade simulator, the real-data evidence pack and the ledger commit. Executed all nine directed checks, reimplemented CONTEXT 3.4+3.5 from the spec text, ran a 33-mutant matrix, re-derived the pack and the named real golden from the stores, and judged B169-B177. **VERDICT PASS.**
files: created docs/reviews/REVIEW_8.md, tests/test_review8_probes.py (10 kept probes); edited STATUS.md, PROGRESS.md. NO file under review was modified -- `src/acumen/simulate.py`, `config.py`, `signals.py` and `trade_evidence.py` are byte-identical to their committed blobs (sha256 vs `git show HEAD:<path>`), verified after every mutation run. The evidence pack was regenerated once to test reproducibility and restored with `git checkout` (finding C2 records what the regeneration showed). No fixture touched; CONTEXT.md/plan.md/poc/ untouched. Whole session OFFLINE and every store read was READ-ONLY; no credentialed call, no candle fetched, nothing written to either store.
tests: **1552 passed / 0 failed** from a genuinely clean state (`.pytest_cache` + every `__pycache__` deleted first) = the build's 1542 plus this review's 10 probes. The build's own 1542/0 claim was reproduced first, exactly, and **nothing skipped** -- the store-backed HDFCBANK golden ran. Fixtures touched: NONE -- `git diff chunk7-pass..HEAD -- tests/fixtures poc` is empty, so F9's 15 real TCS days are byte-identical.
decisions:
  - The reviewer KEPT its ten probes (`tests/test_review8_probes.py`). Three mutants that silently rewrite every number in the committed evidence pack survived the entire 1,542-test build suite -- all three in `trade_evidence.run_sweep`'s accumulation loop, which was the one function nothing exercised. Each probe is verified to FAIL on its mutant and pass on HEAD; the seven that duplicate build coverage are kept as explicit named pins on semantics currently protected only as a side effect of a fixture's candle values.
  - Every load-bearing number was re-derived by code importing NOTHING from `src/acumen`: CONTEXT 3.4's state machine and exit walk plus CONTEXT 3.5's sizing, fills and money reimplemented from the spec text for the whole golden table and diffed field by field against the build (0 divergences over 20 rows x 10 fields); the 146-row evidence pack re-derived by a parser doing plain integer arithmetic on its own printed rows; HDFCBANK 2026-06-10 re-derived from the raw minute parquet with my own 15-minute aggregation.
  - Two mutants were PROVED EQUIVALENT rather than filed as gaps: `raw.get("risk_per_trade", 1000)` and its cost twin are unreachable, because `load_config` already refuses a missing key at its required-keys check before any lookup runs. Proved by execution (a config with either key deleted raises, naming the key), not by reading the code.
  - B170's DECISION is approved but its recorded rationale is corrected in the review: `12.34 * 100` is exactly `1234.0` in CPython, not `1233.9999999999998`. The decision stands on witnesses I measured instead -- 9,174 rupee amounts between 0.01 and 2000.00 lose a paisa under `int(value * 100)`, e.g. `0.29 -> 28`. Recorded because the PROGRESS `decisions:` block is the permanent record of WHY, and a wrong why survives into every later reading.
questions: none raised. No open item touches chunk 8 -- OPEN-1 is resolved and its amount is in config; the three candidates the builder checked against CONTEXT (cost on a qty-zero day, the square-off candle when the 15:00 stamp is absent, the rupee->paise factor) were re-checked here and none is a spec hole.
gate: n/a -- chunk 8 has no trader gate. The chunk-4 and chunk-6 gates stay closed and were not touched.
status-ledger: chunk 8: reviewed-PASS
state-for-next-session: **Chunk 8 is CLOSED at PASS and chunk 9 is unblocked.** What was proved independently: the ENTIRE golden money table reproduces to the paisa from a spec-text reimplementation -- F1/F2 (risk 5.00 -> qty 200, +Rs 3,000.00 / +Rs 2,900.00), F3 SHORT (risk 8.00 -> 125, +Rs 3,000.00), F4 gap both branches (71 shares, +Rs 2,982.00 / -Rs 994.00) and both F4 short mirrors, both square-off sides (166 shares, +Rs 1,494.00 / +Rs 1,394.00), the B159 entry-candle square-off (flat, -Rs 100.00), both qty-zero mirrors (0 shares, no fill, no cost, day CONSUMED), all four exact-touch fills, both both-touched candles paying the STOP, and the 1-share-at-exactly-the-budget day -- with BOTH sizing bounds holding on all 20 rows. SHORT SIGN DISCIPLINE holds on every branch by recomputation, not by symmetry. Fills are LEVELS: a candle blowing Rs 10 past the target still pays `qty x 3 x risk`, and `exit_price_paise` structurally cannot read a candle extreme (its `_Bar` protocol exposes only stamp + close). **33-mutant matrix leaves 0 survivors** (28 caught by the build, 2 proved equivalent, 3 closed by kept probes); all mutated files restored byte-identical by sha256. The evidence pack re-derives completely: partition 146/88/56 = chunk 7's, all 146 rows and all four totals recomputed with 0 violations, all 8 invariants and every rare-shape ZERO confirmed, and the builder's cited exact-budget day found and exceeded (**16** exact-budget days, four of them 400-share). Findings: 1 MEDIUM (`run_sweep`'s accumulation loop had zero test coverage -- three mutations of it survived the whole build suite; **CLOSED** by probes in this commit), 1 LOW (the committed `docs/evidence/chunk8_sweep.md` is not byte-reproducible from its own committed generator -- the "Arithmetic check" sentence sits after the rare-shapes table in the file and before it in the generator's output; **no number moves**, fix it whenever the pack is next regenerated) and 5 INFO. B169-B177 all APPROVED. NEXT ACTIONS, in order: (1) **chunk 9, the backtest runner + portfolio ledger** -- it must wire the REAL chunk-3 factor table (this sweep's empty table is proved correct for its own 290-day window ONLY), recompute gate 1P per day (CONTEXT 4.6's stated duty), read the disclosed-residual register before any per-symbol statistic (IOC and TATASTEEL are settled-but-partial under B149), and add the Q40-d capital-infeasibility flags, which are deliberately NOT in chunk 8; (2) chunk 12's validation pack owes a real GAP-entry day (REVIEW_7 Q3) and now also a real QTY-ZERO day -- this window produced neither, and the pack says so in its own counted table. This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-29 16:50] chunk 8 · build · done
scope: HOUSEKEEPING (the architect's three new git/evidence rules appended to CLAUDE.md verbatim) + BUILT the trade simulator -- `src/acumen/simulate.py` (CONTEXT 3.5, PURE: SignalDay in, TradeRecord out) with its money wired in from `config.yaml` through the loader, and the chunk-7 sweep re-run and PRICED into a committed evidence pack under the new rule.
files: created src/acumen/simulate.py, src/acumen/trade_evidence.py, tests/test_simulate.py (120), tests/test_trade_evidence.py (14), docs/evidence/chunk8_sweep.py, docs/evidence/chunk8_sweep.md; edited CLAUDE.md (architect-authored text, three bullets, committed ALONE as 79c3c79), config.yaml (+`cost_per_trade: 100`, CONTEXT 3.5/R1-Q23), src/acumen/config.py (the new key + `require_risk_per_trade_paise()` / `cost_per_trade_paise()`), tests/test_config.py (every synthetic config body gains the new required key; 9 new tests -- no existing assertion changed or removed), STATUS.md, PROGRESS.md. NO change to CONTEXT.md, plan.md, poc/, any fixture, or any engine file under review (`signals.py`, `poc.py`, `bias.py` untouched). Whole session OFFLINE and every store read was READ-ONLY -- no credentialed call, no candle fetched, nothing written to either store.
tests: **1542 passed / 0 failed** from a genuinely clean state (every `__pycache__` and `.pytest_cache` deleted first); was 1396 at chunk7-pass -- +120 simulator, +14 evidence, +12 config. Fixtures touched: NONE (`git diff chunk7-pass..HEAD -- tests/fixtures poc` is empty). Beyond the suite: a self-run mutation matrix over the sizing and pricing operators -- **17 mutants, 17 CAUGHT** (ceil instead of floor, long/short gross flipped, cost added not subtracted, target priced at the stop and vice versa, square-off priced at the bar OPEN / at the LAST bar instead of the marked one, a non-executed day charged a cost, the qty-zero flag dropped, an unsizable day inventing a fill, the gap flag lost, the risk budget silently defaulted, a qty-zero day dropped or reported unconsumed, and half-up rounding of the share count); `simulate.py` verified byte-identical (sha256) after the harness restored it. The committed pack was also re-derived independently: a scratch script parsed all 146 printed rows and recomputed risk, target, qty, both sizing bounds, the fill level, gross, net and the four totals with plain arithmetic importing nothing from `src/acumen` -- **0 violations, every total reconciles to the paisa**.
decisions:
  - B169 `cost_per_trade` becomes a config key (CONTEXT 3.5's flat 100 rupees, R1-Q23) rather than a constant in `simulate.py`, mirroring `risk_per_trade`. The card names config+loader as the source for both, and a money constant typed into an engine module is invisible to the operator and to the architect's spec sync. The key is REQUIRED to be present (a missing money key must not resolve to a default) and a null is refused outright -- unlike OPEN-1, no open item ever stood behind this number.
  - B170 Both rupee amounts cross into the paise domain exactly ONCE, in the loader, through `Decimal(str(value)) * 100`, and an amount that is not a whole number of paise is REFUSED rather than rounded. `value * 100` on 12.34 rupees is 1233.9999999999998 in float; CONTEXT 7-E11 keeps that out of a sizing divisor.
  - B171 EVERY evaluated stock-day yields exactly one `TradeRecord`, including the days that traded nothing, so chunk 9 counts records instead of inferring a day's fate from a null. `executed` (qty > 0) and `signalled` (an entry existed) separate the three shapes the report has to disclose apart.
  - B172 A qty-zero day and a signal-unsizable day carry NO fill price (`exit_paise is None`) while keeping the entry, stop, target and the exit KIND for the replay pack. Nothing was bought or sold, so writing a counterfactual fill would let a later chunk sum a trade that never existed.
  - B173 The square-off is priced at the CLOSE of the candle chunk 7's marker NAMES -- on a whole day the 15:00-stamped one (R1-Q18/E12), and on the B159 shape (nothing traded after the entry candle) the entry candle's own close, flagged `square_off_at_the_entry_candle`. Chunk 7 chose the candle and REVIEW_7 approved that; chunk 8 prices it and does not re-decide it. The real-data run asserts both: all 36 square-offs filled at their marked candle's close and all 36 markers were the 15:00-stamped candle.
  - B174 The evidence GENERATOR lives at `docs/evidence/chunk8_sweep.py` -- the literal reading of the new rule ("commits the generating script and its output under docs/evidence/") -- with the implementation in the package at `src/acumen/trade_evidence.py` so it can be unit-tested and imported by the suite. The launcher carries the same bare-clone bootstrap the other launchers use (REVIEW_2 F12).
  - B175 The evidence run takes the NEWEST cached instrument-master dump rather than today's, and PRINTS the filename it used. `load_instrument_master` would need the network for today's dump and this session is offline; tick sizes are per-symbol constants, not a daily quantity, so the choice is safe -- and naming the file in the pack is what makes it auditable rather than assumed.
  - B176 The named real golden (HDFCBANK 2026-06-10) is asserted FROM THE STORE, as the card requires, and SKIPS with the exact missing path when `data/` is absent -- it is gitignored (CONTEXT 6), so a bare clone genuinely cannot run it. A second test needs no store at all: it pins the committed pack's own printed line to the same numbers, so the code and the evidence file cannot drift apart silently.
  - B177 The invariant report in the pack is unit-tested to be able to FAIL (one test per line, feeding it a record that violates that invariant). A PASS line that cannot fail is decoration, not evidence.
questions: **none raised.** Three candidates were checked against CONTEXT and none is a spec hole: (i) the cost on a qty-zero day -- CONTEXT 3.5 charges "per round-trip trade" and there is no round trip, so zero, which is also the card's instruction; (ii) which candle a square-off prices when the 15:00 stamp is absent -- chunk 7 already MARKS the candle (B159/B160, REVIEW_7-approved) and chunk 8 prices what is marked, so nothing is re-decided here; (iii) the rupee-to-paise conversion factor -- a unit definition, not a spec parameter, and it lives in the loader with the amounts. Q-15 stays RESOLVED; no open item touches chunk 8.
gate: n/a -- chunk 8 has no trader gate. The chunk-4 and chunk-6 gates stay closed and were not touched; plan.md's old "BLOCKED BY OPEN-1" note on this chunk is spent (OPEN-1 was resolved in Round 3 and the amount is in config).
status-ledger: chunk 8: built
state-for-next-session: **Chunk 8 is BUILT and UNREVIEWED; the QC review (fresh session, both personas) is the next scheduled session, over `79c3c79..HEAD`.** What exists: `simulate.py` prices a `SignalDay` into a `TradeRecord` -- sizing by `floor(risk/per-share risk)` with the risk amount handed in (never hardcoded; a tripwire test asserts no money literal in the module), exit pricing at the LEVEL for target and stop (including the both-touched candle, whose stop was already chosen upstream) and at the marked candle's close for a square-off, and money as `qty x (exit - entry)` signed by side, minus the flat cost once per EXECUTED trade. Nothing is dropped: a qty-zero day, a degenerate `entry == SL` day and a no-cross day each produce a record with zero money and their own flags, and `consumed` still says what CONTEXT 3.4-2 says. The reviewer should attack, in order: (1) the SIGN of the short mirror on every branch (F3 and both F4 mirrors are the goldens, but the property tests sweep only the long side of the 3R/1R identities); (2) the exact-touch and both-touched fills, where the level and the candle extreme differ; (3) the square-off's candle choice against CONTEXT 3.4-5 and E12 -- especially the B159 entry-candle case; (4) the floor at both bounds (a risk of exactly Rs 1,000 sizes ONE share; one paisa more sizes zero, and the real sweep contains a 400-share day at exactly the budget); (5) whether any record could ever be dropped or double-counted by chunk 9's counters. NEXT ACTIONS after the review: (a) chunk 9 (backtest runner + portfolio ledger) is the next build -- it must wire the REAL chunk-3 factor table (this sweep's empty table is proved correct for its own window only, REVIEW_7 section 6), recompute gate 1P per day (CONTEXT 4.6's stated duty), read the residual register before any per-symbol statistic, and add the Q40-d capital-infeasibility flags, which are deliberately NOT in this chunk; (b) chunk 12's pack still owes a real GAP-entry day and now also a real qty-zero day -- this window produced neither (both branches are covered only by hand-computed fixtures, and the pack says so in its own counted table). This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-29 14:35] chunk 7 · review · done
scope: QC REVIEW (both personas, fresh session) of `c941c64..0209f17` -- the two architect spec commits (v1.3, v1.4), the chunk-7 prep, the signal-engine build, and the Q-15 fix. Executed all eight directed checks, reimplemented CONTEXT 3.4 independently, ran a 52-mutant matrix, re-derived the HDFCBANK walk and the 290-day sweep from the stores, and judged B154-B168. **VERDICT PASS.**
files: created docs/reviews/REVIEW_7.md, tests/test_review7_probes.py (6 kept probes); edited STATUS.md, PROGRESS.md. NO file under review was modified -- `src/acumen/signals.py`, `signal_engine.py` and `bias.py` are byte-identical to their committed blobs (sha256 vs `git show HEAD:<path>`). No fixture touched; CONTEXT.md/plan.md/poc/ untouched. Whole session OFFLINE and every store read was READ-ONLY; no credentialed call, no candle fetched, nothing written to either store.
tests: 1396 passed / 0 failed from a genuinely clean state (.pytest_cache + every __pycache__ deleted first) = the build's 1390 plus this review's 6 probes. Fixtures touched: NONE -- `git diff c941c64..HEAD -- tests/fixtures poc` is the two ADDED F5 sub-fixtures plus a PROVENANCE section, 22 insertions / 0 deletions, and every digest recomputes to its pin.
decisions:
  - The reviewer KEPT its six probes (`tests/test_review7_probes.py`). Four mutants that change a real trade survived the build's suite; a probe that only lives in a review file cannot stop the next session reintroducing them. Each probe is verified to FAIL on its mutant and pass on HEAD, and each carries its hand-computed numbers in the docstring.
  - Every load-bearing number was re-derived by code importing NOTHING from `src/acumen`: CONTEXT 3.4 reimplemented from the spec text for F1-F4 and the mirrors; CONTEXT 3.2/3.3 and the 4.6 gate battery reimplemented over the raw parquet stores for the HDFCBANK, ICICIBANK and RELIANCE walks. Nothing in this review is reported on the builder's word.
  - Two surviving mutants were proved EQUIVALENT rather than filed as gaps: the float-POC guard (the type guard below it still refuses a float, message included) and the Rule-3 outside-bar predicate (a 63-pair sweep shows all 17 newly-routed pairs are already decided by inside-bar carry, Rule 1 or Rule 2). Proving equivalence beats writing a test that pins a tautology.
  - The empty-factor-table disclosure was checked against the CA cache itself rather than against the build's price-move argument. All five in-window events are ordinary dividends under CONTEXT 4.2's 2% threshold (worst 1.78%), so k=1 and the empty table is exactly correct for the window -- a stronger result than the one recorded.
questions: none raised. No open item touches chunk 7 -- Q-15 was the last, and it is RESOLVED and executed.
gate: n/a -- chunk 7 has no trader gate. The chunk-4 and chunk-6 gates stay closed and were not touched.
status-ledger: chunk 7: reviewed-PASS
state-for-next-session: **Chunk 7 is CLOSED at PASS and chunk 8 is unblocked.** What was proved independently: CONTEXT 3.4 reimplemented from the spec text reproduces F1 golden (2037/2032/2052, risk 5, NORMAL branch because low == POC), F2 golden (same, via WAIT-BELOW -> arm 2027 -> re-cross), F3 (1980/**1988 = the candle HIGH**/**1956**, risk 8) and F4 (2042/**2028**/2084) plus every short mirror, all to the paisa; the Q-15 risk-floor proof is now an assertion and holds (F1 risk 12, F2 risk 10, both gap stops at or below the POC). The **52-mutant matrix leaves 0 survivors** -- 41 caught by the build, 9 by the kept probes, 2 proved equivalent -- covering strict-vs-inclusive on every 3.4 comparison, the Q41-A "enters directly" mutant, the gap boundary both sides, E7, SL-wins-ties, the 15:00/15:15 boundary both ways, all four E10 fallback mutants and every consumption flip. **HDFCBANK 2026-06-10 reproduces exactly** from the stores with my own arithmetic: Rule-3 outside bar with the 1-min high break at 09:15 (741.90) 126 minutes before the low break (11:21), all three gates pass, POC **739.80** (tick 5, totalTicks 182, tpr 8, 23 rows, EXACT volume conservation), ARMED at 738.20, trigger 740.95, **not** a gap (low 738.10 < POC), SL 738.10, TP 749.50, target on the 13:15-closing candle whose high is 755.95. The **290-day sweep reproduces 146/88/56 with zero exceptions**, zero gate refusals, zero invariant violations across all 146 entered days, and -- newly measured here -- **zero backtest-vs-live drift** on the entry decision when each day is truncated to the entry candle (plus 189 prefix-stability checks), which is the CONTEXT 6 invariant chunk 13's replay test will target. Findings: 2 MEDIUM (both boundary-operator COVERAGE gaps, not wrong behaviour -- the short gap-predicate mirror and all four exit-touch operators at the exact level; both CLOSED by kept probes in this commit), 2 LOW (the prep commit lacks its `(unreviewed)` suffix; `DayGates.usable` was unconsumed and untested on the gate-1P-only path, now pinned) and 4 INFO. B154-B168 all APPROVED. NEXT ACTIONS, in order: (1) **chunk 8, the trade simulator** -- it depends only on this chunk and OPEN-1 is resolved (risk Rs 1,000 is in `config.yaml`); it consumes `SignalDay.entry` (entry/stop/target/risk, integer paise) and `SignalDay.exit_event` (kind + candle) and must respect `consumed` on the unsizable days; (2) chunk 9 must wire the REAL chunk-3 factor table into the bias engine -- the chunk-7 walk used an empty one, which this review proved correct for its 290-day window only; (3) chunk 12's validation pack should deliberately sample a real GAP-entry day if one exists in full history -- 290 real days produced none, so the R2-Q33 rule has no real-data witness yet (finding Q3). This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-29 12:50] chunk 7 · fix · done
scope: ONE item -- executed the architect's **Q-15 ruling (option a)**: recorded the ruling verbatim, applied the architect's CONTEXT **v1.4** text exactly as supplied, and flipped the F1/F2 golden designation per decision B164 (the POC-2032 evaluation becomes THE golden; the POC-2030 one is relabelled a recorded MEASUREMENT of the gap branch). No engine line changed -- the ruling leaves CONTEXT 3.4 untouched, and `signals.py` already implements it verbatim. Chunk 7 remains **unreviewed**; this fix joins the chunk-7 review.
files: edited CONTEXT.md (architect-supplied text, three edits, committed ALONE as 7d4757e: header 1.3 -> 1.4; §8's F1 and F2 rows; §10's v1.4 log row -- this session authored none of it and edited nothing else in the file), QUESTIONS.md (Q-15 heading relabelled RESOLVED + the ruling verbatim + the execution record), tests/test_signals.py (four test renames + docstrings; two property assertions added to the measurement pair), STATUS.md, PROGRESS.md. NO change to src/, to plan.md, to any fixture, or to any expected value. Whole session OFFLINE -- no credentialed call, nothing written to either store.
tests: 1390 passed / 0 failed (unchanged from the build's 1390 -- this fix renames and re-designates tests, it adds and deletes none). Fixtures touched: NONE (`git diff` over `tests/fixtures poc` is empty).
decisions:
  - B166 The POC-2030 parametrization of F1/F2 is KEPT and relabelled `test_measurement_..._at_the_superseded_poc_2030_take_the_gap_branch`, not deleted -- a departure from Q-15's own closing sentence ("if (a), the first pair is deleted"), made on the architect's instruction in this session's card. It is no longer a golden and no longer claims to be CONTEXT 8; what it still measures is CONTEXT 3.4's gap branch on those candles, which the ruling did not change.
  - B167 The two measurement tests now assert the gap-branch PROPERTIES Q-15's arithmetic rests on -- `stop_source == STOP_FROM_PREVIOUS_CLOSE`, `stop <= POC`, `risk >= 7` -- beside the unchanged hand-computed numbers (F1 2025/12/2073, F2 2027/10/2067). The reason the fixture's POC had to be the thing that moved becomes an assertion instead of prose in a docstring. Nothing was re-measured: every expected value is the build session's.
  - B168 The golden pair keeps its exact assertions and gains only the citation (`test_f1_golden_context_8_entry_sl_tp_to_the_paisa_at_the_ruled_poc_2032` and its F2 twin, docstrings citing the Q-15 ruling and CONTEXT v1.4 §8). Renaming rather than rewriting keeps the reviewer's diff to designation only -- `git diff` shows no changed assertion in either golden.
questions: none raised. **Q-15 CLOSED** (class A, the only item chunk 7 owed the architect) -- ruled option (a), recorded and executed here. No open item now touches chunk 7.
gate: n/a (chunk 7 has no trader gate; the chunk-4 and chunk-6 gates stay closed, untouched by this session).
status-ledger: chunk 7: built  # unchanged state, line amended to record the Q-15 execution + CONTEXT v1.4; QC REVIEW still OWED.
state-for-next-session: Chunk 7 is BUILT + Q-15-executed and **unreviewed**. CONTEXT is at v1.4 and F1/F2 now pin POC 2032, so the chunk-7 reviewer reviews the build and this fix together (fresh session, both personas, plan.md chunk-7 card): `signals.py` + `signal_engine.py` against CONTEXT 3.4/4.6, F1-F4 at signal level, and specifically that the golden/measurement designation matches the v1.4 spec text. Chunk 8 (the simulator) depends on chunk 7 being `reviewed-PASS` and must not start before it.

## [2026-07-29 11:57] chunk 7 · build · done
scope: PREP (the housekeeping CONTEXT v1.3 authorises: Round-3 receipts, both trader gates closed, the OPEN-4 tie-rule code change) + BUILT the signal engine -- `src/acumen/signals.py` (CONTEXT 3.4, PURE) and `src/acumen/signal_engine.py` (the orchestration that wires bias -> the CONTEXT 4.6 gate battery -> POC -> signals per stock-day). ONE class-A conflict found and raised as **Q-15**: CONTEXT 8's F1/F2 numbers and CONTEXT 3.4's gap predicate cannot both hold.
files: created src/acumen/signals.py, src/acumen/signal_engine.py, tests/test_signals.py (50), tests/test_signal_engine.py (19), tests/fixtures/minute/SYNTH_2099-01-07_1min.csv + SYNTH_2099-01-08_1min.csv (the GREEN and DOJI F5 sub-fixtures CONTEXT v1.3 §8 now names); edited src/acumen/bias.py (the tie predicate + RULE_3_TIE + tie_case), src/acumen/bias_engine.py (open4 -> tie_case), tests/test_bias.py (F5's three sub-fixtures + the colour-independence pins), tests/test_review4_probes.py (two probes re-pinned to the ruling), tests/test_fixture_integrity.py (two digests), tests/fixtures/PROVENANCE.md, QUESTIONS.md, STATUS.md, PROGRESS.md. CONTEXT.md committed EXACTLY as the architect supplied it (c941c64) and never edited; plan.md untouched; no file under poc/ touched; NO existing fixture's bytes changed (`git diff chunk6-pass..HEAD -- tests/fixtures poc` is the two new CSVs + a PROVENANCE section, 22 insertions, 0 deletions). Whole session OFFLINE -- no credentialed call, no candle fetched, nothing written to either store.
tests: 1390 passed / 0 failed (was 1316 at chunk6-pass: +5 from the prep, +69 from the build). Fixtures touched: NONE changed; two ADDED under the architect-signed CONTEXT v1.3 §8 F5 change. F9's 15 REAL TCS days are byte-identical and green (`git diff chunk6-pass..HEAD -- tests/fixtures/f9_tcs_*.csv` is empty; both F9 tests pass) -- no real day is a Rule-3 tie day, which is why the tie needed a synthetic fixture at all.
decisions:
  - B154 Every session time in `signals.py` is DERIVED from a bar ORDINAL on the 09:15 grid (`bar_open_stamp`/`bar_close_stamp`), never typed as a clock literal -- the same discipline as chunk-6's B112. It also makes a spec fact structural rather than coincidental: the CONTEXT 3.4 reference candle IS the last candle of the CONTEXT 3.3 profile window, so `REFERENCE_BAR == poc.SPEC_WINDOW_CANDLES` and a test pins the two together. They cannot drift.
  - B155 `SignalDay` carries a full `transitions` trail -- one record per evaluated candle, INCLUDING the candles that change nothing. Chunk 12's replay pack and chunk 9's disclosures have to explain why a day did nothing; the alternative is a second implementation of the state machine written later to reconstruct it.
  - B156 A three-way `poc_side` (above / below / AT) rather than two-way comparisons, because CONTEXT 3.4 treats "exactly on the POC" as its own case in three separate sentences (it does not arm, does not trigger, does not set a side). A two-way predicate silently folds it into one of the others.
  - B157 `risk <= 0` -- not only `entry == SL` -- is refused under CONTEXT 3.4-3's degenerate sentence: no trade, day CONSUMED, logged. A negative risk is the same defect one step further (the sizing divisor goes negative), and the alternatives -- flipping the stop or sizing it anyway -- would be inventing strategy. Measured reachability: on any day whose reference candle exists the stop is bounded by the POC and the entry is strictly beyond it, so risk >= 1 paisa ALWAYS; the branch is only reachable on an E10-fallback gap day whose preceding candle is older than the reference. Both facts are pinned (a fixture and a property test).
  - B158 A gap entry with NO preceding 15-minute candle records "consumed, no trade" with its own log line rather than raising. It is unreachable on a whole day (the reference rule already requires a traded minute at or before 11:14, which buckets into an earlier candle), but a pure function chunk 9 runs over 400k days should not raise on a shape nobody has proved impossible.
  - B159 The square-off is a MARKER naming a candle, not a price -- chunk 8 prices it (the card's own split). When nothing traded after the entry candle the marker names the entry candle itself, because that close IS the day's last traded 15-minute price; inventing any other number here would be pricing.
  - B160 The gap stop's "previous 15-minute candle" is the last candle that actually TRADED before the entry candle, not the previous grid stamp. CONTEXT 3.4 calls it "the last traded price before the jump", and under the completeness ruling (2026-07-26) an absent stamp is a tradeless quarter-hour, not missing data.
  - B161 `BiasResult.open4` is renamed `tie_case` and the three colour-keyed tie tags collapse into one `RULE_3_TIE`. OPEN-4 is resolved, so a field named after an open question would be stale; the day itself is still the rare hand-checkable one plan.md's chunk-12 card wants listed, and the docstring says so in those words.
  - B162 The orchestration records exactly ONE refusal reason per day, in a fixed order (no minutes -> gate battery -> bias -> POC), so chunk 9's counts partition the days instead of double-counting a day that fails two ways. Within the battery, a day with NO raw daily row is counted under **gate 1P** and never under gate 1: gate 1 did not fail, it could not run, and folding it into gate 1's numerator is what the Q-14 ruling forbids.
  - B163 A float POC is refused at the door of `evaluate_day`. CONTEXT 7-E11 bans float equality on prices; accepting a float would make every comparison in the engine a float comparison, silently.
  - B164 F1 and F2 are each built TWICE off the SAME candles -- once at CONTEXT 8's POC 2030 and once at the POC that makes the trader's own triple consistent -- rather than picking one. Q-15 is the architect's to rule; this way whichever way it goes, one of the two is already a passing golden and the other is already a recorded measurement, and nothing is re-measured afterwards.
  - B165 `signals.py` imports BULLISH/BEARISH from `bias.py` for `side_for_bias` instead of re-declaring the strings, so the two engines cannot disagree about what a bias value is. Both are pure, so the import costs nothing.
questions: **NEW Q-15 (class A, OPEN -- STOP, blocks ONE assertion of F1 and F2).** CONTEXT 8 pins F1/F2 at `POC 2030, entry 2037, SL 2032 (risk 5), TP 2052`, but CONTEXT 3.4's gap predicate is "entry candle's low > POC" and the only candle that yields SL 2032 from the NORMAL rule has a low of 2032 -- above 2030 -- so 3.4 routes it to the gap branch and a different stop. Proved unsatisfiable rather than asserted: while a day is ARMED every close sits at or below the POC and the arming close is strictly below it, so the gap branch can only ever produce a stop <= POC = 2030, i.e. a risk >= 7, never F1's 5. The two options are stated with their consequences (move F1/F2's POC to >= 2032, or narrow the gap predicate to a real discontinuity); the engine implements 3.4 verbatim meanwhile and both readings are pinned. F3 and F4 are unaffected and reproduce CONTEXT 8 exactly. Also RECORDED (not raised): the seven ROUND-3 FINAL RECEIPTS, and the relabelling of Q-8, Q-9, Q-13 and Q-14 as RESOLVED (status lines only).
gate: **CLOSED x2 (recorded this session, not owned by it):** chunk 4 -> docs/gate_chunk4_bias_evidence.md (the F9 bias table CONFIRMED by the trader) and chunk 6 -> docs/gate_chunk6_poc_evidence.md (Q42 + the BHARTIARTL row-count oracle). Both inside their plan.md §2 deadline (before the chunk-9 run). Chunk 7 has no gate of its own.
status-ledger: chunk 7: built
state-for-next-session: `python -m pytest` is green **1390/0** offline. The signal engine is complete on BOTH sides: reference resolution incl. the E10 fallback, ARMED / WAIT-BELOW / WAIT-ABOVE / side-unset, the Round-3 Q41-A wait rule, first-cross consumption incl. the unsizable case, gap detection and its stop source, the 3x target, and the exit EVENTS (stop / target / stop-wins-on-both / square-off), with the trigger window running from the 11:30 close through the 15:00 close inclusive. CONTEXT 8's **F3 and F4 reproduce exactly** (entry 1980 / SL 1988 / TP 1956; entry 2042 / SL 2028 / TP 2084); **F1 and F2 reproduce CONTEXT 8's entry/SL/TP exactly at POC 2032 and are ALSO pinned at CONTEXT 8's POC 2030 under the gap branch** -- see Q-15, the one thing the architect owes this chunk. Every long fixture has a hand-computed SHORT mirror. Verified beyond the fixtures on REAL data, read-only and offline: 290 stock-days across TCS, RELIANCE, HDFCBANK, ICICIBANK and BHARTIARTL (2026-05-01..2026-07-24) ran end to end with zero exceptions and zero gate refusals -- 146 entered, 88 armed-with-no-cross, 56 never-armed -- and HDFCBANK 2026-06-10 is walked candle by candle in the session report (a REAL Rule-3 outside-bar bias day, the first in this repo: chunk 4's R3 fixture was synthetic). NEXT ACTIONS, in order: (1) the chunk-7 REVIEW is type QC (quant_reviewer + code_reviewer, fresh session) -- attack the state machine's strictness at every boundary, the mirror, and the consumption rule; (2) the ARCHITECT rules **Q-15** (it decides two golden fixtures and, on the F1 day, 200 shares vs 125); (3) chunk 8 (simulator) is unblocked on OPEN-1 (risk Rs 1,000 is in config) and depends only on this chunk's review -- it consumes `SignalDay.entry` (entry / stop / target / risk, all integer paise) and `SignalDay.exit_event` (the kind and the candle), and it must respect `consumed` on the unsizable days; (4) chunk 9 gets the gate battery for free -- `SignalPipeline.gate_day` recomputes gate 1 + gate 2 + gate 1P per day from the two local stores, which is CONTEXT 4.6's stated duty, and it must also wire the REAL chunk-3 factor table into the bias engine (this session's real-data walk used an empty factor table, justified by measurement -- HDFCBANK's largest day-over-day close move in the window is 5.12%, no corporate-action signature -- but that is a walkthrough convenience, not the backtest's contract). This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-29 01:30] chunk 5B · review · done
scope: RE-REVIEW (fresh session, both personas) of the FIX-5 commit ONLY -- `88546cb..7943ce6` -- against REVIEW_5B's FAIL and its 22 findings, the architect's Q-14 ruling, and the architect's closing Q-14 addendum on the definition of done. Reran the suite from clean, executed all seven directed checks, re-derived every load-bearing number independently of `src/acumen`, attacked gate 1P beyond its own fixtures, judged B141-B153. **VERDICT PASS.** The DoD is NOT MET at 94.6917% and the architect has formally ACCEPTED that shortfall; the miss is the price the ruling made visible, not a defect of this commit.
files: created docs/reviews/REVIEW_5B_2.md, tests/test_review5b2_probes.py (10 kept reviewer probes); edited QUESTIONS.md (the ARCHITECT-AUTHORIZED closing addendum to Q-14, verbatim, nothing else), STATUS.md, PROGRESS.md. NO file under review was modified; no fixture touched; CONTEXT.md/plan.md/poc/ untouched. All store access was READ-ONLY and the whole session was OFFLINE (no --allow-network, no credentialed call, no candle fetched). The report regeneration went to a scratch path outside the repo; docs/backfill_minute_report.md was never rewritten.
tests: 1306 passed / 0 failed from a genuinely clean state (.pytest_cache + every __pycache__ deleted first), matching the FIX-5 claim exactly; 1316 passed / 0 failed with this session's 10 probes added. Fixtures touched: NONE -- `git diff 88546cb..7943ce6 -- tests/fixtures poc` is empty.
decisions:
  - The reviewer KEPT its ten probes (`tests/test_review5b2_probes.py`), unlike REVIEW_5B which deliberately kept none. The reason has reversed: the architect has now ruled that gate 1P exists, so pinning its contract pre-empts nothing. The probes cover the shapes the FIX-5 suite did not carry -- a wrong scale that hides on one end, the boundary at BOTH bounds at once, the measured limit of containment, gate 1P's denominator at the WIRING level, the acceptance rule's refusal on the shape that actually occurred (BSE), and B143 proved by AST over real bindings.
  - Every load-bearing number was re-derived by code importing NOTHING from `src/acumen`: the full-universe gate-1P sweep (folded the parquet minute store against the raw bhavcopy under the Q-4 series whitelist, integer arithmetic only), the seven coverage readings, `cluster_prefix`, the floor totals off the 97 committed maps, and the residual-register rows. Nothing in this review is reported on the builder's word.
  - The AST reachability check was rewritten after the coarse name-based graph produced a FALSE positive: `binary_search_floor` takes its classifier as a PARAMETER and the LIVE searcher carries a nested function of the same name, so a name-keyed graph invents a path to `getCandleData`. The kept probe walks real bindings and parameter lists instead.
questions: none raised. ONE architect-authorized append executed: the closing addendum to Q-14 (the chunk-5B definition of done), recorded verbatim as a blockquote in the same form every ruling in this chain carries, with nothing else added to the file.
gate: n/a -- chunk 5B has no TRADER gate.
status-ledger: chunk 5B: reviewed-PASS (DoD shortfall architect-accepted)
state-for-next-session: **chunk 5B is CLOSED at PASS and chunk 9 is unblocked on the data side.** What was proved, independently: the gate-1P partition reproduces to the day on ALL 210 symbols -- 434,769 stored symbol-days, 424,370 pass, 10,399 fail = 3,096 above / 7,125 below / 178 no-oracle, **zero mismatches against the ledger on any of the five counters for any symbol**; all seven coverage readings reproduce to the digit (A 95.2569 / B 95.1575 / C 95.2162 / D 94.9829 / E 95.2179 / F 95.1772 / **G 94.6917, 1,341 days short**); every wrong-scale day REVIEW_5B named by date is now EXCLUDED, and NMDC 2018-03-27 -- which the review measured at 1.4173x -- is repaired to the paisa (fold [12000,12225] vs raw [12000,12225]); the cluster signature reproduces exactly (NMDC 135 of 244 = 55.3%, GAIL 64 of 316 = 20.3%, a whole-span rate test REFUSES BOTH, the step measures 135 and 64, which is precisely why B146 exists); the two accepted floors' boundaries are UNIQUELY determined by adjacent-session probes; BSE's resolved 13-probe volume floor was refused by acceptance and nothing was written; and the recovery path cannot reach a credentialed call. The whole REVIEW_5B punch list is closed (Q1-Q6, B127->B152, C1-C16), with C11 the only carry-over. ELEVEN findings, none blocking: 2 LOW + 3 INFO quant, 2 LOW + 4 INFO code. The two that are worth the architect's eye: (i) **an unresolved VOLUME-side floor search silently inherits the PRICE splice** -- `measure_event_floor` records a searched-but-undecided side as "never searched", so `applies_on_volume` falls back to the price floor; measured on the only case (NMDC), it moves k_volume by -0.0012% on 135 days, three orders of magnitude inside gate 1's band, cannot change a gate verdict and cannot let a wrong day pass (B148's acceptance refuses any floor that loses gate-1 days) -- a one-word semantic fix owed on the NEXT data work, not now, because the era is FROZEN; (ii) **all 178 "no raw daily row" failures are the daily store's own lag**, not damage -- the last ingested bhavcopy is 2026-07-24 while the minute store holds 2026-07-25/27/28, and counting stored minute days past 2026-07-24 gives exactly 178 across 173 symbols; they are 13.3% of the 1,341-day shortfall and clearable by a bhavcopy ingest that touches no minute candle, no map and no factor. NEXT ACTIONS, in order: (1) **chunk 7 (signal engine) is unblocked and independent** -- nothing in it touches this data; (2) chunk 9 must recompute gate 1P per day rather than look for a list of excluded days -- the register is per SYMBOL and the ledger persists only ten sample dates each (finding C1), and the gate is PURE with both stores local, so recomputation is exact and cheap; (3) chunk 9 should read the residual register before computing any per-symbol statistic -- IOC (41.9% price-proven) and TATASTEEL (65.8%) are SETTLED because quarantine reads the gate-1 volume rate alone (approved decision B149, finding C6). This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-29 01:40] chunk 5B · fix · done
scope: FIX-5 -- recorded and executed the architect's Q-14 ruling (GATE 1P, the per-day PRICE gate, joins CONTEXT 4.5's battery permanently; PER-SIDE vendor application floors; ONE bounded recovery pass; then the data era FREEZES) and closed the whole REVIEW_5B punch list. The entire session was OFFLINE: not one candle, corporate action or login was fetched. **The Q-14 defect is closed and its cost is now visible: 10,399 stored symbol-days are price-unproven, excluded and counted under gate 1P's own reason, and the post-Q-14 definition of done is NOT MET at 94.6917%.**
files: created src/acumen/price_recovery.py, tests/test_fix5_gate1p.py (18 tests), tests/test_fix5_price_recovery.py (35 tests); edited src/acumen/quality_gates.py (price_containment_gate + PriceContainmentResult + PRICE_CONTAINMENT_MIN_PAISE/PRICE_CONTAINMENT_REL/price_containment_limit + REASON_GATE1P + GATE1P_NO_ORACLE/ABOVE/BELOW), src/acumen/vendor_adjustment.py (EventFloor per-side fields + floor_price/applies_on_volume/per_side, factors_for_day dropping the two sides independently, event_applies_on_volume, _floored_events per side, build_map's two forced sets + the either-side floor index, StoredDay + classify_stored_price_day/classify_stored_volume_day/classify_floor_day_volume, search_event_floor_stored, per-side JSON round-trip, persist_map through atomic_io, load_map's JSONDecodeError guard, the containment constants single-sourced from quality_gates), src/acumen/universe_backfill.py (gate 1P in gate_symbol/GateTally/SymbolRecord/apply_tally, the overlap-aware intersections, cluster_prefix/gate1p_cluster_events/gate1p_recovery_events/SIGNATURE_GATE1P, same_price_domain, claim_floors_from_map, sync_row_from_map/count_unprovable_days, regate_universe/_recover_one/residual_verdict, --regate and --recover-prices, GATE_DEFINITION + FLOOR_DISCIPLINE bumps, LEGACY_FLOOR_DISCIPLINE, build_map_for rebuilding WITH the previously measured floors, report sections 1a + 3f + the gate-1P exclusion row + the Q2/C9/C15 corrections), src/acumen/build_adjustment_map.py (the operator CLI carries floors forward), src/acumen/minute_backfill.py (the C12 attribution), QUESTIONS.md (the Q-14 ruling verbatim + its execution notes + the append-only C7/C8 corrections), docs/backfill_minute_report.md (regenerated), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO fixture under tests/fixtures/ or poc/ touched (`git status --porcelain tests/fixtures poc` is empty).
tests: 1306 passed / 0 failed offline (1253 inherited + 53 new). Fixtures touched: NONE.
decisions:
  - B141 GATE 1P is a CONTAINMENT, not an equality. The ruling's own words are "must sit INSIDE", and the asymmetry is real: the exchange's daily high is the maximum over EVERY trade, including auction and block prints the continuous 1-minute series never carries, so a fold high BELOW the daily high is ordinary microstructure while one ABOVE it is impossible on raw prices. The other reading (the two-sided equality auction relief's condition (b) uses) would fail every day carrying an auction print. A wrong SCALE moves both ends the same way, so one of them always escapes -- which is why containment still catches 0.1x and 5x.
  - B142 gate 1P's denominator is EVERY stored day, and a day with no raw daily row FAILS it. The ruling says so in as many words, and it is what closes REVIEW_5B finding Q4: those 178 days were in neither numerator nor denominator, so they were neither passed nor counted as excluded. Every stored symbol-day now sits in exactly one bucket.
  - B143 the recovery pass measures its floors from the STORE, spending no credentialed call. The observable a probe buys is `fetched/raw`; the ingest wrote `stored = fetched / k_applied`, so `event-in` is "the stored day is contained in raw" (gate 1P itself) and `event-out` is "the stored day multiplied BACK by the event's own factor is contained". Identical oracle, identical tolerance, identical bisection -- and the ruling's own closing note is that both sides of the check are LOCAL. It also makes the measurement reproducible offline by anyone holding the two stores, which a live probe never is.
  - B144 the 80% quarantine floor is recorded as a Class-B choice of this repo's (finding C16); the old comment attributed it to "the architect" with no ruling or card behind it. Nothing downstream depends on the exact number -- a quarantined symbol's days are excluded and counted either way.
  - B145 `_CLUSTER_RATE` (0.90), `_SCATTER_RATE` (0.50) and the un-asked-for `PATTERN_MIXED` bucket are recorded as Class-B (finding C16). The Q-12-addendum ruling names two patterns and no numbers; these are this repo's reading of its own words, they are REPORT-ONLY, and nothing in the run consumes the verdict.
  - B146 the gate-1P cluster signature is measured as a STEP, not as a whole-span failure rate: the longest old-end block of failing days that ENDS on a failing day, at least MIN_CLIFF_DAYS long, failing at >= FLOOR_HUNT_ERA_CLIFF_RATE, with a clean remainder above it. Same two constants as the FIX-4 volume cliff, and it degenerates to exactly that cliff when the whole span fails. Measured why: NMDC's clustered era fails 135 of 244 days -- 55% on a whole-span reading and a perfect step at 133 -- so a rate test would have admitted nothing.
  - B147 a cluster is a property of an ERA and a floor belongs to an EVENT, so the admitted candidate list is the clustered era's OWN committed chain (its events with a factor not already 1). The bisection then decides which of them actually carries a splice -- NMDC admitted two and the search answered "no splice" for the wrong one. An UN-PROVABLE era admits nothing at all: it commits no chain, so there is no factor to drop and no floor could change one stored price.
  - B148 acceptance is a DRY RUN first and greedy per event: each floor is scored by rescaling the stored days arithmetically (`stored x k_before/k_after` on the price side, the inverse on the volume side) and re-running both per-day gates. A floor is kept only if MORE days pass BOTH gates and no fewer pass gate 1, so a floor that improves only ONE gate is refused -- BSE's volume floor at 2017-12-20 would have moved 80 days into gate 1 while none of them became usable, and it was discarded rather than committed. A rejected floor is never written, so the store is never touched speculatively.
  - B149 QUARANTINE still reads the gate-1 rate alone. The ruling makes a gate-1P failure exclude a DAY and says nothing about symbols; changing the quarantine trigger would be a silent decision of a different kind. The gate-1P rate IS added to the floor-hunt scope, because that is where the ruling's own hunt lives -- SRF sits at 99.5% on gate 1 and stored 216 days at five times the traded price.
  - B150 (finding Q5) a ledger row's floor CLAIMS are re-derived from the MAP that carries their evidence, on every re-gate, never from the pass's own result. `run_floor_pass` ASSIGNED, so a floor measured earlier and carried forward was erased -- ASTRAL and NESTLEIND each held a resolved floor in force while their rows read `floors_resolved = 0` and the report printed "no event carries a vendor application floor". Reading the claims off the map makes the claim/evidence invariant true by construction and preserves `floor_ex_dates`, decision B138's forced-override handle.
  - B151 committing a fresh floor MERGES with the floors the map already carried. `with_floors` replaces the tuple, so the Q-14 pass would otherwise have dropped the FIX-3/FIX-4 floors of any symbol it recovered -- the same class of loss as Q5, one level down.
  - B152 (the recording REVIEW_5B asked for; B127's widening). B127's recorded text says a floor is "carried only when the event still resolves to the same committed price factor". FIX-4 widened `carry_floors_forward` with two further KEEP branches -- a floor is also kept when the fresh map commits NOTHING for the event (`after is None`) or commits exactly ABSENT. The widening is correct and necessary: a floored event SHOULD resolve to absent below its floor, so a floor that forces an event out of every era makes its canonical factor 1, which the same-factor test reads as a change and drops, re-opening a hunt that would measure the same floor again forever. In both kept cases dropping the event from a day's chain is the identity, so the carry cannot alter one stored price. Recorded here because the superseded B127 text was the only carry-rule statement the architect scanned.
  - B153 the containment tolerance is defined ONCE, in `quality_gates` (`PRICE_CONTAINMENT_MIN_PAISE` / `PRICE_CONTAINMENT_REL` / `price_containment_limit`), and `vendor_adjustment` imports it; likewise the raw-gap-near-zero predicate is now one `same_price_domain` shared by the signature gate and the report classifier (finding C13). The era oracle and the per-day gate must be the same number by construction.
  - OPERATOR ACTION, disclosed: after the run, one derived-index maintenance pass re-derived every ledger row's floor CLAIMS from its committed map using `claim_floors_from_map` -- the same code path the runner now uses on every re-gate (the fix landed after the run had started). Five rows changed, all in the direction finding Q5 named: ASTRAL and NESTLEIND now claim the floors their maps have carried since FIX-4, and DIXON/RELIANCE/VEDL gained the ex-dates their counts already implied. Nothing was measured and no gate was re-run; the map is the authority and this only made the derived index agree with it.
questions: none raised as a new class-A hole. ONE ruling RECORDED VERBATIM and executed: Q-14 (the per-day PRICE gate). Its closing sentence is honoured -- **the data era is FROZEN** and everything still flagged is a disclosed residual in report section 3f, not chased.
gate: n/a
status-ledger: chunk 5B: built (FIX-5 complete, unreviewed) -- DoD NOT MET at 94.69% under the post-Q-14 reading
state-for-next-session: The universe is re-gated for all 210 symbols (204 settled, 6 quarantined) under `GATE_DEFINITION = gate1p-price-containment+gate2-completeness+auction-relief-2026-07-28`; `python -m pytest` is green 1306/0 offline. **GATE 1P results.** 434,769 stored symbol-days; 424,370 pass gate 1P and **10,399 fail** -- 3,096 with the fold HIGH above the exchange's own daily high, 7,125 with the fold LOW below its daily low, and 178 with no raw daily row at all (REVIEW_5B's finding Q4, now gated and failed rather than invisible). Every one of the review's Q1 populations is caught: IOC 1,412, TATASTEEL 832, SRF 218, NMDC 372 (from 507), RECLTD 68, APLAPOLLO 47. **RECOVERY.** 21 symbols carried enough price-unproven days for a cluster to be possible and were entered; 7 events were admitted by the gate-1P cluster signature across 3 of them; **2 per-side floors were ACCEPTED** -- NMDC's demerger 2022-10-27 (price floor 2018-10-12, 12 probes; volume side undecided, its factor is 1.0000119) and GAIL's 2022-09-06 (price AND volume floors both 2018-06-28, 12 probes each) -- repairing 199 stored days and moving 197 into "passes both per-day gates". BSE's volume-only floor at 2017-12-20 resolved in 13 probes and was REJECTED by acceptance because it moved no day into usable. No candle was fetched: the classifier reads the store (B143). **COVERAGE, all six of the review's readings recomputed, and they agree with the review to the day once the +64/+66 GAIL/NMDC recovery is accounted for:** A 413,978/434,591 = 95.2569% MET; B 413,546 = 95.1575% MET; C (overlap-aware) 413,801 = 95.2162% MET; D (the naive subtraction the review caught as finding Q3) 412,787 = 94.9829% NOT MET and now printed only as the correction; E 413,978/434,769 = 95.2179% MET; F 413,801/434,769 = 95.1772% MET; **G, the post-Q-14 reading (gate 1 AND gate 2 AND gate 1P over every stored day), 411,690/434,769 = 94.6917% -- NOT MET, 1,341 passing symbol-days short.** That is the honest price of the ruling: the chunk met its DoD only because nothing proved price per day, and it no longer does. **THE DISCLOSED-RESIDUAL REGISTER (report section 3f) is what chunk 9 carries**, and every row now states a measured reason rather than a guess: IOC 1,412 / TATASTEEL 832 / PETRONET 187 / RECLTD 68 and others fail because their days sit inside UN-PROVABLE eras, which commit no chain -- there is no factor a floor could drop, so the Q-11 fallback stands; VBL, NESTLEIND, ASTRAL, UPL, NTPC and IEX are quarantined and their whole history is already excluded; ASTRAL 730, UPL 682, OIL 370, VEDL 304, BEL 249, SRF 218, BPCL 196, PGEL 80, APLAPOLLO 47 show no contiguous step, so "never blanket" means no floor was hunted; 182 further symbols carry 424 days between them, fewer than 20 each. NEXT ACTIONS, in order: (1) chunk 5B's RE-REVIEW (fresh session) covers these fixes and is the gate on chunk 9; (2) the architect may want to read reading G beside reading C before chunk 9 runs -- the 2,111-day gap between them IS the gate-1P exclusion, and whether the chunk-5B DoD is judged on C or on G is the architect's call, not a session's; (3) chunk 7 (signal engine) is unblocked and independent. No stored candle was re-downloaded and no probe window was fetched by this session. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-28 21:05] chunk 5B · review · done
scope: fresh QC review (quant_reviewer + code_reviewer, type upgraded by the architect) of the WHOLE chunk-5B stack -- nine commits 269c07e..708e144 excluding the three chunk-6 commits -- covering the never-reviewed FIX-2/FIX-3/FIX-4 measurement logic. Reran the suite from clean, executed all twelve architect-directed checks and both persona checklists, re-derived every load-bearing number independently of src/acumen, judged B93-B140. **VERDICT FAIL** on one HIGH data-correctness finding; everything else holds.
files: created docs/reviews/REVIEW_5B.md; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture touched; CONTEXT.md/plan.md/poc/ untouched; no test added or changed. All store access was READ-ONLY and the whole session was OFFLINE (no --allow-network, no credentialed call). Report regeneration was written to a scratch path, never over docs/backfill_minute_report.md.
tests: 1253 passed / 0 failed from a clean state (.pytest_cache + every __pycache__ deleted first), matching the FIX-4 entry's claim exactly. Fixtures touched: NONE -- git diff 269c07e..708e144 over tests/fixtures + poc is empty, and so is the same diff against chunk5A-pass.
decisions:
  - Reviewer added NO test to the repo, deliberately. The finding that decides the verdict is a missing GATE, not a missing test; pinning it before the architect rules on whether that gate should exist would pre-empt a Class-A decision. The measurement is reproducible in a dozen lines against the parquet stores and is described in the review.
  - Every claimed defect was put through an adversarial refutation pass before being recorded (112 raw findings -> 54 survived; all 31 MEDIUMs were downgraded or refuted, one MEDIUM was upgraded to HIGH), and then re-verified by the reviewer personally against primary data. Nothing is reported on a subagent's word.
questions: **NEW Q-14 (class A, OPEN, BLOCKING chunk 9)** -- raised by finding Q1. CONTEXT 4.5's gate 1 proves a day's VOLUME; the Q-11 oracle proves an ERA's price over that era's probe days. 1,958 stored symbol-days fall between the two: their 1-minute prices sit at 0.1x-5x the price the exchange printed, and every one PASSES the gate battery. Should a per-day PRICE containment check -- the existing max(2 paise, 0.1%) oracle applied to each stored day against the raw daily high/low -- join the gate battery, and are the days it flags excluded, re-measured, or disclosed? Adding to CONTEXT 4.5's gate battery is the architect's, not a session's.
gate: n/a -- chunk 5B has no TRADER gate.
status-ledger: chunk 5B: built (FIX-4 complete) -- REVIEWED **FAIL**, fix session owed
state-for-next-session: docs/reviews/REVIEW_5B.md carries 22 findings: ONE HIGH (Q1, the verdict), five LOW and one INFO on the quant side, six LOW and ten INFO on the code side. **Q1 is the only blocker.** Measured read-only from the parquet stores against the raw bhavcopy: IOC 1,042 days at 0.667x, TATASTEEL 498 at 0.100x, SRF 216 at 5.000x, NMDC 134 at 1.417x, RECLTD 65 at 0.750x, APLAPOLLO 3 at 2.000x -- 1,958 symbol-days, ALL on SETTLED symbols, so all inside the 413,914-day usable headline and all reachable by chunk 9. TATASTEEL 2020-07-28 is stored with a 1-minute high of Rs 36.00 against a bhavcopy high of Rs 360.00 at a gate-1 gap of +0.071%. The mechanism is measured, not inferred: price and volume factors are arbitrated independently (as Q-11 directs) and `_price_contained` quantifies over an era's PROBE DAYS ONLY, so a vendor splice inside an era but older than its probe window leaves the price wrong while the volume reconciles -- NMDC's provable era pre-2019-03-22 carries k_price 0.235189 against k_volume 0.333337 over four probe days for a ~600-day span. This is NOT a builder error and NOT a licensed residual: no ruling asked for a per-day price check, and ADDENDUM 4 licenses DISCLOSED residuals while these days are disclosed nowhere and counted as passes. NEXT ACTIONS, in order: (1) the ARCHITECT rules Q-14 -- it is the gate on chunk 9 and nothing else in the stack blocks; (2) a FIX session executes the ruling, re-runs the affected symbols from the ALREADY-STORED candles (no re-download: both sides of the check are local) and regenerates the report; (3) the same fix session closes the three counting defects Q3/Q5/Q6 (report line numbers named in the review), records the B127 widening as a Class-B decision, and closes C1/C2/C3 with tests and C4 by routing persist_map through atomic_io; (4) re-review covers the fixes only. What PASSED and needs no rework: the DoD is genuinely MET (95.24% with relief, 95.14% strict, and 95.20% on the stricter both-gates reading once the report's own double-subtraction is corrected -- the published ~412,723 understates it); the signature gate reproduces the eleven-vs-six split exactly; CANBK's floor bisection is uniquely determined by its persisted probes; the auction relief reproduces day-for-day (43/43 on HDFCBANK from the raw stores); all three gate-3 spot-recomputes match to 2dp; the Q-5 evidence reproduces; both operator repairs derive from clean recomputation with nothing hand-asserting a result; 36 of 37 Class-B decisions approved (B127 challenged on RECORDING only); no test weakened anywhere; report regeneration deterministic; no secrets, no AI attribution, local main == origin/main. This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-28 18:20] chunk 5B · fix · done
scope: FIX-4, the FINAL data fix -- recorded and executed the architect's LAST data ruling (Q-11 ADDENDUM 4: floors may be hunted inside UN-PROVABLE eras, signature-gated, superseding decision B123), swept the whole universe from the ALREADY-STORED candles (probe windows were the only downloads), and froze the residuals. Coverage 93.48% -> 95.24%: **the chunk-5B >= 95% definition of done is MET**. Quarantined 14 -> 10 (FIX-3) -> 6; gate 3 17 -> 15 failures over the same 118 ex-dates, every one in a disclosed-residual register.
files: edited src/acumen/vendor_adjustment.py (EventFloor.event_price_factor + absent_throughout, _search_absent_throughout, binary_search_floor(absent_floor_date=), era_hypothesis, era_chain_without over bare choices, search_event_floor_live(hypotheses=, allow_absent_throughout=), floor-aware build_map(floors=) + _floored_events + _resolve_pass(forced=), carry_floors_forward merge/keep rules, is_measurable_session + its two enforcement points, MAP_MODEL v4, floors in to_dict/from_dict), src/acumen/minute_backfill.py (BASELINE_AS_FETCHED_FLOORED, candidate dedup, baseline_correction), src/acumen/universe_backfill.py (gate3_signature_events/era_cliff_events/signature_gated_events, era_hypotheses_for, floor_candidate_days(hypotheses), hunt_symbol_floors(events/signatures/skip/probe_cache), _hunt_rounds, run_floor_pass acceptance path, floor_hunt_in_scope/floor_hunt_owed + FLOOR_DISCIPLINE, REBUILD_DISCIPLINE bump, halt ceiling scoped in-loop, report sections 3e + residual register + DoD verdict + _cell escaping), QUESTIONS.md (the ruling verbatim + execution notes), docs/backfill_minute_report.md (regenerated), STATUS.md, PROGRESS.md. Created tests/test_fix4_unprovable_floors.py (54 tests). Edited tests/test_fix3_runner_wiring.py + tests/test_universe_backfill.py where a contract MOVED by ruling (both re-pinned, neither weakened). CONTEXT.md/plan.md untouched; NO fixture under tests/fixtures/ or poc/ touched.
tests: 1253 passed / 0 failed (1200 inherited + 54 new, minus one FIX-3 test replaced by two tighter ones). Fixtures touched: NONE.
decisions:
  - B132 clause (ii) is read STRICTLY: in an un-provable era only the event being floored may take our own CONTEXT 4.2 factor; every OTHER event must carry a previously COMMITTED source or the era is refused. Measured why: on IOC the vendor applies NO special dividend, so filling an uncommitted slot with `ours` puts BOTH hypotheses ~3% off and turns every decisive probe undecided. Filling it would be the policy-guessing measurement replaced.
  - B133 the gate-3 signature is the NEAREST-HYPOTHESIS test on MAGNITUDES (`|raw gap| < |k-1|/2`), not a percentage threshold: the two models predict gaps of size 0 and |k-1|. It is scale-free (a 5:1 split and a 5% special dividend are judged alike) and admits EXACTLY the eleven rows the architect named, rejecting the six that are a different defect.
  - B134 a floor sitting at or above the ex-date ("absent from every chain in our history") is a resolved outcome, not an abandoned search -- but it requires the newest, the oldest AND a midpoint probe to all answer event-out. One probe is not a measurement of a span; a single event-in or undecided leaves it UNRESOLVED.
  - B135 acceptance is the MAP BUILDER's own oracle: measured floors are passed INTO build_map rather than layered onto a committed map, so a promoted era satisfies the same 2-paise containment and the same unwidened gate-1 band as every other era. A floored event is also no longer an unknown for the probe-gap guard, which is what lets a cascade of "under-determined" older eras unwind (VEDL: 13 eras promoted).
  - B136 the hunt COMPOSES over at most MAX_FLOOR_HUNT_ROUNDS = 3 rounds: each round's promotions become the next round's committed sources. Bounded and deterministic -- a round that resolves no new floor, or promotes no era, is the last; the probe cache is shared so no day is fetched twice.
  - B137 the baseline classifier gains `as-fetched-floored` (the vendor's own chain on a floored day), and EVERY provenance is now corrected by the multiple it matched rather than by the day's target chain. Identical arithmetic on a floorless map; without it a newly-promoted era's days classify UNKNOWN and the floor repairs nothing. Coinciding candidates are de-duplicated so the derived tolerance cannot collapse.
  - B138 a floor a previous pass measured is its OWN admission for a re-measurement (`force_ex_dates` bypasses the signature gate): the repair erases the failure signature that admitted it, so requiring the signature again makes a second measurement impossible -- the trap B125 caught on the failure-rate gate, one level up.
  - B139 the Q-5 ruling reaches the MEASUREMENT days: a weekend-dated session is not a trading day, so it is neither chosen as a probe day nor folded into an era. Enforced at BOTH ends because a probe window is a date RANGE and the vendor returns the excluded session inside it. Measured: NSE's Saturday 2024-05-18 recovers 0.259/0.196 of raw volume on BDL/INOXWIND against 0.500/0.246 on the sessions beside it, and because gate-1 must hold on EVERY probe day it made the only correct chain un-provable -- 2,391 stored days. No oracle was widened: band, containment and candidate sets are byte-identical.
  - B140 the run-level quarantine ceiling is scoped to the run's own symbols INSIDE the loop as well as before it (B131 fixed only the pre-flight), so a `--symbols` subset is judged on its own outcomes. Identical behaviour for a full-universe run.
  - OPERATOR REPAIR 1, disclosed: two runner processes briefly overlapped on the same store (the first survived a wrapper exit) and a parquet write collided. Both write atomically and the map application is idempotent by baseline classification, so nothing was torn; but 63 ledger rows were written under that race, so their `rebuild_discipline`/`floor_discipline` markers were CLEARED and every one was re-derived by a single clean process. Clearing a marker schedules recomputation from the stores; it asserts no result.
  - OPERATOR REPAIR 2, disclosed: BDL's map was rebuilt by the aborted sweep before the fold-level Q-5 filter existed and was stamped with the current model marker optimistically. The marker was corrected to the model that actually built it and the row re-opened; the re-measurement then settled BDL at 99.7% on its own evidence.
questions: none raised as a new class-A hole. ONE ruling RECORDED VERBATIM and executed: Q-11 ADDENDUM 4 (floors in un-provable eras -- the FINAL data ruling). Its closing sentence is honoured: residuals after this pass are DISCLOSED, not chased.
gate: n/a
status-ledger: chunk 5B: built (FIX-4 complete, unreviewed)
state-for-next-session: The universe run is COMPLETE and terminal for all 210 symbols (204 settled, 6 quarantined) at fetch date 2026-07-27; `python -m pytest` is green 1253/0 offline. **The chunk-5B definition of done (>= 95% of symbol-days pass gates) is MET at 95.24%** (413,914 of 434,591 symbol-days; 95.14% on the strict band without auction relief). The DISCLOSED RESIDUAL register in docs/backfill_minute_report.md section 5 is what chunk 9 must carry: 6 quarantined symbols (ASTRAL, IEX, NESTLEIND, NTPC, UPL, VBL -- 14,294 symbol-days), 15 gate-3 failures over 118 ex-dates with their raw and adjusted gaps, 4,526 unknown-baseline days of which only 257 also fail gate 1, and 300 un-provable days. Measured causes, for the record and NOT for another fix pass: ASTRAL's pre-2019-09-16 era carries an exact 0.600000 vendor chain that is no product of its known events; UPL and VBL answer NEITHER hypothesis beside their own ex-dates; NTPC's 2019-02-06 floor resolved absent-throughout and its era still does not reconcile; IEX's promoted era leaves 246 days whose provenance the classifier declines to name. One further measured cause the ruling deliberately does not reach: IOC's cascade of 14 un-provable eras traces to ONE probe day (2023-11-06) whose 1-minute fold LOW sits 0.1035% above the bhavcopy LOW -- 0.3 paise past the ruled containment band -- after which the probe-gap guard takes every older era down with it. The band is the architect's and was not touched. NEXT ACTIONS, in order: (1) chunk 5B's REVIEW (type C, fresh session) is owed and is the gate on chunk 9; (2) chunk 7 (signal engine) is unblocked and independent of this. No stored candle was re-downloaded by this session -- only probe windows. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-28 14:05] chunk 5B · fix · done
scope: FIX-3 -- recorded and executed THREE architect rulings on the completed-run report (vendor APPLICATION FLOORS; AUCTION RELIEF for the deferred +5.0% ceiling; COMPOUND + UNPARSED map nodes), diagnosed COLPAL's "unbuildable map", re-ran the whole universe from the ALREADY-STORED candles (probes were the only per-symbol network calls except BAJAJFINSV/COLPAL, which had no stored candles at all and were ingested for the first time), and regenerated the report. Coverage 90.78% -> 93.48%; quarantined 14 -> 10; gate 3 18 -> 17 failures over 118 ex-dates.
files: edited src/acumen/vendor_adjustment.py (EventComponent/compose_event, KIND_UNPARSED, EventFloor/FloorProbe/binary_search_floor/classify_floor_day/search_event_floor_live/probe_one_day/era_chain_without, AdjustmentMap.floors + era_chain_for_day + event_applies_on + floor-aware factors_for_day, with_floors, carry_floors_forward, canonical_event_factors, MAP_MODEL, floors in to_dict/from_dict), src/acumen/quality_gates.py (auction_relief + AuctionReliefResult + AUCTION_RELIEF_MAX_SHORTFALL_PCT), src/acumen/minute_backfill.py (BASELINE_OVER_DIVIDED_TWICE / BASELINE_PRE_FLOOR_DIVIDED / BASELINE_FLOOR_OVERREACHED, set-derived tolerance, net_map_factors(hypothesis=), floor-aware rebuild), src/acumen/universe_backfill.py (relief in gate_symbol, effective vs strict gate-1, floor hunt + scope + carry-forward + claim/evidence invariant, widened reroute, gate-3 numeric rows, report sections 3c/3d + the gate-3 table, halt-ceiling scoping), src/acumen/build_adjustment_map.py (unparsed nodes on the single-symbol CLI), QUESTIONS.md (three rulings verbatim + execution notes), docs/backfill_minute_report.md (regenerated), STATUS.md, PROGRESS.md. Created tests/test_auction_relief.py, tests/test_fix3_map_model.py, tests/test_fix3_runner_wiring.py. CONTEXT.md/plan.md untouched; NO fixture under tests/fixtures/ or poc/ touched.
tests: 1200 passed / 0 failed (1099 inherited + 101 new across the three FIX-3 files). Fixtures touched: NONE.
decisions:
  - B122 a relieved day is handed to gate 2 as `volume_reconciled=True`. The completeness ruling excludes for missing minutes only "ON A DAY WHERE GATE-1 ALSO FAILS"; a relieved day's verdict is pass-by-relief, and relief conditions (b)/(c) are direct evidence nothing was lost -- the exact hypothesis that trigger exists to catch. The report counts relieved-days-also-carrying-tradeless-minutes so the size is visible (92 of 432).
  - B123 floors apply to PROVABLE eras only. An un-provable era has no committed per-event chain to drop an event FROM; the ruling's own closing sentence keeps it un-provable. This is why IOC/OIL/GAIL/PETRONET/BEL/HINDPETRO could not be floored -- their old eras are un-provable (see the residual gate-3 rows).
  - B124 the hunt scope reads the EFFECTIVE gate-1 rate (relief included). A PNBHOUSING-shaped symbol whose above-ceiling failures are a market property has no adjustment problem, and ~11 probes an event would be measuring noise.
  - B125 within a hunted symbol an event is searched only when its pre-ex provable-era span fails >= 10% of its days -- with a FORCED override for an event a previous pass already floored, because a span repaired BY a floor no longer fails and the gate would otherwise hide its own justification. Every skip is recorded with its reason.
  - B126 the stored-day baseline gains `over-divided-twice` (measured: CANBK's pre-2022-05-10 days sat at exactly 25x raw = 1/k^2 for its 0.2 split -- damage from the superseded one-way rebuild) plus the two floor provenances `pre-floor-divided` / `floor-overreached`; hypotheses are generated from the ERA chain (not the floor-reduced chain) and the match tolerance is DERIVED from the candidate set (half the closest relative gap, capped at 2%) so extending the set can never let two hypotheses claim one ratio.
  - B127 a measured floor is CARRIED onto a rebuilt map (a floor is a property of the vendor's archive, not of when we last looked), carried only when the event still resolves to the same committed price factor, and a dropped floor RE-OPENS the hunt. A record may only claim a floor while its map carries the evidence -- enforced at resume level too.
  - B128 a transient SmartApiError leaves the floor hunt OPEN for the next run (CONTEXT 4.3: access-denied bursts are NORMAL and retried, never an answer); deterministic failures close it so a run cannot loop forever. Found live -- CANBK's first re-measure died on a rate-limit burst.
  - B129 "price-suspect" in ruling 3(ii) is read as EVERY ParseException on the symbol -- the same set the Q-11 routing rule already treats as map-forcing. An informational unparsed subject resolves to `absent` (cost 1 < measured's 2) and changes no chain, so the conservative reading costs a probe window and nothing else.
  - B130 a compound node's volume `ours` is the product of the SHARE-COUNT components only (1.0 for a cash dividend, None if any component has no volume rule); the node is share-count only when EVERY component is, so a mixed node never claims volume-scales-with-price.
  - B131 the halt ceiling counts only quarantined symbols WITHIN the run's own symbol list. Previously a `--symbols` subset was unstartable the moment anything anywhere was quarantined -- the operator's own diagnostic path refused for a reason unrelated to the symbol. Identical behaviour for a full-universe run.
  - OPERATOR REPAIR, disclosed: CANBK's ledger row was hand-edited ONCE to re-open its floor hunt (`floors_hunted=false`, `floor_ex_dates=["2024-05-15"]`). Only the EVENT ex-date was seeded, never a floor value -- the search re-measured the floor from the vendor and independently reproduced 2022-05-10 in 13 probes, identical to the original measurement. The ledger is a derived index (RunLedger.load's own docstring); the seed scheduled a re-measurement, it did not assert a result.
questions: none raised as a new class-A hole. Three rulings RECORDED VERBATIM and executed: Q-11 ADDENDUM 2 (vendor application floors), Q-11 ADDENDUM 3 (compound + unparsed nodes), Q-12 ADDENDUM 2 (auction relief -- the deferred +5.0% ceiling, answered; the band is byte-identical and `volume_gate` untouched).
gate: n/a
status-ledger: chunk 5B: built (FIX-3 complete, unreviewed)
state-for-next-session: The universe run is COMPLETE and terminal for all 210 symbols (200 settled, 10 quarantined) at fetch date 2026-07-27; `python -m pytest` is green 1200/0 offline. **The chunk-5B definition of done (>= 95% of symbol-days pass gates) is NOT MET at 93.48%** -- reported honestly with the whole breakdown in docs/backfill_minute_report.md: of the 6.52% not passing, 5.20% is the 10 still-quarantined symbols (ASTRAL, BDL, HINDPETRO, IEX, INOXWIND, NESTLEIND, NTPC, UPL, VBL, VEDL) and 1.31% is settled-symbol failures. NEXT ACTIONS, in order: (1) the ARCHITECT reads the report -- the residual gate-3 rows carry a clear, numbers-backed signature the floor ruling does not yet reach: eleven of the seventeen failures show a RAW gap near zero against an ADJUSTED gap of 50-950% (IOC 2016-10-18 raw 0.17% vs adjusted 100.34%; PETRONET 2017-07-03 raw -0.87% vs 98.26%; HINDPETRO 2017-07-11 raw -0.35% vs 49.47%; BEL 2017-03-16 raw 5.22% vs 952.21%), which means the two closes are ALREADY in the same price domain, i.e. the pre-ex side was never un-adjusted -- exactly a vendor application floor, sitting inside an UN-PROVABLE era that decision B123 keeps the hunt out of. Extending floors to un-provable eras is a class-A ruling, not an implementation choice, and is the single highest-value lever left on coverage; (2) chunk 5B's own REVIEW (type C, fresh session) is still owed and is the gate on chunk 9; (3) chunk 7 (signal engine) is unblocked and independent of this. No stored candle was re-downloaded by this session except the probe windows and the first-time ingest of BAJAJFINSV and COLPAL. This session pushed to origin/main -- SHA stated in the report block.

---

## [2026-07-26 19:40] chunk 6 · review · done
scope: fresh QC review (quant_reviewer + code_reviewer) of chunk 6 -- the POC engine -- over the two chunk-6 commits 8b053c2..6ed863c; reran the whole suite from a clean state, executed all 9 architect-directed checks, REIMPLEMENTED CONTEXT 3.3 from scratch (importing nothing from src/acumen) and recomputed all 25 frozen poc_prorata values plus F6 at two ticks no fixture uses, ran a 16-mutant matrix, re-derived every number in the trader-gate pack from the frozen CSVs and the parquet stores read-only, swept the engine over 11,905 real stored symbol-days, judged B111-B121. VERDICT PASS.
files: created docs/reviews/REVIEW_6.md, tests/test_review6_probes.py (5 tests, each verified to fail on its mutant); edited QUESTIONS.md (Q-13 REVIEWER ADDENDUM -- measurements only, nothing decided), STATUS.md, PROGRESS.md. NO file under review was modified; no fixture touched; CONTEXT.md/plan.md/poc/ untouched. The mutation matrix mutated src in place then restored exact bytes via git checkout (tree verified clean after every mutant). All store access was READ-ONLY.
tests: 1099 passed / 0 failed (1094 from the build reran green unchanged -- matching the entry's claim exactly, 57 in test_poc.py + 16 in test_poc_evidence.py -- plus 5 reviewer probes), from a clean state (.pytest_cache + every __pycache__ deleted first). Fixtures touched: NONE -- git diff chunk5A-pass..6ed863c over tests/fixtures + poc/ is empty; 64 fixture paths unchanged.
decisions:
  - Reviewer-added test file kept (persona step 4): tests/test_review6_probes.py -- (1) an ast probe over every src/acumen module that fails on a numeric literal passed to row_size= or tick_paise= (closes the two SURVIVING mutants: N hardcoded as 24 and tick hardcoded as 5 inside poc_evidence.run, both of which left the whole chunk-6 suite green); (2) totalTicks rounds half-EVEN, pinning decision B114 (truncation AND half-up both survived); (3) F6 rebuilt from CONTEXT 3.3's sentences at tick 1 paise and 25 paise -- edges, remainder shape, contiguity, midpoints, both N; (4) the tpr tie set re-derived from the spec's own "realized row count" definition rather than from ticks_per_row.
  - Mutation matrix, 16 mutants on the row math / containment / spreading / tie / window / validity: 14 CAUGHT, 3 SURVIVED (M8 N-literal, M9b tick-literal, M13+M13b totalTicks rounding mode), all three now tripwired and each verified to fail on its mutant. One further mutant is EQUIVALENT (a no-op `filled += 0`) and is not counted.
  - Independent recompute, not a re-run of the builder's code: 25/25 frozen poc_prorata values reproduced at worst error 4.0e-13 (the builder's claim confirmed to the digit); the coarser tie direction moves exactly six; Q-13's BHARTIARTL statistics reproduced from the store exactly (262/2418 tie days, POC differs 262/262, median Rs 0.35, worst Rs 18.55, 23 beyond one row height); all 7 gate raw-ness proofs and both windows on both chart days re-derived and matching, including BHARTIARTL 2026-07-17's 1914.60 vs 1914.50 one-tick separation.
questions: none raised as a new class-A hole. Recorded as a **Q-13 REVIEWER ADDENDUM** (measurements only): (1) the interim and its statistics independently confirmed; (2) the gate's discriminating day BHARTIARTL 2026-07-17 sits on the tie under BOTH windows -- the Q42 window conclusion is safe either way (Rs 0.10 gap in both directions) but the absolute price he is asked to match is Q-13-dependent, and one reading off that chart would settle Q42 and Q-13 together (26 rows under the interim, 22 under the coarser); (3) a SECOND, smaller silence in the same sentence -- CONTEXT 3.3's `round` names no MODE, and because the instrument master carries TODAY's tick while NSE widened ticks, 42-68% of stored days have an off-grid range whose dominant residual is exactly half a tick: half-even vs half-up changes totalTicks on 449 of 2,418 BHARTIARTL days (18.6%) and the POC on 10 (median Rs 0.40, worst Rs 9.85), so the architect may want to fold it into Q-13's answer; (4) a recording request -- Q-13 carries no verbatim ARCHITECT'S RULING block and the interim confirmation the review was told exists is not in the repo.
gate: **PENDING** (unchanged) -- docs/gate_chunk6_poc_evidence.md. The review verified the pack's every number but a gate is the trader's to close, not a reviewer's. Q42 plus, ideally, one POC price and one visible row count off the BHARTIARTL/ICICIBANK charts closes it.
status-ledger: chunk 6: reviewed-PASS
state-for-next-session: Chunk 6 is reviewed-PASS at tag chunk6-pass; `python -m pytest` is green (1099/0) from a clean state. Ten findings in docs/reviews/REVIEW_6.md -- none blocking, none requiring a fix session, none a CONTEXT.md deviation. FOUR MEDIUM: Q1 (the gate pack does not tell the trader that its one discriminating day is also a Q-13 tie day -- recommend ONE added sentence before it reaches him; the pack is the architect's document so the review did not edit it), Q2 (the totalTicks rounding MODE is a second unstated silence, measured, now pinned), C1 and C2 (the two coverage gaps, both CLOSED by the kept reviewer tests). SIX INFO/LOW: Q2b (no verbatim architect ruling for Q-13 in the repo), Q3 (the master's tick is TODAY's tick, so 42-68% of historical days build an off-grid grid -- spec-compliant, volume-safe, but it makes decision B116 load-bearing rather than a corner case and lands in chunks 9/12), Q4 (F7(b)'s "no alternative STRICTLY nearer" is honest, not weakened -- prorata is strictly nearest on 2/5 anchors and tied-nearest on 3/5, never beaten), C3 (a zero-volume bar skips the impossible-OHLC check), C4 (6ed863c omits the "(unreviewed)" suffix), C6 (the evidence generator computes profiles with volume_reconciled=True hardcoded and reports gate 1 separately -- a report, not a guard). All eleven Class-B decisions B111-B121 are APPROVED, three with notes (B114 understated its own reach, B116 is live code not a hypothetical guard, B117's "F7 unaffected" claim verified as 0 point bars in 3,000 window minutes). NEXT ACTIONS: (1) chunk 7 (signal engine) depends on chunks 4 and 6 and is now UNBLOCKED -- it consumes DayProfile.poc_paise, an exact Fraction in paise that may legally be half-paise, and must never round it (CONTEXT 3.3); (2) the ARCHITECT relays Q42 and Q-13 to the trader -- per finding Q1 a single BHARTIARTL 2026-07-17 reading with the rows countable answers BOTH -- and closes the chunk-6 gate against docs/gate_chunk6_poc_evidence.md before the chunk-9 run (plan.md §2 deadline); (3) chunk 5B's own review (type C) is still owed and is a separate session. This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-26 17:15] chunk 6 · build · done
scope: BUILT the POC engine (`src/acumen/poc.py`, CONTEXT 3.3) as pure functions -- window slice, TradingView row math, prorata spreading, POC midpoint with the higher-row tie -- plus the F6/F7 goldens, the volume-conservation property, the boundary units, and the chunk-6 TRADER-GATE evidence pack computing every day under BOTH candidate windows (Q-8/Q42). Window validity is the AMENDED completeness rule: gate 1 is the only test and no minute is counted anywhere. One class-A spec silence was found by measurement, NOT decided silently: the ticks-per-row rounding TIE (Q-13). The minute store was READ ONLY -- the universe run owns it and was live throughout.
files: created src/acumen/poc.py (the engine, PURE), src/acumen/poc_evidence.py (the gate pack generator, I/O), scripts/poc_window_evidence.py (launcher), tests/test_poc.py (57), tests/test_poc_evidence.py (16), docs/gate_chunk6_poc_evidence.md (the evidence pack). Edited pyproject.toml (the `acumen-poc-evidence` entry point), QUESTIONS.md (Q-13 + the zero-volume finding), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed, none regenerated); NOTHING written to data/minute_store.
tests: 1094 passed / 0 failed (was 1021 at chunk5B FIX-2; +73), verified OFFLINE. Fixtures touched: NONE -- F6 and F7 are asserted against the frozen poc/data CSVs, the frozen tests/fixtures/tick_sizes.json and the frozen instrument-master sample. No existing test was changed or weakened. The one live action of the session was a 2-request credentialed scratch read for a chart symbol the background run had not settled (see gate).
decisions:
  - B111 The profile window is a PARAMETER with a spec-cited module constant as its default, NOT a new config.yaml key. Q-8 is OPEN, and a config knob would let any later session flip a strategy-critical window without an architect ruling; the 9-candle alternative is reachable only from the evidence generator and is named `evidence-9-candle` in the code.
  - B112 The window is held as a COUNT of 15-minute candles from the session open and the 1-minute stamps are DERIVED (`09:15 + 15*n - 1`). CONTEXT 3.3 states the same window twice (stamps 09:15..11:14, and "the candle closing at 11:15 is included"); deriving one from the other means the two descriptions cannot drift, and no clock literal is typed into the engine.
  - B113 Row volumes are exact `fractions.Fraction`, not floats. CONTEXT 7-E11 bans float equality and the POC's tie rule IS an equality test -- with Fractions the "maximum row" comparison and the tie are exact, and the profile sums to the window volume EXACTLY rather than to within a tolerance.
  - B114 `totalTicks` rounds half-EVEN (Python's own `round`, and the rounding minute_unadjust already uses for money). On the tick grid the division is exact, so the mode only ever decides an off-grid residual -- but it is stated rather than inherited from a float.
  - B115 The ticks-per-row TIE keeps the SMALLER tpr (the finer profile). This is CONTEXT 3.3's one silence, RAISED as Q-13 with measured impact rather than chosen quietly; the interim is the direction that reproduces all 25 frozen `poc_prorata` values (the other moves six of them).
  - B116 The topmost row's ALLOCATION bound stretches to `top` when the tick grid rounds short of it, while its MIDPOINT stays on the grid edge. That keeps CONTEXT 3.3's "the topmost row includes top" true in both senses -- no traded price falls outside the profile, and the rows tile [bottom, top] so prorata conserves volume exactly in both rounding directions.
  - B117 A point bar (`high == low`) puts its FULL volume in the containing row, per CONTEXT 3.3's own sentence. This DIFFERS from the archived PoC script (`poc/poc3_volume_poc_test.py`), whose prorata branch computes a zero overlap for such a bar and drops its volume. The difference is invisible on the calibration data (measured: 0 point bars in all 25 frozen windows, so F7 is unaffected) but it is required by the conservation property and by the spec's text.
  - B118 An empty window returns no POC under its own reason code (`no-poc-no-candles-in-window`), distinct from the two gate-1 reasons. It is the empty set -- `max(high)` over no bars does not exist -- not a re-introduced minute count; measured at 9-11 days per symbol over ~2,400 stored days, mostly non-standard sessions.
  - B119 `volume_reconciled=None` (gate 1 could not run) yields no POC, the same conservative reading decision B104 took in the integrity gate: the ruling's licence is "gate-1 PASSES", and an unrun gate has not passed.
  - B120 The gate-evidence generator lives in `src/acumen/poc_evidence.py` with a thin `scripts/` launcher and a console entry point, so `poc.py` stays pure (CONTEXT 6). A structural test forbids any store-write symbol (`write_bars`, `record_window`, `atomic_write`, `rebuild_symbol_raw`) appearing in it -- this session's read-only store rule proved, not promised.
  - B121 plan.md's chunk-6 card asks for "E4 fixture (a window with 6 missing minutes -> no POC)". That fixture is built INVERTED (6 absent stamps on a gate-1-passing day STILL yield a POC), because the architect's 2026-07-26 amendment retired the trigger it tested. The card text predates the amendment; the test names the ruling in its docstring so the difference is not read as drift.
questions: **NEW Q-13 (class A, OPEN, non-blocking).** CONTEXT 3.3 says the ticks-per-row rounding direction is the one "so the realized row count is closest to requested N" but does not say what happens when both directions are EQUALLY close. Measured over the stored history at N=24: the tie is hit on 10.7%-20.0% of stock-days (TCS 487/2430, DIXON 321/2192, BHARTIARTL 262/2429, ABB 259/2429), and on EVERY one of those days the two directions give a different POC price (median Rs 0.35-3.50 apart, worst Rs 187.50 on DIXON, beyond one row height on ~8% of them). The 22-Jul calibration does not settle it: six of the 25 frozen days sit on the tie but NONE of the five days the trader gave a reading for does. INTERIM executed = keep the finer profile, because it reproduces all 25 frozen `poc_prorata` values to 4e-13 while the other direction moves six of them; pinned by two tests so it cannot flip silently. Also recorded as a FINDING (not a hole): a window holding bars but zero total volume resolves, by CONTEXT 3.3's unconditional tie rule, to the topmost row's midpoint -- implemented literally and FLAGGED (`DayProfile.zero_volume_profile`) so chunk 9 can count it; measured frequency 0 of 9,480 stored symbol-days.
gate: **PENDING** -- docs/gate_chunk6_poc_evidence.md. Every calibration day and both Q32 chart days are computed under BOTH windows, with each day first PROVEN raw (gate-1 volume reconciliation plus an exact 1-min-vs-daily high/low match, 7/7 PASS). Six of the seven days cannot separate the two windows; **BHARTIARTL 2026-07-17 does** (8-candle 1914.60 vs 9-candle 1914.50). No POC reading has been relayed for either chart symbol -- the Q32 screenshots confirmed the SETTINGS (Row Size 24), not a POC price -- so the gate cannot close until Q42 (and, ideally, his POC number off those two charts) comes back.
status-ledger: chunk 6: built
state-for-next-session: `src/acumen/poc.py` is complete and pure; 1094/0 offline. The engine computes the SPEC 8-candle window everywhere -- the 9-candle window exists only inside the evidence generator. The universe backfill was LIVE in the background for this whole session and was never written to; the one network action was a 2-request scratch read of ICICIBANK 2026-07-17 (the run had not reached it), which was later confirmed identical to the store's own copy once the run settled that symbol. NEXT ACTIONS: (1) the chunk-6 REVIEW is type QC (quant_reviewer + code_reviewer) and should attack the row math beyond F6/F7 -- especially the tie-break interim (Q-13), the top-inclusive/off-grid allocation, and the point-bar rule that deliberately differs from the archived PoC script; (2) the ARCHITECT relays Q42 (8 vs 9 candles) and Q-13 (the rounding tie -- note it needs only a ROW COUNT off any chart, not a POC reading) to the trader, and the gate closes against docs/gate_chunk6_poc_evidence.md; (3) chunk 7 (signal engine) depends on chunks 4 and 6 and may start once this review is PASS -- it consumes `DayProfile.poc_paise`, an exact Fraction in paise that may be half-paise, and must never round it (CONTEXT 3.3).

## [2026-07-26 16:10] chunk 5B · fix · handed-off (run in progress)
scope: chunk5B FIX-2 -- RECORDED three architect rulings verbatim (Q-12 volume estimator + candidates; the CONTEXT 4.5/7-E4 completeness amendment; the Q-12-addendum quarantine recovery) and EXECUTED all three, then applied them to the already-fetched store and resumed the live run. Found and fixed one regression of my own making (a one-way "is this day raw" guard that divided 37 genuinely-raw ASHOKLEY days a second time) and one robustness defect the rewrite-heavy path provoked (a Windows os.replace denial that killed the run mid-symbol). No candle of the stored history was re-downloaded.
files: edited src/acumen/vendor_adjustment.py (volume_estimator + price_passing_probe_days + _day_price_contained; SOURCE_PRICE_FACTOR + _VOLUME_COST; _resolve_pass takes costs/price_factors and a None cumulative; _refine_scalars takes the MIN over price-passing days; MAP_VOLUME_ESTIMATOR + volume_estimator_id + map_is_current + persistence), src/acumen/quality_gates.py (integrity_gate takes the gate-1 verdict; negative-values trigger; missing_excluded/liquidity_note; module docstring carries the E4 retirement), src/acumen/aggregate.py (E4 note for chunk 6), src/acumen/minute_backfill.py (stored_day_baseline + _nearest_baseline + the three BASELINE_* constants; net_map_factors; rebuild_symbol_raw_with_map rewritten around baseline classification + applied_factors; RebuildResult.unknown_baseline_days), src/acumen/atomic_io.py (_replace_with_retry), src/acumen/universe_backfill.py (GATE_DEFINITION + REBUILD_DISCIPLINE markers; needs_reprocessing; gate_symbol passes the gate-1 verdict and collects the gate-2 trigger breakdown + liquidity stats + gate-1 failure inputs; gate1_failure_profile + Gate1FailureProfile + the PATTERN_* verdicts; SymbolRecord before/after snapshot + apply_tally/apply_profile/carry_prior_from; reroute_quarantined_to_map; _settle; the map is applied to already-stored days; report sections 3a/3b, the gate-2 redefinition table, the failure-pattern analysis and the deferred-ceiling evidence), tests/test_vendor_adjustment.py (+15), tests/test_quality_gates.py (+8), tests/test_universe_backfill.py (+10), tests/test_atomic_io.py (+2), QUESTIONS.md (three rulings verbatim + what each executed), docs/backfill_minute_report.md (regenerated), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched. DATA (gitignored): data/adjustment_maps/*.json (all 20 pre-ruling maps deleted and rebuilt from probe windows), data/minute_store (re-un-adjusted in place), data/universe_backfill/ledger.json, logs/universe_backfill_2026-07-26.log.
tests: 1021 passed / 0 failed (was 984 at chunk5B build; +37), verified OFFLINE. Fixtures touched: NONE. Three pre-existing assertions were UPDATED to the ruled behaviour, not weakened, each with the reason in its docstring: the ACME rights' volume source is now `price-factor` (the ruled candidate order) with a NEW companion test proving a divergence gate 1 cannot absorb still FORCES `measured`; the synthetic-vendor report assertion likewise; and the reroute's un-provable branch now asserts the day is left untouched rather than restored.
decisions:
  - B101 The Q-12 estimator is applied wherever a volume scalar is FORMED, not only in the per-era solve: `_refine_scalars` takes the MINIMUM over the union of an event's price-passing probe days instead of the median. The ruling names one estimator; leaving the cross-era refinement on a median would have re-introduced the bias it forbids at the last step.
  - B102 A chosen price factor of exactly 1 is NOT offered as the volume `price-factor` candidate -- that IS what `absent` means, and relabelling a vendor omission as a measurement would make the audit row lie. A carried price-factor event whose price flipped to absent in an older era emits `absent`, so the volume tracks the price flip honestly.
  - B103 The measured VOLUME candidate is withheld entirely below 3 price-passing probe days (the ruling's "minimum 3 such days, else no measured-volume candidate"), which can newly mark un-provable an era that a 1-2-day median used to fit. That is the ruling: two points are not a measurement of a contaminated observable.
  - B104 Gate 2 with an UNRUN gate 1 (`volume_reconciled=None`, no raw daily row) keeps the pre-amendment behaviour and excludes on missing minutes. The ruling's licence is "gate-1 PASSES"; an unrun gate has not passed.
  - B105 The negative-values trigger tests VOLUME and OPEN as well as high/low/close. CONTEXT 4.5's OHLC-sanity trigger deliberately omits `open`, but "negative values" names no field, and a negative share count is as impossible as a negative price and would poison gate 1's own sum.
  - B106 Applying a map to a TABLE-path store uses the NET factor `k_map / k_table` in ONE division, never a second full division on top of the first (which would divide by `k_table` twice and round twice). A net factor of 1 is the exact identity.
  - B107 The quarantine-recovery route decision is STICKY on the ledger row (`map_required_by_recovery`). The classifier reads the CA table, which does not know a recovery happened, so without this a re-processing run would drop a recovered symbol back onto the price-blind cheap path that quarantined it.
  - B108 The map-backed rebuild CLASSIFIES a stored day's baseline (`stored_day_baseline`: raw / as-fetched / over-divided / unknown) instead of asking the one-way "is it raw?". The one-way test is wrong in the dangerous direction, because "not raw" means "divide it" -- measured: 37 genuinely-raw ASHOKLEY days sit 0.1%-0.5% off the bhavcopy high (a print no continuous 1-minute candle holds), were called not-raw, and were divided twice. Recognising `1/k` as a hypothesis both prevents that and REPAIRS a doubly-divided day, making the rebuild idempotent by construction rather than by bookkeeping. An unidentifiable baseline is LEFT UNTOUCHED and counted -- never guessed. `_stored_day_is_raw` is kept for the Q-10 factor-table path, where the alternative to "raw" is "leave it alone".
  - B109 Two ledger markers (`GATE_DEFINITION`, `REBUILD_DISCIPLINE`) make a settled row re-processable when a ruling moves under it, and self-clearing once the pass has run. This is what applied all three rulings to the 53 already-processed symbols from the STORED candles, with `backfill_symbol` skipping every window already present.
  - B110 `atomic_write_with`/`atomic_write_bytes` retry a denied rename up to 6 times with exponential backoff. The temp file is complete and fsynced BEFORE the rename, so a retry re-attempts only the atomic step. The bound matters as much as the retry: a denial that never clears is a real fault and is raised unchanged.
questions: **THREE ARCHITECT RULINGS RECORDED VERBATIM AND EXECUTED.** (1) Q-12 (volume estimator + candidates) -- CLOSED. (2) A CONTEXT 4.5/7-E4 AMENDMENT (completeness = volume reconciliation, not minute counts) recorded as an architect-owned amendment, effective now by that record; CONTEXT v1.3 will carry it and the architect owns the CONTEXT edit. It also ANSWERS the chunk-5B gate-2-vs-liquidity finding. (3) The Q-12 ADDENDUM (quarantine recovery) -- executed, with the gate-1 +5.0%-ceiling question FLAGGED with per-symbol evidence in the report and NOT tuned, exactly as deferred. NO new question raised. Two things are flagged for the reviewer/architect rather than decided: (a) where a vendor's volume factor genuinely differs from its price factor by LESS than gate 1's band can detect, the ruled candidate order commits the price factor and the residual rides inside the band -- inherent to the ruled arbitration, pinned by two tests, and the band is the acceptance criterion; (b) all three symbols still quarantined are `clustered-before-ex-date (adjustment problem)`, NOT the liquidity shape the deferred ceiling question is about -- so the ceiling is not what is costing them.
gate: n/a -- chunk 5B has no TRADER gate. Its DoD (>= 95% of symbol-days pass gates) is measured by docs/backfill_minute_report.md when the run finishes; at hand-off it reads 93.6% over 54 of 210 symbols, up from 77.6% over 16.
status-ledger: chunk 5B: built (run in progress)
state-for-next-session: All three rulings are recorded verbatim in QUESTIONS.md ABOVE their code and executed; 1021/0 offline. The rulings were applied to the ALREADY-FETCHED store with no candle of the stored history re-downloaded (only probe windows, which ARE the price oracle the maps are measured against): all 20 pre-ruling adjustment maps were deleted and rebuilt under the ruled estimator, the stored days were re-un-adjusted in place, and the gates were re-run from the store. RECOVERIES: ABB 67.2% -> 99.4%, ADANIENT 8.1% -> 98.8% (both by clause (ii) -- the demerger/rights price factor carrying the volume), AUBANK 45.9% -> 99.8% and BEL 39.3% -> 89.4% and APLAPOLLO 77.8% -> 96.4% and BLUESTARCO 52.5% -> 99.1% (all four by the quarantine-recovery reroute). Quarantined fell from 9 to 3 (ASTRAL, BDL, CANBK), and all three are `clustered-before-ex-date` adjustment problems with per-era failure rates printed. Coverage over the first 20 symbols: gate 1 85.80% -> 96.63%, usable symbol-days 31,427 -> 40,502. Gate 2 excluded 5,367 -> 932 over those 20, and 25,798 previously-excluded liquidity days are now INCLUDED universe-wide. The new negative-values trigger found a real vendor defect on its first run: 2024-03-02 (a SATURDAY -- an NSE disaster-recovery special session) is served with NEGATIVE 1-minute volumes on essentially every symbol. THE RUN IS LIVE AND IN PROGRESS in the background (57 of 210 at hand-off, writing logs/universe_backfill_2026-07-26.log); it is interrupt-safe and the SAME command resumes. NEXT ACTIONS: (1) the OPERATOR lets the run finish (or resumes it with `python scripts/universe_backfill.py --allow-network`) and regenerates the report with `--report-only`; (2) the ARCHITECT reads the completed report's "Deferred to the architect: the gate-1 +5.0% ceiling on illiquid names" section -- the evidence is there and no band was moved; (3) BAJAJFINSV is refused because two price-moving events share ex-date 2022-09-13 and the map keys eras by ex-date -- an upstream re-key the architect should rule on, and COLPAL still has two unparsed subjects; (4) the chunk-5B REVIEW is type C (code_reviewer only) and should review this fix INCLUDING the two self-inflicted defects above and their repairs; (5) chunk 6 (POC) is unblocked and must build E4 in its REDEFINED form -- gate-1-passing day, no minute count (an ast probe in tests/test_quality_gates.py fails if the retired trigger returns).

## [2026-07-26 14:20] chunk 5B · build · handed-off (run in progress)
scope: chunk 5B PREP (closed REVIEW_5A F1/F2/F3 + the owed REVIEW_2 F7 DailyStore.verify) then BUILT the full-universe 1-minute runner (`universe_backfill.py` + `acumen-universe-backfill`), verified the daily-store ORACLE clean before touching the network, classified all 210 routes, and LAUNCHED the live run (resumable; in progress at hand-off). Two real defects found by the live run itself were fixed with tests; one class-A hole (Q-12) was raised as a STOP rather than decided.
files: created src/acumen/adjustment_route.py (PURE routing classifier), src/acumen/universe_backfill.py, scripts/universe_backfill.py, tests/test_adjustment_route.py (18), tests/test_daily_store_verify.py (12), tests/test_universe_backfill.py (28). Edited src/acumen/daily_store.py (verify() + StoreAnomaly/VerifyResult + the row-count floor constants), src/acumen/minute_backfill.py (fetch_corp_action_history + corp_actions_for_symbol + SymbolCorpActions split out of build_symbol_factors; --adjustment-map/--map-data-dir; load_adjustment_map_for; the routing REFUSAL; map-backed --rebuild), src/acumen/minute_store.py (iter_days, month-at-a-time), src/acumen/quality_gates.py (adjustment_continuity_gate = CONTEXT 4.5 gate 3's literal form), pyproject.toml (entry point), tests/test_minute_backfill.py (+6), tests/test_minute_store.py (+4), tests/test_quality_gates.py (+5), docs/gate_chunk5A_open8_evidence.md (F3 footnote corrected in place), QUESTIONS.md (Q-11 ROUTING addendum verbatim + Q-12 + the gate-2/liquidity finding), docs/backfill_minute_report.md (generated), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): data/universe_backfill/ledger.json, data/adjustment_maps/*.json, data/minute_store (the run), logs/universe_backfill_2026-07-26.log.
tests: 984 passed / 0 failed (was 911 at chunk5A-pass; +73), verified OFFLINE. Fixtures touched: NONE. The live SmartAPI/NSE pulls are the runner and scratchpad diagnostics, never pytest.
decisions:
  - B93 "Non-share-count" in the routing ruling is read as the ruling's own enumeration -- rights, special dividend, demerger -- i.e. events that MOVE a price without moving the share count. An ordinary dividend and a buyback carry k = 1 (CONTEXT 4.2), move no price and fragment no era; reading them in would make EVERY symbol map-required and contradict the ruling's own "bonus/split-only symbols may use the factor-table path".
  - B94 The classifier scopes events to the un-adjustment window (D, F] at BOTH ends. An ex-date at or before the symbol's minute clamp, or after the fetch date F, appears in no stored day's window and cannot affect one stored price. The upper bound was MISSING in the first build and the live run proved it costs symbols: ULTRACEMCO (special dividend ex 2026-07-30) and POWERINDIA/GVT&D (dividends ex 2026-08-21) were map-required for events that have not gone ex, and -- having no probeable era -- would have been refused outright. Fixed in chunk5B-fix with tests.
  - B95 DailyStore.verify() splits ANOMALIES (blocking) from NOTES (reported, non-blocking). The full-market row-count floor does not describe a WEEKEND-dated session, because Q-5 (already ruled, executed chunk 3) excludes weekend sessions from trading days entirely -- so the real 2012-11-11 case (a 14-row Sunday gold-ETF session, ledger row_count agreeing with the rows on disk) is a NOTE, not a store defect. A low row count on a WEEKDAY stays an anomaly: nothing in the spec separates a weekday special session from a truncated write, so the conservative reading holds. This is applying an existing ruling, not inventing one.
  - B96 The universe run reads the whole daily store and the whole NSE CA history ONCE (build_daily_cache + CachedDailyStore, a DailyStore-shaped read-only adapter). Per-symbol reads would reopen the same monthly parquets tens of thousands of times; chunk-5A code paths stay byte-identical because the adapter answers with the same columns and the same empty-frame contract. The Q-4 whitelist selection is applied exactly as DailyStore.daily applies it, including the loud raise on two whitelist series for one symbol-date.
  - B97 Map eras are DERIVED per symbol (era_intervals / era_probe_windows) instead of hand-picked as the FIX-4 runbook did for RELIANCE: sorting the in-scope ex-dates cuts [clamp, end] into intervals whose keys shrink by exactly ONE event per step, which is what the map builder's probe-gap guard requires; each era is probed on the LAST few trading days before its next ex-date (the ruling's pre-ex probe days). The identity era is never probed -- factors_for_day returns the exact identity for an empty key. Measured cost for the live universe: 397 probe fetches, zero unprobeable eras.
  - B98 gate 3 CHAINS events that share an ex-date. Found live: 360ONE carries a 1:1 bonus AND a face-value split 2->1 BOTH ex 2023-03-02, so the raw price quarters overnight; testing either k = 0.5 alone reported a bogus 50% break on a perfectly continuous series (CONTEXT 4.2: "chain multiple events"). Fixed before launch, pinned by test.
  - B99 The report is named docs/backfill_minute_report.md (the architect's prompt) rather than the chunk-5B card's docs/backfill_report.md -- this is the MINUTE backfill and the daily one may want that name. Flagged so the difference is not read as drift.
  - B100 A map-required symbol with no probeable era is REFUSED (status map-required-but-unbuildable), never dropped to the factor-table fallback: with no price oracle a wrong price passes gate-1 volume unseen (REVIEW_5A F2). Measured: after the B94 fix this leaves COLPAL alone (two genuinely unparsed 2017/2019 subjects), listed with its reasons.
questions: **Q-11 ROUTING ADDENDUM recorded verbatim and EXECUTED** (classifier + CLI refusal + runner). **NEW Q-12 (class A, OPEN, STOP -- non-blocking, costs COVERAGE not correctness):** Q-11's MEDIAN estimator is unbiased on the PRICE side but not on the VOLUME side, where the 1-minute sum systematically under-counts the daily total (the pre-open auction gate-1's asymmetric band exists for). The median therefore lands ~half the probe days below the true factor, the unwidened -0.1% floor rejects them, and an era that a single exact factor reconciles is marked un-provable -- and one failed era cascades to every older era via the probe-gap guard. Measured on TWO live symbols: ABB (demerger ex 2019-12-20) price factor 0.897600 on all four probe days, volume median 0.8986 fails one day by 0.011pp and costs ~790 symbol-days; ADANIENT (rights ex 2025-11-17) our TERP 0.969485 matches the vendor to six decimals on all four days and reconciles volume on all four (+0.682/+0.093/+0.351/+0.071%), while the median 0.972903 fails two and costs 92% of the symbol's history. Options (a) low-quantile/min estimator, (b) add the measured PRICE factor to the volume candidate set, (c) accept and disclose. Executed as RULED; every affected day is excluded + counted (CONTEXT 7-E3) so nothing wrong is stored, and any ruling can be applied to the already-fetched store via the idempotent rebuild_symbol_raw_with_map WITHOUT re-downloading a candle. **Also recorded (a FINDING, not a hole): gate 2 vs LIQUIDITY** -- the vendor omits minutes with no trade, so CONTEXT 4.5 gate 2's "missing > 15 of 375" excludes 34% of ABB's days and 41% of APLAPOLLO's for a liquidity reason; measured 318/293/325/338 traded minutes on four consecutive ABB days in 2019 against CONTEXT 4.3's PoC "375/375" (measured on 5 LIQUID symbols in 2026). No code deviates; the report prints average traded minutes per day and flags every symbol losing > 10% of its days to gate 2.
gate: n/a -- chunk 5B has no TRADER gate. Its DoD (>= 95% of symbol-days pass gates) is measured by docs/backfill_minute_report.md when the run finishes.
status-ledger: chunk 5B: built (run in progress)
state-for-next-session: The daily store is VERIFIED (REVIEW_2 F7 discharged: 319 months, 6606/6606 file-present dates carry rows, 10,533,151 rows, ZERO anomalies, one Q-5 note for the 2012-11-11 Sunday gold-ETF session). The universe run is LIVE and IN PROGRESS in the background, writing logs/universe_backfill_2026-07-26.log; it is interrupt-safe and the SAME command resumes (proved on the real run: it was stopped after 8 symbols to pick up the B94 fix and resumed at window granularity without refetching a settled window). Routes over the live universe: 127 table-path, 83 map-required, 397 probe fetches, zero unprobeable eras. Early results: table-path symbols come in at 99%+ gate-1; map-required symbols are mixed (AMBUJACEM 2/2 and ASHOKLEY 3/3 eras provable; ABB 0/1 and ADANIENT 0/3, both Q-12). Quarantine ceiling is 21 of 210 -- if Q-12 keeps map-required symbols failing, the run may HALT itself and surface, which is the designed behaviour, not a fault. NEXT ACTIONS: (1) the OPERATOR lets the run finish (or resumes it with the identical command) and then regenerates the report with `--report-only`; (2) the ARCHITECT rules Q-12 -- and note that any ruling is applied to the ALREADY-FETCHED store by deleting the affected data/adjustment_maps/<SYM>.json and re-running, with NO candle re-downloaded (rebuild_symbol_raw_with_map is idempotent and identity-guarded); (3) the chunk-5B REVIEW is type C (code_reviewer only, plan.md 3) and reviews this build INCLUDING the run's report; (4) chunk 6 (POC) is unblocked and depends only on 5A. Observation for the reviewer/architect, not a decision: a TABLE-PATH symbol can also fail gate 1 (APLAPOLLO 77.8%, quarantined) because the routing rule only guarantees a PRICE oracle for non-share-count events -- routing quarantined table-path symbols through the map as a second pass is an option the architect may want.

## [2026-07-25 22:40] chunk 5A · review · done
scope: fresh QC review (quant_reviewer + code_reviewer) of the WHOLE chunk-5A stack, six commits 2d55919..9f06b6d (prep+build+fix Q-10+fix2 k_shares+fix3 demerger-scope/Q-11 STOP+fix4 Q-11 measured reconstruction) -- reran the suite from clean+offline, executed all 10 architect-directed checks, HAND-VERIFIED the RELIANCE-shaped era-flip resolution end-to-end (demerger measured 0.9079 in {D,B1}, flips to absent in older eras; rights measured 0.9873 price/0.9877 vol independently; min-cost lands the unique cost-3 chain), recomputed the RELIANCE map k_price per era (0.453931/0.493652/0.246826 all match), ran a 9-mutant matrix on the un-adjust math, constructed the adversarial preference-masking case + a determinism/order-invariance check and kept both as probes, audited the 27/27 evidence table + secrets + fixtures + commit hygiene, judged B63-B92. VERDICT PASS.
files: created docs/reviews/REVIEW_5A.md, tests/test_review5A_probes.py (2 tests, both verified to fail on their mutant); edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture touched; CONTEXT.md/plan.md untouched. Mutation matrix mutated src in place then restored exact bytes via git checkout (tree verified clean after each).
tests: 911 passed / 0 failed (909 from the stack reran green unchanged + 2 reviewer probes), verified from a clean state (.pytest_cache + every __pycache__ deleted first), fully offline. Fixtures touched: NONE -- git diff 656bfbc..9f06b6d over tests/fixtures+poc/data shows only the ADDED instrument_master_sample.json + a PROVENANCE append; poc/data byte-unchanged; 56 digests pinned, one added, none changed.
decisions:
  - Reviewer-added test file kept (persona step 4): tests/test_review5A_probes.py -- (1) the min-cost preference masks ONLY a sub-0.1% (immaterial, below the microstructure floor) vendor variant while the per-day containment oracle FORCES `measured` for any material (>0.1%) one (verified to fail when containment is disabled); (2) the committed map factors are invariant to probe-day order (medians are order-free) and a rebuild from identical inputs is byte-identical. Both pass on HEAD.
  - Mutation matrix (9 mutants on price divide, volume multiply, era-carry direction, half-even rounding x2, identity-guard price-check, per-day containment, probe-gap guard, <=1-solve guard): 8 CAUGHT, 1 EQUIVALENT (the <=1-solve inner filter is unreachable given the probe-gap guard -- a SOLVE option exists only for a NEW event and the probe-gap guard excludes any era with >1 new event before _resolve_pass runs). No genuine coverage gap.
questions: none raised. Q-10/Q-11 (+ both addenda) are correctly RESOLVED-executed: each ruling is verbatim in QUESTIONS.md BEFORE its code, the code implements only the FINAL state (the fix2/fix3 factor-table path survives as the documented fallback, not a bypass), tier-2-rights un-provability survived, and k_shares is subsumed by measured volume factors. Q-8/Q-9 are correctly RAISED with interim behaviors that block gates/branches, not chunk 5A.
gate: n/a -- chunk 5A has no TRADER gate. OPEN-8 is answered (ADJUSTED) and its E11 contradiction remedied by Q-10/Q-11; the evidence pack + QUESTIONS.md carry the acceptance.
status-ledger: chunk 5A: reviewed-PASS
state-for-next-session: Chunk 5A is reviewed-PASS at tag chunk5A-pass; python -m pytest is green (911/0) from a clean state and fully offline. Seven findings in docs/reviews/REVIEW_5A.md -- none blocking, none requiring a fix session. TWO are LOW and both are chunk-5B-forward + gate-1-safe today: F1 (the FIX-4 AdjustmentMap is built/proven/consumable but the operator ingest+rebuild CLIs do NOT yet load a persisted map -- backfill_symbol(adjustment_map=...) and rebuild_symbol_raw_with_map exist + are tested but have no src caller; the CLI uses the Q-10 factor-table fallback, which for an era-inconsistent symbol stores gate-1-EXCLUDED prices -- chunk 5B must wire map consumption), F2 (the factor-table fallback has no price oracle, so gate-1 volume cannot catch a price-only special-dividend mis-adjustment; chunk 5B should route ANY non-share-count-event symbol through the map path, not just demerger/rights symbols). Five INFO: F3 (evidence footnote 1 cites a stale "median residual" reason; code is per-day containment + 0.1% floor), F4 (the four fix commits omit the "(unreviewed)" suffix), F5 (the newest fix PROGRESS entry did not textually state the pushed SHA), F6 (the preference masks a <=0.1% variant -- the ruling's disclosed tolerance), F7 (the audit probe_days list order tracks input order; factors are invariant). NEXT ACTION: chunk 5B (full-universe backfill) -- it depends on 5A (now reviewed-PASS); it MUST wire map consumption per F1/F2 for demerger/rights/special-dividend symbols and build+commit an AdjustmentMap per such symbol. Chunk 6 (POC) remains unblocked (the store holds RAW same-day prices; E11 satisfied). This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-25 21:15] chunk 5A · fix · done
scope: chunk5A FIX-4 -- EXECUTED the architect's Q-11 ruling. Replaced FIX-2/FIX-3's rule-guessing (a single policy for the vendor's era-inconsistent demerger scope) with per-event MEASUREMENT arbitrated by the raw-daily oracle. Built `vendor_adjustment.py` (PURE map builder + un-adjust consumption), the live measurement/persistence wrappers, the operator runbook, wired the ingest path to consume the map, and ran the LIVE credentialed RELIANCE acceptance (all 6 windows provable + gate-1 PASS + containment) and the TCS regression (map {2018 bonus, ours, 0.5}; store rebuild no-op; gate-1 year table unchanged).
files: created src/acumen/vendor_adjustment.py (PURE: EventSpec/ProbeDay/EraMeasurement/EventChoice/EraResolution/AdjustmentMap; build_map + _resolve_pass backward pass + _refine_scalars; unadjust_with_map consumption; to_dict/from_dict/persist_map/load_map; measure_symbol_live/build_symbol_map_live I/O), src/acumen/build_adjustment_map.py + scripts/build_adjustment_map.py (operator runbook + launcher), tests/test_vendor_adjustment.py (16 offline tests). Edited src/acumen/minute_backfill.py (backfill_symbol/_fetch_and_store_window take an optional adjustment_map and consume it; new rebuild_symbol_raw_with_map with a gate-1 identity guard), pyproject.toml (acumen-build-adjustment-map entry point), QUESTIONS.md (Q-11 ruling verbatim + RESOLVED-executed note), docs/gate_chunk5A_open8_evidence.md (FIX-4 section: mechanism, RELIANCE map + per-window numbers, TCS regression, honest FIX-2/FIX-3 premise corrections), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): built+persisted data/adjustment_maps/RELIANCE.json and TCS.json via live pulls; the operator's data/minute_store (TCS) was NOT rewritten (read-only regression). The NSE CA day-cache + daily store were read-only.
tests: 909 passed / 0 failed (was 885 at fix3; +24, all in test_vendor_adjustment.py -- 16 core + 8 adversarial-review regressions). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state. The live SmartAPI/NSE pulls are the operator runbook + scratchpad scripts, never pytest.
decisions:
  - B86 Per-event measurement, not policy (Q-11). `R(D)=fetched/raw` (fold HIGH/LOW vs the RAW daily store) is the direct observable = product of the vendor's applied factors in (D,F]. Days sharing an in-window event set form an ERA; the map resolves each event to {ours, measured k̂, not-applied} for price and (independently) volume, arbitrated by 2-paise price containment + gate-1's band. Working BACKWARDS from F; min-cost prefers ours>absent>measured; a source is decided at an event's newest appearance and carried older; ONLY a no-ours event (demerger) may FLIP measured<->absent across eras (the era-inconsistency). At most one freshly-solved unknown per era -> measurement, never free fitting.
  - B87 Single measured scalar per event = the MEDIAN over ALL its pre-ex probe days (phase-2 refinement), isolating the one event by dividing out the others' resolved factors -- exactly the ruling's "median ratio over pre-ex probe days". This tightened RELIANCE's 2016 era from 3->2 paise and is why the rights is ONE scalar (price 0.987305 / vol 0.987707) across the 2016 AND 2019 eras.
  - B88 Price provability uses the MEDIAN containment residual (not max): the fetched fold high/low can differ from the official daily high/low by a few paise on an odd day for a microstructure reason (an odd-lot/block trade the bhavcopy counts but the continuous 1-min series does not -- the same reason gate-1's band skews positive). A WRONG factor misses by hundreds of paise on EVERY day, so the median separates choices cleanly; the identity era's lone 10-paise day (2024-11-07) stays provable. The -0.1% gate-1 floor is NOT widened (ruling).
  - B89 Rights & demerger VOLUME are measured too (independently of price). The vendor scaled volume for the rights (measured 0.987707, != our TERP) and for the demerger in its recent era (0.907921) -- the exact reason FIX-3's share-count-only k_shares failed gate-1 at -1.24%/-10.14%. This RESOLVES decision B81 by measurement rather than a convention.
  - B90 Ingest consumes the map (`backfill_symbol(..., adjustment_map=...)` -> `unadjust_with_map`); the Q-10 factor-table path stays as the fallback for clean bonus/split-only symbols (TCS) and when no map exists. `rebuild_symbol_raw_with_map` carries a gate-1 IDENTITY GUARD (skip a day whose stored volume already reconciles -> already RAW), making a rebuild of an already-raw store a NO-OP and safe to re-run (unlike the Q-10 rebuild).
  - B91 Era key = the in-window event set {ex : D < ex <= F}; the map keys eras by it and a day whose era is not in the map (unprobed) or is un-provable returns None from factors_for_day -> gate 1 excludes and counts it (CONTEXT 7-E3, surgical clamp). A symbol whose vendor floor falls WITHIN one era key with no intervening event to split it (a demerger-only symbol) has a bimodal era -> no single factor reconciles -> un-provable + excluded (tested). An EMPTY in-window set (a recent post-last-CA day) is the exact identity (1,1) even without a probed identity era -- a recent RAW day is never wrongly excluded.
  - B92 Adversarial-review hardening (an in-session multi-agent review found real defects; all fixed with regression tests, none changed the RELIANCE/TCS results): (1) `_price_contained` now checks EVERY probe day within max(2 paise, 0.1% of raw) -- NOT the median -- so a BIMODAL era (the vendor floor inside one era key, a minority of days on a different factor that gate-1 volume cannot see) is caught and marked un-provable; the 0.1% relative floor still absorbs market microstructure (fold-vs-daily-high, ~0.01%) while rejecting the smallest wrong factor (~0.33%). (2) build_map RE-VALIDATES the phase-2-refined chain against the oracle before committing provable=True (the refinement can shift a scalar). (3) A PROBE GAP (an era introducing >1 event not covered by a newer era -> per-event attribution under-determined) is marked un-provable, never a fabricated decomposition. (4) `rebuild_symbol_raw_with_map`'s identity guard now checks price AND volume (a price-only special dividend leaves volume reconciled; a volume-only guard would skip it with price still adjusted -- a wrong price to the backtest) and skips a day with no raw daily row. (5) measure_era drops zero-volume/degenerate probe days (no div-by-zero); build_map refuses two events sharing an ex-date (STOP, not silent-collapse).
questions: **Q-11 RESOLVED -- executed** (ruling recorded verbatim in QUESTIONS.md BEFORE the code; every clause is code with a test behind it). No NEW class-A hole raised. NOTE for the architect (not a STOP): the map is committed per (symbol, era) and covers only the ERAS actually probed; chunk 5B must probe each demerger symbol's eras (the runbook generates one call per window) or those spans stay un-provable/excluded -- the disclosed surgical clamp. The vendor's demerger-adjustment FLOOR between 2019 and 2022 was not pinned (not needed: 2020-2021 RELIANCE minutes are not in the acceptance) -- chunk 5B pins it per symbol if it wants those spans.
gate: n/a -- chunk 5A has no TRADER gate. FIX-4 EXECUTES Q-11; acceptance + regression evidence are in docs/gate_chunk5A_open8_evidence.md (FIX-4 section) and QUESTIONS.md Q-11.
status-ledger: chunk 5A: built (+ fix Q-10 + fix2 k_shares + fix3 demerger-scope + fix4 Q-11 vendor-adjustment reconstruction, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + Q-10 fix + fix2 + fix3 + this FIX-4 are BUILT, not reviewed. `python -m pytest` is green (909/0) from a clean state, fully offline. FIX-4 EXECUTES Q-11: `vendor_adjustment.py` reconstructs the vendor's per-event minute adjustment by MEASUREMENT (the map: per event per era, {ours|measured|absent} for price and volume, oracle = 2-paise containment + gate-1 band). The LIVE RELIANCE acceptance passed on ALL six windows (2016-10, 2019-07, 2022-07, 2023-06, 2023-09, 2024-11): every probe day provable + gate-1 PASS + containment 0-2 paise; the map shows demerger absent 2016/2019, measured 0.9079 for 2022->2023-07, rights measured 0.9873 price + 0.9877 volume, bonuses ours 0.5. TCS regression: map {2018 bonus, ours, 0.5}, gate-1 year table unchanged (2016 100% / 2017 100% / 2018 97.2%), map-rebuild is a no-op (identity guard). NEXT ACTIONS: (1) the FRESH 5A review reviews the WHOLE of chunk 5A INCLUDING FIX-4 (personas/quant_reviewer.md + personas/code_reviewer.md, plan.md QC) -- pay attention to B86 (the measurement-vs-fitting boundary: <=1 fresh unknown per era), B87 (single-scalar refinement), B88 (median containment + the identity-era microstructure day), B89 (volume measured independently), B90 (map consumption + rebuild identity guard); (2) chunk 5B backfills the full universe on this ingest path -- for each demerger/rights symbol it builds+commits an AdjustmentMap (probing that symbol's eras) then backfills consuming it; a symbol whose eras it does not probe stays un-provable/excluded (disclosed surgical clamp); (3) chunk 6 (POC) remains unblocked (the store holds RAW same-day prices).

---

## [2026-07-25 19:30] chunk 5A · fix · done (with a STOP raised)
scope: chunk5A FIX-3, the single demerger-minute follow-up FIX-2 flagged. Executed the architect's Q-10 ADDENDUM 2 ruling -- demergers EXCLUDED from the minute un-adjustment (they were never in the k chains; the change is they no longer mark a minute span un-provable), while tier-2/pending rights still do and the daily bias-engine demerger suppression (CONTEXT 3.2) is untouched. Then ran the directed LIVE credentialed RELIANCE re-run through the fixed path -- which CONTRADICTED the ruling's premise and raised a new STOP (Q-11).
files: edited src/acumen/minute_unadjust.py (new PURE helper `unprovable_suppression_dates()` filters out KIND_DEMERGER; `unadjust_bars` routes through it; import KIND_DEMERGER; docstrings -- Provability bullet, arg docs, `_unknown_factor_between` reason string now "tier-2 unrecoverable rights"), src/acumen/minute_backfill.py (`rebuild_symbol_raw` uses the helper; `_print_symbol_factors` labels each suppression's minute effect), tests/test_minute_unadjust.py (replaced the demerger-marks-unprovable test with: a helper unit test, a demerger-provable-but-tier2-rights-still-unprovable test, and a cross-layer test asserting BOTH minutes-provable AND bias-pair-suppressed; added DailyStore/calendar/BiasEngine imports + a local `_daily_store` helper), QUESTIONS.md (Q-10 ADDENDUM 2 ruling verbatim + honest EXECUTED note + **new Q-11 STOP**), docs/gate_chunk5A_open8_evidence.md (FIX-3 section with the live tables), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): live SmartAPI 1-min pulls were SCRATCHPAD scripts only; the operator's data/minute_store (TCS) was NOT touched; the NSE CA day-cache + daily store were read-only.
tests: 885 passed / 0 failed (was 883 at fix2; net +2 -- replaced 1 demerger test with 2 focused ones + 1 cross-layer test). Fixtures touched: NONE. Full suite verified green OFFLINE. The live SmartAPI/NSE pulls are SCRATCHPAD scripts, never pytest.
decisions:
  - B84 Demerger-scope helper. `minute_unadjust.unprovable_suppression_dates(suppressions)` centralises the Q-10 ADDENDUM 2 rule (drop KIND_DEMERGER, keep tier-2 rights) in ONE place; `unadjust_bars` and `rebuild_symbol_raw` both call it, so the two paths cannot drift. Demergers were already absent from k_price/k_shares (a demerger is a Suppression, never a Factor), so "excluded from both k chains" needed no numeric change -- only the un-provability marking changed. The bias-engine suppression path (unions ALL suppressions incl. demergers) is a separate consumer and is untouched.
  - B85 FIX-3 LIVE VERDICT (credentialed SmartAPI, 2026-07-25) -- **CONTRADICTS the ruling's premise; STOP raised (Q-11).** Provability is now correct (every tested day provable, unprovable_days=[]), but gate-1 does NOT pass and the 2016-only "not demerger-adjusted" finding does not generalise: 2022-07 and 2023-06 (pre-demerger) un-adjust to ~0.908 (the Jio demerger factor still IN the 1-min feed) with gate-1 volume ~-10.1%; 2023-09 (post-demerger) un-adjusts to EXACTLY 1.00000 (gate-1 +0.00%); 2016/2019 stay ~0.997 (rights residual only, gate-1 ~-0.29% via k_price -- just past the -0.1% floor, decision B81). So the vendor's demerger adjustment is INCONSISTENT across its history. The FIX-3 change is gate-1-SAFE (every affected day is excluded + counted, nothing silently traded) but rescues NO RELIANCE minute data. Recorded verbatim numbers in QUESTIONS.md Q-11 and the evidence pack; executed the ruling as written (architect owns it) and flagged the contradiction rather than silently overriding.
questions: **Q-10 ADDENDUM 2 recorded** (ruling verbatim + honest EXECUTED note). **NEW Q-11 (class A, OPEN, STOP)**: the ruling's premise ("1-min feed not demerger-adjusted") is contradicted by live evidence -- the feed IS demerger-adjusted for 2020-2023 pre-demerger data but not 2016; no single demerger-scope rule un-adjusts RELIANCE correctly. gate-1 keeps it safe (excludes), but chunk 5B is BLOCKED on RELIANCE-like demerger symbols until the architect rules (options a-d in Q-11). Secondary: the pre-2020 rights residual (~0.29%) also fails gate-1's -0.1% floor (B81 / band-tuning question).
gate: n/a -- chunk 5A has no TRADER gate. This FIX-3 executes Q-10 ADDENDUM 2; acceptance + the STOP evidence are in docs/gate_chunk5A_open8_evidence.md (FIX-3 section) and QUESTIONS.md Q-11.
status-ledger: chunk 5A: built (+ fix Q-10 + fix2 k_shares + fix3 demerger-scope/Q-11-STOP, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + Q-10 fix + fix2 + this FIX-3 are BUILT, not reviewed. `python -m pytest` is green (885/0) from a clean state, fully offline. FIX-3 executes the demerger-scope ruling (demergers no longer mark minute spans un-provable; tier-2/pending rights still do; bias suppression untouched) and is gate-1-safe. **The single most important thing: the FIX-3 live re-run RAISED Q-11 (class A, STOP).** The SmartAPI 1-minute demerger adjustment is INCONSISTENT across history (2016 not adjusted, 2022-2023 adjusted), so the ruling's premise is wrong and no single demerger-scope rule un-adjusts RELIANCE correctly; gate-1 excludes the affected days (safe) but the demerger-exclusion rescues no RELIANCE data. NEXT ACTIONS: (1) the FRESH 5A review reviews the whole of chunk 5A INCLUDING this FIX-3 -- pay attention to B84 (the helper centralising the demerger filter) and B85 (the live contradiction); (2) the ARCHITECT rules Q-11 (era-aware demerger scope / demerger-aware clamp / accept-and-disclose / switch-source) BEFORE chunk 5B backfills RELIANCE-like demerger symbols; (3) chunk 5B on NON-demerger symbols is not blocked and can proceed on this ingest path once 5A is reviewed-PASS.

---

## [2026-07-25 18:40] chunk 5A · fix · done
scope: chunk5A FIX-2 (two items from the architect's reading of the FIX report). (1) VOLUME FACTOR REFINEMENT: split the single Q-10 k_cum into k_price (ALL factors -> divides price) and k_shares (SHARE-COUNT factors only: bonus/split/consolidation -> multiplies volume), so a special dividend no longer over-corrects volume. (2) EXECUTED the staged 4b RELIANCE probe LIVE (credentialed SmartAPI, polite pacing): the 2024-10 provable window reconciles, and the 2016 demerger probe returns a decisive verdict.
files: edited src/acumen/minute_unadjust.py (cumulative_factor gains a `kinds` filter; unadjust_price_paise uses k_price, unadjust_volume uses k_shares; unadjust_bars computes both per day; DayUnadjust.k_cum -> k_price + new k_shares field; identity = both==1; docstrings), src/acumen/corp_actions.py (new SHARE_COUNT_KINDS = {bonus, split}), src/acumen/minute_backfill.py (rebuild_symbol_raw identity-skip now requires k_price==1 AND k_shares==1; factor-print tags price+volume vs price-only; report strings), tests/test_minute_unadjust.py (special-dividend+bonus volume-by-bonus-only unit + gate-1 tests; kinds-filter test; k_cum->k_price/k_shares renames; DayUnadjust keyword constructions), QUESTIONS.md (Q-10 ADDENDUM: volume ruling verbatim + RELIANCE probe verdict), docs/gate_chunk5A_open8_evidence.md (FIX-2 section). CONTEXT.md/plan.md untouched; no file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored, not committed): refreshed the NSE CA day-cache under data/nse/ while building the RELIANCE factor table live; the operator's data/minute_store (TCS only) was NOT touched -- the probe used a throwaway scratchpad store.
tests: 883 passed / 0 failed (was 880 at chunk5A-fix; +3 net -- kinds-filter, special-dividend+bonus unadjust, gate-1-via-k_shares). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state (caches cleared first). The live SmartAPI/NSE pulls are a SCRATCHPAD SCRIPT, never pytest.
decisions:
  - B81 k_price/k_shares split. `corp_actions.SHARE_COUNT_KINDS = {KIND_BONUS, KIND_SPLIT}` is exactly the ruling's "bonus, split, consolidation" (a consolidation is a reverse split, already parsed as KIND_SPLIT). `cumulative_factor(..., kinds=None)` = k_price (all factors, divides price, unchanged from Q-10); `kinds=SHARE_COUNT_KINDS` = k_shares (multiplies volume). A special dividend is in k_price only, so it no longer over-corrects volume (the FIX-report hazard). TCS (bonus-only) is UNCHANGED because k_price==k_shares for it; F10 (post-CA identity) is UNCHANGED. A RIGHTS issue IS a share-count change but the ruling's verbatim enumeration lists only bonus/split/consolidation and a rights k is a TERP blend, not a clean share ratio -- executed OUT of k_shares as written, flagged for the architect.
  - B82 rebuild_symbol_raw identity-skip now requires BOTH k_price==1 AND k_shares==1. k_price alone would wrongly SKIP a day whose only in-window factor is a special dividend (k_shares==1 but k_price<1 -- the price still needs dividing). TCS never hits this (bonus-only), but the guard is now correct in general.
  - B83 4b RELIANCE VERDICT (live, credentialed, 2026-07-25). 2a (2024-10-25, bonus-only): un-adjusts to the raw daily H/L 268870/264400 EXACTLY (0 paise, within 2); gate-1 volume via k_shares gap 0.546% -> PASS. 2b (2016 demerger probe): median R_unadj (our-un-adjusted / raw daily) = 0.99666 (~1.000), NOT the ~0.908 a demerger-adjusted feed would show -> **the SmartAPI 1-MINUTE feed is NOT demerger-adjusted; full RELIANCE minute history is PROVABLE; the surgical clamp fallback is NOT triggered.** Corollary: this DIFFERS from the ONE_DAY feed (chunk-4: RELIANCE daily 2020-07-13 ratio 0.4539 IS demerger-adjusted) -- the two feeds adjust different event sets. 2c: identity-skip guard confirmed on real data (post-bonus 2024-11-04 byte-for-byte, skipped; pre-bonus 2024-10-25 rewritten). Minor caveat: ~0.33% price residual on pre-2020-rights days (vendor rights-equiv ~0.9873 vs our TERP 0.99061) -- within gate-1's band, tick-flagged not excluded.
questions: **Q-10 ADDENDUM recorded** (volume ruling verbatim; RELIANCE probe verdict with numbers -- QUESTIONS.md + evidence pack). No NEW class-A hole raised, but ONE follow-up FLAGGED for the architect (before chunk 5B): the ruling conditioned the un-provable/clamp fallback on "if the vendor demerger-adjusts"; 2b settles that = NO for minutes, so a pre-demerger RELIANCE minute day is actually provable -- yet the code still treats the demerger suppression as an unknown-factor event that marks such days UN-PROVABLE (gate-1 would exclude them; the clamp could move to post-2023, falsely dropping ~7 years of provable data). NOT changed this session (Class-A policy the architect ruled on conditionally; out of FIX-2's two-item scope). The demerger's bias-pair suppression in the daily/bias engine (CONTEXT 3.2) is a separate concern and stays.
gate: n/a -- chunk 5A has no TRADER gate. This FIX-2 executes the Q-10 addendum; acceptance evidence is docs/gate_chunk5A_open8_evidence.md (FIX-2 section).
status-ledger: chunk 5A: built (+ chunk5A-fix Q-10 + chunk5A-fix2 k_shares/RELIANCE-probe, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + the Q-10 fix + this FIX-2 are BUILT, not reviewed. `python -m pytest` is green (883/0) from a clean state and fully offline. The un-adjustment now splits k_price (price) from k_shares (volume, share-count only) per the architect's ruling; TCS/F10 identities are unchanged; the special-dividend over-correction is gone. The 4b RELIANCE probe ran LIVE and settled the open empirical question: the 1-minute feed is NOT demerger-adjusted, so full RELIANCE minute history is provable (no surgical clamp). NEXT ACTIONS: (1) the FRESH 5A review session reviews the whole of chunk 5A INCLUDING this FIX-2 -- pay attention to B81 (k_price/k_shares correctness; the rights-excluded-from-k_shares note), B82 (identity-skip needs both factors), and B83 (the live RELIANCE numbers); (2) the ARCHITECT rules the flagged follow-up (does a demerger still mark a MINUTE day un-provable, now that minutes are known NOT to be demerger-adjusted?) BEFORE chunk 5B backfills RELIANCE, else 5B will falsely exclude 2016-2023 RELIANCE minutes; (3) chunk 5B runs the full-universe backfill on this ingest path and will surface real demerger/pending-rights symbols.

---

## [2026-07-25 17:10] chunk 5A · fix · done
scope: executed the architect's Q-10 ruling (OPEN-8 remedy) -- un-adjust SmartAPI 1-minute candles back to RAW on ingest using the chunk-3 factor table, so the minute store holds RAW ONLY and CONTEXT 7-E11 stands unchanged. Built the pure un-adjustment core, wired it into the ingest path + a rebuild migration + a per-symbol factor-table assembler, added the fetch_date to the window ledger, and produced the TCS acceptance evidence on real data (gate-1 by year 0%/0%/56.5% -> 100%/100%/97.2%).
files: created src/acumen/minute_unadjust.py (PURE), tests/test_minute_unadjust.py; edited src/acumen/minute_backfill.py (SymbolFactors + build_symbol_factors; un-adjust in _fetch_and_store_window; rebuild_symbol_raw + unadjust_stored_day + gate1_by_year; --rebuild/--acceptance CLI + factor-table assembly in run()), src/acumen/minute_store.py (fetch_date column on the window ledger + tolerant read of a pre-column ledger; RAW docstring), tests/test_f10_volume_gate.py (the 4d identity assertion), QUESTIONS.md (Q-10 ruling verbatim + RESOLVED-executed), docs/gate_chunk5A_open8_evidence.md (Q-10 REMEDY section: ruling + 4a/4b/4c/4d), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; no file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored, not committed): rebuilt data/minute_store TCS to RAW in place (pre-2018-bonus days divided by k=0.5; post-bonus days identity/untouched); refreshed the NSE CA day-cache under data/nse/ca while assembling the TCS factor table.
tests: 880 passed / 0 failed (was 860 at the 5A build; +20 this fix -- 18 in test_minute_unadjust.py, +1 F10 identity, +1 pre-existing delta). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state (the un-adjust core is pure; the ingest/rebuild integration tests use fakes + a real tmp Parquet store; the live NSE-CA fetch + store rebuild are SCRIPTS, never pytest).
decisions:
  - B73 The un-adjustment is the exact INVERSE of chunk-3's pairwise adjust: the ruling's `k_cum = product of factors with ex-date in (D, F]` IS `corp_actions.factors_between(factors, D, F)` (the same half-open (previous, current] window the bias engine adjusts with). So minute_unadjust adds NO factor logic -- it divides price / multiplies volume by the same chunk-3 table. Decimal throughout, one half-even rounding at the end (CONTEXT 7-E11), exactly as adjust_pair rounds once.
  - B74 `k_cum == 1` (a recent, post-last-CA day whose factor window is empty) is the EXACT identity: fetched is returned byte-for-byte, and tick-snap is SKIPPED (never re-grid an already-raw price against a possibly-coarser current tick). This is why F10 (all 2026) is untouched -- asserted directly (test_f10_days_unadjust_to_the_exact_identity), and why gate-1 for 2019+ is byte-identical before/after.
  - B75 Tick-snap: an un-adjusted price within 2 paise of the nearest tick is snapped onto it (E11 "tick grid preserved"); one further off is LEFT as the divided value and the day is flagged+counted (never mis-snapped across > tolerance). TCS's current master tick is 0.10 but pre-2024 TCS traded on a 0.05 grid, so old TCS days flag (cosmetic diagnostic) rather than corrupt -- gate-1 is the correctness proof and passes 100%.
  - B76 Un-provable spans (an event with NO factor in (D, F] -- a demerger Suppression or a Q-6-pending rights) are marked un-provable: the partial un-adjustment is still stored so the day is visible, but gate-1 (the ruling's per-day PROOF) fails it -> excluded + counted (CONTEXT 7-E3). A systematically-failing leading span moves the symbol's minute clamp to post-event (systematic_unprovable_floor, the ruling's surgical fallback). TCS has neither, so nothing is excluded and the clamp is unchanged.
  - B77 The window ledger gained a `fetch_date` column (k_cum is fetch-dated). window_outcomes now reads the file's OWN schema and fills fetch_date=None for a ledger written before the column (the existing TCS ledger), so nothing breaks; fetch_date_for_day falls back to attempted_at.date() for those rows. New backfills record fetch_date explicitly (pinned by test).
  - B78 build_symbol_factors assembles the per-symbol table from the NSE CA history (opt-in, day-cached) with cum-closes read from the RAW daily store, so bonus/split/rights/special-dividend factors are concrete; demergers -> suppressions, unpriced rights -> pending ex-dates. TCS resolves to ONE factor: 2018-05-31 Bonus 1:1 k=0.5 (cum with a Rs 29 dividend; bonus takes parse precedence) -- no demergers, no pending rights, so TCS is fully provable.
  - B79 rebuild_symbol_raw un-adjusts an already-fetched store to RAW in place, SKIPPING identity days (k_cum==1: already raw, no rewrite) so it touches only the days that change (~pre-2018-bonus for TCS). The persisted TCS store now reads raw everywhere (2016-10-03 first-min open Rs 2450.00 not the adjusted Rs 1225; 2018-05-30 Rs 3509 -> 2018-06-01 Rs 1754, the raw bonus-halving). A Windows os.replace hiccup stopped an earlier run on an identity month (a no-op); the skip-identity fix is why it now only rewrites the meaningful days. Non-idempotent against a second run on an already-raw store -- guarded by checking 2016 gate-1 is still ~0% before rebuilding.
  - B80 4b (RELIANCE) needs a live credentialed SmartAPI re-pull, which this automated session did NOT run. The RELIANCE math is already proven via a golden on the REAL OPEN-8 numbers (fetched 2024-10-25 H/L 134435/132200 / 0.5 -> raw 268870/264400 to the paisa). The 2024-10 window is provable (bonus-only). A 2016 RELIANCE window is the demerger PROBE: if the vendor demerger-adjusts the 1-minute feed (as it does the daily feed) the window is un-provable -> excluded + clamp moves; if not, it reconciles 1.000x. The re-pull settles which; both are handled by the code. Flagged for the operator with exact commands in the evidence pack.
questions: **Q-10 RESOLVED -- executed** (ruling recorded verbatim in QUESTIONS.md BEFORE the code; un-adjust-on-ingest is code with tests behind every clause: raw_price = fetched/k_cum, raw_volume = fetched x k_cum, one half-even round, tick-snap both sides, fetch-date ledger, un-provable/suppression, identity). **OPEN-8 stays RESOLVED = ADJUSTED** (that was the finding); Q-10 is the remedy for the E11 contradiction the finding created. No new class-A hole raised. ONE empirical follow-up flagged (not a STOP): whether the SmartAPI 1-MINUTE feed demerger-adjusts (the OPEN-8 probe only tested a post-demerger RELIANCE 1-min day); the RELIANCE 2016 re-pull (4b) settles it, and the code already handles both outcomes.
gate: n/a -- chunk 5A has no TRADER gate. This fix EXECUTES the Q-10 ruling; the acceptance evidence pack is docs/gate_chunk5A_open8_evidence.md (Q-10 REMEDY section).
status-ledger: chunk 5A: built (+ chunk5A-fix Q-10, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + this Q-10 fix are BUILT, not reviewed. `python -m pytest` is green (880/0) from a clean state and fully offline. The TCS minute store now holds RAW same-day prices (rebuilt in place); gate-1 by year on the persisted store is 100%/100%/97.2% for 2016/2017/2018, unchanged (identity) for 2019+. CONTEXT 7-E11 is UNCHANGED (the store now satisfies it). Chunk 6 (POC) is UNBLOCKED by Q-10. NEXT ACTIONS: (1) a FRESH session reviews chunk 5A INCLUDING this fix against personas/quant_reviewer.md AND personas/code_reviewer.md (plan.md review type QC) and writes docs/reviews/REVIEW_5A.md -- pay special attention to the un-adjust=inverse-of-adjust identity (B73), the identity/tick-snap guards (B74/B75), the un-provable/clamp fallback (B76), and the fetch-date ledger back-compat (B77); (2) the OPERATOR runs the 4b RELIANCE re-pull to settle the demerger-minute question and, if it un-adjusts cleanly for the universe, proceeds; (3) chunk 5B (full-universe backfill) runs on the same un-adjusting ingest path -- it will build each symbol's factor table via build_symbol_factors and will surface real demerger/pending-rights symbols whose pre-event spans clamp (the surgical fallback), which chunk 5B must report. IMPORTANT for the reviewer: the un-adjust volume direction (x k_cum) is exactly per the ruling; note that dividend factors scale price but not vendor volume, so a symbol with a SPECIAL dividend in the window could see a small volume over-correction -- immaterial within gate-1's band, but a reviewer should weigh it (TCS has no special dividend in-window, so it does not arise here).
scope: recorded the trader's Round-3 receipts (OPEN-1 resolved=1000, Q32 confirmed, new Q-8/Q-9) and built chunk 5A -- the SmartAPI client, instrument-master loader, resumable 1-minute Parquet store + window ledger, the CONTEXT 4.5 gates 1-3, the 15-minute aggregator, and the REAL chunk-4 minute loader -- then ran the live TCS backfill Oct-2016->today and RESOLVED OPEN-8: the SmartAPI 1-minute feed is ADJUSTED (STOP).
files: created src/acumen/smartapi_client.py, src/acumen/instrument_master.py, src/acumen/aggregate.py, src/acumen/quality_gates.py, src/acumen/minute_store.py, src/acumen/minute_backfill.py, scripts/minute_backfill.py, tests/test_smartapi_client.py, tests/test_instrument_master.py, tests/test_aggregate.py, tests/test_quality_gates.py, tests/test_minute_store.py, tests/test_minute_backfill.py, tests/test_f10_volume_gate.py, tests/fixtures/instrument_master_sample.json, docs/gate_chunk5A_open8_evidence.md; edited (PART A prep, separate commit 2d55919) config.yaml + tests/test_config.py + QUESTIONS.md; edited (build) pyproject.toml (acumen-minute-backfill entry point), tests/test_fixture_integrity.py (digest pin), tests/fixtures/PROVENANCE.md, QUESTIONS.md (OPEN-8 resolution + Q-10), STATUS.md, PROGRESS.md. DATA (gitignored, not committed): built data/minute_store (TCS 2016-10-03..2026-07-25, ~906k 1-min candles) via the live run; cached the daily instrument master under cache/. No file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 860 passed / 0 failed (was 749 after PART A prep; +111 this chunk). Fixtures touched: NONE existing changed. ONE new frozen fixture (instrument_master_sample.json, a DERIVED cut of 5 real NSE -EQ rows + 2 distractors) added and digest-pinned; provenance in PROVENANCE.md. Full suite verified green OFFLINE (the socket guard is untouched; every test runs on frozen fixtures or injected fakes). The live SmartAPI pulls are SCRIPTS only, never pytest.
decisions:
  - B63 SIX-module split by the CONTEXT-6 pure/IO rule: smartapi_client (network I/O + pure parse_candles), instrument_master (download/cache + pure find_instrument), aggregate (PURE 15-min), quality_gates (PURE gates 1-3), minute_store (storage), minute_backfill (orchestration + CLI + the real minute_loader). Pure pieces test with no network; the network sits behind allow_network / injected fakes.
  - B64 The SmartAPI client is fully injectable (connect_factory / totp_provider / sleep), so its retry ladder, session-refresh, the ONE_DAY 00:00 trap and the +05:30->naive normalization are all tested offline against a fake SmartConnect. Credentials come from .env via config.env_value, are held in a self-redacting Credentials object, and never reach a repr/log/exception (CLAUDE.md rule 4).
  - B65 tick_size = master paise / 100 ALWAYS (CONTEXT 4.3), returned as an exact Decimal; the PoC's `tick/100 if tick>=1` heuristic is deliberately NOT carried, and a non-whole-paise tick raises rather than being rounded. The 5 F7 ticks reproduce tick_sizes.json (Q-2 tripwire); production reads the master.
  - B66 Minute store keyed symbol x month, per-DATE replacement (idempotent AND safe across the 30-day window seam -- two windows sharing a month never clobber), fsync-atomic writes, a per-symbol window ledger (present/empty/error; present+empty terminal, error retried) making the backfill resumable.
  - B67 Instrument selection matches exch_seg==NSE AND symbol==<SYMBOL>-EQ (both), so a BSE row of the same name or an NFO derivative sharing `name` is never chosen; missing -> raise, never a guess.
  - B68 **PoC-discovered live: SmartAPI ONE_MINUTE caps the RESPONSE at 8000 candles** and silently drops the oldest when a window exceeds it (a 30-day window returned exactly 8000, first-stamped 13:25). The backfill window is therefore 28 calendar days (<= 20 trading days = 7500 candles < 8000), which cannot truncate; a defensive flag fires if any window ever hits the cap, and gate 2 excludes any residual truncated day.
  - B69 Gate-3's candidate factor k is read from the RAW daily store's OWN price gap across the ex-date (raw_close(ex)/raw_close(pre-ex)), not an external constant -- self-contained and robust because RAW (ratio ~1.0) and ADJUSTED (~k) are far apart.
  - B70 OPEN-8 RESOLVED = ADJUSTED -> STOP. Recorded the verdict + numeric evidence in QUESTIONS.md and docs/gate_chunk5A_open8_evidence.md; raised Q-10 (the E11 raw-same-day contradiction) as class-A, BLOCKING chunk 6. NO un-adjustment applied and NO E11 edit made -- both are the architect's call (STOP rule). The store holds the feed as fetched, and gate 1 flags every affected old day.
  - B71 The vendor SmartConnect logs full request headers (Bearer JWT + API key) via logzero on a failed request; _quiet_library_logging raises those loggers above ERROR before any request, so a transient timeout cannot spill a credential (CLAUDE.md rule 4). Pinned by test.
  - B72 The real minute_loader returns the store's day as bias.Candle in stamp order, or None when the day has no candles (the R1-Q6 old-date carry) -- satisfying the chunk-4 MinuteLoader interface with no change to bias_engine.
  - Review-anticipation fixes (4 self-review findings, all confirmed + verified, fixed with regression tests before commit): gate-2 now tests ONLY high<low and close-outside-[low,high] (removed an unspecified open-in-range check); gate-2 no longer excludes a whole day for an out-of-session bar (CONTEXT 7-E2 is candle-level; a Muhurat-only day still excludes via missing>15); removed the dead _TRANSIENT_MARKERS list + its misleading docstring (unrecognized non-success replies default to retry, which is correct); pending_windows now compares the stored window_end so a later run with a larger --to re-fetches the extended tail instead of silently skipping it.
questions: **OPEN-1 RESOLVED** (risk_per_trade=1000, executed). **Q32 CONFIRMED** (Row Size 24). **OPEN-8 RESOLVED = ADJUSTED** (gate-3, STOP). **NEW Q-8** (POC 8- vs 9-candle window; blocks chunk-6 gate closure, not build). **NEW Q-9** (reference==POC above-branch; blocks one chunk-7 branch). **NEW Q-10** (class A, OPEN, STOP): OPEN-8=ADJUSTED contradicts CONTEXT 7-E11's raw-same-day intraday assumption -> BLOCKS chunk 6 until the architect chooses un-adjust-on-ingest (via the chunk-3 factor table) vs amend-E11 vs restrict-window vs switch-source.
gate: n/a -- chunk 5A has no TRADER gate. It RESOLVES OPEN-8, and the verdict is ADJUSTED (STOP): the evidence pack is docs/gate_chunk5A_open8_evidence.md and chunk 6 is blocked on Q-10.
status-ledger: chunk 5A: built
state-for-next-session: Chunk 5A is BUILT, not reviewed. `python -m pytest` is green (860/0) from a clean state and fully offline. History ends at `chunk5A: smartapi client and minute backfill (unreviewed)`; the prep commit `chunk5A-prep: record Round-3 receipts; set risk_per_trade=1000 (OPEN-1 resolved)` (2d55919) precedes it. GOLDENS all pass: F10 (25 PoC days re-derived through the new ingest+store+gate pipeline; every gap% in [+0.02%,+3.6%] and passing the band), the 15-min aggregator golden (TCS 2026-07-14 vs an independent groupby), the F7 tick cross-check, and the clamp golden (pre-2016-09 plans no windows). LIVE RUN (TCS): depth 2016-10-03, 2430 symbol-days to 2026-07-25, 129 windows all present/0 errors/~906k candles; gate-1 pass rate 0%/0%/56.5%/98-100% by year (the ADJUSTED signature); gate-2 excluded 18 genuine non-standard/partial days (Diwali Muhurat, 2020 COVID halts, today's open session). OPEN-8 = ADJUSTED with RELIANCE ratios 0.5/0.5/0.50011 and TCS pre-2018-bonus price x0.5 / volume x2. **The single most important thing for the next sessions:** chunk 6 (POC) is BLOCKED by Q-10 -- the stored 1-minute prices are back-adjusted, so for a day before a later CA they are NOT raw same-day rupees; the architect must resolve Q-10 (un-adjust vs amend E11) before chunk 6 consumes minute data. NEXT ACTIONS: (1) a FRESH session reviews chunk 5A against personas/quant_reviewer.md AND personas/code_reviewer.md (plan.md review type QC) and writes docs/reviews/REVIEW_5A.md -- pay special attention to the OPEN-8 evidence, the 8000-candle window cap (B68), and the four self-review fixes; (2) the architect resolves Q-10 before chunk 6; (3) chunk 5B (full-universe backfill) can proceed on the same infrastructure once 5A is reviewed-PASS (it will hit Q-10 the same way, and its gate-1 will flag the same old-year adjustment across the universe).

---

## [2026-07-25 13:38] ops · github · done
scope: connected the repo to its private GitHub mirror (origin = github.com/chinmoypaul8897/acumen), pushed the COMPLETE history + all five pass-tags, added a machinery-only README, and codified remote push discipline in CLAUDE.md. Not a chunk — no strategy/code/test changes.
files: created README.md; edited CLAUDE.md (Git rules: one appended bullet — remote push discipline) and PROGRESS.md (this entry). STATUS.md UNTOUCHED (ops, not a chunk). No file under src/, tests/, poc/, fixtures touched; CONTEXT.md/plan.md untouched.
tests: NOT rerun — zero code/test/fixture changes this session. Suite last verified green 748/0 at chunk4-pass (656bfbc), unchanged. Fixtures touched: NONE.
decisions:
  - OPS1 The architect-authorized CLAUDE.md text was appended as a SINGLE unwrapped bullet to match the existing Git-rules formatting (every other rule in that section is one line); the wording is verbatim — only the prompt's cosmetic line-wrapping was normalized, no word added/removed/changed. Flagged so a future reader comparing the prompt's multi-line quote to the one-line bullet does not read the whitespace as a deviation.
questions: none. No CONTEXT.md gap hit — this was a pure operations task with the remote URL, README shape, and CLAUDE.md text all fixed by the architect.
gate: n/a
status-ledger: STATUS.md UNTOUCHED — ops session, not a chunk. All chunk states unchanged (chunk 4 stays reviewed-PASS; chunk4-pass tag intact on the mirror).
state-for-next-session: Remote is LIVE and mirrored — pre-flight passed (clean tree, main, identity chinmoy-paul <chinmoypaul8897@gmail.com>; no secret/data/cache/logs path ever in history; .env ignored; five pass-tags all on "reviewed PASS" commits; repo 1.22 MiB). origin was verified EMPTY before the first push; after pushing, remote main == local main and every tag matches (side-by-side SHAs in this session's GITHUB OPS REPORT). PUSH RULE NOW IN FORCE (CLAUDE.md Git rules): EVERY session's final duty is `git push origin main --tags` and its final report must state the pushed SHA; the mirror is backup-only (no web-UI commits/edits/merges, no force-push; STOP and ask the architect on divergence; operator pushes manually if a session dies before pushing). NEXT ACTION unchanged from the chunk-4 review: building may proceed on chunks 5A/6 (depend only on chunk 2, reviewed-PASS) or chunk 7 (depends on 4+6); the chunk-4 TRADER GATE is still pending and must close before chunk 9's run. Whichever session runs next MUST push at end.

---

## [2026-07-25 11:30] chunk 4 · review · done
scope: fresh QC review of chunk 4 (quant_reviewer + code_reviewer, plan.md review type QC) covering FOUR commits d2ea387..a0b595a -- reran the suite from clean and offline, executed all 11 architect-directed checks, reimplemented CONTEXT 3.2 from scratch to grade F9 (15/15, 0 mismatches), built the 20-mutant operator-by-operator boundary matrix on bias.py (10 caught / 4 equivalent / 6 genuine coverage gaps), brute-forced the precedence-guard/RULE_CARRY unreachability over 261,282 candles, re-derived the Q-6/Q-7 redistribution (rights 5/1/1, denominator 296), formally reviewed the incident commit (fsync-before-replace both writers, --rebuild-ledger on a synthetic broken store, quarantine-never-deletes, corrupt-ledger names the command), AST-proved bias.py purity, judged B53-B62, wrote docs/reviews/REVIEW_4.md. VERDICT PASS.
files: created docs/reviews/REVIEW_4.md, tests/test_review4_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; CONTEXT.md/plan.md untouched.
tests: 748 passed / 0 failed (739 from the build reran green unchanged + 9 reviewer probes), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE -- all 7 new chunk-4 fixture digests recomputed independently and match their pins; git diff over tests/fixtures+poc/data across the span shows only ADDED files. The 9 new tests were verified to KILL their mutants: re-applying M13 (>= bmin -> > bmin) and M17 (first-break > -> >=) turned the corresponding tripwires red; bias.py restored byte-identical to HEAD after.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review4_probes.py (9 tests). It closes the six Rule-3/tie/first-break boundary coverage gaps the mutation matrix found (M13-M18: R3 close guards exactly on bodyMin/bodyMax; strict-break = a 1-min touch of P's extreme is not a break), pins the precedence-guard/RULE_CARRY unreachability over a dense deterministic grid (the build's property test covered only RULE_CARRY, randomly), and pins the demerger AND Q-6 tier-2 rights suppression RESUME at E+3 (the build pinned blocking at E+1 but not resume). Every one passes on HEAD and was verified to fail on its mutant.
  - The mutation matrix ran in an isolated git worktree; all other probes read the frozen fixtures, the operator store read-only, or a hermetic tmp_path store. ZERO network traffic. The worktree was restored and removed; bias.py is byte-identical to HEAD.
questions: none raised. Q-6/Q-7 are correctly RESOLVED and EXECUTED -- the ruling texts are verbatim in QUESTIONS.md in the prep commit that precedes the build, and every tier is code with a test behind it (tier-1 face reconstruction defuses the GREENPLY trap, tier-2 emits a Suppression consumed like a demerger, tier-3 overrides refuse an uncited entry; Q-7's three bands classify by D/P_cum with the 2% boundary inclusive). OPEN-8 evidence recorded. No new class-A hole hit: every bias operator matches CONTEXT 3.2 character-for-character.
gate: pending -- the chunk-4 TRADER GATE is a SEPARATE field from code review. Code review is PASS; the gate stays OPEN. The trader must confirm the F9 real-day bias table AND the two OPEN-4 assumptions (green-tie mirror -> bearish; doji -> carry) via the evidence pack docs/gate_chunk4_bias_evidence.md before the chunk-9 run (M3, plan.md §2). Gate outcome will be recorded in STATUS.md as gate-closed(evidence path) by a later session.
status-ledger: chunk 4: reviewed-PASS
state-for-next-session: Chunk 4 is reviewed-PASS at tag chunk4-pass; python -m pytest is green (748/0) from a clean state and fully offline. Six findings are in docs/reviews/REVIEW_4.md -- none blocking, none requiring a fix session. F1 is LOW (six Rule-3/tie/first-break boundary coverage gaps, closed in-review by the added probe file; the code was already correct at every boundary). F2 is LOW (suppression matches on exact trading-date equality; a demerger/rights ex-date on a NON-trading day would evade the block -- unreachable today since NSE ex-dates are trading days, but chunk 9 should assert/normalize it). F3-F6 INFO: (F3) the frozen-window dividend band counts 245/26/18 are not reproducible against the non-frozen store (re-run 197/49/26+17 pending; both sum 289; the classifier is correct and the counts were never a committed test -- do not treat them as an anchor; the rights 5/1/1 and denominator 296 ARE reproducible); (F4) F9 is adjustment-free so the real-CA-in-window pairwise path is only synthetic-tested -- real-day R3 + CA verification is chunk 12; (F5) OPEN-4 green-tie/doji remain UNCONFIRMED assumptions -- the trader gate must close before chunk 9; (F6) the corrupt-ledger error embeds the pyarrow exception type as context, intentional. NEXT ACTIONS: (1) Paul relays the chunk-4 evidence pack to the trader and records the CONFIRMED gate outcome; (2) building may proceed on chunks 5A/6 (both depend only on chunk 2, reviewed-PASS) or chunk 7 (depends on 4+6) -- chunk 4 being reviewed-PASS unblocks its dependents even though the trader gate is still pending (plan.md §2: code review-PASS unblocks building; the gate must close before chunk 9's run).

---

## [2026-07-25 10:25] chunk 4 · build · done
scope: recorded+executed the architect's Q-6 (rights, three tiers) and Q-7 (special dividend, D/P_cum) rulings on the CA-engine side, fixed REVIEW_3 F3, appended the Q-4 IL/D1 note, built and verified the curated data-quirk mechanism and ingested the store's last error date (2020-07-13), then built chunk 4 proper: the CONTEXT 3.2 bias engine as pure functions, its orchestration layer, and the F5/F9 goldens + the trader-gate evidence pack.
files: created src/acumen/bias.py (pure engine), src/acumen/bias_engine.py (orchestration + minute-loader interface + csv_minute_loader), src/acumen/data_quirks.py, src/acumen/rights_overrides.json (committed, empty), tests/test_bias.py, tests/test_bias_engine.py, tests/test_data_quirks.py, tests/test_data_quirks_roundtrip.py, tests/fixtures/f9_tcs_daily.csv, tests/fixtures/f9_tcs_expected.csv, tests/fixtures/quirk_2020-07-13_archive_cut.csv, tests/fixtures/smartapi_oneday_TCS_2020-07-13.json, tests/fixtures/smartapi_oneday_RELIANCE_2020-07-13.json, tests/fixtures/minute/SYNTH_2099-01-05_1min.csv, tests/fixtures/minute/SYNTH_2099-01-06_1min.csv, docs/gate_chunk4_bias_evidence.md; edited src/acumen/corp_actions.py (Q-6/Q-7 execution + F3), src/acumen/ca_report.py (suppressions + unresolved-rights report), src/acumen/bhavcopy.py (quirk hook), QUESTIONS.md (Q-6/Q-7 rulings, Q-4 note, OPEN-8 evidence), tests/test_corp_actions.py, tests/test_ca_goldens.py, tests/test_series_selection.py, tests/test_fixture_integrity.py, tests/fixtures/PROVENANCE.md, STATUS.md. DATA (gitignored, not committed): ingested exactly one date into data/daily_store (2020-07-13, architect-authorized); no other store write. No file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 739 passed / 0 failed (was 686 after the incident; +53 this chunk). Fixtures touched: NONE existing changed -- all pinned digests intact. SEVEN new frozen fixtures added and digest-pinned (2 F9 DERIVED cuts, 2 SmartAPI DERIVED responses, 1 malformed-archive DERIVED cut, 2 SYNTHETIC minute CSVs); provenance for each in PROVENANCE.md. The one ingested store date is data, not a fixture.
decisions:
  - B53 Q-6 tier 1 reconstructs the face value AT a rights ex-date from the symbol's parsed FACE-VALUE-SPLIT history (reconstruct_face_value_paise), NOT from the row's as-of-query faceVal, which is the GREENPLY trap this item documents. The stale faceVal is used ONLY when no split touches the symbol (nothing moved it). S = face + premium; the F3 guard then rejects any S >= P.
  - B54 Q-6 tier 2 emits a Suppression (new dataclass) exactly like a demerger; suppression_dates() unions both, and the bias engine suppresses D where D-1 or D-2 is a suppression ex-date. A rights issue we cannot price is treated like a demerger (no factor, no trade across it), never guessed.
  - B55 Q-6 tier 3 is a committed EMPTY rights_overrides.json; load_rights_overrides() RAISES on any entry that cites no NSE circular. An override that cannot be audited to a circular is the silent guess the whole item forbids.
  - B56 Q-7 removes the pre-announcement close from factor_for entirely (it exists in no held source); a dividend is classified by D/P_cum in three bands (<1% ordinary-fast, 1-2% ordinary-near-threshold, >=2% special). classification is carried on Factor so special_dividend_verifications() needs no re-derivation. The special formula k=1-D/P_cum is unchanged.
  - B57 The bias engine is TWO modules: bias.py is pure (candles in, bias out; the injected minute_loader is a zero-arg provider so evaluate_pair stays pure and calls it only on a Rule-3 day) and bias_engine.py does the I/O (store reads, pairwise adjustment, suppression, seeding). This is CLAUDE.md's pure-engine rule made structural.
  - B58 evaluate_pair follows CONTEXT 3.2's order literally: inside -> R1 -> R2 -> R3 -> carry. The precedence note falls out for free -- an outside bar closing beyond the body is decided by R1 because R1 runs first (verified: the minute loader is not even consulted). RULE_CARRY ("no rule fires") is provably unreachable for a single-sweep bar and is kept only as the spec's catch-all (property test over 5000 random candles).
  - B59 The Rule-3 tie predicate is close-vs-open on the tie 1-minute candle (the trader's Q31 authority): red -> bullish (his answer), green -> bearish (OPEN-4 mirror, flagged), doji -> carry + log (OPEN-4). Both OPEN-4 branches set an open4 flag the evidence pack surfaces for confirmation.
  - B60 F9 is a CONTIGUOUS real-TCS window (2025-06-25..2025-08-29) frozen as a DERIVED cut, chosen adjustment-free (TCS had no split/bonus/special-dividend; max day-over-day gap 7.0% is a genuine move), so raw == adjusted and the trader checks the same numbers on his chart. 15 days selected with a comfortable margin on the deciding inequality -- no boundary-equal case (those live in test_bias.py as synthetic candles). The F9 test is hermetic: it loads the cut into a temp store, so a bare clone runs it with no data/daily_store.
  - B61 The data-quirk mechanism (data_quirks.py) is consulted ONLY after normal validation fails and corrects ONLY a file malformed in exactly the documented way; the store validator is never weakened. Entry 1 (2020-07-13, content year 0020->2020) was verified genuine three ways before the one-date ingest -- see the OPEN-8 note below.
  - B62 The pairwise CA adjustment in the orchestration adjusts the PREVIOUS candle into the CURRENT candle's scale via factors_between + adjust_pair (chunk 3), so the pure engine only ever compares numbers in one scale (CONTEXT 3.2 / 7-E11). Empty factors = no adjustment, which is the correct and tested behaviour for the F9 window.
questions: **Q-6 RESOLVED -- executed** (three-tier rights: recoverable-S via face reconstruction + suppression + committed overrides; the ruling text is verbatim in QUESTIONS.md before the code). **Q-7 RESOLVED -- executed** (D/P_cum classifier + verification list; ruling verbatim). **Q-4** gained the IL/D1 note. **OPEN-8 evidence recorded** in QUESTIONS.md at the architect's direction (CONTEXT ownership stays with the architect): SmartAPI ONE_DAY candles are corporate-action back-adjusted -- RELIANCE 2020-07-13 raw 1935 vs SmartAPI 878.36 (ratio 0.454 = 1:1 bonus x Jio demerger); this answers DAILIES only, and chunk 5A gate-3 (1-min adjustment status) still runs in full. **Gate lesson for chunk 5A's builder:** cross-source price comparisons are only valid raw-to-raw with no intervening corporate-action events, or after bringing both sides to the same scale. No NEW class-A hole was hit in PART B -- CONTEXT 3.2 answered every rule, and the one real outside-bar day in the F9 window correctly took the documented missing-1-min carry.
gate: pending -- evidence pack at docs/gate_chunk4_bias_evidence.md (15 real TCS days in plain English + the two OPEN-4 confirmations). Deadline per plan.md §2: before chunk 9's run. Paul relays to the trader; the ask is "reply CONFIRMED or tell us which row looks wrong".
status-ledger: chunk 4: built
state-for-next-session: Chunk 4 is BUILT, not reviewed. `python -m pytest` is green (739/0) from a clean state and fully offline. History ends at `chunk4: bias engine (unreviewed)`; two prep commits precede it (`chunk4-prep: record+execute Q-6/Q-7 rulings; REVIEW_3 F3; Q-4 series note` and `chunk4-prep: curated data-quirk mechanism; ingest 2020-07-13 ...`). GOLDENS: F5 (the Q31 tie, CONTEXT 8 exact numbers, via the synthetic red-tie minute fixture -> BULLISH) and F9 (15 real TCS days, engine matches every hand computation) both pass. The store now reads error: 0 (6606 file-present, 3096 confirmed-404, span 2000-01-01..2026-07-24) after the 2020-07-13 ingest. Known limits a reviewer should weigh: (a) the R3 and tie goldens are SYNTHETIC minute fixtures -- real-day Rule-3 verification is chunk 12 (per the card); (b) F9 is adjustment-free by construction, so the orchestration's pairwise-adjustment path is exercised only by a synthetic split test, not by a real CA-in-window day; (c) the SmartAPI ONE_DAY evidence was a one-off read-only call and only two symbols; (d) the frozen-window redistribution numbers were computed against the operator's store this session (read-only) and are reported below, not committed as a test (the store is not a fixture). NEXT ACTION: a FRESH session reviews chunk 4 against `personas/quant_reviewer.md` AND `personas/code_reviewer.md` (plan.md review type QC) and writes `docs/reviews/REVIEW_4.md` -- chunk 5A/6/7 gating aside, the chunk-4 TRADER GATE must be closed (trader CONFIRMED) before chunk 9's run (M3).

---

## [2026-07-25 02:40] incident · fix · done
scope: recovered data/daily_store after a power-loss truncated its ledger mid-backfill (REVIEW_2 F6 manifesting; architect approved pulling the F6 fix forward from chunk 13). Diagnosed read-only, quarantined the corrupt files (never deleted), rebuilt the ledger from the surviving monthlies via a new permanent --rebuild-ledger CLI, hardened atomic_io with fsync, and made a corrupt ledger raise clear guidance instead of a pyarrow traceback. Scope was recovery + the F6 hardening ONLY — no strategy code, no chunk work.
files: edited src/acumen/atomic_io.py (fsync file before os.replace in both writers; best-effort POSIX dir fsync; Windows limitation documented), src/acumen/daily_store.py (DailyStore.rebuild_ledger_from_monthlies(); startup-integrity error in outcomes() naming --rebuild-ledger; REBUILD_LEDGER_COMMAND + REBUILT_LEDGER_REASON constants; import url_for), src/acumen/backfill_daily.py (--rebuild-ledger flag + _rebuild_ledger printer; --from/--to now optional with a clear error; main() maps DailyStoreError to a clean exit 2); created tests/test_incident_recovery.py (12 tests). DATA (gitignored, not committed): quarantined 3 corrupt files to data/daily_store/_quarantine/2026-07-25/ with a MANIFEST; wrote a fresh data/daily_store/ledger.parquet. NO fixture touched; no file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 686 passed / 0 failed (was 674 at the chunk-3 review; +12 incident). Fixtures touched: NONE. The 12 new tests cover: fsync-precedes-rename asserted via monkeypatch (both writers); a failed fsync leaves the prior file intact; a corrupt ledger raises DailyStoreError naming --rebuild-ledger (not a raw pyarrow traceback); main() exits 2 with guidance; rebuild marks present dates and returns 404/error/absent dates to pending; rebuild skips a corrupt month; rebuild from a QUARANTINED month; rebuilt ledger reads back through the normal path; the --rebuild-ledger CLI needs no dates/network and reports a skipped corrupt month; a normal run with no dates asks for them or --rebuild-ledger.
decisions:
  - I1 Quarantine lives OUTSIDE the store's daily/ tree (data/daily_store/_quarantine/2026-07-25/, path-preserving + MANIFEST.txt), so the rebuild's `daily/*/bhavcopy_*.parquet` glob never re-scans it. Corrupt files are MOVED, never deleted (they are the only forensic record of the failure).
  - I2 The rebuild DERIVES the ledger from the row files alone and calls _write_ledger directly — it never reads the old ledger, so it works precisely when the ledger is the corrupt file. Each recovered date is file-present with source_format read from its stored rows, url reconstructed via url_for(day, fmt), http_status=200, row_count = that date's row count, reason=REBUILT_LEDGER_REASON (provenance), and attempted_at=None (the original fetch time is unknown; inventing one would be a lie the ledger later reads as fact).
  - I3 A monthly that will not open is SKIPPED and reported, not fatal — so --rebuild-ledger is safe even if a corrupt month was not quarantined first (its dates just return to pending). Lost confirmed-404/error knowledge is not fabricated; those dates become absent and re-resolve from fresh evidence on the next run (Q-3 safeguard 1 preserved).
  - I4 atomic_io fsyncs the file's DATA before os.replace in BOTH atomic_write_bytes and atomic_write_with (the un-fsynced path was the F6 hole every store/ledger write went through); the directory fsync that makes the rename itself durable is POSIX-only best-effort — Windows cannot open a directory fd and NTFS journals the rename, so it is a documented no-op there. The data-durability half, which is what prevents the incident's valid-name-over-garbage failure, runs on both platforms. This machine is Windows, so only the file fsync is active here; that is the half that mattered.
  - I5 --from/--to became optional at the parser so --rebuild-ledger can run range-free and offline; a normal run missing them prints a clear error naming --rebuild-ledger and exits 2. main() catches DailyStoreError and prints the guidance (exit 2) rather than letting a traceback reach the operator.
  - I6 The corrupt raw_bhav CSV (2009-03-13_archive.csv) was quarantined too, though it is the operator audit trail (--raw-dir), not the store the rebuild reads. It is a genuine corrupt power-loss artifact and a fixture-provenance hazard (REVIEW_2 F3, since the DERIVED fixtures were cut from that archive), so leaving it in place could poison a future fixture cut. Recorded as distinct from the two store files that drove recovery.
questions: none. Scope was fixed by the architect (recovery + F6 only) and CONTEXT.md answered everything needed (Q-3 ledger semantics govern what "pending again" means; F6's own text specified the fix). No new class-A hole hit; REVIEW_2 F7 remains open and is now partially mitigated (noted in its fix log) but not closed.
gate: n/a
status-ledger: STATUS.md UNTOUCHED — this is an incident recovery, not a chunk. Chunk 3 stays reviewed-PASS.
state-for-next-session: The store is recovered and consistent. data/daily_store/ledger.parquet reads cleanly and holds 2,313 file-present dates (2,303 archive + 10 udiff, span 2000-01-03 .. 2026-07-24); its file-present set equals the distinct date set across the 112 surviving monthlies exactly. The corrupt trio (ledger, daily/2009/bhavcopy_2009-03.parquet, raw_bhav/2009-03-13_archive.csv) sits in data/daily_store/_quarantine/2026-07-25/ with a MANIFEST. March-2009 (incl. the incident date 2009-03-13) and every un-backfilled date are pending again. `python -m pytest` is green (686/0). NEXT ACTION for the OPERATOR — resume the backfill; it skips the 2,313 settled dates and re-fetches 2009-03 onward through the new fsync-safe path: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (add `--raw-dir data\raw_bhav` if you want the audit trail; note it is not resumable — REVIEW_2 F3). If a ledger is ever truncated again, one command recovers it: `python scripts/backfill_daily.py --rebuild-ledger --store data\daily_store`. NEXT ACTION for the PROJECT is unchanged: build chunk 4 (bias engine), which still depends only on chunk 3 (reviewed-PASS).

---

## [2026-07-25 02:00] chunk 3 · review · done
scope: fresh QC review of chunk 3 (quant_reviewer + code_reviewer, plan.md review type QC) — reran the suite from clean and offline, executed all 11 architect-directed checks, opened NSE's calculator XLSX with openpyxl and recomputed our k against EVERY worked cell, hand-recomputed the five parser events and the three adjustment gaps from the raw snapshots, mutation-tested the Indian bonus convention (13 catchers) plus 17 more spec/discipline invariants in a throwaway copy, reproduced the 27/282/24/0 join partition and both shared-ratio tallies, re-attacked REVIEW_2 M-F1/M-F2, judged B35–B52, wrote docs/reviews/REVIEW_3.md. VERDICT PASS.
files: created docs/reviews/REVIEW_3.md, tests/test_review3_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 674 passed / 0 failed (668 from the build reran green unchanged + 6 reviewer probes), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE — all 28 poc/data CSVs, the 3 chunk-1 files, the 13 new chunk-3 fixtures and the XLSX oracle match their pinned SHA-256s (recomputed independently, 20/20 for chunk-1/2/3). The 6 new tests were verified to KILL the one surviving mutant from my run (round-per-event instead of round-once): 3 of them fail on that mutation, all pass on HEAD.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review3_probes.py (6 tests). It closes the single coverage gap this review found — adjust_pair's "round the chain once" (B46) had no tripwire because the build's own chain test used exact intermediates (0.5, 0.2) — plus keeps the strongest adversarial probes: an inexact-chain round-once pin, a half-even tie pin, the debt-series-cannot-factor-the-equity case (TATASTEEL-N2), a multi-ratio Indian-convention pin, and the pairwise-is-the-law inequality note.
  - openpyxl was installed into the REVIEW's own scratchpad via `pip install --target`, never into the operator's interpreter (confirmed `import openpyxl` still fails there). The XLSX was read cell-by-cell independently of the build's zipfile+ElementTree reader, and our k reproduced every worked strike/lot/future cell of all three sheets, confirming k=1/AF for bonus+split and k=AF for rights from the sheets' own formulas.
  - This review issued ZERO network traffic. M-F1 and M-F2 were re-attacked on a virtual clock with a fake session; the scratch-store discipline was proven by observing the main-store ledger grow ~15 bytes/2s with NO test running (the operator's concurrent backfill, PID 25236), and by static analysis that no chunk-3 module writes data/daily_store.
questions: none raised. Q-4 and Q-5 are correctly RESOLVED and EXECUTED — the ruling texts are verbatim in QUESTIONS.md in the prep commit that precedes any corp_actions code, and every clause is code with a test behind it (whitelist selection incl. the six-series NTPC pick, the two-whitelist raise, the debt-only empty result, the unknown-series surfacing incl. IL/D1, and the Q-5 excluded-Saturday regression = REVIEW_2 F11's exact case). Q-6 and Q-7 are correctly RAISED and open — 296 of 328 priced events are pending with named reasons, and grep+AST confirm no k=1 default hides on the rights/dividend paths.
gate: n/a
status-ledger: chunk 3: reviewed-PASS
state-for-next-session: Chunk 3 is reviewed-PASS at tag chunk3-pass; python -m pytest is green (674/0) from a clean state and fully offline. Four findings are in docs/reviews/REVIEW_3.md — none blocking, none requiring a fix session. F1 is LOW (a test-coverage gap on B46's round-once, closed in-review by the added probe file; the code was already correct). F2/F3/F4 are INFO: (F2) pairwise adjustment is the spec-defined comparison and a fully-adjusted series can differ by a rounding tie — a note for chunks 4/9, with a kept regression test; (F3) rights_factor returns k>1 for S>=P without raising — economically impossible and unreachable since all rights are Q-6-pending, flagged for whoever answers Q-6; (F4) the inherited REVIEW_2 F3-resume-half / F6-fsync / F7-read-side-invariant remain open and are honestly carried in that fix log. NEXT ACTION: build chunk 4 (bias engine, review type QC, TRADER GATE) — it depends only on chunk 3, which is now reviewed-PASS. Chunk 4 is the first session that consumes factors on real dates; it will hit Q-6/Q-7 the moment a real rights issue or a special dividend falls inside a bias pair — both are STOP-blocked and surfaced as pending, not guessed. Paul's 25-year bhavcopy backfill was still running against data/daily_store throughout this review (PID 25236) and was never touched.

---

## [2026-07-25 00:45] chunk 3 · build · done
scope: recorded the architect's Q-4 and Q-5 rulings verbatim and EXECUTED both, applied the six REVIEW_2 findings the architect scheduled, then built chunk 3 — the corporate-action engine: three fetchers with the repo's gentle/cached/opt-in discipline, ten verbatim source snapshots, the subject parser with its exception report, the CONTEXT 4.2 factor table, `adjust_pair`, the demerger table, fixture F8 against NSE's own calculator, and the cross-source join.
files: created src/acumen/corp_actions.py, src/acumen/ca_report.py, src/acumen/backfill_daily.py (moved out of scripts/), tests/test_corp_actions.py, tests/test_ca_goldens.py, tests/test_series_selection.py, tests/fixtures/series_edge_cases_synthetic.csv, tests/fixtures/ca/ (10 VERBATIM snapshots + 2 DERIVED bhavcopy cuts), docs/nse_adjustment_calculator.xlsx; edited src/acumen/daily_store.py (Q-4 selection + series report + weekend count), src/acumen/calendar.py (Q-5 exclusion + counts), src/acumen/nse_http.py (F1 pacing, F2 retry policy, cached_json fetcher hook), src/acumen/bhavcopy.py (F4 example), scripts/backfill_daily.py (now a launcher), pyproject.toml (two console entry points), tests/test_backfill_script.py, tests/test_bhavcopy.py, tests/test_daily_store.py, tests/test_derived_calendar.py, tests/test_review2_probes.py, tests/test_fixture_integrity.py, tests/fixtures/PROVENANCE.md, QUESTIONS.md, docs/reviews/REVIEW_2.md (append-only fix log), STATUS.md, PROGRESS.md. No file under poc/ was touched; **no write of any kind to data/daily_store**.
tests: 668 passed / 0 failed (was 513 at the chunk-2 review; +38 prep, +117 chunk 3), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE existing — all 28 poc/data CSVs and all 6 chunk-1/2 files still match their pinned SHA-256s. Thirteen NEW frozen files added and pinned by digest in the same commits (1 SYNTHETIC for the Q-4 edge cases, 10 VERBATIM CA snapshots, 2 DERIVED bhavcopy cuts), plus the F8 oracle XLSX pinned in docs/. Provenance for every one is in tests/fixtures/PROVENANCE.md, including each source CSV's own digest, line count and kept-row count.
decisions:
  - B35 The Q-4 whitelist's RAISE takes priority over its precedence order. The ruling states both "EQ, else BE, else BZ" and "two whitelist series on one symbol-date → raise loudly"; since the raise fires first, the order never actually decides anything. It is kept as the documented reading of the ruling and as the report's ordering, not as live logic.
  - B36 `INSTRUMENT_SERIES` is one module constant and the ONLY series choice in `src/` (pinned by an `ast` walk). The non-instrument families are matched by FAMILY (`N*` debt, `P*` partly-paid, `BL`) rather than by an enumerated list, because NSE mints codes within a family freely — NTPC alone carried N4/N6/N7/NB/NC/ND across two days.
  - B37 `coverage_summary` counts weekend sessions from the LEDGER only (cheap, opens no month file); the series report is a separate, heavier query the backfill run calls once. A summary that had to read 300 month files would stop being callable after a Ctrl-C.
  - B38 The backfill implementation moved into the package; `scripts/backfill_daily.py` is a launcher whose module body executes nothing, with the `sys.path` fallback INSIDE a function that only `__main__` calls and that fires only when the package is not installed. Removing it entirely needs `pip install -e .`, and this session would not change the operator's environment while his 25-year backfill was running against that exact command (REVIEW_2 F12, half closed — stated as such in the fix log).
  - B39 `nse_http.cached_json` gained a `fetcher` hook rather than corp_actions growing its own cache. Three sources need three sessions, two decoders and their own pacing; only the POLICY (one live pull per day, opt-in, atomic write) must be shared, and now it provably is.
  - B40 Parse precedence puts DEMERGER first, ahead of every ratio kind. A demerger suppresses trading entirely (CONTEXT 3.2) while every other kind only scales a price, so a missed demerger is the costliest possible miss; "Scheme Of Arrangement / Bonus 1:2" resolves to demerger, and there is a test for exactly that.
  - B41 A dividend subject's cash components are SUMMED per ex-date ("Final Dividend Rs 6 Plus Special Dividend Rs 4" → 1000 paise). CONTEXT 4.2 classifies by SIZE against a reference price, not by the word "special", so what its threshold needs is the total cash per share leaving the company that day.
  - B42 The `informational` bucket requires BOTH a recognised meeting phrase AND no amount AND no ratio. Without the second half it would become a dustbin: "Annual General Meeting / Something Rs 5 Per Share" raises instead, because a subject carrying money this parser did not understand must be reported.
  - B43 A consolidation (reverse split) is treated as CONTEXT 4.2's own "face value A→B, k = B/A" row, but ONLY when the subject carries the From/To values a split needs. BSE's bare "Consolidation of Shares" therefore stays an exception rather than becoming a guess.
  - B44 The money regex accepts BSE's `Rs. - 5.5000` dash form. Measured: without it, every one of the 348 BSE dividend rows in the frozen windows became a parse exception — a parser bug wearing an exception report as camouflage.
  - B45 Corporate actions on a non-instrument SERIES are filtered out before parsing (opt-out via a flag). A coupon on a listed NCD is not an action on the equity we trade; this is the Q-4 ruling applied to a second data source, and it is what keeps the exception report readable — it removed every GS/IV/RR distribution row without a single special case.
  - B46 `adjust_pair` multiplies the whole factor chain at full `Decimal` precision and rounds ONCE, half-even, to integer paise (CONTEXT 7-E11). Rounding per event lets two chained events drift a paisa; half-even is unbiased, so a 25-year history of adjustments does not creep upward. An empty chain returns the price untouched with no rounding at all.
  - B47 Rights and dividend factors REQUIRE their prices and raise when absent; `build_factor_table` returns them as `pending` with the reason. A missing price must never become k = 1, because from the outside that is indistinguishable from "this event needs no adjustment" (Q-6, Q-7).
  - B48 The cross-source join matches on the KIND as well as the symbol-date. Found by the join itself: ROTO declared a bonus AND a dividend on 2023-07-07 and MAANALU a bonus AND a split on 2023-07-27, and a symbol-date-only join called both a disagreement when all three sources agreed on every event.
  - B49 BSE is joined by security NAME (CONTEXT 4.2: it publishes no ISIN), so a miss is reported as `not-found`, never as a disagreement. JMCPROJECT is the live case: NSE has its rights issue, BSE lists nothing under that name.
  - B50 Every network pull this session made ran at 1 request / 4s instead of the usual 1/2s, because the operator's 25-year backfill was running against the same infrastructure in another process. The combined rate stayed at the repo's usual pace.
  - B51 F8 READS NSE's calculator XLSX (stdlib `zipfile` + `ElementTree`, no new dependency) instead of only transcribing its numbers. A transcription cannot notice that it has drifted from its oracle; this also pins the relation CONTEXT 4.2 leaves implicit — NSE's bonus/split AF divides a strike (k = 1/AF) while its rights AF multiplies one (k = AF).
  - B52 `acumen.ca_report` is an entry point, not a library: it prints the two reports the card names, offline from the snapshots by default. It reads the F&O list from the cached pull if there is one and falls back to the frozen snapshot, so a reviewer with no cache gets the same output.
questions: **Q-4 RESOLVED — executed** (ruling recorded verbatim before any code; whitelist selection, the two-whitelist raise, the empty-not-error case and the unknown-series report are all code with tests behind them). **Q-5 RESOLVED — executed** (weekend sessions excluded from trading days, bias pairs and trading; still surfaced and counted; REVIEW_2 F11's measured case is now the regression test). **Q-6 RAISED** (class A, open, non-blocking for chunk 3, BLOCKS rights factors on real events): CONTEXT 4.2's rights formula needs the issue price S, but every NSE rights subject in the frozen windows states a PREMIUM instead, and the row's own `faceVal` is the face value as of the QUERY — GREENPLY's January-2016 rows already show 1, the post-split value. **Q-7 RAISED** (class A, open, non-blocking, BLOCKS automatic special-dividend classification): CONTEXT 4.2 tests the 2% threshold against the PRE-ANNOUNCEMENT close, and `caBroadcastDate` is null on all 438 rows of all three frozen windows, so the announcement date is not in the data. Both are surfaced in the report as `pending` factors rather than being guessed. NOT raised as a new question, but recorded here for the architect: the Q-4 unknown-series report has already found two series the ruling's families do not name, on real F&O-universe symbols — `IL` (HDFCBANK 9 days, KOTAKBANK 2, MCX 1) and `D1` (INDHOTEL 29 days) in the Dec-2015..Jan-2016 window. The ruling already decides their treatment (never the instrument, reported), so nothing is blocked.
gate: n/a
status-ledger: chunk 3: built
state-for-next-session: Chunk 3 is BUILT, not reviewed. `python -m pytest` is green (668/0) from a clean state and fully offline. History ends at `chunk3: corporate-action engine (unreviewed)`; the prep commit `chunk3-prep: record+execute Q-4/Q-5 rulings; apply REVIEW_2 fixes` precedes the four chunk-3 commits. GOLDENS all pass and are printed in the session report: F8 hand-derived (bonus 1:2 → 2/3 · split FV10→2 → 0.2 · rights 1:4@200 on P=300 → 280/300 · special dividend 2.5% → 1 − D/P_cum) AND F8 against NSE's own calculator read cell-by-cell (our rights k matches its 0.9265654979940694 to the last digit it holds) · the five parser goldens · adjustment sanity across all three verified ex-dates (raw −32.44% / −79.63% / −49.76% → adjusted +1.34% / +1.85% / +0.49%, and every consecutive pair in every window inside 20%) · cross-source join 333 comparisons, 22/22 ratios identical to BSE, 3/3 to Yahoo, ZERO disagreements. Known limits a reviewer should weigh: (a) rights and dividend factors are DELIBERATELY unbuildable until Q-6/Q-7 are answered — 296 of the 328 priced events in the frozen windows are `pending`, which is the STOP rule working, not a gap; (b) the frozen windows are three months out of 25 years, so the parser's exception rate over full history is unmeasured — the report exists to measure it during chunk 5B/9; (c) the live fetchers were exercised once each, by hand, to take the snapshots; their retry branches are covered only by stubbed sessions; (d) the adjustment-sanity golden runs on three symbols, and the ±10-trading-day live check ran against a SCRATCH store (`data/ca_check_store`), never the operator's; (e) `--raw-dir` is still not resumable (REVIEW_2 F3's second half, outside the architect's scope for this session). OPERATOR NOTE: the 25-year backfill was running throughout this session and was never touched — at the last read it had settled 1,259 dates (863 file-present, 396 confirmed-404, ZERO errors) and had already reached one weekend session, Saturday 2003-03-22, which Q-5 now excludes and counts. NEXT ACTION: a FRESH session reviews chunk 3 against `personas/quant_reviewer.md` AND `personas/code_reviewer.md` (plan.md review type QC) and writes `docs/reviews/REVIEW_3.md` — chunk 4 must not start until STATUS.md shows `chunk 3: reviewed-PASS`.

---

## [2026-07-24 23:06] chunk 2 · review · done
scope: fresh code-review of chunk 2 (code_reviewer persona, plan.md review type C) — reran the suite from clean, executed the architect's eleven directed checks, recomputed the 25-day cross-source table and the TCS golden from the frozen poc/data with code that imports nothing from acumen, re-attacked the REVIEW_1 F1/F2 fixes, crash-tested the atomic write path four ways, reproduced byte-idempotence and the Q-4 ambiguity, judged B19-B34, wrote docs/reviews/REVIEW_2.md. VERDICT PASS.
files: created docs/reviews/REVIEW_2.md, tests/test_review2_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 513 passed / 0 failed (462 from the build + 51 added by this review), verified from a clean state with .pytest_cache and every __pycache__ deleted first. The builder's 462 reran green unchanged. Fixtures touched: NONE — all 28 poc/data CSVs and all 6 tests/fixtures files hash identically to their pinned SHA-256s AND to their git HEAD blobs (34/34). The 51 new tests were mutation-tested on a throwaway repo copy against ten mutants (error recorded as confirmed-404; single format tried; atomic_write_with catching Exception not BaseException; float price path; ledger written before rows; calendar deriving over an unsettled date; daily() picking the first row; socket patches removed; float64 price dtypes; re-ingest appending) — 10/10 caught, control green.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review2_probes.py. It pins what the build left un-pinned — the socket guard re-attacked through eight clients including pandas.read_csv(url) and a closed local port, the atomic path interrupted by KeyboardInterrupt/SystemExit/MemoryError and inside os.replace itself, the ledger's write ORDER isolated (rows before ledger), the cross-source table recomputed by a second implementation, every comparable bias pair in the window rather than three samples, and the Q-5 weekend-session consequence measured.
  - The cross-source goldens were recomputed before the builder's test file was read in detail, from the frozen CSVs with plain csv+Decimal, so the builder's numbers could not anchor mine. Agreement is 25/25 exact on volume and O/H/L and 25/25 on the close-inside-the-closing-half-hour invariant, with the CONTEXT 4.5 gate-1 band observed at +0.025%..+3.581%.
  - This review issued ZERO network traffic. Where a probe needed an address it used 192.0.2.1 (RFC 5737 TEST-NET-1, unroutable) or a closed loopback port — the chunk-1 review had to send real requests to prove F1 existed; proving it fixed needs none.
  - Two tests deliberately pin current defects (Findings 1 and 2) and are written to FAIL loudly if the defect is fixed, each naming the finding to close — the convention REVIEW_1 established.
  - Finding 4 was found by a probe of my own that failed: asserting the builder's stated float example reproduced the opposite result. The claim `float("2251.10") * 100 == 225109.99999999997` is false (it is exactly 225110.0); the decision it justifies is nevertheless right, and I measured the true damage (10 of 217 printed fixture values, e.g. TCS close 2189.20 -> 218919).
questions: none raised. Q-3 is correctly RESOLVED — the ruling is recorded verbatim in the prep commit BEFORE any chunk-2 code, and I verified every clause is code with a test behind it (ledger vocabulary spelled as the ruling spells it; error never downgraded to confirmed-404 at three independent layers; derived == published over the ingested window, re-derived a third way from the ledger CSV; the current year refused by construction). Q-4 and Q-5 are correctly RAISED and are class A, open. Q-4 is confirmed BLOCKING for chunk 3 and is worse than its own summary: NTPC carries SIX series on 2018-01-01 (EQ, N4, N6, N7, NB, NC) and a different sixth on 2018-01-02. Q-5's consequence is now measured and recorded in REVIEW_2 Finding 11 for the architect: on a derived calendar a weekend session is is_trading_day True AND is_standard_session True (E2 does not exclude it), and the following Monday's CONTEXT 3.2 pair moves by one whole candle.
gate: n/a
status-ledger: chunk 2: reviewed-PASS
state-for-next-session: Chunk 2 is reviewed-PASS at tag chunk2-pass; python -m pytest is green (513/0) from a clean state and fully offline. Twelve findings are in docs/reviews/REVIEW_2.md — none blocking, none requiring a fix session. Two are MEDIUM and both live in the shared fetch layer chunk 5A will inherit: F1 (the cookie warm-up calls _throttle without the caller's min_interval, so the download path fires the warm-up 0.5s after a refused request instead of 2.0s — the one place the card's pacing rule is broken, and it fires exactly when NSE is refusing us) and F2 (fetch_binary does not retry an HTTP 200 carrying a bot-shield HTML page while fetch_json does — measured 1 request vs 4 — so B31's "the policy cannot drift between the two paths" is broader than the enforcement; no data is corrupted because the date lands in `error`, which is never a holiday). Five LOW: F3 (--raw-dir is neither atomic nor resumable, and it is what the DERIVED fixtures were cut from), F4 (B24's float example is numerically false in five places — substitute 2189.20 from this repo's own fixture), F5 (four comments cite Q-4 for the weekend question, which is Q-5), F6 (atomic_write_with does not fsync — process-crash safe, not power-loss safe), F7 (nothing cross-checks that a file-present ledger date actually has rows; the write order makes it crash-safe but there is no read-side invariant — suggest a DailyStore.verify() before chunk 9's full run). Five INFO: F8 (ledger rewrite is O(n^2) — measured ~20-24 min over a 9,700-date run against >=5.4h of mandatory pacing, i.e. not the bottleneck), F9 (the closing-half-hour invariant is only guaranteed when trades occurred in that window — do not adopt it universally in chunk 5A's gate; also LastPric == the last 1-minute close 25/25, an exact comparison the goldens leave unused), F10 (UDIFF_FIRST_DATE is a hardcoded date and is the right call — spec law, and being wrong about it is harmless because both formats are tried), F11 (Q-5's measured consequence, for the architect), F12 (the backfill script's sys.path insert is its only import-time statement). NEXT ACTION: build chunk 3 (corporate-action engine, review type QC) — but note its first act will hit Q-4: DailyStore.daily() RAISES for any symbol-day carrying several series, so a full symbol history cannot be read until the architect rules. Paul's full 25-year backfill can run at any time, in parallel: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (hours; safe to interrupt; re-run to resume) — expect ~5.4h of pure pacing plus ~40 min of file I/O.

---

## [2026-07-24 21:18] chunk 2 · build · done
scope: recorded the architect's Q-3 ruling and fixed REVIEW_1 F1/F2 as a prep commit, then built chunk 2 — bhavcopy downloader for BOTH published formats with a per-date outcome ledger, a parquet daily store keyed symbol x date, the Q-3 derived trading calendar, and the operator's resumable backfill script; proved the pipe by live-ingesting the two bounded windows the card names.
files: created src/acumen/atomic_io.py, src/acumen/bhavcopy.py, src/acumen/daily_store.py, scripts/backfill_daily.py, tests/test_atomic_io.py, tests/test_bhavcopy.py, tests/test_daily_store.py, tests/test_derived_calendar.py, tests/test_daily_goldens.py, tests/test_backfill_script.py, tests/fixtures/PROVENANCE.md, tests/fixtures/bhavcopy_udiff_sample.csv, tests/fixtures/bhavcopy_archive_sample.csv, tests/fixtures/daily_ledger_window.csv; edited src/acumen/nse_http.py (atomic cache write, damaged-cache repair, fetch_binary + NseNotFoundError, per-call throttle interval), src/acumen/calendar.py (derived-calendar constructors + two optional fields), tests/conftest.py (socket-level guard), tests/test_review1_probes.py (F1/F2 pins rewritten), tests/test_fixture_integrity.py (3 digests + 2 new tests), QUESTIONS.md, docs/reviews/REVIEW_1.md (append-only fix log), STATUS.md, PROGRESS.md. No file under poc/ was touched.
tests: 462 passed / 0 failed (was 284 at chunk-1 review; +11 prep, +167 chunk 2), verified from a clean state with .pytest_cache and every __pycache__ deleted first. Fixtures touched: NONE existing — all 28 poc/data CSVs and all 3 chunk-1 tests/fixtures files still match their pinned SHA-256s; three NEW files added under tests/fixtures/ and pinned by digest in the same commits (bhavcopy_udiff_sample.csv bd477ba4…, bhavcopy_archive_sample.csv 40d0281a…, daily_ledger_window.csv 73b97a05…; digests live in tests/test_fixture_integrity.py, provenance in tests/fixtures/PROVENANCE.md).
decisions:
  - B19 A date is only recorded as `confirmed-404` after BOTH published formats answer 404. CONTEXT 4.1 dates the UDiFF cutover only to the MONTH ("since Jul-2024"), so trusting a single boundary date would turn a run of real trading days into holidays if it is off by a few days — and under Q-3 that error is permanent and silent. Costs one extra request per non-trading day (~1.5h over the full 25-year run); buys immunity to a wrong boundary.
  - B20 Weekends are ATTEMPTED, not skipped by rule. The Q-3 ruling derives trading days from what NSE actually published, and NSE does hold weekend sessions; skipping Saturdays would replace the evidence with an assumption. It is also what produces the card's `confirmed-404` golden for Sunday 2026-07-19.
  - B21 The store is one parquet file per calendar MONTH (`daily/<YYYY>/bhavcopy_<YYYY>-<MM>.parquet`), not per date. ~21 dates x ~3,400 symbols is a healthy file; one file per date would leave chunks 3/4 opening 6,000+ files to read one symbol's history. Re-ingesting a date rewrites its month file with that date's rows replaced.
  - B22 The parquet schema is declared explicitly rather than inferred: an inferred schema changes shape when a month happens to have no nulls in a column, and a store whose dtypes drift between months is a store whose reads drift too.
  - B23 Reads come back as pandas nullable `Int64`, not the default conversion. A nullable int column converted the default way becomes float64, and a float cannot hold a turnover in paise exactly (TCS 2026-07-20: 497,162,363,920 paise).
  - B24 Prices are parsed to paise via `Decimal` on the CSV TEXT, and a value that is not a whole number of paise is REFUSED rather than rounded. `int(float("2189.20") * 100)` is 218919 — a paisa short of TCS's real 2026-07-15 close; rounding that is a guess about a price (CONTEXT 7-E11). *[Example corrected in place 2026-07-24 by the chunk-3 prep commit per REVIEW_2 Finding 4: the original entry cited `float("2251.10") * 100 == 225109.99999999997`, which is false — that product is exactly 225110.0. The decision is unchanged; only its illustration was wrong. The same false example survives verbatim in the body of commit 52791e0, which is history and is not rewritten.]*
  - B25 The column set kept is CONTEXT 4.1's OHLCV plus identity/liquidity fields (series, ISIN, instrument type, last, prev close, turnover, trades, source format). "Store everything" is applied to ROWS — every symbol and every series — not to the derivative-only UDiFF columns, which are empty in a cash-market file.
  - B26 `DailyStore.daily()` RAISES when a symbol-day is not unique instead of picking a row. Real data forces the issue and it is not rare (see Q-4). A silent choice would hand chunk 4 a block-deal or debenture candle on exactly the eventful days.
  - B27 `TradingCalendar` gained `trading_days` and `covered_days` (both optional, both `None` for a published calendar, chunk-1 behaviour bit-identical and pinned by a test) rather than a separate DerivedCalendar class. Everything downstream — `bias_pair`, `prev_trading_day`, the E2 filter — must behave identically whichever calendar it is handed, and one class is the only way to guarantee that.
  - B28 The derived calendar refuses to build unless EVERY date in its range is settled, instead of deriving around gaps and errors. This is Q-3 safeguard 1 made structural: an interrupted backfill cannot become a calendar of invented holidays. It also implements safeguard 3 for free — the current year can never be complete, so the published endpoint stays authoritative for today by construction.
  - B29 `scripts/` is not a package and nothing under `src/` imports it; its tests load it by path. It is an entry point, not a library.
  - B30 The backfill script gained `--raw-dir`, archiving each date's CSV exactly as NSE published it. `Download` carries `raw_csv` for this. It is the operator's audit trail and it is what the three DERIVED fixtures were cut from — a re-serialisation of parsed rows would not have been evidence (chunk-1 B16 applied to files).
  - B31 A shared retry loop now backs both `fetch_json` and the new `fetch_binary`, and `NseNotFoundError` makes 404 a TYPE rather than a string comparison. One loop so the CONTEXT 4.3 policy cannot drift between the JSON and download paths; a distinct type because the whole ledger depends on telling "absent" from "failed".
  - B32 `src/acumen/atomic_io.py` is one implementation of write-temp-then-replace, used by the day cache and by every store write, and it removes the temp file on any `BaseException` — `KeyboardInterrupt` is the case it exists for, since the operator is told the backfill is safe to interrupt.
  - B33 The offline guard now blocks at the SOCKET (`socket.socket.connect`/`connect_ex`, `socket.create_connection`) with the `requests` patches kept on top for their better message. `pytest-socket` was considered and declined: one more pinned dependency for ~10 lines, on a repo whose dependency list is deliberately short.
  - B34 The cross-source golden compares only what the two sources genuinely share, so no tolerance is invented anywhere: volume and O/H/L are asserted EXACT (25/25), while the daily close is NOT compared to the last 1-minute close (NSE's close is the last-30-minute weighted average — TCS 2026-07-20: 2251.10 vs an LTP of 2251.00, and the bhavcopy carries both). What is asserted for the close is the invariant that follows from its definition: it lies inside the 15:00-15:29 range. The volume-shortfall band is CONTEXT 4.5 gate 1's own [-0.1%, +5.0%], a spec number.
questions: Q-3 RESOLVED (the ruling was recorded verbatim in the prep commit and every clause is now code with a test behind it; the derived-vs-published cross-check passes over the ingested window). Q-4 RAISED (class A, open, non-blocking for chunk 2, BLOCKS chunks 3/4/9): NSE publishes the same symbol under several SERIES on the same date and CONTEXT names no series anywhere — measured, not hypothetical: 11 F&O-universe symbols are ambiguous on 2018-01-01 alone (NTPC EQ+5 debt series, IRFC 7 debt series and NO equity, JSWSTEEL EQ+P2 partly-paid), and BIOCON EQ+BL on 2026-07-14. The store keeps every row and `daily()` raises rather than choosing; no `"EQ"` default exists in `src/`. Q-5 RAISED (class A, open, non-blocking): NSE's occasional weekend sessions publish a bhavcopy, so the Q-3 ruling makes them trading days, which CONTEXT 7-E2 may or may not want excluded — and the answer moves the following Monday's bias pair too. Neither ingested window contains one; the code follows the ruling and exposes `weekend_sessions` to make the case visible.
gate: n/a
status-ledger: chunk 2: built
state-for-next-session: Chunk 2 is BUILT, not reviewed. `python -m pytest` is green (462/0) from a clean state and fully offline — the conftest guard now fails any test that opens a socket, not merely one that uses `requests`. History ends at `chunk2: add the operator backfill script and the chunk-2 goldens (unreviewed)`; the prep commit `chunk2-prep: ...` precedes the four chunk-2 commits. LIVE PROOF this session (outside pytest): 2026-07-13..2026-07-24 = 12 attempted, 10 present, 2 confirmed-404, 0 error; 2018-01-01..2018-01-15 = 15 attempted, 11 present, 4 confirmed-404, 0 error. GOLDENS all pass: TCS 2026-07-20 close 225110 paise / volume 2,202,693 exactly as docs/RESULTS.md section A · cross-source 25/25 exact on volume and O/H/L vs the frozen poc/data SmartAPI values, close inside the closing half-hour 25/25, volume shortfall 0.025%-3.581% inside CONTEXT 4.5 gate 1's band · re-ingest byte-idempotent · derived trading days == published over the ingested window, and the two calendars agree on the CONTEXT 3.2 bias pair · ledger shows confirmed-404 for Sunday 2026-07-19 and file-present for 2026-07-20. Known limits a reviewer should weigh: (a) only 27 dates are ingested locally — the 25-year run is the operator's, and the store on this machine is NOT the store chunk 3 will read; (b) `DailyStore.daily()` raises for a symbol-day with several series, so chunk 3 CANNOT read a full history until Q-4 is answered — that is the single most important open item; (c) the derived calendar refuses a partially-covered year by design, so `from_daily_store(store, [2026])` will keep failing until 2026 ends (safeguard 3, intended); (d) the retry/backoff and 403-warm-up branches of the download path are covered only by stubbed sessions — the live run hit zero errors, so the error path has no live evidence. NEXT ACTION: a FRESH session reviews chunk 2 against personas/code_reviewer.md (plan.md review type C) and writes docs/reviews/REVIEW_2.md — chunk 3 must not start until STATUS.md shows `chunk 2: reviewed-PASS`. Paul's full backfill can run at any time, in parallel with the review: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (hours; safe to interrupt; re-run the same command to resume).

---

## [2026-07-24 20:28] chunk 1 · review · done
scope: fresh code-review of chunk 1 (code_reviewer persona, plan.md review type C) — reran the suite from clean, executed the architect's ten directed checks, recomputed all seven bias_pair goldens independently from the frozen holiday JSON, attacked the offline guard and the day cache, judged B1-B18, wrote docs/reviews/REVIEW_1.md. VERDICT PASS.
files: created docs/reviews/REVIEW_1.md, tests/test_review1_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 284 passed / 0 failed (248 from the build + 36 added by this review), verified from a clean state with .pytest_cache and every __pycache__ deleted first. The builder's 248 reran green unchanged. Fixtures touched: NONE — all 28 poc/data CSVs and all 3 tests/fixtures files hash identically to their pinned digests AND to their git HEAD blobs. The 36 new tests were mutation-tested against four engine mutants (swapped bias pair, holiday-blind walk-back, moved session close, wrong containment operator) — 4/4 caught, control green.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review1_probes.py. It pins the four things the build left open — what the offline guard actually covers, E2/E12 containment at every boundary stamp including the 15-min 15:00 case the build omitted, naive-IST enforcement across the whole public surface rather than two entry points, and the (D-1, D-2) pair recomputed by a SECOND implementation built straight from the raw JSON so the goldens are not graded by the code they test.
  - The bias goldens were verified by hand first and by code second, then extended past the fixture to all 243 in-year pairs. The builder's in-test reasoning comments were read only after my own derivation agreed, so they could not anchor it.
  - Finding 1 was proved by issuing real traffic (one TCP connect and one GET to nseindia.com's public home page) because no offline experiment can show that a guard fails to intercept a live connection. One page view, well inside CONTEXT 4.1's politeness rule. The KEPT test sends nothing — it uses a closed loopback port.
  - Two tests deliberately pin current defects (Findings 1 and 2) and are written to FAIL loudly if the defect is fixed, each with a message naming the finding to close. A defect that no test can see is how a fix silently regresses.
questions: none raised. Q-1 and Q-2 are correctly CLOSED with evidence. Q-3 is correctly RAISED by the build and is class A, open — but see the item recorded in REVIEW_1 section 5: the architect's Q-3 ruling relayed to this session (option (a) with safeguards, execution in chunk 2) is NOT recorded in QUESTIONS.md, which still shows Q-3 as open with three unchosen options. This review did not act on the ruling, per instruction. It must be written into Q-3 before the chunk-2 session opens, or chunk 2 will read its own dependency as unanswered.
gate: n/a
status-ledger: chunk 1: reviewed-PASS
state-for-next-session: Chunk 1 is reviewed-PASS at tag chunk1-pass; python -m pytest is green (284/0) from a clean state and fully offline. Ten findings are in docs/reviews/REVIEW_1.md — none blocking, none requiring a fix session. Two are MEDIUM and land in code chunk 2 inherits: F1 (the conftest network guard patches requests.Session only — a raw socket and urllib both reached NSE from inside a test; decision B2's recorded claim is broader than the enforcement) and F2 (nse_http.write_cache writes in place, so a crash mid-write leaves a truncated cache that even allow_network=True cannot repair — the operator must delete the file). F3-F7 are LOW (prev_trading_day answers for an uncovered year; E2 containment does not imply E12 grid alignment; the PROGRESS entry names no digests; the prep commit's message convention; one config test still loads the real .env). F8-F10 are INFO for chunks 5A/9/13. NEXT ACTION: build chunk 2 (daily store / bhavcopy) — its card reads docs/RESULTS.md per the Q-1 ruling. Before that session opens, the architect must record the Q-3 ruling in QUESTIONS.md; chunk 2 is the session that executes it.

---

## [2026-07-24 20:41] chunk 1 · build · done
scope: executed the two architect rulings + REVIEW_0 F3/F4 as a prep commit, then built chunk 1 — F&O universe loader, NSE trading calendar (trading days, prev-trading-day, CONTEXT 3.2 bias pairs), the E2 session filter, and a shared gentle once-a-day NSE fetch layer; all tests offline against two frozen endpoint snapshots pulled once this session.
files: created src/acumen/nse_http.py, src/acumen/universe.py, src/acumen/calendar.py, tests/conftest.py, tests/test_nse_http.py, tests/test_universe.py, tests/test_calendar.py, tests/fixtures/universe_snapshot.json, tests/fixtures/holidays_2026.json, tests/fixtures/tick_sizes.json; edited tests/test_smoke.py (F3), tests/test_config.py (F4), tests/test_fixture_integrity.py (digests for the three new fixtures), QUESTIONS.md, STATUS.md, PROGRESS.md; moved (git mv, 100% renames) RESULTS.md + acumen_poc.md + TradingView_POC_Calibration_Guide.docx -> docs/ and requirements.txt -> poc/. No file under src/acumen/config.py or poc/ was modified.
tests: 248 passed / 0 failed (was 91 at chunk-0 review; +1 prep, +156 chunk 1). Verified from a clean state with .pytest_cache and every __pycache__ deleted. Fixtures touched: NONE existing — the 28 poc/data CSVs still match their pinned SHA-256s; three NEW frozen files added under tests/fixtures/ and pinned by digest in the same commit. The two endpoint snapshots were fetched ONCE each during this session (2026-07-24, one live pull per endpoint, no repeat) and are stored verbatim.
decisions:
  - B1 A third module, `nse_http.py`, holds the shared gentle-fetch + day-cache logic. The card names only universe.py and calendar.py, but both endpoints need identical access discipline (CONTEXT 4.1 "once/day max"); the alternatives were duplicating the retry/cache code or making calendar.py import universe.py.
  - B2 The network is opt-in: every fetch path defaults to `allow_network=False` and raises rather than reaching out. Enforced, not claimed — tests/conftest.py installs an autouse guard that FAILS any test making a real HTTP request, so a future default flipped to True cannot stay green while hammering NSE on every commit.
  - B3 `TradingCalendar` carries `covered_years` and RAISES `CalendarError` for any date outside them. The endpoint publishes one year at a time; treating an unloaded year as holiday-free would invent a dozen-odd phantom trading days a year and silently mis-pair every bias spanning one. This is the STOP rule made executable — see Q-3.
  - B4 `bias_pair` returns `BiasPair(current, previous)`, a NamedTuple that is still a plain tuple. CONTEXT 3.2's "current" is candle(D-1), the LATER of the two; named fields remove a silent bias inversion from chunk 4's reach.
  - B5 The E2 candle validator is defined by bar CONTAINMENT: a bar stamped t is in-session when `t >= 09:15` and `t + minutes <= 15:30`. Derived from E12 (open-stamped bars cover [t, t+minutes)) plus CONTEXT 4.5 gate 2 ("375 minutes 09:15-15:29"), so it needs no assumption and handles 1-min (09:15..15:29) and 15-min (09:15..15:15) bars with one rule.
  - B6 `SESSION_OPEN`/`SESSION_CLOSE` are module constants citing CONTEXT 3.1, deliberately NOT config keys: the exchange session is spec law, and a config key would let it move without an architect spec change.
  - B7 NSE's "26-Jan-2026" is parsed with an explicit month map, never `strptime("%d-%b-%Y")` — %b is LOCALE-dependent and would fail on a non-English machine. The parse is cross-checked in tests against the `weekDay` field NSE ships with each row (20/20 agree).
  - B8 Naive-IST is enforced loudly (CONTEXT 7-E8): a `datetime` passed where a `date` is expected raises rather than being truncated, and any tz-aware value raises. Directly addresses REVIEW_0 Finding 7 (the frozen PoC CSVs carry +05:30 stamps) — normalization is the ingest layer's job, not the calendar's.
  - B9 Universe symbols are stripped, upper-cased, de-duplicated and SORTED — the order is a property of the data, not of NSE's `serialNumber` field.
  - B10 Only an EMPTY universe is an error; no plausibility floor (e.g. "must be >= 100") was invented. CONTEXT states ~210 but sets no minimum, and a threshold nobody specified is exactly the magic number CLAUDE.md's code standards ban. The 210 count is asserted in tests, where it belongs.
  - B11 A stale cache is never served silently. `fetch_universe`/`fetch_calendar` raise and name the cache's date; `load_cached_universe`/`load_cached_calendar` are the explicit stale path and return `(fetched_on, value)` so a caller that accepts yesterday's list must say so in its own code.
  - B12 A damaged cache file raises instead of being treated as "nothing cached" — consistent with chunk-0 B5 (a typo must fail loudly, never fall back).
  - B13 The day-cache lives at `<data_dir>/nse/*.json`, i.e. under the `/data/` that .gitignore anchors (chunk-0 B8) — never committed. A test pins that location.
  - B14 The cookie warm-up fires at most ONCE per fetch call: a second one tells us nothing new and doubles our footprint on an endpoint we pull once a day.
  - B15 Nothing in the parse path reads config; `default_cache_dir()` is a separate opt-in that calls `load_config(include_env=False)`, so the universe/calendar path never touches the operator's credentials (same spirit as REVIEW_0 F4).
  - B16 The two snapshots are stored VERBATIM (raw payload bytes, no wrapper, no annotation). Provenance is recorded in the test docstrings and here instead of inside the file, because an edited snapshot is no longer evidence of what the endpoint returns. A test asserts the payload shape is still raw.
  - B17 tests/fixtures/ gets per-file SHA-256 tripwires but deliberately NO exact-set assertion (unlike poc/data): later chunks legitimately add fixtures there; what may never happen is one of these files changing.
  - B18 REVIEW_0 F4 was applied to exactly the six tests the finding names. The seventh `include_env=True` call site, `test_env_values_never_reach_the_config_object`, was left as-is because that code path IS its subject; noted in the test module docstring.
questions: Q-1 CLOSED (architect ruling (a) executed: three documents -> docs/, requirements.txt -> poc/; chunk 2 must read docs/RESULTS.md). Q-2 CLOSED (architect ruling (a) executed: tests/fixtures/tick_sizes.json, five ticks in rupees, `_note` states "tests only - production code must always read the instrument master"; the ruling licenses NO tick table in src/). Q-3 RAISED (class A, open, non-blocking for chunk 1, BLOCKS chunk 9's full run): CONTEXT 4.1's holiday endpoint publishes ONE year — the frozen pull is 20 CM holidays, all 2026 — but CONTEXT 2 wants 25 years of backtest and CONTEXT 3.2's pair is defined on TRADING days. Three options given, including deriving trading days from chunk 2's bhavcopy coverage. Not decided here; the calendar refuses uncovered years instead (B3).
gate: n/a
status-ledger: chunk 1: built
state-for-next-session: Chunk 1 is BUILT, not reviewed. `python -m pytest` is green (248/0) from a clean state and offline — conftest.py fails any test that touches the network, and no test exercises a live pull. History ends at `chunk1: universe and calendar (unreviewed)`; the prep commit `chunk1-prep: ...` immediately precedes the four chunk-1 commits. GOLDENS all pass and are printed in the session report: 2026 CM holidays = 20 · universe = 210 with TCS and RELIANCE present and all 5 indexes excluded · seven hand-computed bias_pair cases incl. Tue 27-Jan-2026 -> (Fri 23-Jan, Thu 22-Jan) across Republic Day + weekend · is_trading_day False on 2026-01-26 and on Sunday 2026-01-25. Known limits a reviewer should weigh: (a) the calendar covers 2026 ONLY and raises for any other year by design — Q-3; (b) the live pull has no CLI entry point, so the operator refreshes by calling `fetch_universe(allow_network=True)` / `fetch_calendar(allow_network=True)` — chunk 13 owns the morning-refresh job and the card did not ask for one here; (c) the live path itself was exercised exactly once, by hand, to take the snapshots — the retry/backoff branches are covered only by stubbed sessions. NEXT ACTION: a FRESH session reviews chunk 1 against personas/code_reviewer.md (plan.md review type C) and writes docs/reviews/REVIEW_1.md — chunk 2 must not start until STATUS.md shows `chunk 1: reviewed-PASS`.

---

## [2026-07-24 19:09] chunk 0 · review · done
scope: fresh code-review of chunk 0 (code_reviewer persona) — reran the suite, verified the frozen fixtures two independent ways, judged B1-B12, wrote docs/reviews/REVIEW_0.md. VERDICT PASS.
files: created docs/reviews/REVIEW_0.md, tests/test_fixture_integrity.py; edited QUESTIONS.md (Q-1 marked resolved per architect ruling; Q-2 raised), STATUS.md, PROGRESS.md. NO file under review was modified; no file was moved.
tests: 91 passed / 0 failed (60 builder + 31 added by this review). Builder's 60 reran green from a clean state (.pytest_cache and all __pycache__ deleted first). Fixtures touched: NONE — all 28 poc/data CSVs hash identically to their git HEAD blobs, and the new digest test was mutation-tested against temp COPIES only.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_fixture_integrity.py pins SHA-256 per fixture, the exact 28-file set, 375-candle length and CRLF preservation. Rationale: the build asserted only that fixtures EXIST, so CLAUDE.md rule 3 had no tripwire — a later session could silently regenerate one and stay green.
  - Fixture integrity was established by recomputation, not by trusting the builder's digest: an independent reimplementation of the CONTEXT 3.3 row math reproduces all 25 poc_prorata values in volume_poc_summary.csv exactly, including all five CONTEXT 8 F7 anchors. Evidence, not assertion.
  - The verification scripts were written to the session scratchpad, not the repo — chunk 6 owns the POC engine and a review may not pre-build it.
questions: Q-1 marked "RESOLVED — execution scheduled chunk 1" with the architect's ruling (option (a): three documents to docs/, requirements.txt to poc/) recorded in full; NO file moved by this session. Q-2 RAISED (class A, non-blocking, first needed at chunk 6): CONTEXT 8 F7 calls the poc/data CSVs "the authoritative input", but recomputing the POC also needs the per-symbol tickSize, which lives nowhere in the repo (only the gitignored 35 MB cache/scrip_master.json). Measured values recorded in Q-2 so they survive cache deletion: TCS 0.10, RELIANCE 0.10, HDFCBANK 0.05, DIXON 1.00, MANAPPURAM 0.05.
gate: n/a
status-ledger: chunk 0: reviewed-PASS
state-for-next-session: Chunk 0 is reviewed-PASS at tag chunk0-pass; python -m pytest is green (91/0) from a bare clone. Seven findings are in docs/reviews/REVIEW_0.md — all LOW or INFO, none blocking, none requiring a fix session: F1 (builder's aggregate digest is not reproducible; superseded by the new per-file digest test), F2 (fixed by this review), F3 (B7's ASCII claim is broader than the test that enforces it), F4 (six config tests load the real .env; suggest include_env=False), F5/F6/F7 are forward-looking notes for chunks 5A and 6. NEXT ACTION: build chunk 1 (universe & calendar) — and, per the architect's Q-1 ruling, that same session moves RESULTS.md, acumen_poc.md and TradingView_POC_Calibration_Guide.docx to docs/ and requirements.txt to poc/, then marks Q-1 closed.

---

## [2026-07-24 18:19] chunk 0 · build · done
scope: turned the PoC folder into the project repo — CONTEXT §6 layout, pinned pyproject, config loader with risk_per_trade required-empty, ledgers, git history.
files: created .gitattributes, pyproject.toml, config.yaml, src/acumen/__init__.py, src/acumen/config.py, tests/test_smoke.py, tests/test_config.py, tests/fixtures/.gitkeep, docs/reviews/.gitkeep, PROGRESS.md, QUESTIONS.md, STATUS.md; rewrote .gitignore; moved common.py + poc1..poc5_*.py → poc/ and data/*.csv (28) → poc/data/; untouched: CLAUDE.md, CONTEXT.md, plan.md, personas/*, RESULTS.md, acumen_poc.md, TradingView_POC_Calibration_Guide.docx, requirements.txt, .env, cache/.
tests: 60 passed / 0 failed (python -m pytest). Fixtures touched: NONE — the 28 poc/data CSVs were moved, not rewritten; SHA-256 of all 34 moved files taken before and after the move, 34/34 identical (aggregate digest of the 28 CSVs: f7e06fad99b8d8ac6005a5f4b6ba9df61641b4973a021e9f1d12bf92ac004100).
decisions:
  - B1 Dependencies pinned exactly (`==`) to the versions verified working here (smartapi-python 1.5.5, pyotp 2.10.0, python-dotenv 1.2.2, requests 2.32.3, websocket-client 1.9.0, logzero 1.7.0, pandas 3.0.3, pyarrow 25.0.0, PyYAML 6.0.3, pytest 9.1.1) — one operator laptop, so reproducibility beats range flexibility.
  - B2 pytest resolves the src layout via `[tool.pytest.ini_options] pythonpath = ["src"]` rather than an editable install — a bare clone runs green with no install step. The package was NOT pip-installed in this session.
  - B3 Project version is single-sourced from `acumen.__version__` (setuptools dynamic metadata) — one place to bump.
  - B4 config.yaml carries only the three keys the card names (risk_per_trade, row_size, paths); capital and the ₹100 cost (CONTEXT §3.5) are left to the chunk that consumes them — no unused config surface to drift.
  - B5 Unknown or missing top-level config keys are a hard ConfigError — a typo must fail loudly, never fall back to a default (CLAUDE.md rule 1).
  - B6 Secrets never enter the Config object: `.env` values are fetched on demand by `env_value()`, and its error message names the VARIABLE only, never a value (CLAUDE.md rule 4).
  - B7 Runtime files (`*.py`, `config.yaml`) are ASCII-only, enforced by a test: the operator's console encoding is cp1252, where printing a traceback whose source line carries `₹` or `§` raises UnicodeEncodeError and hides the real error. Spec symbols stay in the markdown documents; code cites "CONTEXT 3.5" in CLAUDE.md's own style.
  - B8 `.gitignore` anchors `/data/`, `/cache/`, `/logs/` with a leading slash — an unanchored `data/` would also ignore `poc/data/`, i.e. the frozen F7/F10 golden fixtures. A test asserts both the anchored form and the absence of the unanchored one.
  - B9 `.gitignore` adds `/logs/` and `.pytest_cache/` beyond the card's list — both are generated artifacts, which CLAUDE.md's git rules forbid committing (the PoC had left two 0-byte `logs/*/app.log` files).
  - B10 PoC scripts moved verbatim, not repaired: their `Path(__file__).parent/"data"` now resolves to `poc/data` (correct), `cache` to `poc/cache` (hence that ignore entry) and `.env` to `poc/.env` (absent — the archive would need `--env` plumbing to re-run). CONTEXT §6 calls poc/ "archived PoC scripts", so it is preserved as-run.
  - B11 Added `.gitattributes` marking `poc/data/**` and `tests/fixtures/**` as `-text`: this machine has `core.autocrlf=true` and the frozen CSVs are CRLF on disk, so without it git would store them line-ending-normalized and any clone would receive different bytes than the fixtures were calibrated on (CONTEXT §8 F7/F10).
  - B12 Nothing was deleted anywhere in this chunk: root `cache/scrip_master.json` (35 MB, gitignored), `logs/`, `__pycache__/` and the four un-specified root documents were left in place; the card scoped the move to PoC scripts + PoC data.
questions: Q-1 (class A, non-blocking) — CONTEXT §6's layout does not name RESULTS.md, acumen_poc.md, TradingView_POC_Calibration_Guide.docx or requirements.txt, so chunk 0 left them at the repo root instead of guessing; chunk 2 cites RESULTS.md, so its path needs an architect ruling before then.
gate: n/a
status-ledger: chunk 0: built
state-for-next-session: Repo is at the CONTEXT §6 layout on branch `main`, `python -m pytest` is green (60/0) from a bare clone, and the history ends at `chunk0: repo scaffold (unreviewed)`. Environment note: pytest 9.1.1, PyYAML 6.0.3 and pyarrow 25.0.0 were installed into the operator's global Python 3.12.2 this session; the other pinned deps were already present from the PoC. NEXT ACTION: a FRESH session reviews chunk 0 against personas/code_reviewer.md and writes docs/reviews/REVIEW_0.md — chunk 1 must not start until STATUS.md shows `chunk 0: reviewed-PASS`.
