# PROGRESS.md — session log

**Newest entry on TOP.** Every session appends one entry using the exact template from
plan.md §6 (reproduced below). An entry is the only place a Class-B implementation decision
is recorded — a deviation recorded nowhere is a defect, even if the code is right (plan.md §5).

<!-- TEMPLATE — copy exactly (plan.md §6)

## [YYYY-MM-DD HH:MM] chunk <N> · <build|review|fix> · <done|blocked|handed-off>
scope: <one line — what this session did>
files: <created/changed paths>
tests: <pass count / fail count; fixtures touched>
decisions: <Class-B items with 1-line rationale; "none">
questions: <QUESTIONS.md items raised; "none">
gate: <n/a | pending | closed — evidence path>
status-ledger: <the STATUS.md line this session set>
state-for-next-session: <exact current situation + the single next action>

-->

---

## [2026-07-26 14:20] chunk 5B · build · handed-off (run in progress)
scope: chunk 5B PREP (closed REVIEW_5A F1/F2/F3 + the owed REVIEW_2 F7 DailyStore.verify) then BUILT the full-universe 1-minute runner (`universe_backfill.py` + `acumen-universe-backfill`), verified the daily-store ORACLE clean before touching the network, classified all 210 routes, and LAUNCHED the live run (resumable; in progress at hand-off). Two real defects found by the live run itself were fixed with tests; one class-A hole (Q-12) was raised as a STOP rather than decided.
files: created src/acumen/adjustment_route.py (PURE routing classifier), src/acumen/universe_backfill.py, scripts/universe_backfill.py, tests/test_adjustment_route.py (18), tests/test_daily_store_verify.py (12), tests/test_universe_backfill.py (28). Edited src/acumen/daily_store.py (verify() + StoreAnomaly/VerifyResult + the row-count floor constants), src/acumen/minute_backfill.py (fetch_corp_action_history + corp_actions_for_symbol + SymbolCorpActions split out of build_symbol_factors; --adjustment-map/--map-data-dir; load_adjustment_map_for; the routing REFUSAL; map-backed --rebuild), src/acumen/minute_store.py (iter_days, month-at-a-time), src/acumen/quality_gates.py (adjustment_continuity_gate = CONTEXT 4.5 gate 3's literal form), pyproject.toml (entry point), tests/test_minute_backfill.py (+6), tests/test_minute_store.py (+4), tests/test_quality_gates.py (+5), docs/gate_chunk5A_open8_evidence.md (F3 footnote corrected in place), QUESTIONS.md (Q-11 ROUTING addendum verbatim + Q-12 + the gate-2/liquidity finding), docs/backfill_minute_report.md (generated), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): data/universe_backfill/ledger.json, data/adjustment_maps/*.json, data/minute_store (the run), logs/universe_backfill_2026-07-26.log.
tests: 984 passed / 0 failed (was 911 at chunk5A-pass; +73), verified OFFLINE. Fixtures touched: NONE. The live SmartAPI/NSE pulls are the runner and scratchpad diagnostics, never pytest.
decisions:
  - B93 "Non-share-count" in the routing ruling is read as the ruling's own enumeration -- rights, special dividend, demerger -- i.e. events that MOVE a price without moving the share count. An ordinary dividend and a buyback carry k = 1 (CONTEXT 4.2), move no price and fragment no era; reading them in would make EVERY symbol map-required and contradict the ruling's own "bonus/split-only symbols may use the factor-table path".
  - B94 The classifier scopes events to the un-adjustment window (D, F] at BOTH ends. An ex-date at or before the symbol's minute clamp, or after the fetch date F, appears in no stored day's window and cannot affect one stored price. The upper bound was MISSING in the first build and the live run proved it costs symbols: ULTRACEMCO (special dividend ex 2026-07-30) and POWERINDIA/GVT&D (dividends ex 2026-08-21) were map-required for events that have not gone ex, and -- having no probeable era -- would have been refused outright. Fixed in chunk5B-fix with tests.
  - B95 DailyStore.verify() splits ANOMALIES (blocking) from NOTES (reported, non-blocking). The full-market row-count floor does not describe a WEEKEND-dated session, because Q-5 (already ruled, executed chunk 3) excludes weekend sessions from trading days entirely -- so the real 2012-11-11 case (a 14-row Sunday gold-ETF session, ledger row_count agreeing with the rows on disk) is a NOTE, not a store defect. A low row count on a WEEKDAY stays an anomaly: nothing in the spec separates a weekday special session from a truncated write, so the conservative reading holds. This is applying an existing ruling, not inventing one.
  - B96 The universe run reads the whole daily store and the whole NSE CA history ONCE (build_daily_cache + CachedDailyStore, a DailyStore-shaped read-only adapter). Per-symbol reads would reopen the same monthly parquets tens of thousands of times; chunk-5A code paths stay byte-identical because the adapter answers with the same columns and the same empty-frame contract. The Q-4 whitelist selection is applied exactly as DailyStore.daily applies it, including the loud raise on two whitelist series for one symbol-date.
  - B97 Map eras are DERIVED per symbol (era_intervals / era_probe_windows) instead of hand-picked as the FIX-4 runbook did for RELIANCE: sorting the in-scope ex-dates cuts [clamp, end] into intervals whose keys shrink by exactly ONE event per step, which is what the map builder's probe-gap guard requires; each era is probed on the LAST few trading days before its next ex-date (the ruling's pre-ex probe days). The identity era is never probed -- factors_for_day returns the exact identity for an empty key. Measured cost for the live universe: 397 probe fetches, zero unprobeable eras.
  - B98 gate 3 CHAINS events that share an ex-date. Found live: 360ONE carries a 1:1 bonus AND a face-value split 2->1 BOTH ex 2023-03-02, so the raw price quarters overnight; testing either k = 0.5 alone reported a bogus 50% break on a perfectly continuous series (CONTEXT 4.2: "chain multiple events"). Fixed before launch, pinned by test.
  - B99 The report is named docs/backfill_minute_report.md (the architect's prompt) rather than the chunk-5B card's docs/backfill_report.md -- this is the MINUTE backfill and the daily one may want that name. Flagged so the difference is not read as drift.
  - B100 A map-required symbol with no probeable era is REFUSED (status map-required-but-unbuildable), never dropped to the factor-table fallback: with no price oracle a wrong price passes gate-1 volume unseen (REVIEW_5A F2). Measured: after the B94 fix this leaves COLPAL alone (two genuinely unparsed 2017/2019 subjects), listed with its reasons.
questions: **Q-11 ROUTING ADDENDUM recorded verbatim and EXECUTED** (classifier + CLI refusal + runner). **NEW Q-12 (class A, OPEN, STOP -- non-blocking, costs COVERAGE not correctness):** Q-11's MEDIAN estimator is unbiased on the PRICE side but not on the VOLUME side, where the 1-minute sum systematically under-counts the daily total (the pre-open auction gate-1's asymmetric band exists for). The median therefore lands ~half the probe days below the true factor, the unwidened -0.1% floor rejects them, and an era that a single exact factor reconciles is marked un-provable -- and one failed era cascades to every older era via the probe-gap guard. Measured on TWO live symbols: ABB (demerger ex 2019-12-20) price factor 0.897600 on all four probe days, volume median 0.8986 fails one day by 0.011pp and costs ~790 symbol-days; ADANIENT (rights ex 2025-11-17) our TERP 0.969485 matches the vendor to six decimals on all four days and reconciles volume on all four (+0.682/+0.093/+0.351/+0.071%), while the median 0.972903 fails two and costs 92% of the symbol's history. Options (a) low-quantile/min estimator, (b) add the measured PRICE factor to the volume candidate set, (c) accept and disclose. Executed as RULED; every affected day is excluded + counted (CONTEXT 7-E3) so nothing wrong is stored, and any ruling can be applied to the already-fetched store via the idempotent rebuild_symbol_raw_with_map WITHOUT re-downloading a candle. **Also recorded (a FINDING, not a hole): gate 2 vs LIQUIDITY** -- the vendor omits minutes with no trade, so CONTEXT 4.5 gate 2's "missing > 15 of 375" excludes 34% of ABB's days and 41% of APLAPOLLO's for a liquidity reason; measured 318/293/325/338 traded minutes on four consecutive ABB days in 2019 against CONTEXT 4.3's PoC "375/375" (measured on 5 LIQUID symbols in 2026). No code deviates; the report prints average traded minutes per day and flags every symbol losing > 10% of its days to gate 2.
gate: n/a -- chunk 5B has no TRADER gate. Its DoD (>= 95% of symbol-days pass gates) is measured by docs/backfill_minute_report.md when the run finishes.
status-ledger: chunk 5B: built (run in progress)
state-for-next-session: The daily store is VERIFIED (REVIEW_2 F7 discharged: 319 months, 6606/6606 file-present dates carry rows, 10,533,151 rows, ZERO anomalies, one Q-5 note for the 2012-11-11 Sunday gold-ETF session). The universe run is LIVE and IN PROGRESS in the background, writing logs/universe_backfill_2026-07-26.log; it is interrupt-safe and the SAME command resumes (proved on the real run: it was stopped after 8 symbols to pick up the B94 fix and resumed at window granularity without refetching a settled window). Routes over the live universe: 127 table-path, 83 map-required, 397 probe fetches, zero unprobeable eras. Early results: table-path symbols come in at 99%+ gate-1; map-required symbols are mixed (AMBUJACEM 2/2 and ASHOKLEY 3/3 eras provable; ABB 0/1 and ADANIENT 0/3, both Q-12). Quarantine ceiling is 21 of 210 -- if Q-12 keeps map-required symbols failing, the run may HALT itself and surface, which is the designed behaviour, not a fault. NEXT ACTIONS: (1) the OPERATOR lets the run finish (or resumes it with the identical command) and then regenerates the report with `--report-only`; (2) the ARCHITECT rules Q-12 -- and note that any ruling is applied to the ALREADY-FETCHED store by deleting the affected data/adjustment_maps/<SYM>.json and re-running, with NO candle re-downloaded (rebuild_symbol_raw_with_map is idempotent and identity-guarded); (3) the chunk-5B REVIEW is type C (code_reviewer only, plan.md 3) and reviews this build INCLUDING the run's report; (4) chunk 6 (POC) is unblocked and depends only on 5A. Observation for the reviewer/architect, not a decision: a TABLE-PATH symbol can also fail gate 1 (APLAPOLLO 77.8%, quarantined) because the routing rule only guarantees a PRICE oracle for non-share-count events -- routing quarantined table-path symbols through the map as a second pass is an option the architect may want.

## [2026-07-25 22:40] chunk 5A · review · done
scope: fresh QC review (quant_reviewer + code_reviewer) of the WHOLE chunk-5A stack, six commits 2d55919..9f06b6d (prep+build+fix Q-10+fix2 k_shares+fix3 demerger-scope/Q-11 STOP+fix4 Q-11 measured reconstruction) -- reran the suite from clean+offline, executed all 10 architect-directed checks, HAND-VERIFIED the RELIANCE-shaped era-flip resolution end-to-end (demerger measured 0.9079 in {D,B1}, flips to absent in older eras; rights measured 0.9873 price/0.9877 vol independently; min-cost lands the unique cost-3 chain), recomputed the RELIANCE map k_price per era (0.453931/0.493652/0.246826 all match), ran a 9-mutant matrix on the un-adjust math, constructed the adversarial preference-masking case + a determinism/order-invariance check and kept both as probes, audited the 27/27 evidence table + secrets + fixtures + commit hygiene, judged B63-B92. VERDICT PASS.
files: created docs/reviews/REVIEW_5A.md, tests/test_review5A_probes.py (2 tests, both verified to fail on their mutant); edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture touched; CONTEXT.md/plan.md untouched. Mutation matrix mutated src in place then restored exact bytes via git checkout (tree verified clean after each).
tests: 911 passed / 0 failed (909 from the stack reran green unchanged + 2 reviewer probes), verified from a clean state (.pytest_cache + every __pycache__ deleted first), fully offline. Fixtures touched: NONE -- git diff 656bfbc..9f06b6d over tests/fixtures+poc/data shows only the ADDED instrument_master_sample.json + a PROVENANCE append; poc/data byte-unchanged; 56 digests pinned, one added, none changed.
decisions:
  - Reviewer-added test file kept (persona step 4): tests/test_review5A_probes.py -- (1) the min-cost preference masks ONLY a sub-0.1% (immaterial, below the microstructure floor) vendor variant while the per-day containment oracle FORCES `measured` for any material (>0.1%) one (verified to fail when containment is disabled); (2) the committed map factors are invariant to probe-day order (medians are order-free) and a rebuild from identical inputs is byte-identical. Both pass on HEAD.
  - Mutation matrix (9 mutants on price divide, volume multiply, era-carry direction, half-even rounding x2, identity-guard price-check, per-day containment, probe-gap guard, <=1-solve guard): 8 CAUGHT, 1 EQUIVALENT (the <=1-solve inner filter is unreachable given the probe-gap guard -- a SOLVE option exists only for a NEW event and the probe-gap guard excludes any era with >1 new event before _resolve_pass runs). No genuine coverage gap.
questions: none raised. Q-10/Q-11 (+ both addenda) are correctly RESOLVED-executed: each ruling is verbatim in QUESTIONS.md BEFORE its code, the code implements only the FINAL state (the fix2/fix3 factor-table path survives as the documented fallback, not a bypass), tier-2-rights un-provability survived, and k_shares is subsumed by measured volume factors. Q-8/Q-9 are correctly RAISED with interim behaviors that block gates/branches, not chunk 5A.
gate: n/a -- chunk 5A has no TRADER gate. OPEN-8 is answered (ADJUSTED) and its E11 contradiction remedied by Q-10/Q-11; the evidence pack + QUESTIONS.md carry the acceptance.
status-ledger: chunk 5A: reviewed-PASS
state-for-next-session: Chunk 5A is reviewed-PASS at tag chunk5A-pass; python -m pytest is green (911/0) from a clean state and fully offline. Seven findings in docs/reviews/REVIEW_5A.md -- none blocking, none requiring a fix session. TWO are LOW and both are chunk-5B-forward + gate-1-safe today: F1 (the FIX-4 AdjustmentMap is built/proven/consumable but the operator ingest+rebuild CLIs do NOT yet load a persisted map -- backfill_symbol(adjustment_map=...) and rebuild_symbol_raw_with_map exist + are tested but have no src caller; the CLI uses the Q-10 factor-table fallback, which for an era-inconsistent symbol stores gate-1-EXCLUDED prices -- chunk 5B must wire map consumption), F2 (the factor-table fallback has no price oracle, so gate-1 volume cannot catch a price-only special-dividend mis-adjustment; chunk 5B should route ANY non-share-count-event symbol through the map path, not just demerger/rights symbols). Five INFO: F3 (evidence footnote 1 cites a stale "median residual" reason; code is per-day containment + 0.1% floor), F4 (the four fix commits omit the "(unreviewed)" suffix), F5 (the newest fix PROGRESS entry did not textually state the pushed SHA), F6 (the preference masks a <=0.1% variant -- the ruling's disclosed tolerance), F7 (the audit probe_days list order tracks input order; factors are invariant). NEXT ACTION: chunk 5B (full-universe backfill) -- it depends on 5A (now reviewed-PASS); it MUST wire map consumption per F1/F2 for demerger/rights/special-dividend symbols and build+commit an AdjustmentMap per such symbol. Chunk 6 (POC) remains unblocked (the store holds RAW same-day prices; E11 satisfied). This session pushed to origin/main -- SHA stated in the review report block.

---

## [2026-07-25 21:15] chunk 5A · fix · done
scope: chunk5A FIX-4 -- EXECUTED the architect's Q-11 ruling. Replaced FIX-2/FIX-3's rule-guessing (a single policy for the vendor's era-inconsistent demerger scope) with per-event MEASUREMENT arbitrated by the raw-daily oracle. Built `vendor_adjustment.py` (PURE map builder + un-adjust consumption), the live measurement/persistence wrappers, the operator runbook, wired the ingest path to consume the map, and ran the LIVE credentialed RELIANCE acceptance (all 6 windows provable + gate-1 PASS + containment) and the TCS regression (map {2018 bonus, ours, 0.5}; store rebuild no-op; gate-1 year table unchanged).
files: created src/acumen/vendor_adjustment.py (PURE: EventSpec/ProbeDay/EraMeasurement/EventChoice/EraResolution/AdjustmentMap; build_map + _resolve_pass backward pass + _refine_scalars; unadjust_with_map consumption; to_dict/from_dict/persist_map/load_map; measure_symbol_live/build_symbol_map_live I/O), src/acumen/build_adjustment_map.py + scripts/build_adjustment_map.py (operator runbook + launcher), tests/test_vendor_adjustment.py (16 offline tests). Edited src/acumen/minute_backfill.py (backfill_symbol/_fetch_and_store_window take an optional adjustment_map and consume it; new rebuild_symbol_raw_with_map with a gate-1 identity guard), pyproject.toml (acumen-build-adjustment-map entry point), QUESTIONS.md (Q-11 ruling verbatim + RESOLVED-executed note), docs/gate_chunk5A_open8_evidence.md (FIX-4 section: mechanism, RELIANCE map + per-window numbers, TCS regression, honest FIX-2/FIX-3 premise corrections), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): built+persisted data/adjustment_maps/RELIANCE.json and TCS.json via live pulls; the operator's data/minute_store (TCS) was NOT rewritten (read-only regression). The NSE CA day-cache + daily store were read-only.
tests: 909 passed / 0 failed (was 885 at fix3; +24, all in test_vendor_adjustment.py -- 16 core + 8 adversarial-review regressions). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state. The live SmartAPI/NSE pulls are the operator runbook + scratchpad scripts, never pytest.
decisions:
  - B86 Per-event measurement, not policy (Q-11). `R(D)=fetched/raw` (fold HIGH/LOW vs the RAW daily store) is the direct observable = product of the vendor's applied factors in (D,F]. Days sharing an in-window event set form an ERA; the map resolves each event to {ours, measured k̂, not-applied} for price and (independently) volume, arbitrated by 2-paise price containment + gate-1's band. Working BACKWARDS from F; min-cost prefers ours>absent>measured; a source is decided at an event's newest appearance and carried older; ONLY a no-ours event (demerger) may FLIP measured<->absent across eras (the era-inconsistency). At most one freshly-solved unknown per era -> measurement, never free fitting.
  - B87 Single measured scalar per event = the MEDIAN over ALL its pre-ex probe days (phase-2 refinement), isolating the one event by dividing out the others' resolved factors -- exactly the ruling's "median ratio over pre-ex probe days". This tightened RELIANCE's 2016 era from 3->2 paise and is why the rights is ONE scalar (price 0.987305 / vol 0.987707) across the 2016 AND 2019 eras.
  - B88 Price provability uses the MEDIAN containment residual (not max): the fetched fold high/low can differ from the official daily high/low by a few paise on an odd day for a microstructure reason (an odd-lot/block trade the bhavcopy counts but the continuous 1-min series does not -- the same reason gate-1's band skews positive). A WRONG factor misses by hundreds of paise on EVERY day, so the median separates choices cleanly; the identity era's lone 10-paise day (2024-11-07) stays provable. The -0.1% gate-1 floor is NOT widened (ruling).
  - B89 Rights & demerger VOLUME are measured too (independently of price). The vendor scaled volume for the rights (measured 0.987707, != our TERP) and for the demerger in its recent era (0.907921) -- the exact reason FIX-3's share-count-only k_shares failed gate-1 at -1.24%/-10.14%. This RESOLVES decision B81 by measurement rather than a convention.
  - B90 Ingest consumes the map (`backfill_symbol(..., adjustment_map=...)` -> `unadjust_with_map`); the Q-10 factor-table path stays as the fallback for clean bonus/split-only symbols (TCS) and when no map exists. `rebuild_symbol_raw_with_map` carries a gate-1 IDENTITY GUARD (skip a day whose stored volume already reconciles -> already RAW), making a rebuild of an already-raw store a NO-OP and safe to re-run (unlike the Q-10 rebuild).
  - B91 Era key = the in-window event set {ex : D < ex <= F}; the map keys eras by it and a day whose era is not in the map (unprobed) or is un-provable returns None from factors_for_day -> gate 1 excludes and counts it (CONTEXT 7-E3, surgical clamp). A symbol whose vendor floor falls WITHIN one era key with no intervening event to split it (a demerger-only symbol) has a bimodal era -> no single factor reconciles -> un-provable + excluded (tested). An EMPTY in-window set (a recent post-last-CA day) is the exact identity (1,1) even without a probed identity era -- a recent RAW day is never wrongly excluded.
  - B92 Adversarial-review hardening (an in-session multi-agent review found real defects; all fixed with regression tests, none changed the RELIANCE/TCS results): (1) `_price_contained` now checks EVERY probe day within max(2 paise, 0.1% of raw) -- NOT the median -- so a BIMODAL era (the vendor floor inside one era key, a minority of days on a different factor that gate-1 volume cannot see) is caught and marked un-provable; the 0.1% relative floor still absorbs market microstructure (fold-vs-daily-high, ~0.01%) while rejecting the smallest wrong factor (~0.33%). (2) build_map RE-VALIDATES the phase-2-refined chain against the oracle before committing provable=True (the refinement can shift a scalar). (3) A PROBE GAP (an era introducing >1 event not covered by a newer era -> per-event attribution under-determined) is marked un-provable, never a fabricated decomposition. (4) `rebuild_symbol_raw_with_map`'s identity guard now checks price AND volume (a price-only special dividend leaves volume reconciled; a volume-only guard would skip it with price still adjusted -- a wrong price to the backtest) and skips a day with no raw daily row. (5) measure_era drops zero-volume/degenerate probe days (no div-by-zero); build_map refuses two events sharing an ex-date (STOP, not silent-collapse).
questions: **Q-11 RESOLVED -- executed** (ruling recorded verbatim in QUESTIONS.md BEFORE the code; every clause is code with a test behind it). No NEW class-A hole raised. NOTE for the architect (not a STOP): the map is committed per (symbol, era) and covers only the ERAS actually probed; chunk 5B must probe each demerger symbol's eras (the runbook generates one call per window) or those spans stay un-provable/excluded -- the disclosed surgical clamp. The vendor's demerger-adjustment FLOOR between 2019 and 2022 was not pinned (not needed: 2020-2021 RELIANCE minutes are not in the acceptance) -- chunk 5B pins it per symbol if it wants those spans.
gate: n/a -- chunk 5A has no TRADER gate. FIX-4 EXECUTES Q-11; acceptance + regression evidence are in docs/gate_chunk5A_open8_evidence.md (FIX-4 section) and QUESTIONS.md Q-11.
status-ledger: chunk 5A: built (+ fix Q-10 + fix2 k_shares + fix3 demerger-scope + fix4 Q-11 vendor-adjustment reconstruction, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + Q-10 fix + fix2 + fix3 + this FIX-4 are BUILT, not reviewed. `python -m pytest` is green (909/0) from a clean state, fully offline. FIX-4 EXECUTES Q-11: `vendor_adjustment.py` reconstructs the vendor's per-event minute adjustment by MEASUREMENT (the map: per event per era, {ours|measured|absent} for price and volume, oracle = 2-paise containment + gate-1 band). The LIVE RELIANCE acceptance passed on ALL six windows (2016-10, 2019-07, 2022-07, 2023-06, 2023-09, 2024-11): every probe day provable + gate-1 PASS + containment 0-2 paise; the map shows demerger absent 2016/2019, measured 0.9079 for 2022->2023-07, rights measured 0.9873 price + 0.9877 volume, bonuses ours 0.5. TCS regression: map {2018 bonus, ours, 0.5}, gate-1 year table unchanged (2016 100% / 2017 100% / 2018 97.2%), map-rebuild is a no-op (identity guard). NEXT ACTIONS: (1) the FRESH 5A review reviews the WHOLE of chunk 5A INCLUDING FIX-4 (personas/quant_reviewer.md + personas/code_reviewer.md, plan.md QC) -- pay attention to B86 (the measurement-vs-fitting boundary: <=1 fresh unknown per era), B87 (single-scalar refinement), B88 (median containment + the identity-era microstructure day), B89 (volume measured independently), B90 (map consumption + rebuild identity guard); (2) chunk 5B backfills the full universe on this ingest path -- for each demerger/rights symbol it builds+commits an AdjustmentMap (probing that symbol's eras) then backfills consuming it; a symbol whose eras it does not probe stays un-provable/excluded (disclosed surgical clamp); (3) chunk 6 (POC) remains unblocked (the store holds RAW same-day prices).

---

## [2026-07-25 19:30] chunk 5A · fix · done (with a STOP raised)
scope: chunk5A FIX-3, the single demerger-minute follow-up FIX-2 flagged. Executed the architect's Q-10 ADDENDUM 2 ruling -- demergers EXCLUDED from the minute un-adjustment (they were never in the k chains; the change is they no longer mark a minute span un-provable), while tier-2/pending rights still do and the daily bias-engine demerger suppression (CONTEXT 3.2) is untouched. Then ran the directed LIVE credentialed RELIANCE re-run through the fixed path -- which CONTRADICTED the ruling's premise and raised a new STOP (Q-11).
files: edited src/acumen/minute_unadjust.py (new PURE helper `unprovable_suppression_dates()` filters out KIND_DEMERGER; `unadjust_bars` routes through it; import KIND_DEMERGER; docstrings -- Provability bullet, arg docs, `_unknown_factor_between` reason string now "tier-2 unrecoverable rights"), src/acumen/minute_backfill.py (`rebuild_symbol_raw` uses the helper; `_print_symbol_factors` labels each suppression's minute effect), tests/test_minute_unadjust.py (replaced the demerger-marks-unprovable test with: a helper unit test, a demerger-provable-but-tier2-rights-still-unprovable test, and a cross-layer test asserting BOTH minutes-provable AND bias-pair-suppressed; added DailyStore/calendar/BiasEngine imports + a local `_daily_store` helper), QUESTIONS.md (Q-10 ADDENDUM 2 ruling verbatim + honest EXECUTED note + **new Q-11 STOP**), docs/gate_chunk5A_open8_evidence.md (FIX-3 section with the live tables), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; NO file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored): live SmartAPI 1-min pulls were SCRATCHPAD scripts only; the operator's data/minute_store (TCS) was NOT touched; the NSE CA day-cache + daily store were read-only.
tests: 885 passed / 0 failed (was 883 at fix2; net +2 -- replaced 1 demerger test with 2 focused ones + 1 cross-layer test). Fixtures touched: NONE. Full suite verified green OFFLINE. The live SmartAPI/NSE pulls are SCRATCHPAD scripts, never pytest.
decisions:
  - B84 Demerger-scope helper. `minute_unadjust.unprovable_suppression_dates(suppressions)` centralises the Q-10 ADDENDUM 2 rule (drop KIND_DEMERGER, keep tier-2 rights) in ONE place; `unadjust_bars` and `rebuild_symbol_raw` both call it, so the two paths cannot drift. Demergers were already absent from k_price/k_shares (a demerger is a Suppression, never a Factor), so "excluded from both k chains" needed no numeric change -- only the un-provability marking changed. The bias-engine suppression path (unions ALL suppressions incl. demergers) is a separate consumer and is untouched.
  - B85 FIX-3 LIVE VERDICT (credentialed SmartAPI, 2026-07-25) -- **CONTRADICTS the ruling's premise; STOP raised (Q-11).** Provability is now correct (every tested day provable, unprovable_days=[]), but gate-1 does NOT pass and the 2016-only "not demerger-adjusted" finding does not generalise: 2022-07 and 2023-06 (pre-demerger) un-adjust to ~0.908 (the Jio demerger factor still IN the 1-min feed) with gate-1 volume ~-10.1%; 2023-09 (post-demerger) un-adjusts to EXACTLY 1.00000 (gate-1 +0.00%); 2016/2019 stay ~0.997 (rights residual only, gate-1 ~-0.29% via k_price -- just past the -0.1% floor, decision B81). So the vendor's demerger adjustment is INCONSISTENT across its history. The FIX-3 change is gate-1-SAFE (every affected day is excluded + counted, nothing silently traded) but rescues NO RELIANCE minute data. Recorded verbatim numbers in QUESTIONS.md Q-11 and the evidence pack; executed the ruling as written (architect owns it) and flagged the contradiction rather than silently overriding.
questions: **Q-10 ADDENDUM 2 recorded** (ruling verbatim + honest EXECUTED note). **NEW Q-11 (class A, OPEN, STOP)**: the ruling's premise ("1-min feed not demerger-adjusted") is contradicted by live evidence -- the feed IS demerger-adjusted for 2020-2023 pre-demerger data but not 2016; no single demerger-scope rule un-adjusts RELIANCE correctly. gate-1 keeps it safe (excludes), but chunk 5B is BLOCKED on RELIANCE-like demerger symbols until the architect rules (options a-d in Q-11). Secondary: the pre-2020 rights residual (~0.29%) also fails gate-1's -0.1% floor (B81 / band-tuning question).
gate: n/a -- chunk 5A has no TRADER gate. This FIX-3 executes Q-10 ADDENDUM 2; acceptance + the STOP evidence are in docs/gate_chunk5A_open8_evidence.md (FIX-3 section) and QUESTIONS.md Q-11.
status-ledger: chunk 5A: built (+ fix Q-10 + fix2 k_shares + fix3 demerger-scope/Q-11-STOP, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + Q-10 fix + fix2 + this FIX-3 are BUILT, not reviewed. `python -m pytest` is green (885/0) from a clean state, fully offline. FIX-3 executes the demerger-scope ruling (demergers no longer mark minute spans un-provable; tier-2/pending rights still do; bias suppression untouched) and is gate-1-safe. **The single most important thing: the FIX-3 live re-run RAISED Q-11 (class A, STOP).** The SmartAPI 1-minute demerger adjustment is INCONSISTENT across history (2016 not adjusted, 2022-2023 adjusted), so the ruling's premise is wrong and no single demerger-scope rule un-adjusts RELIANCE correctly; gate-1 excludes the affected days (safe) but the demerger-exclusion rescues no RELIANCE data. NEXT ACTIONS: (1) the FRESH 5A review reviews the whole of chunk 5A INCLUDING this FIX-3 -- pay attention to B84 (the helper centralising the demerger filter) and B85 (the live contradiction); (2) the ARCHITECT rules Q-11 (era-aware demerger scope / demerger-aware clamp / accept-and-disclose / switch-source) BEFORE chunk 5B backfills RELIANCE-like demerger symbols; (3) chunk 5B on NON-demerger symbols is not blocked and can proceed on this ingest path once 5A is reviewed-PASS.

---

## [2026-07-25 18:40] chunk 5A · fix · done
scope: chunk5A FIX-2 (two items from the architect's reading of the FIX report). (1) VOLUME FACTOR REFINEMENT: split the single Q-10 k_cum into k_price (ALL factors -> divides price) and k_shares (SHARE-COUNT factors only: bonus/split/consolidation -> multiplies volume), so a special dividend no longer over-corrects volume. (2) EXECUTED the staged 4b RELIANCE probe LIVE (credentialed SmartAPI, polite pacing): the 2024-10 provable window reconciles, and the 2016 demerger probe returns a decisive verdict.
files: edited src/acumen/minute_unadjust.py (cumulative_factor gains a `kinds` filter; unadjust_price_paise uses k_price, unadjust_volume uses k_shares; unadjust_bars computes both per day; DayUnadjust.k_cum -> k_price + new k_shares field; identity = both==1; docstrings), src/acumen/corp_actions.py (new SHARE_COUNT_KINDS = {bonus, split}), src/acumen/minute_backfill.py (rebuild_symbol_raw identity-skip now requires k_price==1 AND k_shares==1; factor-print tags price+volume vs price-only; report strings), tests/test_minute_unadjust.py (special-dividend+bonus volume-by-bonus-only unit + gate-1 tests; kinds-filter test; k_cum->k_price/k_shares renames; DayUnadjust keyword constructions), QUESTIONS.md (Q-10 ADDENDUM: volume ruling verbatim + RELIANCE probe verdict), docs/gate_chunk5A_open8_evidence.md (FIX-2 section). CONTEXT.md/plan.md untouched; no file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored, not committed): refreshed the NSE CA day-cache under data/nse/ while building the RELIANCE factor table live; the operator's data/minute_store (TCS only) was NOT touched -- the probe used a throwaway scratchpad store.
tests: 883 passed / 0 failed (was 880 at chunk5A-fix; +3 net -- kinds-filter, special-dividend+bonus unadjust, gate-1-via-k_shares). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state (caches cleared first). The live SmartAPI/NSE pulls are a SCRATCHPAD SCRIPT, never pytest.
decisions:
  - B81 k_price/k_shares split. `corp_actions.SHARE_COUNT_KINDS = {KIND_BONUS, KIND_SPLIT}` is exactly the ruling's "bonus, split, consolidation" (a consolidation is a reverse split, already parsed as KIND_SPLIT). `cumulative_factor(..., kinds=None)` = k_price (all factors, divides price, unchanged from Q-10); `kinds=SHARE_COUNT_KINDS` = k_shares (multiplies volume). A special dividend is in k_price only, so it no longer over-corrects volume (the FIX-report hazard). TCS (bonus-only) is UNCHANGED because k_price==k_shares for it; F10 (post-CA identity) is UNCHANGED. A RIGHTS issue IS a share-count change but the ruling's verbatim enumeration lists only bonus/split/consolidation and a rights k is a TERP blend, not a clean share ratio -- executed OUT of k_shares as written, flagged for the architect.
  - B82 rebuild_symbol_raw identity-skip now requires BOTH k_price==1 AND k_shares==1. k_price alone would wrongly SKIP a day whose only in-window factor is a special dividend (k_shares==1 but k_price<1 -- the price still needs dividing). TCS never hits this (bonus-only), but the guard is now correct in general.
  - B83 4b RELIANCE VERDICT (live, credentialed, 2026-07-25). 2a (2024-10-25, bonus-only): un-adjusts to the raw daily H/L 268870/264400 EXACTLY (0 paise, within 2); gate-1 volume via k_shares gap 0.546% -> PASS. 2b (2016 demerger probe): median R_unadj (our-un-adjusted / raw daily) = 0.99666 (~1.000), NOT the ~0.908 a demerger-adjusted feed would show -> **the SmartAPI 1-MINUTE feed is NOT demerger-adjusted; full RELIANCE minute history is PROVABLE; the surgical clamp fallback is NOT triggered.** Corollary: this DIFFERS from the ONE_DAY feed (chunk-4: RELIANCE daily 2020-07-13 ratio 0.4539 IS demerger-adjusted) -- the two feeds adjust different event sets. 2c: identity-skip guard confirmed on real data (post-bonus 2024-11-04 byte-for-byte, skipped; pre-bonus 2024-10-25 rewritten). Minor caveat: ~0.33% price residual on pre-2020-rights days (vendor rights-equiv ~0.9873 vs our TERP 0.99061) -- within gate-1's band, tick-flagged not excluded.
questions: **Q-10 ADDENDUM recorded** (volume ruling verbatim; RELIANCE probe verdict with numbers -- QUESTIONS.md + evidence pack). No NEW class-A hole raised, but ONE follow-up FLAGGED for the architect (before chunk 5B): the ruling conditioned the un-provable/clamp fallback on "if the vendor demerger-adjusts"; 2b settles that = NO for minutes, so a pre-demerger RELIANCE minute day is actually provable -- yet the code still treats the demerger suppression as an unknown-factor event that marks such days UN-PROVABLE (gate-1 would exclude them; the clamp could move to post-2023, falsely dropping ~7 years of provable data). NOT changed this session (Class-A policy the architect ruled on conditionally; out of FIX-2's two-item scope). The demerger's bias-pair suppression in the daily/bias engine (CONTEXT 3.2) is a separate concern and stays.
gate: n/a -- chunk 5A has no TRADER gate. This FIX-2 executes the Q-10 addendum; acceptance evidence is docs/gate_chunk5A_open8_evidence.md (FIX-2 section).
status-ledger: chunk 5A: built (+ chunk5A-fix Q-10 + chunk5A-fix2 k_shares/RELIANCE-probe, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + the Q-10 fix + this FIX-2 are BUILT, not reviewed. `python -m pytest` is green (883/0) from a clean state and fully offline. The un-adjustment now splits k_price (price) from k_shares (volume, share-count only) per the architect's ruling; TCS/F10 identities are unchanged; the special-dividend over-correction is gone. The 4b RELIANCE probe ran LIVE and settled the open empirical question: the 1-minute feed is NOT demerger-adjusted, so full RELIANCE minute history is provable (no surgical clamp). NEXT ACTIONS: (1) the FRESH 5A review session reviews the whole of chunk 5A INCLUDING this FIX-2 -- pay attention to B81 (k_price/k_shares correctness; the rights-excluded-from-k_shares note), B82 (identity-skip needs both factors), and B83 (the live RELIANCE numbers); (2) the ARCHITECT rules the flagged follow-up (does a demerger still mark a MINUTE day un-provable, now that minutes are known NOT to be demerger-adjusted?) BEFORE chunk 5B backfills RELIANCE, else 5B will falsely exclude 2016-2023 RELIANCE minutes; (3) chunk 5B runs the full-universe backfill on this ingest path and will surface real demerger/pending-rights symbols.

---

## [2026-07-25 17:10] chunk 5A · fix · done
scope: executed the architect's Q-10 ruling (OPEN-8 remedy) -- un-adjust SmartAPI 1-minute candles back to RAW on ingest using the chunk-3 factor table, so the minute store holds RAW ONLY and CONTEXT 7-E11 stands unchanged. Built the pure un-adjustment core, wired it into the ingest path + a rebuild migration + a per-symbol factor-table assembler, added the fetch_date to the window ledger, and produced the TCS acceptance evidence on real data (gate-1 by year 0%/0%/56.5% -> 100%/100%/97.2%).
files: created src/acumen/minute_unadjust.py (PURE), tests/test_minute_unadjust.py; edited src/acumen/minute_backfill.py (SymbolFactors + build_symbol_factors; un-adjust in _fetch_and_store_window; rebuild_symbol_raw + unadjust_stored_day + gate1_by_year; --rebuild/--acceptance CLI + factor-table assembly in run()), src/acumen/minute_store.py (fetch_date column on the window ledger + tolerant read of a pre-column ledger; RAW docstring), tests/test_f10_volume_gate.py (the 4d identity assertion), QUESTIONS.md (Q-10 ruling verbatim + RESOLVED-executed), docs/gate_chunk5A_open8_evidence.md (Q-10 REMEDY section: ruling + 4a/4b/4c/4d), STATUS.md, PROGRESS.md. CONTEXT.md/plan.md untouched; no file under poc/ or tests/fixtures/ touched (no fixture changed). DATA (gitignored, not committed): rebuilt data/minute_store TCS to RAW in place (pre-2018-bonus days divided by k=0.5; post-bonus days identity/untouched); refreshed the NSE CA day-cache under data/nse/ca while assembling the TCS factor table.
tests: 880 passed / 0 failed (was 860 at the 5A build; +20 this fix -- 18 in test_minute_unadjust.py, +1 F10 identity, +1 pre-existing delta). Fixtures touched: NONE. Full suite verified green OFFLINE from a clean state (the un-adjust core is pure; the ingest/rebuild integration tests use fakes + a real tmp Parquet store; the live NSE-CA fetch + store rebuild are SCRIPTS, never pytest).
decisions:
  - B73 The un-adjustment is the exact INVERSE of chunk-3's pairwise adjust: the ruling's `k_cum = product of factors with ex-date in (D, F]` IS `corp_actions.factors_between(factors, D, F)` (the same half-open (previous, current] window the bias engine adjusts with). So minute_unadjust adds NO factor logic -- it divides price / multiplies volume by the same chunk-3 table. Decimal throughout, one half-even rounding at the end (CONTEXT 7-E11), exactly as adjust_pair rounds once.
  - B74 `k_cum == 1` (a recent, post-last-CA day whose factor window is empty) is the EXACT identity: fetched is returned byte-for-byte, and tick-snap is SKIPPED (never re-grid an already-raw price against a possibly-coarser current tick). This is why F10 (all 2026) is untouched -- asserted directly (test_f10_days_unadjust_to_the_exact_identity), and why gate-1 for 2019+ is byte-identical before/after.
  - B75 Tick-snap: an un-adjusted price within 2 paise of the nearest tick is snapped onto it (E11 "tick grid preserved"); one further off is LEFT as the divided value and the day is flagged+counted (never mis-snapped across > tolerance). TCS's current master tick is 0.10 but pre-2024 TCS traded on a 0.05 grid, so old TCS days flag (cosmetic diagnostic) rather than corrupt -- gate-1 is the correctness proof and passes 100%.
  - B76 Un-provable spans (an event with NO factor in (D, F] -- a demerger Suppression or a Q-6-pending rights) are marked un-provable: the partial un-adjustment is still stored so the day is visible, but gate-1 (the ruling's per-day PROOF) fails it -> excluded + counted (CONTEXT 7-E3). A systematically-failing leading span moves the symbol's minute clamp to post-event (systematic_unprovable_floor, the ruling's surgical fallback). TCS has neither, so nothing is excluded and the clamp is unchanged.
  - B77 The window ledger gained a `fetch_date` column (k_cum is fetch-dated). window_outcomes now reads the file's OWN schema and fills fetch_date=None for a ledger written before the column (the existing TCS ledger), so nothing breaks; fetch_date_for_day falls back to attempted_at.date() for those rows. New backfills record fetch_date explicitly (pinned by test).
  - B78 build_symbol_factors assembles the per-symbol table from the NSE CA history (opt-in, day-cached) with cum-closes read from the RAW daily store, so bonus/split/rights/special-dividend factors are concrete; demergers -> suppressions, unpriced rights -> pending ex-dates. TCS resolves to ONE factor: 2018-05-31 Bonus 1:1 k=0.5 (cum with a Rs 29 dividend; bonus takes parse precedence) -- no demergers, no pending rights, so TCS is fully provable.
  - B79 rebuild_symbol_raw un-adjusts an already-fetched store to RAW in place, SKIPPING identity days (k_cum==1: already raw, no rewrite) so it touches only the days that change (~pre-2018-bonus for TCS). The persisted TCS store now reads raw everywhere (2016-10-03 first-min open Rs 2450.00 not the adjusted Rs 1225; 2018-05-30 Rs 3509 -> 2018-06-01 Rs 1754, the raw bonus-halving). A Windows os.replace hiccup stopped an earlier run on an identity month (a no-op); the skip-identity fix is why it now only rewrites the meaningful days. Non-idempotent against a second run on an already-raw store -- guarded by checking 2016 gate-1 is still ~0% before rebuilding.
  - B80 4b (RELIANCE) needs a live credentialed SmartAPI re-pull, which this automated session did NOT run. The RELIANCE math is already proven via a golden on the REAL OPEN-8 numbers (fetched 2024-10-25 H/L 134435/132200 / 0.5 -> raw 268870/264400 to the paisa). The 2024-10 window is provable (bonus-only). A 2016 RELIANCE window is the demerger PROBE: if the vendor demerger-adjusts the 1-minute feed (as it does the daily feed) the window is un-provable -> excluded + clamp moves; if not, it reconciles 1.000x. The re-pull settles which; both are handled by the code. Flagged for the operator with exact commands in the evidence pack.
questions: **Q-10 RESOLVED -- executed** (ruling recorded verbatim in QUESTIONS.md BEFORE the code; un-adjust-on-ingest is code with tests behind every clause: raw_price = fetched/k_cum, raw_volume = fetched x k_cum, one half-even round, tick-snap both sides, fetch-date ledger, un-provable/suppression, identity). **OPEN-8 stays RESOLVED = ADJUSTED** (that was the finding); Q-10 is the remedy for the E11 contradiction the finding created. No new class-A hole raised. ONE empirical follow-up flagged (not a STOP): whether the SmartAPI 1-MINUTE feed demerger-adjusts (the OPEN-8 probe only tested a post-demerger RELIANCE 1-min day); the RELIANCE 2016 re-pull (4b) settles it, and the code already handles both outcomes.
gate: n/a -- chunk 5A has no TRADER gate. This fix EXECUTES the Q-10 ruling; the acceptance evidence pack is docs/gate_chunk5A_open8_evidence.md (Q-10 REMEDY section).
status-ledger: chunk 5A: built (+ chunk5A-fix Q-10, unreviewed -- joins the 5A review)
state-for-next-session: Chunk 5A + this Q-10 fix are BUILT, not reviewed. `python -m pytest` is green (880/0) from a clean state and fully offline. The TCS minute store now holds RAW same-day prices (rebuilt in place); gate-1 by year on the persisted store is 100%/100%/97.2% for 2016/2017/2018, unchanged (identity) for 2019+. CONTEXT 7-E11 is UNCHANGED (the store now satisfies it). Chunk 6 (POC) is UNBLOCKED by Q-10. NEXT ACTIONS: (1) a FRESH session reviews chunk 5A INCLUDING this fix against personas/quant_reviewer.md AND personas/code_reviewer.md (plan.md review type QC) and writes docs/reviews/REVIEW_5A.md -- pay special attention to the un-adjust=inverse-of-adjust identity (B73), the identity/tick-snap guards (B74/B75), the un-provable/clamp fallback (B76), and the fetch-date ledger back-compat (B77); (2) the OPERATOR runs the 4b RELIANCE re-pull to settle the demerger-minute question and, if it un-adjusts cleanly for the universe, proceeds; (3) chunk 5B (full-universe backfill) runs on the same un-adjusting ingest path -- it will build each symbol's factor table via build_symbol_factors and will surface real demerger/pending-rights symbols whose pre-event spans clamp (the surgical fallback), which chunk 5B must report. IMPORTANT for the reviewer: the un-adjust volume direction (x k_cum) is exactly per the ruling; note that dividend factors scale price but not vendor volume, so a symbol with a SPECIAL dividend in the window could see a small volume over-correction -- immaterial within gate-1's band, but a reviewer should weigh it (TCS has no special dividend in-window, so it does not arise here).
scope: recorded the trader's Round-3 receipts (OPEN-1 resolved=1000, Q32 confirmed, new Q-8/Q-9) and built chunk 5A -- the SmartAPI client, instrument-master loader, resumable 1-minute Parquet store + window ledger, the CONTEXT 4.5 gates 1-3, the 15-minute aggregator, and the REAL chunk-4 minute loader -- then ran the live TCS backfill Oct-2016->today and RESOLVED OPEN-8: the SmartAPI 1-minute feed is ADJUSTED (STOP).
files: created src/acumen/smartapi_client.py, src/acumen/instrument_master.py, src/acumen/aggregate.py, src/acumen/quality_gates.py, src/acumen/minute_store.py, src/acumen/minute_backfill.py, scripts/minute_backfill.py, tests/test_smartapi_client.py, tests/test_instrument_master.py, tests/test_aggregate.py, tests/test_quality_gates.py, tests/test_minute_store.py, tests/test_minute_backfill.py, tests/test_f10_volume_gate.py, tests/fixtures/instrument_master_sample.json, docs/gate_chunk5A_open8_evidence.md; edited (PART A prep, separate commit 2d55919) config.yaml + tests/test_config.py + QUESTIONS.md; edited (build) pyproject.toml (acumen-minute-backfill entry point), tests/test_fixture_integrity.py (digest pin), tests/fixtures/PROVENANCE.md, QUESTIONS.md (OPEN-8 resolution + Q-10), STATUS.md, PROGRESS.md. DATA (gitignored, not committed): built data/minute_store (TCS 2016-10-03..2026-07-25, ~906k 1-min candles) via the live run; cached the daily instrument master under cache/. No file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 860 passed / 0 failed (was 749 after PART A prep; +111 this chunk). Fixtures touched: NONE existing changed. ONE new frozen fixture (instrument_master_sample.json, a DERIVED cut of 5 real NSE -EQ rows + 2 distractors) added and digest-pinned; provenance in PROVENANCE.md. Full suite verified green OFFLINE (the socket guard is untouched; every test runs on frozen fixtures or injected fakes). The live SmartAPI pulls are SCRIPTS only, never pytest.
decisions:
  - B63 SIX-module split by the CONTEXT-6 pure/IO rule: smartapi_client (network I/O + pure parse_candles), instrument_master (download/cache + pure find_instrument), aggregate (PURE 15-min), quality_gates (PURE gates 1-3), minute_store (storage), minute_backfill (orchestration + CLI + the real minute_loader). Pure pieces test with no network; the network sits behind allow_network / injected fakes.
  - B64 The SmartAPI client is fully injectable (connect_factory / totp_provider / sleep), so its retry ladder, session-refresh, the ONE_DAY 00:00 trap and the +05:30->naive normalization are all tested offline against a fake SmartConnect. Credentials come from .env via config.env_value, are held in a self-redacting Credentials object, and never reach a repr/log/exception (CLAUDE.md rule 4).
  - B65 tick_size = master paise / 100 ALWAYS (CONTEXT 4.3), returned as an exact Decimal; the PoC's `tick/100 if tick>=1` heuristic is deliberately NOT carried, and a non-whole-paise tick raises rather than being rounded. The 5 F7 ticks reproduce tick_sizes.json (Q-2 tripwire); production reads the master.
  - B66 Minute store keyed symbol x month, per-DATE replacement (idempotent AND safe across the 30-day window seam -- two windows sharing a month never clobber), fsync-atomic writes, a per-symbol window ledger (present/empty/error; present+empty terminal, error retried) making the backfill resumable.
  - B67 Instrument selection matches exch_seg==NSE AND symbol==<SYMBOL>-EQ (both), so a BSE row of the same name or an NFO derivative sharing `name` is never chosen; missing -> raise, never a guess.
  - B68 **PoC-discovered live: SmartAPI ONE_MINUTE caps the RESPONSE at 8000 candles** and silently drops the oldest when a window exceeds it (a 30-day window returned exactly 8000, first-stamped 13:25). The backfill window is therefore 28 calendar days (<= 20 trading days = 7500 candles < 8000), which cannot truncate; a defensive flag fires if any window ever hits the cap, and gate 2 excludes any residual truncated day.
  - B69 Gate-3's candidate factor k is read from the RAW daily store's OWN price gap across the ex-date (raw_close(ex)/raw_close(pre-ex)), not an external constant -- self-contained and robust because RAW (ratio ~1.0) and ADJUSTED (~k) are far apart.
  - B70 OPEN-8 RESOLVED = ADJUSTED -> STOP. Recorded the verdict + numeric evidence in QUESTIONS.md and docs/gate_chunk5A_open8_evidence.md; raised Q-10 (the E11 raw-same-day contradiction) as class-A, BLOCKING chunk 6. NO un-adjustment applied and NO E11 edit made -- both are the architect's call (STOP rule). The store holds the feed as fetched, and gate 1 flags every affected old day.
  - B71 The vendor SmartConnect logs full request headers (Bearer JWT + API key) via logzero on a failed request; _quiet_library_logging raises those loggers above ERROR before any request, so a transient timeout cannot spill a credential (CLAUDE.md rule 4). Pinned by test.
  - B72 The real minute_loader returns the store's day as bias.Candle in stamp order, or None when the day has no candles (the R1-Q6 old-date carry) -- satisfying the chunk-4 MinuteLoader interface with no change to bias_engine.
  - Review-anticipation fixes (4 self-review findings, all confirmed + verified, fixed with regression tests before commit): gate-2 now tests ONLY high<low and close-outside-[low,high] (removed an unspecified open-in-range check); gate-2 no longer excludes a whole day for an out-of-session bar (CONTEXT 7-E2 is candle-level; a Muhurat-only day still excludes via missing>15); removed the dead _TRANSIENT_MARKERS list + its misleading docstring (unrecognized non-success replies default to retry, which is correct); pending_windows now compares the stored window_end so a later run with a larger --to re-fetches the extended tail instead of silently skipping it.
questions: **OPEN-1 RESOLVED** (risk_per_trade=1000, executed). **Q32 CONFIRMED** (Row Size 24). **OPEN-8 RESOLVED = ADJUSTED** (gate-3, STOP). **NEW Q-8** (POC 8- vs 9-candle window; blocks chunk-6 gate closure, not build). **NEW Q-9** (reference==POC above-branch; blocks one chunk-7 branch). **NEW Q-10** (class A, OPEN, STOP): OPEN-8=ADJUSTED contradicts CONTEXT 7-E11's raw-same-day intraday assumption -> BLOCKS chunk 6 until the architect chooses un-adjust-on-ingest (via the chunk-3 factor table) vs amend-E11 vs restrict-window vs switch-source.
gate: n/a -- chunk 5A has no TRADER gate. It RESOLVES OPEN-8, and the verdict is ADJUSTED (STOP): the evidence pack is docs/gate_chunk5A_open8_evidence.md and chunk 6 is blocked on Q-10.
status-ledger: chunk 5A: built
state-for-next-session: Chunk 5A is BUILT, not reviewed. `python -m pytest` is green (860/0) from a clean state and fully offline. History ends at `chunk5A: smartapi client and minute backfill (unreviewed)`; the prep commit `chunk5A-prep: record Round-3 receipts; set risk_per_trade=1000 (OPEN-1 resolved)` (2d55919) precedes it. GOLDENS all pass: F10 (25 PoC days re-derived through the new ingest+store+gate pipeline; every gap% in [+0.02%,+3.6%] and passing the band), the 15-min aggregator golden (TCS 2026-07-14 vs an independent groupby), the F7 tick cross-check, and the clamp golden (pre-2016-09 plans no windows). LIVE RUN (TCS): depth 2016-10-03, 2430 symbol-days to 2026-07-25, 129 windows all present/0 errors/~906k candles; gate-1 pass rate 0%/0%/56.5%/98-100% by year (the ADJUSTED signature); gate-2 excluded 18 genuine non-standard/partial days (Diwali Muhurat, 2020 COVID halts, today's open session). OPEN-8 = ADJUSTED with RELIANCE ratios 0.5/0.5/0.50011 and TCS pre-2018-bonus price x0.5 / volume x2. **The single most important thing for the next sessions:** chunk 6 (POC) is BLOCKED by Q-10 -- the stored 1-minute prices are back-adjusted, so for a day before a later CA they are NOT raw same-day rupees; the architect must resolve Q-10 (un-adjust vs amend E11) before chunk 6 consumes minute data. NEXT ACTIONS: (1) a FRESH session reviews chunk 5A against personas/quant_reviewer.md AND personas/code_reviewer.md (plan.md review type QC) and writes docs/reviews/REVIEW_5A.md -- pay special attention to the OPEN-8 evidence, the 8000-candle window cap (B68), and the four self-review fixes; (2) the architect resolves Q-10 before chunk 6; (3) chunk 5B (full-universe backfill) can proceed on the same infrastructure once 5A is reviewed-PASS (it will hit Q-10 the same way, and its gate-1 will flag the same old-year adjustment across the universe).

---

## [2026-07-25 13:38] ops · github · done
scope: connected the repo to its private GitHub mirror (origin = github.com/chinmoypaul8897/acumen), pushed the COMPLETE history + all five pass-tags, added a machinery-only README, and codified remote push discipline in CLAUDE.md. Not a chunk — no strategy/code/test changes.
files: created README.md; edited CLAUDE.md (Git rules: one appended bullet — remote push discipline) and PROGRESS.md (this entry). STATUS.md UNTOUCHED (ops, not a chunk). No file under src/, tests/, poc/, fixtures touched; CONTEXT.md/plan.md untouched.
tests: NOT rerun — zero code/test/fixture changes this session. Suite last verified green 748/0 at chunk4-pass (656bfbc), unchanged. Fixtures touched: NONE.
decisions:
  - OPS1 The architect-authorized CLAUDE.md text was appended as a SINGLE unwrapped bullet to match the existing Git-rules formatting (every other rule in that section is one line); the wording is verbatim — only the prompt's cosmetic line-wrapping was normalized, no word added/removed/changed. Flagged so a future reader comparing the prompt's multi-line quote to the one-line bullet does not read the whitespace as a deviation.
questions: none. No CONTEXT.md gap hit — this was a pure operations task with the remote URL, README shape, and CLAUDE.md text all fixed by the architect.
gate: n/a
status-ledger: STATUS.md UNTOUCHED — ops session, not a chunk. All chunk states unchanged (chunk 4 stays reviewed-PASS; chunk4-pass tag intact on the mirror).
state-for-next-session: Remote is LIVE and mirrored — pre-flight passed (clean tree, main, identity chinmoy-paul <chinmoypaul8897@gmail.com>; no secret/data/cache/logs path ever in history; .env ignored; five pass-tags all on "reviewed PASS" commits; repo 1.22 MiB). origin was verified EMPTY before the first push; after pushing, remote main == local main and every tag matches (side-by-side SHAs in this session's GITHUB OPS REPORT). PUSH RULE NOW IN FORCE (CLAUDE.md Git rules): EVERY session's final duty is `git push origin main --tags` and its final report must state the pushed SHA; the mirror is backup-only (no web-UI commits/edits/merges, no force-push; STOP and ask the architect on divergence; operator pushes manually if a session dies before pushing). NEXT ACTION unchanged from the chunk-4 review: building may proceed on chunks 5A/6 (depend only on chunk 2, reviewed-PASS) or chunk 7 (depends on 4+6); the chunk-4 TRADER GATE is still pending and must close before chunk 9's run. Whichever session runs next MUST push at end.

---

## [2026-07-25 11:30] chunk 4 · review · done
scope: fresh QC review of chunk 4 (quant_reviewer + code_reviewer, plan.md review type QC) covering FOUR commits d2ea387..a0b595a -- reran the suite from clean and offline, executed all 11 architect-directed checks, reimplemented CONTEXT 3.2 from scratch to grade F9 (15/15, 0 mismatches), built the 20-mutant operator-by-operator boundary matrix on bias.py (10 caught / 4 equivalent / 6 genuine coverage gaps), brute-forced the precedence-guard/RULE_CARRY unreachability over 261,282 candles, re-derived the Q-6/Q-7 redistribution (rights 5/1/1, denominator 296), formally reviewed the incident commit (fsync-before-replace both writers, --rebuild-ledger on a synthetic broken store, quarantine-never-deletes, corrupt-ledger names the command), AST-proved bias.py purity, judged B53-B62, wrote docs/reviews/REVIEW_4.md. VERDICT PASS.
files: created docs/reviews/REVIEW_4.md, tests/test_review4_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; CONTEXT.md/plan.md untouched.
tests: 748 passed / 0 failed (739 from the build reran green unchanged + 9 reviewer probes), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE -- all 7 new chunk-4 fixture digests recomputed independently and match their pins; git diff over tests/fixtures+poc/data across the span shows only ADDED files. The 9 new tests were verified to KILL their mutants: re-applying M13 (>= bmin -> > bmin) and M17 (first-break > -> >=) turned the corresponding tripwires red; bias.py restored byte-identical to HEAD after.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review4_probes.py (9 tests). It closes the six Rule-3/tie/first-break boundary coverage gaps the mutation matrix found (M13-M18: R3 close guards exactly on bodyMin/bodyMax; strict-break = a 1-min touch of P's extreme is not a break), pins the precedence-guard/RULE_CARRY unreachability over a dense deterministic grid (the build's property test covered only RULE_CARRY, randomly), and pins the demerger AND Q-6 tier-2 rights suppression RESUME at E+3 (the build pinned blocking at E+1 but not resume). Every one passes on HEAD and was verified to fail on its mutant.
  - The mutation matrix ran in an isolated git worktree; all other probes read the frozen fixtures, the operator store read-only, or a hermetic tmp_path store. ZERO network traffic. The worktree was restored and removed; bias.py is byte-identical to HEAD.
questions: none raised. Q-6/Q-7 are correctly RESOLVED and EXECUTED -- the ruling texts are verbatim in QUESTIONS.md in the prep commit that precedes the build, and every tier is code with a test behind it (tier-1 face reconstruction defuses the GREENPLY trap, tier-2 emits a Suppression consumed like a demerger, tier-3 overrides refuse an uncited entry; Q-7's three bands classify by D/P_cum with the 2% boundary inclusive). OPEN-8 evidence recorded. No new class-A hole hit: every bias operator matches CONTEXT 3.2 character-for-character.
gate: pending -- the chunk-4 TRADER GATE is a SEPARATE field from code review. Code review is PASS; the gate stays OPEN. The trader must confirm the F9 real-day bias table AND the two OPEN-4 assumptions (green-tie mirror -> bearish; doji -> carry) via the evidence pack docs/gate_chunk4_bias_evidence.md before the chunk-9 run (M3, plan.md §2). Gate outcome will be recorded in STATUS.md as gate-closed(evidence path) by a later session.
status-ledger: chunk 4: reviewed-PASS
state-for-next-session: Chunk 4 is reviewed-PASS at tag chunk4-pass; python -m pytest is green (748/0) from a clean state and fully offline. Six findings are in docs/reviews/REVIEW_4.md -- none blocking, none requiring a fix session. F1 is LOW (six Rule-3/tie/first-break boundary coverage gaps, closed in-review by the added probe file; the code was already correct at every boundary). F2 is LOW (suppression matches on exact trading-date equality; a demerger/rights ex-date on a NON-trading day would evade the block -- unreachable today since NSE ex-dates are trading days, but chunk 9 should assert/normalize it). F3-F6 INFO: (F3) the frozen-window dividend band counts 245/26/18 are not reproducible against the non-frozen store (re-run 197/49/26+17 pending; both sum 289; the classifier is correct and the counts were never a committed test -- do not treat them as an anchor; the rights 5/1/1 and denominator 296 ARE reproducible); (F4) F9 is adjustment-free so the real-CA-in-window pairwise path is only synthetic-tested -- real-day R3 + CA verification is chunk 12; (F5) OPEN-4 green-tie/doji remain UNCONFIRMED assumptions -- the trader gate must close before chunk 9; (F6) the corrupt-ledger error embeds the pyarrow exception type as context, intentional. NEXT ACTIONS: (1) Paul relays the chunk-4 evidence pack to the trader and records the CONFIRMED gate outcome; (2) building may proceed on chunks 5A/6 (both depend only on chunk 2, reviewed-PASS) or chunk 7 (depends on 4+6) -- chunk 4 being reviewed-PASS unblocks its dependents even though the trader gate is still pending (plan.md §2: code review-PASS unblocks building; the gate must close before chunk 9's run).

---

## [2026-07-25 10:25] chunk 4 · build · done
scope: recorded+executed the architect's Q-6 (rights, three tiers) and Q-7 (special dividend, D/P_cum) rulings on the CA-engine side, fixed REVIEW_3 F3, appended the Q-4 IL/D1 note, built and verified the curated data-quirk mechanism and ingested the store's last error date (2020-07-13), then built chunk 4 proper: the CONTEXT 3.2 bias engine as pure functions, its orchestration layer, and the F5/F9 goldens + the trader-gate evidence pack.
files: created src/acumen/bias.py (pure engine), src/acumen/bias_engine.py (orchestration + minute-loader interface + csv_minute_loader), src/acumen/data_quirks.py, src/acumen/rights_overrides.json (committed, empty), tests/test_bias.py, tests/test_bias_engine.py, tests/test_data_quirks.py, tests/test_data_quirks_roundtrip.py, tests/fixtures/f9_tcs_daily.csv, tests/fixtures/f9_tcs_expected.csv, tests/fixtures/quirk_2020-07-13_archive_cut.csv, tests/fixtures/smartapi_oneday_TCS_2020-07-13.json, tests/fixtures/smartapi_oneday_RELIANCE_2020-07-13.json, tests/fixtures/minute/SYNTH_2099-01-05_1min.csv, tests/fixtures/minute/SYNTH_2099-01-06_1min.csv, docs/gate_chunk4_bias_evidence.md; edited src/acumen/corp_actions.py (Q-6/Q-7 execution + F3), src/acumen/ca_report.py (suppressions + unresolved-rights report), src/acumen/bhavcopy.py (quirk hook), QUESTIONS.md (Q-6/Q-7 rulings, Q-4 note, OPEN-8 evidence), tests/test_corp_actions.py, tests/test_ca_goldens.py, tests/test_series_selection.py, tests/test_fixture_integrity.py, tests/fixtures/PROVENANCE.md, STATUS.md. DATA (gitignored, not committed): ingested exactly one date into data/daily_store (2020-07-13, architect-authorized); no other store write. No file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 739 passed / 0 failed (was 686 after the incident; +53 this chunk). Fixtures touched: NONE existing changed -- all pinned digests intact. SEVEN new frozen fixtures added and digest-pinned (2 F9 DERIVED cuts, 2 SmartAPI DERIVED responses, 1 malformed-archive DERIVED cut, 2 SYNTHETIC minute CSVs); provenance for each in PROVENANCE.md. The one ingested store date is data, not a fixture.
decisions:
  - B53 Q-6 tier 1 reconstructs the face value AT a rights ex-date from the symbol's parsed FACE-VALUE-SPLIT history (reconstruct_face_value_paise), NOT from the row's as-of-query faceVal, which is the GREENPLY trap this item documents. The stale faceVal is used ONLY when no split touches the symbol (nothing moved it). S = face + premium; the F3 guard then rejects any S >= P.
  - B54 Q-6 tier 2 emits a Suppression (new dataclass) exactly like a demerger; suppression_dates() unions both, and the bias engine suppresses D where D-1 or D-2 is a suppression ex-date. A rights issue we cannot price is treated like a demerger (no factor, no trade across it), never guessed.
  - B55 Q-6 tier 3 is a committed EMPTY rights_overrides.json; load_rights_overrides() RAISES on any entry that cites no NSE circular. An override that cannot be audited to a circular is the silent guess the whole item forbids.
  - B56 Q-7 removes the pre-announcement close from factor_for entirely (it exists in no held source); a dividend is classified by D/P_cum in three bands (<1% ordinary-fast, 1-2% ordinary-near-threshold, >=2% special). classification is carried on Factor so special_dividend_verifications() needs no re-derivation. The special formula k=1-D/P_cum is unchanged.
  - B57 The bias engine is TWO modules: bias.py is pure (candles in, bias out; the injected minute_loader is a zero-arg provider so evaluate_pair stays pure and calls it only on a Rule-3 day) and bias_engine.py does the I/O (store reads, pairwise adjustment, suppression, seeding). This is CLAUDE.md's pure-engine rule made structural.
  - B58 evaluate_pair follows CONTEXT 3.2's order literally: inside -> R1 -> R2 -> R3 -> carry. The precedence note falls out for free -- an outside bar closing beyond the body is decided by R1 because R1 runs first (verified: the minute loader is not even consulted). RULE_CARRY ("no rule fires") is provably unreachable for a single-sweep bar and is kept only as the spec's catch-all (property test over 5000 random candles).
  - B59 The Rule-3 tie predicate is close-vs-open on the tie 1-minute candle (the trader's Q31 authority): red -> bullish (his answer), green -> bearish (OPEN-4 mirror, flagged), doji -> carry + log (OPEN-4). Both OPEN-4 branches set an open4 flag the evidence pack surfaces for confirmation.
  - B60 F9 is a CONTIGUOUS real-TCS window (2025-06-25..2025-08-29) frozen as a DERIVED cut, chosen adjustment-free (TCS had no split/bonus/special-dividend; max day-over-day gap 7.0% is a genuine move), so raw == adjusted and the trader checks the same numbers on his chart. 15 days selected with a comfortable margin on the deciding inequality -- no boundary-equal case (those live in test_bias.py as synthetic candles). The F9 test is hermetic: it loads the cut into a temp store, so a bare clone runs it with no data/daily_store.
  - B61 The data-quirk mechanism (data_quirks.py) is consulted ONLY after normal validation fails and corrects ONLY a file malformed in exactly the documented way; the store validator is never weakened. Entry 1 (2020-07-13, content year 0020->2020) was verified genuine three ways before the one-date ingest -- see the OPEN-8 note below.
  - B62 The pairwise CA adjustment in the orchestration adjusts the PREVIOUS candle into the CURRENT candle's scale via factors_between + adjust_pair (chunk 3), so the pure engine only ever compares numbers in one scale (CONTEXT 3.2 / 7-E11). Empty factors = no adjustment, which is the correct and tested behaviour for the F9 window.
questions: **Q-6 RESOLVED -- executed** (three-tier rights: recoverable-S via face reconstruction + suppression + committed overrides; the ruling text is verbatim in QUESTIONS.md before the code). **Q-7 RESOLVED -- executed** (D/P_cum classifier + verification list; ruling verbatim). **Q-4** gained the IL/D1 note. **OPEN-8 evidence recorded** in QUESTIONS.md at the architect's direction (CONTEXT ownership stays with the architect): SmartAPI ONE_DAY candles are corporate-action back-adjusted -- RELIANCE 2020-07-13 raw 1935 vs SmartAPI 878.36 (ratio 0.454 = 1:1 bonus x Jio demerger); this answers DAILIES only, and chunk 5A gate-3 (1-min adjustment status) still runs in full. **Gate lesson for chunk 5A's builder:** cross-source price comparisons are only valid raw-to-raw with no intervening corporate-action events, or after bringing both sides to the same scale. No NEW class-A hole was hit in PART B -- CONTEXT 3.2 answered every rule, and the one real outside-bar day in the F9 window correctly took the documented missing-1-min carry.
gate: pending -- evidence pack at docs/gate_chunk4_bias_evidence.md (15 real TCS days in plain English + the two OPEN-4 confirmations). Deadline per plan.md §2: before chunk 9's run. Paul relays to the trader; the ask is "reply CONFIRMED or tell us which row looks wrong".
status-ledger: chunk 4: built
state-for-next-session: Chunk 4 is BUILT, not reviewed. `python -m pytest` is green (739/0) from a clean state and fully offline. History ends at `chunk4: bias engine (unreviewed)`; two prep commits precede it (`chunk4-prep: record+execute Q-6/Q-7 rulings; REVIEW_3 F3; Q-4 series note` and `chunk4-prep: curated data-quirk mechanism; ingest 2020-07-13 ...`). GOLDENS: F5 (the Q31 tie, CONTEXT 8 exact numbers, via the synthetic red-tie minute fixture -> BULLISH) and F9 (15 real TCS days, engine matches every hand computation) both pass. The store now reads error: 0 (6606 file-present, 3096 confirmed-404, span 2000-01-01..2026-07-24) after the 2020-07-13 ingest. Known limits a reviewer should weigh: (a) the R3 and tie goldens are SYNTHETIC minute fixtures -- real-day Rule-3 verification is chunk 12 (per the card); (b) F9 is adjustment-free by construction, so the orchestration's pairwise-adjustment path is exercised only by a synthetic split test, not by a real CA-in-window day; (c) the SmartAPI ONE_DAY evidence was a one-off read-only call and only two symbols; (d) the frozen-window redistribution numbers were computed against the operator's store this session (read-only) and are reported below, not committed as a test (the store is not a fixture). NEXT ACTION: a FRESH session reviews chunk 4 against `personas/quant_reviewer.md` AND `personas/code_reviewer.md` (plan.md review type QC) and writes `docs/reviews/REVIEW_4.md` -- chunk 5A/6/7 gating aside, the chunk-4 TRADER GATE must be closed (trader CONFIRMED) before chunk 9's run (M3).

---

## [2026-07-25 02:40] incident · fix · done
scope: recovered data/daily_store after a power-loss truncated its ledger mid-backfill (REVIEW_2 F6 manifesting; architect approved pulling the F6 fix forward from chunk 13). Diagnosed read-only, quarantined the corrupt files (never deleted), rebuilt the ledger from the surviving monthlies via a new permanent --rebuild-ledger CLI, hardened atomic_io with fsync, and made a corrupt ledger raise clear guidance instead of a pyarrow traceback. Scope was recovery + the F6 hardening ONLY — no strategy code, no chunk work.
files: edited src/acumen/atomic_io.py (fsync file before os.replace in both writers; best-effort POSIX dir fsync; Windows limitation documented), src/acumen/daily_store.py (DailyStore.rebuild_ledger_from_monthlies(); startup-integrity error in outcomes() naming --rebuild-ledger; REBUILD_LEDGER_COMMAND + REBUILT_LEDGER_REASON constants; import url_for), src/acumen/backfill_daily.py (--rebuild-ledger flag + _rebuild_ledger printer; --from/--to now optional with a clear error; main() maps DailyStoreError to a clean exit 2); created tests/test_incident_recovery.py (12 tests). DATA (gitignored, not committed): quarantined 3 corrupt files to data/daily_store/_quarantine/2026-07-25/ with a MANIFEST; wrote a fresh data/daily_store/ledger.parquet. NO fixture touched; no file under poc/ touched; CONTEXT.md/plan.md untouched.
tests: 686 passed / 0 failed (was 674 at the chunk-3 review; +12 incident). Fixtures touched: NONE. The 12 new tests cover: fsync-precedes-rename asserted via monkeypatch (both writers); a failed fsync leaves the prior file intact; a corrupt ledger raises DailyStoreError naming --rebuild-ledger (not a raw pyarrow traceback); main() exits 2 with guidance; rebuild marks present dates and returns 404/error/absent dates to pending; rebuild skips a corrupt month; rebuild from a QUARANTINED month; rebuilt ledger reads back through the normal path; the --rebuild-ledger CLI needs no dates/network and reports a skipped corrupt month; a normal run with no dates asks for them or --rebuild-ledger.
decisions:
  - I1 Quarantine lives OUTSIDE the store's daily/ tree (data/daily_store/_quarantine/2026-07-25/, path-preserving + MANIFEST.txt), so the rebuild's `daily/*/bhavcopy_*.parquet` glob never re-scans it. Corrupt files are MOVED, never deleted (they are the only forensic record of the failure).
  - I2 The rebuild DERIVES the ledger from the row files alone and calls _write_ledger directly — it never reads the old ledger, so it works precisely when the ledger is the corrupt file. Each recovered date is file-present with source_format read from its stored rows, url reconstructed via url_for(day, fmt), http_status=200, row_count = that date's row count, reason=REBUILT_LEDGER_REASON (provenance), and attempted_at=None (the original fetch time is unknown; inventing one would be a lie the ledger later reads as fact).
  - I3 A monthly that will not open is SKIPPED and reported, not fatal — so --rebuild-ledger is safe even if a corrupt month was not quarantined first (its dates just return to pending). Lost confirmed-404/error knowledge is not fabricated; those dates become absent and re-resolve from fresh evidence on the next run (Q-3 safeguard 1 preserved).
  - I4 atomic_io fsyncs the file's DATA before os.replace in BOTH atomic_write_bytes and atomic_write_with (the un-fsynced path was the F6 hole every store/ledger write went through); the directory fsync that makes the rename itself durable is POSIX-only best-effort — Windows cannot open a directory fd and NTFS journals the rename, so it is a documented no-op there. The data-durability half, which is what prevents the incident's valid-name-over-garbage failure, runs on both platforms. This machine is Windows, so only the file fsync is active here; that is the half that mattered.
  - I5 --from/--to became optional at the parser so --rebuild-ledger can run range-free and offline; a normal run missing them prints a clear error naming --rebuild-ledger and exits 2. main() catches DailyStoreError and prints the guidance (exit 2) rather than letting a traceback reach the operator.
  - I6 The corrupt raw_bhav CSV (2009-03-13_archive.csv) was quarantined too, though it is the operator audit trail (--raw-dir), not the store the rebuild reads. It is a genuine corrupt power-loss artifact and a fixture-provenance hazard (REVIEW_2 F3, since the DERIVED fixtures were cut from that archive), so leaving it in place could poison a future fixture cut. Recorded as distinct from the two store files that drove recovery.
questions: none. Scope was fixed by the architect (recovery + F6 only) and CONTEXT.md answered everything needed (Q-3 ledger semantics govern what "pending again" means; F6's own text specified the fix). No new class-A hole hit; REVIEW_2 F7 remains open and is now partially mitigated (noted in its fix log) but not closed.
gate: n/a
status-ledger: STATUS.md UNTOUCHED — this is an incident recovery, not a chunk. Chunk 3 stays reviewed-PASS.
state-for-next-session: The store is recovered and consistent. data/daily_store/ledger.parquet reads cleanly and holds 2,313 file-present dates (2,303 archive + 10 udiff, span 2000-01-03 .. 2026-07-24); its file-present set equals the distinct date set across the 112 surviving monthlies exactly. The corrupt trio (ledger, daily/2009/bhavcopy_2009-03.parquet, raw_bhav/2009-03-13_archive.csv) sits in data/daily_store/_quarantine/2026-07-25/ with a MANIFEST. March-2009 (incl. the incident date 2009-03-13) and every un-backfilled date are pending again. `python -m pytest` is green (686/0). NEXT ACTION for the OPERATOR — resume the backfill; it skips the 2,313 settled dates and re-fetches 2009-03 onward through the new fsync-safe path: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (add `--raw-dir data\raw_bhav` if you want the audit trail; note it is not resumable — REVIEW_2 F3). If a ledger is ever truncated again, one command recovers it: `python scripts/backfill_daily.py --rebuild-ledger --store data\daily_store`. NEXT ACTION for the PROJECT is unchanged: build chunk 4 (bias engine), which still depends only on chunk 3 (reviewed-PASS).

---

## [2026-07-25 02:00] chunk 3 · review · done
scope: fresh QC review of chunk 3 (quant_reviewer + code_reviewer, plan.md review type QC) — reran the suite from clean and offline, executed all 11 architect-directed checks, opened NSE's calculator XLSX with openpyxl and recomputed our k against EVERY worked cell, hand-recomputed the five parser events and the three adjustment gaps from the raw snapshots, mutation-tested the Indian bonus convention (13 catchers) plus 17 more spec/discipline invariants in a throwaway copy, reproduced the 27/282/24/0 join partition and both shared-ratio tallies, re-attacked REVIEW_2 M-F1/M-F2, judged B35–B52, wrote docs/reviews/REVIEW_3.md. VERDICT PASS.
files: created docs/reviews/REVIEW_3.md, tests/test_review3_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 674 passed / 0 failed (668 from the build reran green unchanged + 6 reviewer probes), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE — all 28 poc/data CSVs, the 3 chunk-1 files, the 13 new chunk-3 fixtures and the XLSX oracle match their pinned SHA-256s (recomputed independently, 20/20 for chunk-1/2/3). The 6 new tests were verified to KILL the one surviving mutant from my run (round-per-event instead of round-once): 3 of them fail on that mutation, all pass on HEAD.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review3_probes.py (6 tests). It closes the single coverage gap this review found — adjust_pair's "round the chain once" (B46) had no tripwire because the build's own chain test used exact intermediates (0.5, 0.2) — plus keeps the strongest adversarial probes: an inexact-chain round-once pin, a half-even tie pin, the debt-series-cannot-factor-the-equity case (TATASTEEL-N2), a multi-ratio Indian-convention pin, and the pairwise-is-the-law inequality note.
  - openpyxl was installed into the REVIEW's own scratchpad via `pip install --target`, never into the operator's interpreter (confirmed `import openpyxl` still fails there). The XLSX was read cell-by-cell independently of the build's zipfile+ElementTree reader, and our k reproduced every worked strike/lot/future cell of all three sheets, confirming k=1/AF for bonus+split and k=AF for rights from the sheets' own formulas.
  - This review issued ZERO network traffic. M-F1 and M-F2 were re-attacked on a virtual clock with a fake session; the scratch-store discipline was proven by observing the main-store ledger grow ~15 bytes/2s with NO test running (the operator's concurrent backfill, PID 25236), and by static analysis that no chunk-3 module writes data/daily_store.
questions: none raised. Q-4 and Q-5 are correctly RESOLVED and EXECUTED — the ruling texts are verbatim in QUESTIONS.md in the prep commit that precedes any corp_actions code, and every clause is code with a test behind it (whitelist selection incl. the six-series NTPC pick, the two-whitelist raise, the debt-only empty result, the unknown-series surfacing incl. IL/D1, and the Q-5 excluded-Saturday regression = REVIEW_2 F11's exact case). Q-6 and Q-7 are correctly RAISED and open — 296 of 328 priced events are pending with named reasons, and grep+AST confirm no k=1 default hides on the rights/dividend paths.
gate: n/a
status-ledger: chunk 3: reviewed-PASS
state-for-next-session: Chunk 3 is reviewed-PASS at tag chunk3-pass; python -m pytest is green (674/0) from a clean state and fully offline. Four findings are in docs/reviews/REVIEW_3.md — none blocking, none requiring a fix session. F1 is LOW (a test-coverage gap on B46's round-once, closed in-review by the added probe file; the code was already correct). F2/F3/F4 are INFO: (F2) pairwise adjustment is the spec-defined comparison and a fully-adjusted series can differ by a rounding tie — a note for chunks 4/9, with a kept regression test; (F3) rights_factor returns k>1 for S>=P without raising — economically impossible and unreachable since all rights are Q-6-pending, flagged for whoever answers Q-6; (F4) the inherited REVIEW_2 F3-resume-half / F6-fsync / F7-read-side-invariant remain open and are honestly carried in that fix log. NEXT ACTION: build chunk 4 (bias engine, review type QC, TRADER GATE) — it depends only on chunk 3, which is now reviewed-PASS. Chunk 4 is the first session that consumes factors on real dates; it will hit Q-6/Q-7 the moment a real rights issue or a special dividend falls inside a bias pair — both are STOP-blocked and surfaced as pending, not guessed. Paul's 25-year bhavcopy backfill was still running against data/daily_store throughout this review (PID 25236) and was never touched.

---

## [2026-07-25 00:45] chunk 3 · build · done
scope: recorded the architect's Q-4 and Q-5 rulings verbatim and EXECUTED both, applied the six REVIEW_2 findings the architect scheduled, then built chunk 3 — the corporate-action engine: three fetchers with the repo's gentle/cached/opt-in discipline, ten verbatim source snapshots, the subject parser with its exception report, the CONTEXT 4.2 factor table, `adjust_pair`, the demerger table, fixture F8 against NSE's own calculator, and the cross-source join.
files: created src/acumen/corp_actions.py, src/acumen/ca_report.py, src/acumen/backfill_daily.py (moved out of scripts/), tests/test_corp_actions.py, tests/test_ca_goldens.py, tests/test_series_selection.py, tests/fixtures/series_edge_cases_synthetic.csv, tests/fixtures/ca/ (10 VERBATIM snapshots + 2 DERIVED bhavcopy cuts), docs/nse_adjustment_calculator.xlsx; edited src/acumen/daily_store.py (Q-4 selection + series report + weekend count), src/acumen/calendar.py (Q-5 exclusion + counts), src/acumen/nse_http.py (F1 pacing, F2 retry policy, cached_json fetcher hook), src/acumen/bhavcopy.py (F4 example), scripts/backfill_daily.py (now a launcher), pyproject.toml (two console entry points), tests/test_backfill_script.py, tests/test_bhavcopy.py, tests/test_daily_store.py, tests/test_derived_calendar.py, tests/test_review2_probes.py, tests/test_fixture_integrity.py, tests/fixtures/PROVENANCE.md, QUESTIONS.md, docs/reviews/REVIEW_2.md (append-only fix log), STATUS.md, PROGRESS.md. No file under poc/ was touched; **no write of any kind to data/daily_store**.
tests: 668 passed / 0 failed (was 513 at the chunk-2 review; +38 prep, +117 chunk 3), verified from a clean state with .pytest_cache and every __pycache__ deleted first, fully offline. Fixtures touched: NONE existing — all 28 poc/data CSVs and all 6 chunk-1/2 files still match their pinned SHA-256s. Thirteen NEW frozen files added and pinned by digest in the same commits (1 SYNTHETIC for the Q-4 edge cases, 10 VERBATIM CA snapshots, 2 DERIVED bhavcopy cuts), plus the F8 oracle XLSX pinned in docs/. Provenance for every one is in tests/fixtures/PROVENANCE.md, including each source CSV's own digest, line count and kept-row count.
decisions:
  - B35 The Q-4 whitelist's RAISE takes priority over its precedence order. The ruling states both "EQ, else BE, else BZ" and "two whitelist series on one symbol-date → raise loudly"; since the raise fires first, the order never actually decides anything. It is kept as the documented reading of the ruling and as the report's ordering, not as live logic.
  - B36 `INSTRUMENT_SERIES` is one module constant and the ONLY series choice in `src/` (pinned by an `ast` walk). The non-instrument families are matched by FAMILY (`N*` debt, `P*` partly-paid, `BL`) rather than by an enumerated list, because NSE mints codes within a family freely — NTPC alone carried N4/N6/N7/NB/NC/ND across two days.
  - B37 `coverage_summary` counts weekend sessions from the LEDGER only (cheap, opens no month file); the series report is a separate, heavier query the backfill run calls once. A summary that had to read 300 month files would stop being callable after a Ctrl-C.
  - B38 The backfill implementation moved into the package; `scripts/backfill_daily.py` is a launcher whose module body executes nothing, with the `sys.path` fallback INSIDE a function that only `__main__` calls and that fires only when the package is not installed. Removing it entirely needs `pip install -e .`, and this session would not change the operator's environment while his 25-year backfill was running against that exact command (REVIEW_2 F12, half closed — stated as such in the fix log).
  - B39 `nse_http.cached_json` gained a `fetcher` hook rather than corp_actions growing its own cache. Three sources need three sessions, two decoders and their own pacing; only the POLICY (one live pull per day, opt-in, atomic write) must be shared, and now it provably is.
  - B40 Parse precedence puts DEMERGER first, ahead of every ratio kind. A demerger suppresses trading entirely (CONTEXT 3.2) while every other kind only scales a price, so a missed demerger is the costliest possible miss; "Scheme Of Arrangement / Bonus 1:2" resolves to demerger, and there is a test for exactly that.
  - B41 A dividend subject's cash components are SUMMED per ex-date ("Final Dividend Rs 6 Plus Special Dividend Rs 4" → 1000 paise). CONTEXT 4.2 classifies by SIZE against a reference price, not by the word "special", so what its threshold needs is the total cash per share leaving the company that day.
  - B42 The `informational` bucket requires BOTH a recognised meeting phrase AND no amount AND no ratio. Without the second half it would become a dustbin: "Annual General Meeting / Something Rs 5 Per Share" raises instead, because a subject carrying money this parser did not understand must be reported.
  - B43 A consolidation (reverse split) is treated as CONTEXT 4.2's own "face value A→B, k = B/A" row, but ONLY when the subject carries the From/To values a split needs. BSE's bare "Consolidation of Shares" therefore stays an exception rather than becoming a guess.
  - B44 The money regex accepts BSE's `Rs. - 5.5000` dash form. Measured: without it, every one of the 348 BSE dividend rows in the frozen windows became a parse exception — a parser bug wearing an exception report as camouflage.
  - B45 Corporate actions on a non-instrument SERIES are filtered out before parsing (opt-out via a flag). A coupon on a listed NCD is not an action on the equity we trade; this is the Q-4 ruling applied to a second data source, and it is what keeps the exception report readable — it removed every GS/IV/RR distribution row without a single special case.
  - B46 `adjust_pair` multiplies the whole factor chain at full `Decimal` precision and rounds ONCE, half-even, to integer paise (CONTEXT 7-E11). Rounding per event lets two chained events drift a paisa; half-even is unbiased, so a 25-year history of adjustments does not creep upward. An empty chain returns the price untouched with no rounding at all.
  - B47 Rights and dividend factors REQUIRE their prices and raise when absent; `build_factor_table` returns them as `pending` with the reason. A missing price must never become k = 1, because from the outside that is indistinguishable from "this event needs no adjustment" (Q-6, Q-7).
  - B48 The cross-source join matches on the KIND as well as the symbol-date. Found by the join itself: ROTO declared a bonus AND a dividend on 2023-07-07 and MAANALU a bonus AND a split on 2023-07-27, and a symbol-date-only join called both a disagreement when all three sources agreed on every event.
  - B49 BSE is joined by security NAME (CONTEXT 4.2: it publishes no ISIN), so a miss is reported as `not-found`, never as a disagreement. JMCPROJECT is the live case: NSE has its rights issue, BSE lists nothing under that name.
  - B50 Every network pull this session made ran at 1 request / 4s instead of the usual 1/2s, because the operator's 25-year backfill was running against the same infrastructure in another process. The combined rate stayed at the repo's usual pace.
  - B51 F8 READS NSE's calculator XLSX (stdlib `zipfile` + `ElementTree`, no new dependency) instead of only transcribing its numbers. A transcription cannot notice that it has drifted from its oracle; this also pins the relation CONTEXT 4.2 leaves implicit — NSE's bonus/split AF divides a strike (k = 1/AF) while its rights AF multiplies one (k = AF).
  - B52 `acumen.ca_report` is an entry point, not a library: it prints the two reports the card names, offline from the snapshots by default. It reads the F&O list from the cached pull if there is one and falls back to the frozen snapshot, so a reviewer with no cache gets the same output.
questions: **Q-4 RESOLVED — executed** (ruling recorded verbatim before any code; whitelist selection, the two-whitelist raise, the empty-not-error case and the unknown-series report are all code with tests behind them). **Q-5 RESOLVED — executed** (weekend sessions excluded from trading days, bias pairs and trading; still surfaced and counted; REVIEW_2 F11's measured case is now the regression test). **Q-6 RAISED** (class A, open, non-blocking for chunk 3, BLOCKS rights factors on real events): CONTEXT 4.2's rights formula needs the issue price S, but every NSE rights subject in the frozen windows states a PREMIUM instead, and the row's own `faceVal` is the face value as of the QUERY — GREENPLY's January-2016 rows already show 1, the post-split value. **Q-7 RAISED** (class A, open, non-blocking, BLOCKS automatic special-dividend classification): CONTEXT 4.2 tests the 2% threshold against the PRE-ANNOUNCEMENT close, and `caBroadcastDate` is null on all 438 rows of all three frozen windows, so the announcement date is not in the data. Both are surfaced in the report as `pending` factors rather than being guessed. NOT raised as a new question, but recorded here for the architect: the Q-4 unknown-series report has already found two series the ruling's families do not name, on real F&O-universe symbols — `IL` (HDFCBANK 9 days, KOTAKBANK 2, MCX 1) and `D1` (INDHOTEL 29 days) in the Dec-2015..Jan-2016 window. The ruling already decides their treatment (never the instrument, reported), so nothing is blocked.
gate: n/a
status-ledger: chunk 3: built
state-for-next-session: Chunk 3 is BUILT, not reviewed. `python -m pytest` is green (668/0) from a clean state and fully offline. History ends at `chunk3: corporate-action engine (unreviewed)`; the prep commit `chunk3-prep: record+execute Q-4/Q-5 rulings; apply REVIEW_2 fixes` precedes the four chunk-3 commits. GOLDENS all pass and are printed in the session report: F8 hand-derived (bonus 1:2 → 2/3 · split FV10→2 → 0.2 · rights 1:4@200 on P=300 → 280/300 · special dividend 2.5% → 1 − D/P_cum) AND F8 against NSE's own calculator read cell-by-cell (our rights k matches its 0.9265654979940694 to the last digit it holds) · the five parser goldens · adjustment sanity across all three verified ex-dates (raw −32.44% / −79.63% / −49.76% → adjusted +1.34% / +1.85% / +0.49%, and every consecutive pair in every window inside 20%) · cross-source join 333 comparisons, 22/22 ratios identical to BSE, 3/3 to Yahoo, ZERO disagreements. Known limits a reviewer should weigh: (a) rights and dividend factors are DELIBERATELY unbuildable until Q-6/Q-7 are answered — 296 of the 328 priced events in the frozen windows are `pending`, which is the STOP rule working, not a gap; (b) the frozen windows are three months out of 25 years, so the parser's exception rate over full history is unmeasured — the report exists to measure it during chunk 5B/9; (c) the live fetchers were exercised once each, by hand, to take the snapshots; their retry branches are covered only by stubbed sessions; (d) the adjustment-sanity golden runs on three symbols, and the ±10-trading-day live check ran against a SCRATCH store (`data/ca_check_store`), never the operator's; (e) `--raw-dir` is still not resumable (REVIEW_2 F3's second half, outside the architect's scope for this session). OPERATOR NOTE: the 25-year backfill was running throughout this session and was never touched — at the last read it had settled 1,259 dates (863 file-present, 396 confirmed-404, ZERO errors) and had already reached one weekend session, Saturday 2003-03-22, which Q-5 now excludes and counts. NEXT ACTION: a FRESH session reviews chunk 3 against `personas/quant_reviewer.md` AND `personas/code_reviewer.md` (plan.md review type QC) and writes `docs/reviews/REVIEW_3.md` — chunk 4 must not start until STATUS.md shows `chunk 3: reviewed-PASS`.

---

## [2026-07-24 23:06] chunk 2 · review · done
scope: fresh code-review of chunk 2 (code_reviewer persona, plan.md review type C) — reran the suite from clean, executed the architect's eleven directed checks, recomputed the 25-day cross-source table and the TCS golden from the frozen poc/data with code that imports nothing from acumen, re-attacked the REVIEW_1 F1/F2 fixes, crash-tested the atomic write path four ways, reproduced byte-idempotence and the Q-4 ambiguity, judged B19-B34, wrote docs/reviews/REVIEW_2.md. VERDICT PASS.
files: created docs/reviews/REVIEW_2.md, tests/test_review2_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 513 passed / 0 failed (462 from the build + 51 added by this review), verified from a clean state with .pytest_cache and every __pycache__ deleted first. The builder's 462 reran green unchanged. Fixtures touched: NONE — all 28 poc/data CSVs and all 6 tests/fixtures files hash identically to their pinned SHA-256s AND to their git HEAD blobs (34/34). The 51 new tests were mutation-tested on a throwaway repo copy against ten mutants (error recorded as confirmed-404; single format tried; atomic_write_with catching Exception not BaseException; float price path; ledger written before rows; calendar deriving over an unsettled date; daily() picking the first row; socket patches removed; float64 price dtypes; re-ingest appending) — 10/10 caught, control green.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review2_probes.py. It pins what the build left un-pinned — the socket guard re-attacked through eight clients including pandas.read_csv(url) and a closed local port, the atomic path interrupted by KeyboardInterrupt/SystemExit/MemoryError and inside os.replace itself, the ledger's write ORDER isolated (rows before ledger), the cross-source table recomputed by a second implementation, every comparable bias pair in the window rather than three samples, and the Q-5 weekend-session consequence measured.
  - The cross-source goldens were recomputed before the builder's test file was read in detail, from the frozen CSVs with plain csv+Decimal, so the builder's numbers could not anchor mine. Agreement is 25/25 exact on volume and O/H/L and 25/25 on the close-inside-the-closing-half-hour invariant, with the CONTEXT 4.5 gate-1 band observed at +0.025%..+3.581%.
  - This review issued ZERO network traffic. Where a probe needed an address it used 192.0.2.1 (RFC 5737 TEST-NET-1, unroutable) or a closed loopback port — the chunk-1 review had to send real requests to prove F1 existed; proving it fixed needs none.
  - Two tests deliberately pin current defects (Findings 1 and 2) and are written to FAIL loudly if the defect is fixed, each naming the finding to close — the convention REVIEW_1 established.
  - Finding 4 was found by a probe of my own that failed: asserting the builder's stated float example reproduced the opposite result. The claim `float("2251.10") * 100 == 225109.99999999997` is false (it is exactly 225110.0); the decision it justifies is nevertheless right, and I measured the true damage (10 of 217 printed fixture values, e.g. TCS close 2189.20 -> 218919).
questions: none raised. Q-3 is correctly RESOLVED — the ruling is recorded verbatim in the prep commit BEFORE any chunk-2 code, and I verified every clause is code with a test behind it (ledger vocabulary spelled as the ruling spells it; error never downgraded to confirmed-404 at three independent layers; derived == published over the ingested window, re-derived a third way from the ledger CSV; the current year refused by construction). Q-4 and Q-5 are correctly RAISED and are class A, open. Q-4 is confirmed BLOCKING for chunk 3 and is worse than its own summary: NTPC carries SIX series on 2018-01-01 (EQ, N4, N6, N7, NB, NC) and a different sixth on 2018-01-02. Q-5's consequence is now measured and recorded in REVIEW_2 Finding 11 for the architect: on a derived calendar a weekend session is is_trading_day True AND is_standard_session True (E2 does not exclude it), and the following Monday's CONTEXT 3.2 pair moves by one whole candle.
gate: n/a
status-ledger: chunk 2: reviewed-PASS
state-for-next-session: Chunk 2 is reviewed-PASS at tag chunk2-pass; python -m pytest is green (513/0) from a clean state and fully offline. Twelve findings are in docs/reviews/REVIEW_2.md — none blocking, none requiring a fix session. Two are MEDIUM and both live in the shared fetch layer chunk 5A will inherit: F1 (the cookie warm-up calls _throttle without the caller's min_interval, so the download path fires the warm-up 0.5s after a refused request instead of 2.0s — the one place the card's pacing rule is broken, and it fires exactly when NSE is refusing us) and F2 (fetch_binary does not retry an HTTP 200 carrying a bot-shield HTML page while fetch_json does — measured 1 request vs 4 — so B31's "the policy cannot drift between the two paths" is broader than the enforcement; no data is corrupted because the date lands in `error`, which is never a holiday). Five LOW: F3 (--raw-dir is neither atomic nor resumable, and it is what the DERIVED fixtures were cut from), F4 (B24's float example is numerically false in five places — substitute 2189.20 from this repo's own fixture), F5 (four comments cite Q-4 for the weekend question, which is Q-5), F6 (atomic_write_with does not fsync — process-crash safe, not power-loss safe), F7 (nothing cross-checks that a file-present ledger date actually has rows; the write order makes it crash-safe but there is no read-side invariant — suggest a DailyStore.verify() before chunk 9's full run). Five INFO: F8 (ledger rewrite is O(n^2) — measured ~20-24 min over a 9,700-date run against >=5.4h of mandatory pacing, i.e. not the bottleneck), F9 (the closing-half-hour invariant is only guaranteed when trades occurred in that window — do not adopt it universally in chunk 5A's gate; also LastPric == the last 1-minute close 25/25, an exact comparison the goldens leave unused), F10 (UDIFF_FIRST_DATE is a hardcoded date and is the right call — spec law, and being wrong about it is harmless because both formats are tried), F11 (Q-5's measured consequence, for the architect), F12 (the backfill script's sys.path insert is its only import-time statement). NEXT ACTION: build chunk 3 (corporate-action engine, review type QC) — but note its first act will hit Q-4: DailyStore.daily() RAISES for any symbol-day carrying several series, so a full symbol history cannot be read until the architect rules. Paul's full 25-year backfill can run at any time, in parallel: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (hours; safe to interrupt; re-run to resume) — expect ~5.4h of pure pacing plus ~40 min of file I/O.

---

## [2026-07-24 21:18] chunk 2 · build · done
scope: recorded the architect's Q-3 ruling and fixed REVIEW_1 F1/F2 as a prep commit, then built chunk 2 — bhavcopy downloader for BOTH published formats with a per-date outcome ledger, a parquet daily store keyed symbol x date, the Q-3 derived trading calendar, and the operator's resumable backfill script; proved the pipe by live-ingesting the two bounded windows the card names.
files: created src/acumen/atomic_io.py, src/acumen/bhavcopy.py, src/acumen/daily_store.py, scripts/backfill_daily.py, tests/test_atomic_io.py, tests/test_bhavcopy.py, tests/test_daily_store.py, tests/test_derived_calendar.py, tests/test_daily_goldens.py, tests/test_backfill_script.py, tests/fixtures/PROVENANCE.md, tests/fixtures/bhavcopy_udiff_sample.csv, tests/fixtures/bhavcopy_archive_sample.csv, tests/fixtures/daily_ledger_window.csv; edited src/acumen/nse_http.py (atomic cache write, damaged-cache repair, fetch_binary + NseNotFoundError, per-call throttle interval), src/acumen/calendar.py (derived-calendar constructors + two optional fields), tests/conftest.py (socket-level guard), tests/test_review1_probes.py (F1/F2 pins rewritten), tests/test_fixture_integrity.py (3 digests + 2 new tests), QUESTIONS.md, docs/reviews/REVIEW_1.md (append-only fix log), STATUS.md, PROGRESS.md. No file under poc/ was touched.
tests: 462 passed / 0 failed (was 284 at chunk-1 review; +11 prep, +167 chunk 2), verified from a clean state with .pytest_cache and every __pycache__ deleted first. Fixtures touched: NONE existing — all 28 poc/data CSVs and all 3 chunk-1 tests/fixtures files still match their pinned SHA-256s; three NEW files added under tests/fixtures/ and pinned by digest in the same commits (bhavcopy_udiff_sample.csv bd477ba4…, bhavcopy_archive_sample.csv 40d0281a…, daily_ledger_window.csv 73b97a05…; digests live in tests/test_fixture_integrity.py, provenance in tests/fixtures/PROVENANCE.md).
decisions:
  - B19 A date is only recorded as `confirmed-404` after BOTH published formats answer 404. CONTEXT 4.1 dates the UDiFF cutover only to the MONTH ("since Jul-2024"), so trusting a single boundary date would turn a run of real trading days into holidays if it is off by a few days — and under Q-3 that error is permanent and silent. Costs one extra request per non-trading day (~1.5h over the full 25-year run); buys immunity to a wrong boundary.
  - B20 Weekends are ATTEMPTED, not skipped by rule. The Q-3 ruling derives trading days from what NSE actually published, and NSE does hold weekend sessions; skipping Saturdays would replace the evidence with an assumption. It is also what produces the card's `confirmed-404` golden for Sunday 2026-07-19.
  - B21 The store is one parquet file per calendar MONTH (`daily/<YYYY>/bhavcopy_<YYYY>-<MM>.parquet`), not per date. ~21 dates x ~3,400 symbols is a healthy file; one file per date would leave chunks 3/4 opening 6,000+ files to read one symbol's history. Re-ingesting a date rewrites its month file with that date's rows replaced.
  - B22 The parquet schema is declared explicitly rather than inferred: an inferred schema changes shape when a month happens to have no nulls in a column, and a store whose dtypes drift between months is a store whose reads drift too.
  - B23 Reads come back as pandas nullable `Int64`, not the default conversion. A nullable int column converted the default way becomes float64, and a float cannot hold a turnover in paise exactly (TCS 2026-07-20: 497,162,363,920 paise).
  - B24 Prices are parsed to paise via `Decimal` on the CSV TEXT, and a value that is not a whole number of paise is REFUSED rather than rounded. `int(float("2189.20") * 100)` is 218919 — a paisa short of TCS's real 2026-07-15 close; rounding that is a guess about a price (CONTEXT 7-E11). *[Example corrected in place 2026-07-24 by the chunk-3 prep commit per REVIEW_2 Finding 4: the original entry cited `float("2251.10") * 100 == 225109.99999999997`, which is false — that product is exactly 225110.0. The decision is unchanged; only its illustration was wrong. The same false example survives verbatim in the body of commit 52791e0, which is history and is not rewritten.]*
  - B25 The column set kept is CONTEXT 4.1's OHLCV plus identity/liquidity fields (series, ISIN, instrument type, last, prev close, turnover, trades, source format). "Store everything" is applied to ROWS — every symbol and every series — not to the derivative-only UDiFF columns, which are empty in a cash-market file.
  - B26 `DailyStore.daily()` RAISES when a symbol-day is not unique instead of picking a row. Real data forces the issue and it is not rare (see Q-4). A silent choice would hand chunk 4 a block-deal or debenture candle on exactly the eventful days.
  - B27 `TradingCalendar` gained `trading_days` and `covered_days` (both optional, both `None` for a published calendar, chunk-1 behaviour bit-identical and pinned by a test) rather than a separate DerivedCalendar class. Everything downstream — `bias_pair`, `prev_trading_day`, the E2 filter — must behave identically whichever calendar it is handed, and one class is the only way to guarantee that.
  - B28 The derived calendar refuses to build unless EVERY date in its range is settled, instead of deriving around gaps and errors. This is Q-3 safeguard 1 made structural: an interrupted backfill cannot become a calendar of invented holidays. It also implements safeguard 3 for free — the current year can never be complete, so the published endpoint stays authoritative for today by construction.
  - B29 `scripts/` is not a package and nothing under `src/` imports it; its tests load it by path. It is an entry point, not a library.
  - B30 The backfill script gained `--raw-dir`, archiving each date's CSV exactly as NSE published it. `Download` carries `raw_csv` for this. It is the operator's audit trail and it is what the three DERIVED fixtures were cut from — a re-serialisation of parsed rows would not have been evidence (chunk-1 B16 applied to files).
  - B31 A shared retry loop now backs both `fetch_json` and the new `fetch_binary`, and `NseNotFoundError` makes 404 a TYPE rather than a string comparison. One loop so the CONTEXT 4.3 policy cannot drift between the JSON and download paths; a distinct type because the whole ledger depends on telling "absent" from "failed".
  - B32 `src/acumen/atomic_io.py` is one implementation of write-temp-then-replace, used by the day cache and by every store write, and it removes the temp file on any `BaseException` — `KeyboardInterrupt` is the case it exists for, since the operator is told the backfill is safe to interrupt.
  - B33 The offline guard now blocks at the SOCKET (`socket.socket.connect`/`connect_ex`, `socket.create_connection`) with the `requests` patches kept on top for their better message. `pytest-socket` was considered and declined: one more pinned dependency for ~10 lines, on a repo whose dependency list is deliberately short.
  - B34 The cross-source golden compares only what the two sources genuinely share, so no tolerance is invented anywhere: volume and O/H/L are asserted EXACT (25/25), while the daily close is NOT compared to the last 1-minute close (NSE's close is the last-30-minute weighted average — TCS 2026-07-20: 2251.10 vs an LTP of 2251.00, and the bhavcopy carries both). What is asserted for the close is the invariant that follows from its definition: it lies inside the 15:00-15:29 range. The volume-shortfall band is CONTEXT 4.5 gate 1's own [-0.1%, +5.0%], a spec number.
questions: Q-3 RESOLVED (the ruling was recorded verbatim in the prep commit and every clause is now code with a test behind it; the derived-vs-published cross-check passes over the ingested window). Q-4 RAISED (class A, open, non-blocking for chunk 2, BLOCKS chunks 3/4/9): NSE publishes the same symbol under several SERIES on the same date and CONTEXT names no series anywhere — measured, not hypothetical: 11 F&O-universe symbols are ambiguous on 2018-01-01 alone (NTPC EQ+5 debt series, IRFC 7 debt series and NO equity, JSWSTEEL EQ+P2 partly-paid), and BIOCON EQ+BL on 2026-07-14. The store keeps every row and `daily()` raises rather than choosing; no `"EQ"` default exists in `src/`. Q-5 RAISED (class A, open, non-blocking): NSE's occasional weekend sessions publish a bhavcopy, so the Q-3 ruling makes them trading days, which CONTEXT 7-E2 may or may not want excluded — and the answer moves the following Monday's bias pair too. Neither ingested window contains one; the code follows the ruling and exposes `weekend_sessions` to make the case visible.
gate: n/a
status-ledger: chunk 2: built
state-for-next-session: Chunk 2 is BUILT, not reviewed. `python -m pytest` is green (462/0) from a clean state and fully offline — the conftest guard now fails any test that opens a socket, not merely one that uses `requests`. History ends at `chunk2: add the operator backfill script and the chunk-2 goldens (unreviewed)`; the prep commit `chunk2-prep: ...` precedes the four chunk-2 commits. LIVE PROOF this session (outside pytest): 2026-07-13..2026-07-24 = 12 attempted, 10 present, 2 confirmed-404, 0 error; 2018-01-01..2018-01-15 = 15 attempted, 11 present, 4 confirmed-404, 0 error. GOLDENS all pass: TCS 2026-07-20 close 225110 paise / volume 2,202,693 exactly as docs/RESULTS.md section A · cross-source 25/25 exact on volume and O/H/L vs the frozen poc/data SmartAPI values, close inside the closing half-hour 25/25, volume shortfall 0.025%-3.581% inside CONTEXT 4.5 gate 1's band · re-ingest byte-idempotent · derived trading days == published over the ingested window, and the two calendars agree on the CONTEXT 3.2 bias pair · ledger shows confirmed-404 for Sunday 2026-07-19 and file-present for 2026-07-20. Known limits a reviewer should weigh: (a) only 27 dates are ingested locally — the 25-year run is the operator's, and the store on this machine is NOT the store chunk 3 will read; (b) `DailyStore.daily()` raises for a symbol-day with several series, so chunk 3 CANNOT read a full history until Q-4 is answered — that is the single most important open item; (c) the derived calendar refuses a partially-covered year by design, so `from_daily_store(store, [2026])` will keep failing until 2026 ends (safeguard 3, intended); (d) the retry/backoff and 403-warm-up branches of the download path are covered only by stubbed sessions — the live run hit zero errors, so the error path has no live evidence. NEXT ACTION: a FRESH session reviews chunk 2 against personas/code_reviewer.md (plan.md review type C) and writes docs/reviews/REVIEW_2.md — chunk 3 must not start until STATUS.md shows `chunk 2: reviewed-PASS`. Paul's full backfill can run at any time, in parallel with the review: `python scripts/backfill_daily.py --from 2000-01-01 --to 2026-07-24 --store data\daily_store --allow-network` (hours; safe to interrupt; re-run the same command to resume).

---

## [2026-07-24 20:28] chunk 1 · review · done
scope: fresh code-review of chunk 1 (code_reviewer persona, plan.md review type C) — reran the suite from clean, executed the architect's ten directed checks, recomputed all seven bias_pair goldens independently from the frozen holiday JSON, attacked the offline guard and the day cache, judged B1-B18, wrote docs/reviews/REVIEW_1.md. VERDICT PASS.
files: created docs/reviews/REVIEW_1.md, tests/test_review1_probes.py; edited STATUS.md, PROGRESS.md. NO file under review was modified; no fixture was touched; no file was moved.
tests: 284 passed / 0 failed (248 from the build + 36 added by this review), verified from a clean state with .pytest_cache and every __pycache__ deleted first. The builder's 248 reran green unchanged. Fixtures touched: NONE — all 28 poc/data CSVs and all 3 tests/fixtures files hash identically to their pinned digests AND to their git HEAD blobs. The 36 new tests were mutation-tested against four engine mutants (swapped bias pair, holiday-blind walk-back, moved session close, wrong containment operator) — 4/4 caught, control green.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_review1_probes.py. It pins the four things the build left open — what the offline guard actually covers, E2/E12 containment at every boundary stamp including the 15-min 15:00 case the build omitted, naive-IST enforcement across the whole public surface rather than two entry points, and the (D-1, D-2) pair recomputed by a SECOND implementation built straight from the raw JSON so the goldens are not graded by the code they test.
  - The bias goldens were verified by hand first and by code second, then extended past the fixture to all 243 in-year pairs. The builder's in-test reasoning comments were read only after my own derivation agreed, so they could not anchor it.
  - Finding 1 was proved by issuing real traffic (one TCP connect and one GET to nseindia.com's public home page) because no offline experiment can show that a guard fails to intercept a live connection. One page view, well inside CONTEXT 4.1's politeness rule. The KEPT test sends nothing — it uses a closed loopback port.
  - Two tests deliberately pin current defects (Findings 1 and 2) and are written to FAIL loudly if the defect is fixed, each with a message naming the finding to close. A defect that no test can see is how a fix silently regresses.
questions: none raised. Q-1 and Q-2 are correctly CLOSED with evidence. Q-3 is correctly RAISED by the build and is class A, open — but see the item recorded in REVIEW_1 section 5: the architect's Q-3 ruling relayed to this session (option (a) with safeguards, execution in chunk 2) is NOT recorded in QUESTIONS.md, which still shows Q-3 as open with three unchosen options. This review did not act on the ruling, per instruction. It must be written into Q-3 before the chunk-2 session opens, or chunk 2 will read its own dependency as unanswered.
gate: n/a
status-ledger: chunk 1: reviewed-PASS
state-for-next-session: Chunk 1 is reviewed-PASS at tag chunk1-pass; python -m pytest is green (284/0) from a clean state and fully offline. Ten findings are in docs/reviews/REVIEW_1.md — none blocking, none requiring a fix session. Two are MEDIUM and land in code chunk 2 inherits: F1 (the conftest network guard patches requests.Session only — a raw socket and urllib both reached NSE from inside a test; decision B2's recorded claim is broader than the enforcement) and F2 (nse_http.write_cache writes in place, so a crash mid-write leaves a truncated cache that even allow_network=True cannot repair — the operator must delete the file). F3-F7 are LOW (prev_trading_day answers for an uncovered year; E2 containment does not imply E12 grid alignment; the PROGRESS entry names no digests; the prep commit's message convention; one config test still loads the real .env). F8-F10 are INFO for chunks 5A/9/13. NEXT ACTION: build chunk 2 (daily store / bhavcopy) — its card reads docs/RESULTS.md per the Q-1 ruling. Before that session opens, the architect must record the Q-3 ruling in QUESTIONS.md; chunk 2 is the session that executes it.

---

## [2026-07-24 20:41] chunk 1 · build · done
scope: executed the two architect rulings + REVIEW_0 F3/F4 as a prep commit, then built chunk 1 — F&O universe loader, NSE trading calendar (trading days, prev-trading-day, CONTEXT 3.2 bias pairs), the E2 session filter, and a shared gentle once-a-day NSE fetch layer; all tests offline against two frozen endpoint snapshots pulled once this session.
files: created src/acumen/nse_http.py, src/acumen/universe.py, src/acumen/calendar.py, tests/conftest.py, tests/test_nse_http.py, tests/test_universe.py, tests/test_calendar.py, tests/fixtures/universe_snapshot.json, tests/fixtures/holidays_2026.json, tests/fixtures/tick_sizes.json; edited tests/test_smoke.py (F3), tests/test_config.py (F4), tests/test_fixture_integrity.py (digests for the three new fixtures), QUESTIONS.md, STATUS.md, PROGRESS.md; moved (git mv, 100% renames) RESULTS.md + acumen_poc.md + TradingView_POC_Calibration_Guide.docx -> docs/ and requirements.txt -> poc/. No file under src/acumen/config.py or poc/ was modified.
tests: 248 passed / 0 failed (was 91 at chunk-0 review; +1 prep, +156 chunk 1). Verified from a clean state with .pytest_cache and every __pycache__ deleted. Fixtures touched: NONE existing — the 28 poc/data CSVs still match their pinned SHA-256s; three NEW frozen files added under tests/fixtures/ and pinned by digest in the same commit. The two endpoint snapshots were fetched ONCE each during this session (2026-07-24, one live pull per endpoint, no repeat) and are stored verbatim.
decisions:
  - B1 A third module, `nse_http.py`, holds the shared gentle-fetch + day-cache logic. The card names only universe.py and calendar.py, but both endpoints need identical access discipline (CONTEXT 4.1 "once/day max"); the alternatives were duplicating the retry/cache code or making calendar.py import universe.py.
  - B2 The network is opt-in: every fetch path defaults to `allow_network=False` and raises rather than reaching out. Enforced, not claimed — tests/conftest.py installs an autouse guard that FAILS any test making a real HTTP request, so a future default flipped to True cannot stay green while hammering NSE on every commit.
  - B3 `TradingCalendar` carries `covered_years` and RAISES `CalendarError` for any date outside them. The endpoint publishes one year at a time; treating an unloaded year as holiday-free would invent a dozen-odd phantom trading days a year and silently mis-pair every bias spanning one. This is the STOP rule made executable — see Q-3.
  - B4 `bias_pair` returns `BiasPair(current, previous)`, a NamedTuple that is still a plain tuple. CONTEXT 3.2's "current" is candle(D-1), the LATER of the two; named fields remove a silent bias inversion from chunk 4's reach.
  - B5 The E2 candle validator is defined by bar CONTAINMENT: a bar stamped t is in-session when `t >= 09:15` and `t + minutes <= 15:30`. Derived from E12 (open-stamped bars cover [t, t+minutes)) plus CONTEXT 4.5 gate 2 ("375 minutes 09:15-15:29"), so it needs no assumption and handles 1-min (09:15..15:29) and 15-min (09:15..15:15) bars with one rule.
  - B6 `SESSION_OPEN`/`SESSION_CLOSE` are module constants citing CONTEXT 3.1, deliberately NOT config keys: the exchange session is spec law, and a config key would let it move without an architect spec change.
  - B7 NSE's "26-Jan-2026" is parsed with an explicit month map, never `strptime("%d-%b-%Y")` — %b is LOCALE-dependent and would fail on a non-English machine. The parse is cross-checked in tests against the `weekDay` field NSE ships with each row (20/20 agree).
  - B8 Naive-IST is enforced loudly (CONTEXT 7-E8): a `datetime` passed where a `date` is expected raises rather than being truncated, and any tz-aware value raises. Directly addresses REVIEW_0 Finding 7 (the frozen PoC CSVs carry +05:30 stamps) — normalization is the ingest layer's job, not the calendar's.
  - B9 Universe symbols are stripped, upper-cased, de-duplicated and SORTED — the order is a property of the data, not of NSE's `serialNumber` field.
  - B10 Only an EMPTY universe is an error; no plausibility floor (e.g. "must be >= 100") was invented. CONTEXT states ~210 but sets no minimum, and a threshold nobody specified is exactly the magic number CLAUDE.md's code standards ban. The 210 count is asserted in tests, where it belongs.
  - B11 A stale cache is never served silently. `fetch_universe`/`fetch_calendar` raise and name the cache's date; `load_cached_universe`/`load_cached_calendar` are the explicit stale path and return `(fetched_on, value)` so a caller that accepts yesterday's list must say so in its own code.
  - B12 A damaged cache file raises instead of being treated as "nothing cached" — consistent with chunk-0 B5 (a typo must fail loudly, never fall back).
  - B13 The day-cache lives at `<data_dir>/nse/*.json`, i.e. under the `/data/` that .gitignore anchors (chunk-0 B8) — never committed. A test pins that location.
  - B14 The cookie warm-up fires at most ONCE per fetch call: a second one tells us nothing new and doubles our footprint on an endpoint we pull once a day.
  - B15 Nothing in the parse path reads config; `default_cache_dir()` is a separate opt-in that calls `load_config(include_env=False)`, so the universe/calendar path never touches the operator's credentials (same spirit as REVIEW_0 F4).
  - B16 The two snapshots are stored VERBATIM (raw payload bytes, no wrapper, no annotation). Provenance is recorded in the test docstrings and here instead of inside the file, because an edited snapshot is no longer evidence of what the endpoint returns. A test asserts the payload shape is still raw.
  - B17 tests/fixtures/ gets per-file SHA-256 tripwires but deliberately NO exact-set assertion (unlike poc/data): later chunks legitimately add fixtures there; what may never happen is one of these files changing.
  - B18 REVIEW_0 F4 was applied to exactly the six tests the finding names. The seventh `include_env=True` call site, `test_env_values_never_reach_the_config_object`, was left as-is because that code path IS its subject; noted in the test module docstring.
questions: Q-1 CLOSED (architect ruling (a) executed: three documents -> docs/, requirements.txt -> poc/; chunk 2 must read docs/RESULTS.md). Q-2 CLOSED (architect ruling (a) executed: tests/fixtures/tick_sizes.json, five ticks in rupees, `_note` states "tests only - production code must always read the instrument master"; the ruling licenses NO tick table in src/). Q-3 RAISED (class A, open, non-blocking for chunk 1, BLOCKS chunk 9's full run): CONTEXT 4.1's holiday endpoint publishes ONE year — the frozen pull is 20 CM holidays, all 2026 — but CONTEXT 2 wants 25 years of backtest and CONTEXT 3.2's pair is defined on TRADING days. Three options given, including deriving trading days from chunk 2's bhavcopy coverage. Not decided here; the calendar refuses uncovered years instead (B3).
gate: n/a
status-ledger: chunk 1: built
state-for-next-session: Chunk 1 is BUILT, not reviewed. `python -m pytest` is green (248/0) from a clean state and offline — conftest.py fails any test that touches the network, and no test exercises a live pull. History ends at `chunk1: universe and calendar (unreviewed)`; the prep commit `chunk1-prep: ...` immediately precedes the four chunk-1 commits. GOLDENS all pass and are printed in the session report: 2026 CM holidays = 20 · universe = 210 with TCS and RELIANCE present and all 5 indexes excluded · seven hand-computed bias_pair cases incl. Tue 27-Jan-2026 -> (Fri 23-Jan, Thu 22-Jan) across Republic Day + weekend · is_trading_day False on 2026-01-26 and on Sunday 2026-01-25. Known limits a reviewer should weigh: (a) the calendar covers 2026 ONLY and raises for any other year by design — Q-3; (b) the live pull has no CLI entry point, so the operator refreshes by calling `fetch_universe(allow_network=True)` / `fetch_calendar(allow_network=True)` — chunk 13 owns the morning-refresh job and the card did not ask for one here; (c) the live path itself was exercised exactly once, by hand, to take the snapshots — the retry/backoff branches are covered only by stubbed sessions. NEXT ACTION: a FRESH session reviews chunk 1 against personas/code_reviewer.md (plan.md review type C) and writes docs/reviews/REVIEW_1.md — chunk 2 must not start until STATUS.md shows `chunk 1: reviewed-PASS`.

---

## [2026-07-24 19:09] chunk 0 · review · done
scope: fresh code-review of chunk 0 (code_reviewer persona) — reran the suite, verified the frozen fixtures two independent ways, judged B1-B12, wrote docs/reviews/REVIEW_0.md. VERDICT PASS.
files: created docs/reviews/REVIEW_0.md, tests/test_fixture_integrity.py; edited QUESTIONS.md (Q-1 marked resolved per architect ruling; Q-2 raised), STATUS.md, PROGRESS.md. NO file under review was modified; no file was moved.
tests: 91 passed / 0 failed (60 builder + 31 added by this review). Builder's 60 reran green from a clean state (.pytest_cache and all __pycache__ deleted first). Fixtures touched: NONE — all 28 poc/data CSVs hash identically to their git HEAD blobs, and the new digest test was mutation-tested against temp COPIES only.
decisions:
  - Reviewer-added test file kept (persona process step 4): tests/test_fixture_integrity.py pins SHA-256 per fixture, the exact 28-file set, 375-candle length and CRLF preservation. Rationale: the build asserted only that fixtures EXIST, so CLAUDE.md rule 3 had no tripwire — a later session could silently regenerate one and stay green.
  - Fixture integrity was established by recomputation, not by trusting the builder's digest: an independent reimplementation of the CONTEXT 3.3 row math reproduces all 25 poc_prorata values in volume_poc_summary.csv exactly, including all five CONTEXT 8 F7 anchors. Evidence, not assertion.
  - The verification scripts were written to the session scratchpad, not the repo — chunk 6 owns the POC engine and a review may not pre-build it.
questions: Q-1 marked "RESOLVED — execution scheduled chunk 1" with the architect's ruling (option (a): three documents to docs/, requirements.txt to poc/) recorded in full; NO file moved by this session. Q-2 RAISED (class A, non-blocking, first needed at chunk 6): CONTEXT 8 F7 calls the poc/data CSVs "the authoritative input", but recomputing the POC also needs the per-symbol tickSize, which lives nowhere in the repo (only the gitignored 35 MB cache/scrip_master.json). Measured values recorded in Q-2 so they survive cache deletion: TCS 0.10, RELIANCE 0.10, HDFCBANK 0.05, DIXON 1.00, MANAPPURAM 0.05.
gate: n/a
status-ledger: chunk 0: reviewed-PASS
state-for-next-session: Chunk 0 is reviewed-PASS at tag chunk0-pass; python -m pytest is green (91/0) from a bare clone. Seven findings are in docs/reviews/REVIEW_0.md — all LOW or INFO, none blocking, none requiring a fix session: F1 (builder's aggregate digest is not reproducible; superseded by the new per-file digest test), F2 (fixed by this review), F3 (B7's ASCII claim is broader than the test that enforces it), F4 (six config tests load the real .env; suggest include_env=False), F5/F6/F7 are forward-looking notes for chunks 5A and 6. NEXT ACTION: build chunk 1 (universe & calendar) — and, per the architect's Q-1 ruling, that same session moves RESULTS.md, acumen_poc.md and TradingView_POC_Calibration_Guide.docx to docs/ and requirements.txt to poc/, then marks Q-1 closed.

---

## [2026-07-24 18:19] chunk 0 · build · done
scope: turned the PoC folder into the project repo — CONTEXT §6 layout, pinned pyproject, config loader with risk_per_trade required-empty, ledgers, git history.
files: created .gitattributes, pyproject.toml, config.yaml, src/acumen/__init__.py, src/acumen/config.py, tests/test_smoke.py, tests/test_config.py, tests/fixtures/.gitkeep, docs/reviews/.gitkeep, PROGRESS.md, QUESTIONS.md, STATUS.md; rewrote .gitignore; moved common.py + poc1..poc5_*.py → poc/ and data/*.csv (28) → poc/data/; untouched: CLAUDE.md, CONTEXT.md, plan.md, personas/*, RESULTS.md, acumen_poc.md, TradingView_POC_Calibration_Guide.docx, requirements.txt, .env, cache/.
tests: 60 passed / 0 failed (python -m pytest). Fixtures touched: NONE — the 28 poc/data CSVs were moved, not rewritten; SHA-256 of all 34 moved files taken before and after the move, 34/34 identical (aggregate digest of the 28 CSVs: f7e06fad99b8d8ac6005a5f4b6ba9df61641b4973a021e9f1d12bf92ac004100).
decisions:
  - B1 Dependencies pinned exactly (`==`) to the versions verified working here (smartapi-python 1.5.5, pyotp 2.10.0, python-dotenv 1.2.2, requests 2.32.3, websocket-client 1.9.0, logzero 1.7.0, pandas 3.0.3, pyarrow 25.0.0, PyYAML 6.0.3, pytest 9.1.1) — one operator laptop, so reproducibility beats range flexibility.
  - B2 pytest resolves the src layout via `[tool.pytest.ini_options] pythonpath = ["src"]` rather than an editable install — a bare clone runs green with no install step. The package was NOT pip-installed in this session.
  - B3 Project version is single-sourced from `acumen.__version__` (setuptools dynamic metadata) — one place to bump.
  - B4 config.yaml carries only the three keys the card names (risk_per_trade, row_size, paths); capital and the ₹100 cost (CONTEXT §3.5) are left to the chunk that consumes them — no unused config surface to drift.
  - B5 Unknown or missing top-level config keys are a hard ConfigError — a typo must fail loudly, never fall back to a default (CLAUDE.md rule 1).
  - B6 Secrets never enter the Config object: `.env` values are fetched on demand by `env_value()`, and its error message names the VARIABLE only, never a value (CLAUDE.md rule 4).
  - B7 Runtime files (`*.py`, `config.yaml`) are ASCII-only, enforced by a test: the operator's console encoding is cp1252, where printing a traceback whose source line carries `₹` or `§` raises UnicodeEncodeError and hides the real error. Spec symbols stay in the markdown documents; code cites "CONTEXT 3.5" in CLAUDE.md's own style.
  - B8 `.gitignore` anchors `/data/`, `/cache/`, `/logs/` with a leading slash — an unanchored `data/` would also ignore `poc/data/`, i.e. the frozen F7/F10 golden fixtures. A test asserts both the anchored form and the absence of the unanchored one.
  - B9 `.gitignore` adds `/logs/` and `.pytest_cache/` beyond the card's list — both are generated artifacts, which CLAUDE.md's git rules forbid committing (the PoC had left two 0-byte `logs/*/app.log` files).
  - B10 PoC scripts moved verbatim, not repaired: their `Path(__file__).parent/"data"` now resolves to `poc/data` (correct), `cache` to `poc/cache` (hence that ignore entry) and `.env` to `poc/.env` (absent — the archive would need `--env` plumbing to re-run). CONTEXT §6 calls poc/ "archived PoC scripts", so it is preserved as-run.
  - B11 Added `.gitattributes` marking `poc/data/**` and `tests/fixtures/**` as `-text`: this machine has `core.autocrlf=true` and the frozen CSVs are CRLF on disk, so without it git would store them line-ending-normalized and any clone would receive different bytes than the fixtures were calibrated on (CONTEXT §8 F7/F10).
  - B12 Nothing was deleted anywhere in this chunk: root `cache/scrip_master.json` (35 MB, gitignored), `logs/`, `__pycache__/` and the four un-specified root documents were left in place; the card scoped the move to PoC scripts + PoC data.
questions: Q-1 (class A, non-blocking) — CONTEXT §6's layout does not name RESULTS.md, acumen_poc.md, TradingView_POC_Calibration_Guide.docx or requirements.txt, so chunk 0 left them at the repo root instead of guessing; chunk 2 cites RESULTS.md, so its path needs an architect ruling before then.
gate: n/a
status-ledger: chunk 0: built
state-for-next-session: Repo is at the CONTEXT §6 layout on branch `main`, `python -m pytest` is green (60/0) from a bare clone, and the history ends at `chunk0: repo scaffold (unreviewed)`. Environment note: pytest 9.1.1, PyYAML 6.0.3 and pyarrow 25.0.0 were installed into the operator's global Python 3.12.2 this session; the other pinned deps were already present from the PoC. NEXT ACTION: a FRESH session reviews chunk 0 against personas/code_reviewer.md and writes docs/reviews/REVIEW_0.md — chunk 1 must not start until STATUS.md shows `chunk 0: reviewed-PASS`.
