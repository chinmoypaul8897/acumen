# STATUS.md — chunk state ledger

One line per chunk; this file is the ONLY place chunk state lives (plan.md §2).
Every session updates its own chunk's line before it ends (CLAUDE.md end-of-session duties).

States: `todo` | `built` | `reviewed-PASS` | `gate-closed(<evidence path>)`
No chunk may start while a dependency is not `reviewed-PASS` (plan.md §2).

chunk 0: reviewed-PASS
chunk 1: reviewed-PASS
chunk 2: reviewed-PASS
chunk 3: reviewed-PASS
chunk 4: reviewed-PASS · gate-closed(docs/gate_chunk4_bias_evidence.md — trader CONFIRMED, Round 3)  # TRADER GATE CLOSED 2026-07-29: the 15-real-day F9 bias table came back confirmed ("that is what I would have called on each of those days") -- QUESTIONS.md ROUND-3 FINAL RECEIPTS, R3F-a. Closed inside its plan.md §2 deadline (before the chunk-9 run). The pack is unchanged; no number in it moved. The gate's OPEN-4 mirror question came back the OTHER way (Q38/Q39: colour irrelevant) and the tie predicate was rewritten in CONTEXT v1.3 §3.2 + executed in chunk-7 prep -- F5 is now three sub-fixtures (red/green/doji, all BULLISH), F9's real days untouched. tag chunk4-pass
chunk 5A: reviewed-PASS  # QC (both personas) over 2d55919..9f06b6d: prep+build+fix(Q-10)+fix2(k_shares)+fix3(demerger-scope/Q-11 STOP)+fix4(Q-11 measured reconstruction). 911/0 offline; RELIANCE era-flip hand-verified; 9-mutant matrix (8 caught/1 equivalent); no secrets/AI-attribution/test-weakening. 2 LOW findings (map not yet wired to ingest CLI; factor-table fallback price-blind for special dividends) are chunk-5B-forward + gate-1-safe. tag chunk5A-pass
chunk 5B: reviewed-PASS (DoD shortfall architect-accepted)  # RE-REVIEW (both personas, fresh session) of the FIX-5 commit 88546cb..7943ce6 against REVIEW_5B's FAIL + 22 findings. 1306/0 from clean (matching the FIX-5 claim); 1316/0 with 10 kept reviewer probes. **VERDICT PASS.** DoD **NOT MET at 94.6917%** (411,690 / 434,769, 1,341 days short) and the architect's closing Q-14 addendum ACCEPTS that shortfall: under the battery the target was written against, coverage is 95.2162% (reading C) -- the miss is created by the battery's strengthening, not by data regression; the freeze holds; every excluded day is categorized in the residual register. INDEPENDENTLY RE-DERIVED, importing nothing from src/acumen: the gate-1P partition reproduces to the day on **ALL 210 symbols, zero mismatches** (434,769 stored / 424,370 pass / 10,399 fail = 3,096 above + 7,125 below + 178 no-oracle); all seven coverage readings reproduce to the digit; every wrong-scale day REVIEW_5B named is now EXCLUDED and NMDC 2018-03-27 is repaired to the paisa; the cluster signature reproduces exactly (NMDC 135/244 = 55.3%, GAIL 64/316 = 20.3% -- a whole-span rate test REFUSES both, the STEP measures 135 and 64, which is why B146 exists); both accepted floors' boundaries are UNIQUELY determined by adjacent-session probes with full provenance; BSE's resolved 13-probe volume floor was REFUSED by acceptance with nothing written; B143 proved by AST over real bindings (no credentialed call reachable from the recovery path); the report regenerates byte-identically from the ledger. Gate-1P attacks: a wrong scale hiding on one end still fails on the other in both directions; a day AT both bounds passes and one paise past either fails; the residual blind spot measured over 33,031 real passing days -- p50 0.100%, p99 0.250%, only 0.006% of days could hide a 5% factor error and none a 10%. Punch list CLOSED (Q1-Q6, B127->B152, C1-C16; C11 carries over). B141-B153 all 13 APPROVED. No test weakened, deleted, skipped or edited -- FIX-5 touched no existing test; fixtures untouched; no secrets; no AI attribution; local main == origin/main. 11 findings, none blocking (2 LOW + 3 INFO quant, 2 LOW + 4 INFO code); the two worth the architect's eye are an unresolved VOLUME-side search inheriting the PRICE splice (measured cost -0.0012% on NMDC's 135 days; cannot let a wrong day pass a gate; one-word fix owed on the next data work) and the fact that all 178 "no raw daily row" failures are the daily store's lag past 2026-07-24, not damage. docs/reviews/REVIEW_5B_2.md · tag chunk5B-pass
# superseded 2026-07-29: built (FIX-5 complete, unreviewed)  # Q-14 EXECUTED (the per-day PRICE gate). **GATE 1P** joins CONTEXT 4.5's battery permanently -- the stored 1-minute fold interval must sit INSIDE the raw bhavcopy [low, high] within max(2 paise, 0.1%), a day with no raw daily row FAILS it (closes REVIEW_5B Q4's 178 days), and a failure is EXCLUDED + COUNTED under its own reason. Wired beside gate 1 at ingest, re-gate and report; the map model gains PER-SIDE floors (floor_price / floor_volume per event) measured by the SAME bisection under the SAME guards, signature-gated by gate-1P failure CLUSTERS (a contiguous old-end block, >= 20 days, >= 95%, clean remainder -- a STEP, which is what a splice leaves; a whole-span rate would have admitted nothing on NMDC's 135/244). ONE bounded recovery pass ran OFFLINE over the whole universe -- the observable `fetched/raw` is recovered exactly from the store (`stored = fetched / k_applied`), so ZERO credentialed calls and zero candles fetched (B143). 21 symbols entered, 7 events admitted, **2 per-side floors ACCEPTED** (NMDC demerger 2022-10-27 price floor 2018-10-12; GAIL 2022-09-06 price AND volume floors 2018-06-28), 199 days repaired, 197 moved into usable; BSE's volume-only floor measured in 13 probes and REJECTED by acceptance. **RESULTS: 434,769 stored symbol-days, 10,399 fail gate 1P** (3,096 above / 7,125 below / 178 no-oracle) -- every Q1 population caught (IOC 1,412, TATASTEEL 832, NMDC 372, SRF 218, RECLTD 68, APLAPOLLO 47). All six of the review's coverage readings recomputed and they AGREE with the review to the day once the recovery's +64/+66 is accounted for: A 95.2569% / B 95.1575% / C 95.2162% / D 94.9829% (the finding-Q3 error, printed only as the correction) / E 95.2179% / F 95.1772%, all MET; **G, the post-Q-14 reading (gate 1 AND gate 2 AND gate 1P over every stored day) = 411,690/434,769 = 94.6917% -- DoD NOT MET, 1,341 days short.** That is the honest price of the ruling. The DISCLOSED-RESIDUAL register (report section 3f) states a measured reason per symbol and is what chunk 9 carries; the data era is FROZEN. Punch list CLOSED: Q2, Q3 (both-gates counted PER DAY, not subtracted -- 1,014 days), Q4, Q5 (claims re-derived from the MAP; ASTRAL + NESTLEIND now claim their in-force floors, 14 total), Q6 (un-provable days 300 -> 25,366 across 30 symbols, measured from the map against the STORED days), B127 recorded as B152, C1/C2/C3 closed with tests, C4 (persist_map through atomic_io + JSONDecodeError guard), C5/C6, C7/C8 (append-only notes), C9, C10, C12, C13, C14, C15, C16 (B144/B145). 1306/0 offline; fixtures untouched. NO tag (re-review owed).
# superseded 2026-07-28: built (FIX-4 complete) -- REVIEWED **FAIL**, fix session owed  # QC (both personas) over 269c07e..708e144 minus the three chunk-6 commits. 1253/0 from clean. VERDICT **FAIL** on ONE HIGH finding (docs/reviews/REVIEW_5B.md Q1, raised as **Q-14**, class A, BLOCKS chunk 9): gate 1 proves a day's VOLUME while the Q-11 oracle proves an ERA's price over its probe days, and **1,963 stored symbol-days -- all on SETTLED symbols -- sit between the two at 0.1x-5x the traded price and PASS every gate** (IOC 1,042 at 0.667x; TATASTEEL 498 at 0.100x incl. 2020-07-28 stored Rs 36.00 vs bhavcopy Rs 360.00; SRF 216 at 5.0x; NMDC 134 at 1.417x; RECLTD 65 at 0.75x; APLAPOLLO 3 at 2.0x; 5 singletons), violating CONTEXT 7-E11 in the data chunk 9 would consume. Measured over all 433,065 stored days read-only. Everything else HOLDS and needs no rework: DoD genuinely MET (95.24% relief / 95.14% strict / 95.20% on the stricter both-gates reading -- the report's own ~412,723 double-subtracts 1,012 days); signature gate reproduces the eleven-vs-six split exactly; CANBK's floor uniquely determined by its 13 persisted probes; auction relief reproduces 43/43 on HDFCBANK from the raw stores; all 3 gate-3 spot-recomputes match to 2dp; Q-5 evidence reproduces; both operator repairs derive from clean recomputation; B93-B140 judged -- 36 approved, B127 challenged on RECORDING only; no test weakened; report regeneration deterministic; no secrets, no AI attribution. 21 further findings, none blocking. NO tag (FAIL).
# prior build history: prep(F1/F2/F7/F3)+runner+fix(routing upper bound)+FIX-2(three rulings)+FIX-3(three rulings)+FIX-4(Q-11 ADDENDUM 4 -- floors in UN-PROVABLE eras, the FINAL data ruling). 1253/0 offline. FIX-4 recorded + executed the ruling's four guards: (i) SIGNATURE-GATED hunting -- an event enters an un-provable era only by the gate-3 raw-gap-near-zero signature (|raw gap| < |k-1|/2, which admits EXACTLY the eleven rows the architect named and rejects the six that are a different defect) or a >=95% era failure-rate cliff over >=20 gated days; (ii) one fresh unknown -- the era's chain is built from PREVIOUSLY COMMITTED sources plus the floored event's own factor, and a second uncommitted event REFUSES the era rather than assuming `ours`; (iii) acceptance unchanged -- measured floors go back through the MAP BUILDER (floor-aware `build_map`), so an era stands only under the same 2-paise containment + unwidened gate-1 band, and a floored event stops consuming the probe-gap guard's degree of freedom (which is what unwinds a cascade); (iv) full provenance in the map. New outcome: a floor AT the ex-date ("absent from every chain in our history"), requiring three event-out probes, not one. Also: the Q-5 ruling reached the MEASUREMENT days -- a weekend-dated session is no longer probed OR folded into an era (NSE's Saturday 2024-05-18 sat inside BDL's and INOXWIND's probe windows at 0.259/0.196 volume recovery vs 0.500/0.246 beside it, and gate-1 must hold on EVERY probe day, so one excluded session made the only correct chain un-provable). 10 floors over 108 probes; 16 eras PROMOTED; 26 symbols hunted, 38 events admitted across 20. Recoveries: VEDL 26.3->87.2% (13 eras promoted), INOXWIND 24.2->99.2%, BDL 51.6->99.7%, HINDPETRO 72.7->82.1%, CANBK 95.5->99.4%; quarantined 10 -> 6 (ASTRAL, IEX, NESTLEIND, NTPC, UPL, VBL). Gate 3: 118 ex-dates, 17 -> 15 failures, each in the disclosed-residual register with its numbers. **Coverage 93.48% -> 95.24%; the >= 95% DoD is MET** (413,914 of 434,591 symbol-days). Residuals are DISCLOSED, not chased, per the ruling. REVIEW (type C) still owed.
chunk 6: reviewed-PASS · gate-closed(docs/gate_chunk6_poc_evidence.md — Q42 + row-count oracle)  # QC (both personas) over 8b053c2..6ed863c. 1099/0 from clean (1094 build + 5 reviewer probes). CONTEXT 3.3 REIMPLEMENTED from scratch by the reviewer: all 25 frozen poc_prorata values reproduced at worst 4.0e-13, the coarser tie direction moves exactly six; F6 rebuilt by hand at two ticks no fixture uses; 16-mutant matrix (14 caught, 3 survived -> all tripwired in tests/test_review6_probes.py); every number in the gate pack re-derived read-only from the frozen CSVs and the parquet stores incl. BHARTIARTL 2026-07-17's 1914.60 vs 1914.50; engine swept over 11,905 real stored symbol-days with 0 exceptions, EXACT volume conservation and the POC always inside [bottom, top]. B111-B121 all approved. 4 MEDIUM findings (2 spec silences for the architect: the gate's discriminating day is also a Q-13 tie day, and totalTicks' rounding MODE is a second unstated silence measured at 10 POC-moving days per 2,418; 2 coverage gaps, both CLOSED by kept reviewer tests) + 6 INFO/LOW; no CONTEXT deviation, no fix session needed. **TRADER GATE CLOSED 2026-07-29** -- docs/gate_chunk6_poc_evidence.md, exactly as REVIEW_6 finding Q1 predicted: ONE BHARTIARTL 2026-07-17 reading with the rows countable answered Q42 and Q-13 together. Q42: "the box is the first two hours -- 9:15 to 11:15, eight candles" -> the 8-candle SPEC window is confirmed and Q-8 is closed. Q-13: his POC read (1913.9) is INCONCLUSIVE -- the two candidates are Rs 0.05 apart and 0.70/0.75 from his number, inside the Rs 0.05-3.5 feed-noise band CONTEXT §5 documents -- but his ROW COUNT (25) is decisive: one row from the finer profile's 26, three from the coarser profile's 22, so the interim STANDS as a ruling (CONTEXT v1.3 §3.3). No engine change; nothing in the pack moved. QUESTIONS.md ROUND-3 FINAL RECEIPTS R3F-e/R3F-f/R3F-g. tag chunk6-pass
chunk 7: reviewed-PASS  # QC (both personas, fresh session) over c941c64..0209f17 -- the two architect spec commits (v1.3, v1.4), the prep, the build and the Q-15 fix. **1396/0 from clean** (1390 build + 6 kept reviewer probes). CONTEXT 3.4 REIMPLEMENTED from the spec text by the reviewer, importing nothing from src/acumen: F1 golden (2037/2032/2052, risk 5 -- NORMAL branch because the entry candle's low TOUCHES the POC 2032), F2 golden (same, via WAIT-BELOW -> arm 2027 -> re-cross), F3 (1980 / **1988 = the candle HIGH** / **TP 1956**, risk 8) and F4 (2042 / **2028** / 2084) plus EVERY short mirror all reproduce to the paisa; the Q-15 risk-floor proof is asserted and holds (F1 risk 12, F2 risk 10, both gap stops <= POC). **52-mutant matrix: 0 survivors** -- 41 caught by the build, 9 by the kept probes, 2 PROVED equivalent (the redundant float guard; the Rule-3 outside-bar predicate, whose 17 boundary pairs are all decided earlier by inside-bar/Rule 1/Rule 2). Covered strict-vs-inclusive on every 3.4 comparison, the Q41-A "first distinct close ENTERS" mutant, the gap boundary BOTH sides, E7, SL-wins-ties, the 15:00-valid/15:15-dead boundary both ways, all four E10 fallback mutants, every consumption flip, and the new tie predicate (close==bodyMin, bullish precedence, colour re-read). **HDFCBANK 2026-06-10 re-derived from the stores**: Rule-3 outside bar with the 1-min high break at 09:15 (741.90) before any low break (11:21), all three gates pass, POC **739.80** (tick 5, totalTicks 182, tpr 8, 23 rows, EXACT volume conservation), ARMED at 738.20, trigger 740.95, NOT a gap (738.10 < POC), SL 738.10, TP 749.50, target on the 13:15-closing candle (high 755.95). **290-day sweep reproduces 146/88/56, zero exceptions**, zero gate refusals, zero invariant violations on all 146 entered days, and **zero backtest-vs-live drift** when each day is truncated to its entry candle (+189 prefix-stability checks) -- the CONTEXT 6 invariant chunk 13 will target. Empty-factor-table disclosure verified against the CA cache: all five in-window events are ORDINARY dividends under 4.2's 2% threshold (worst 1.78%), so k=1 and the empty table is exactly right for this window; chunk 9's wire-the-real-table duty is recorded. Prep audit clean: both spec commits touch CONTEXT.md only and make exactly the edits their logs claim; seven receipts faithful; both gate closures recorded with evidence paths; the 3 retired colour tests and 2 re-pinned probes are RULING-DRIVEN, not weakening (net strictly stronger: +3 new pins). B154-B168 all APPROVED. 2 MEDIUM findings (both boundary-operator COVERAGE gaps, not wrong behaviour -- the SHORT gap-predicate mirror, and all four exit-touch operators at the exact level; **both CLOSED by kept probes**), 2 LOW (75e3f98 lacks its `(unreviewed)` suffix though it changed engine code; `DayGates.usable` was unconsumed and untested on the gate-1P-only path) and 4 INFO. No CONTEXT deviation, no look-ahead, no test weakened, no fixture byte changed, no secrets, no AI attribution. docs/reviews/REVIEW_7.md · tag chunk7-pass
# superseded 2026-07-29: built  # **Q-15 EXECUTED 2026-07-29 (fix session, still unreviewed).** The architect ruled option (a): F1/F2's ILLUSTRATIVE POC moves to 2032 and CONTEXT 3.4 is untouched -- the example parameter gives way, not the trader's rule, and every number he stated (entry 2037, SL 2032, risk 5, TP 2052) survives. CONTEXT **v1.4** applied exactly as supplied and committed alone (7d4757e: header bump, §8's F1/F2 rows, §10's log row); ruling recorded verbatim under QUESTIONS.md Q-15, which is now **RESOLVED** -- chunk 7 owes the architect nothing further. NO engine line changed (signals.py already implements 3.4 verbatim); the F1/F2 GOLDEN is now the POC-2032 evaluation (CONTEXT 8's entry/SL/TP to the paisa on the NORMAL branch, because the entry candle's low TOUCHES the POC), and the POC-2030 evaluation is KEPT as a recorded MEASUREMENT of the gap branch (prior-close stop, risk >= 7 -- now asserted as properties). Nothing re-measured, no fixture byte changed, 1390/0. B166-B168. QC REVIEW OWED (fresh session, both personas) over the build AND this fix.
# chunk-7 BUILD detail (2026-07-29, unreviewed): src/acumen/signals.py (CONTEXT 3.4, PURE -- reference incl. the E10 fallback, ARMED / WAIT-BELOW / WAIT-ABOVE / side-unset, the Round-3 Q41-A wait rule, first-cross consumption incl. the unsizable case, gap detection + stop source, the 3x target, and the exit EVENTS with the stop winning a both-touched candle) + src/acumen/signal_engine.py (the orchestration: bias -> the CONTEXT 4.6 gate battery recomputed per day incl. GATE 1P -> POC -> signals, one refusal reason per day). 1390/0 offline (was 1316 at chunk6-pass; +5 prep, +69 build). CONTEXT 8: **F3 and F4 reproduce EXACTLY** (1980/1988/1956 and 2042/2028/2084); **F1 and F2 reproduce CONTEXT 8's entry/SL/TP exactly at POC 2032 and are also pinned at CONTEXT 8's POC 2030 under the gap branch** -- see **Q-15 (class A, OPEN)**, the one thing this chunk owes the architect: CONTEXT 8's F1/F2 numbers and CONTEXT 3.4's "low > POC" gap predicate cannot both hold, proved rather than asserted (while ARMED every close is at or below the POC, so the gap branch's stop can never be above it and the risk can never be F1's 5). Every long fixture has a hand-computed SHORT mirror. Beyond the fixtures, read-only and offline: 290 real stock-days over 5 symbols ran end to end with zero exceptions -- 146 entered, 88 armed-no-cross, 56 never-armed -- and HDFCBANK 2026-06-10 (a REAL Rule-3 outside-bar bias day, the first in this repo) is walked candle by candle in the session report. No fixture's bytes changed; F9's 15 real TCS days are byte-identical and green. QC REVIEW OWED (fresh session, both personas).
chunk 8: reviewed-PASS  # QC (both personas, fresh session) over 79c3c79..f079a2a -- the architect's Part A (three git/evidence rules), the simulator build, the evidence pack and the ledger commit. **1552/0 from clean** (1542 build + 10 kept reviewer probes; the build's own 1542/0 reproduced first, no skips). CONTEXT 3.4+3.5 REIMPLEMENTED from the spec text by the reviewer, importing nothing from src/acumen: the ENTIRE golden money table -- F1, F2, F3 short, F4 target+stop, both F4 SHORT mirrors, both square-off sides, the B159 entry-candle square-off, both qty-zero mirrors, all four exact-touch fills, both both-touched candles, the 1-share-at-budget day and a TP blow-through -- **20 rows x 10 fields, ZERO divergences**, with BOTH sizing bounds (`qty x risk <= 100,000 < (qty+1) x risk`) holding on every row. SHORT SIGN DISCIPLINE recomputed on every branch: short target gross POSITIVE (F3 +3,000.00; F4 mirror +2,982.00), short stop NEGATIVE (-994.00 = -qty x risk), short square-off signed by (entry - close) (+1,494.00, identical rupees to the long). FILL SEMANTICS: a candle blowing Rs 10 past the target still pays `qty x 3 x risk` (Rs 2,988.00, not Rs 4,648.00); both-touched pays the STOP; the square-off fills at the MARKED candle's close and at no other candle's close OR open, and the 15:15-stamped candle (whose high would have paid the target) is never read; a non-executed day carries no fill and no cost. **HDFCBANK 2026-06-10 re-derived from the RAW minute parquet** with the reviewer's own 15-min aggregation: risk 285p, qty 350 (99,750 <= 100,000 < 100,035), fill 749.50 (the LEVEL, not the 755.95 high), gross Rs 2,992.50, net Rs 2,892.50. **33-mutant matrix: 0 survivors** -- 28 caught by the build, 2 PROVED EQUIVALENT (a missing money key is already refused upstream by the required-keys check, so the `.get(default)` fallback is unreachable on both keys), 3 real gaps ALL in `trade_evidence.run_sweep` (double-count / drop-non-executed / per-symbol reset), CLOSED by kept probes; all four mutated files restored byte-identical by sha256. EVIDENCE PACK re-derived: the generator re-run read-only reproduces every number, the partition is 146/88/56 = chunk 7's exactly, and a parser importing nothing from src/acumen recomputed all 146 rows (risk, 3R target, qty, both bounds, fill level, gross, net) and all four totals -- **0 violations, every total to the paisa**; all 8 invariants and every rare-shape ZERO verified (no row carries any flag); the cited exact-budget day found and exceeded -- **16** such days, four of them 400-share (250p x 400); honesty language (no-portfolio/no-metric + empty-factor-table) present. CONFIG: no CONTEXT 3.5 money amount hardcoded anywhere in src/acumen (reviewer's own AST sweep of every module); missing risk_per_trade AND cost_per_trade both hard-refuse; fractional-paisa amounts refused on both keys. PURITY: simulate.py imports stdlib+signals only, ZERO float literals, ZERO true division, no clock/IO call -- and `exit_price_paise`'s `_Bar` protocol exposes only stamp+close, so it structurally CANNOT read a candle extreme. B169-B177 all APPROVED (B170's decision approved, its recorded float rationale corrected: 12.34*100 is exactly 1234.0, but 9,174 real amounts do lose a paisa). 1 MEDIUM (the run_sweep loop had zero coverage -- CLOSED by probes), 1 LOW (the committed pack is not byte-reproducible from its own generator: one sentence's position, no number moves) and 5 INFO. No CONTEXT deviation, no look-ahead, no test weakened (test_config 22 -> 30 functions, none removed, no assertion removed), no fixture byte changed, F9 untouched, no secrets, no AI attribution, all three NEW git rules obeyed. docs/reviews/REVIEW_8.md · tag chunk8-pass
# superseded 2026-07-29: built  # CONTEXT 3.5 in `src/acumen/simulate.py` (PURE): sizing, exit PRICING, money -- SignalDay in, TradeRecord out, integer paise throughout, no float literal and no true division in the module. **1542/0 from clean** (was 1396 at chunk7-pass: +120 simulator, +14 evidence, +12 config). Money reaches the engine from `config.yaml` through the loader ONLY -- `risk_per_trade` 1000 rupees (CONTEXT 3.5/Q29) and the NEW `cost_per_trade` 100 rupees (R1-Q23), each converted once through Decimal to exact paise (100,000 / 10,000); a tripwire test asserts the module hardcodes no money amount, and a fractional-paisa amount is refused rather than rounded. GOLDENS, every number hand-computed in its docstring before coding: F1 and F2 (risk 5.00 -> qty 200, target -> gross +Rs 3,000.00 / net +Rs 2,900.00), F3 SHORT (risk 8.00 -> qty 125, +Rs 3,000.00 / +Rs 2,900.00 -- sign discipline), F4 gap (risk 14.00 -> qty 71, target +Rs 2,982.00 / +Rs 2,882.00; stop branch -Rs 994.00 / -Rs 1,094.00) and BOTH F4 short mirrors; square-off both sides priced at the CLOSE of the 15:00-stamped candle (+Rs 1,494.00 / +Rs 1,394.00) with the 15:15-stamped candle proved never looked at; a qty-zero day (per-share risk Rs 1,300 > the whole trade risk -> 0 shares, zero cost, day CONSUMED and COUNTED, no fill price invented); the four REVIEW_7 exact-touch exits priced at the LEVEL; the both-touched candle paying the STOP; and the degenerate entry == SL day recorded consumed with no money. All five CONTEXT 3.5 properties asserted individually AND over a swept 22-point risk grid: qty x risk <= budget ALWAYS, (qty+1) x risk > budget (the floor is tight), target gross == qty x 3 x risk, stop gross == -qty x risk, net == gross - 10,000 iff qty > 0. REAL DATA, read-only and offline: the chunk-7 290-day sweep re-run and PRICED -- partition unchanged (146 entered / 88 / 56, zero exceptions, zero gate refusals) and the exit split identical to chunk 7 (86 stop / 36 square-off / 24 target), 146 executed trades, 53,750 shares, gross Rs 12,665.05, costs Rs 14,600.00, **net -Rs 1,934.95**, 45 winners / 101 losers; **HDFCBANK 2026-06-10 named golden** (risk 2.85 -> qty 350, target 749.50 -> gross Rs 2,992.50, net Rs 2,892.50) asserted in the suite FROM THE STORE plus a second test pinning the committed pack to the same numbers. Evidence committed per CLAUDE.md's new rule: `docs/evidence/chunk8_sweep.py` + `docs/evidence/chunk8_sweep.md` (per-trade table, totals, 8 recomputed invariants all PASS/0 violations, and a COUNTED rare-shape table -- gap entries, qty-zero days, both-touched candles and tie days are all ZERO in this window, said out loud). Self-run mutation matrix over the pricing and sizing operators: 17 mutants, **17 caught**, engine restored byte-identical. B169-B175. NO fixture byte changed; no CONTEXT/plan.md edit; no secrets; whole session OFFLINE. **QC REVIEW OWED** (fresh session, both personas) over 79c3c79..HEAD.
chunk 9A: reviewed-PASS incl. fix (REVIEW_9A_2)  # RE-REVIEW (both personas, fresh session) of the FIX span `4fa85fb..e83bc5e` against the architect's Q-16(a)/Q-16(b)/E13-basis rulings and REVIEW_9A's conditions. **1747/0 from clean** (1728 fix + 19 kept reviewer probes; the fix's own 1728/0 reproduced first, no skips, no xfails). **VERDICT PASS**, 8 findings, none blocking. Everything re-derived importing nothing from src/acumen for the arithmetic. **THE PATH**: 2026-05-05's FOUR concurrent positions walked observation by observation from the raw minute parquet -- 16 stamps, every trade's first mark exactly -Rs 100.00 (B194's cost-at-entry) and every last mark equal to the ledger's net, closing observation Rs 102,705.35 == the day's closing equity; across the run **146 paths / 903 marks all reconcile, 649 observations reproduce, the day-close identity holds 58 of 58, zero marks outside the session**; drawdown **Rs 14,231.00 (12.98%, 06-11 12:15 -> 07-23 11:45, 302 obs)** and run-up **Rs 17,515.10 (19.00%, 197 obs)** to the paisa. The exit-LEVEL rule **BITES**: 107 of 146 exits fill at a level their own candle did not close at (worst error Rs 2,843.75), so B196 is load-bearing. **TUKEY**: Q1 -Rs 1,099.00 / Q3 Rs 997.20 / IQR Rs 2,096.20 / fences [-Rs 4,243.30, Rs 4,141.50] / **0 of 146**, reproduced under a from-scratch type-7 estimator AND numpy.percentile, both hand-computed docstring fixtures verified digit by digit; the zero is a STRUCTURAL property (a fixed-R trade is bounded -Rs 1,100.00..+Rs 2,900.00). **NET BASIS**: pots Rs 96,489.60 / -Rs 98,424.55 summing to the net PnL, **PF 0.9803**, `winners x avg profit == gross profit` exact on the real 146; 7b/7c and Sharpe/Sortino/CAGR/benchmark all unchanged from REVIEW_9A. **THE SEVEN FLIPPED PROBES each FAIL on the pre-fix code in a scratch worktree for their OWN assertion** (three first failed on a helper-signature artifact -- the helper was patched in the pre-fix copy so each probe fails on its defect) and the control passes on both; none is decoration. **C1 CLOSED**: capital gone from all 35 files in src/acumen (own AST sweep), `initial_capital` REQUIRED on five refusal branches, the widened tripwire tripped by injections into `poc.py` AND `universe.py` with both files restored sha256-identically, and the `100*100` rewrite proved value-neutral over 79,800 (pass,total) pairs. **NO COLLATERAL DAMAGE**: run ledger still `c3363f6f...c1e318`, all 36 run artefacts byte-identical across three regenerations, the pack regenerates byte-identically THREE times to scratch (`9e8a77c7...`), both resume manifests hash equal at `b1390597...` while code_sha moved to HEAD (B179 confirmed), fixtures frozen, F9 untouched, every prior-reviewed engine module untouched, no test weakened (106->134 test defs, 256->387 asserts, 0 skips; the four retired names all pinned behaviour the ruling RETIRED and all four are replaced by tests asserting the retirement). B193-B201 all 9 APPROVED. **8 findings, all about labels or untaken branches, none about a wrong number**: Q1 a THIRD before-costs figure ("Gross PnL Rs 12,665.05") outside the one labelled line and the disclosed exception; Q2 the 15-min run-up prints its GIVEBACK as a "recovery" while the row above says "given back"; Q3 the MFE/MAE rows + section 8's excursion invariant are BEFORE-COST on a page declaring one NET basis (net PnL outside [MAE,MFE] on 20 of 146); Q4/Q5 INFO; C1 the C4 rewrite says "the first three are decided by walk_symbol" where exactly TWO are; **C2 MEDIUM, CLOSED by kept probes** -- `_outlier_line`/`_path_line`/`_path_stamp` had ZERO coverage and the pilot only exercises their empty branches, i.e. the branches 9B will take were untested; **C3 the pack cannot be regenerated on any day after its CA day-cache's `fetched_on`** (byte-identity verified here under a harness that only relabels the cache's age, changing no datum), making REVIEW_8 C2's rule unverifiable for future sessions -- PRE-EXISTING, not this span's doing; C4 the tripwire is a literal scan and cannot see a computed magnitude. No CONTEXT deviation, no look-ahead, no fixture byte changed, no secrets, no AI attribution. Six items carried to 9B (REVIEW_9A_2 section 12) plus the two duties C5 and Q8. docs/reviews/REVIEW_9A_2.md · tag chunk9A-fix-pass
# superseded 2026-07-30 by REVIEW_9A_2: fix built (unreviewed fix; REVIEW_9A_2 owed)  # FIX SESSION 2026-07-30 over 4fa85fb..HEAD, executing the architect's Q-16 + E13 PRESENTATION rulings (recorded VERBATIM in QUESTIONS.md, which closes Q-16) and REVIEW_9A's fixable conditions. **1728/0 from clean** (was 1689 at chunk9A-pass: +11 config, +19 portfolio, +9 pack). **Q-16(a):** outliers are Tukey fences on net PnL -- `quantile()` (R/numpy type 7, exact Fractions) + `outliers()` reporting count, summed net, both tails' shares of the net-basis pots, and the DEFINITION beside the number; two hand-computed fixtures (the six-trade ledger: fences [-328,750, +321,250], zero outliers; a new nine-trade one where the quartiles land ON order statistics: fences [-180,000, +140,000], 2 outliers, shares 50/53 and 9/11), both quartile pairs reproducing under numpy.percentile. **Q-16(b):** the worst-case coincidence construction is DELETED (constant, band fields, `intrabar=` switch, the two daily excursion sums that fed it) and replaced by the TRUE 15-minute path -- assembly in the I/O layer (`bt.assemble_trade_paths`, exit candles at their EXIT LEVELS, marks from the same aggregation the signal engine used), metric PURE (`pf.intraday_equity_path` / `path_max_drawdown` / `path_max_run_up`, running extreme seeded at the opening capital, cost charged at the entry mark so the last mark == realized net). Hand-computed fixture: two OVERLAPPING trades on two symbols with a mid-path exit at a level its candle ran past (marking the close instead would be wrong by Rs 1,000.00), every observation worked step by step -- path drawdown Rs 2,500.00 recovered at 12:45, run-up Rs 5,000.00, against a close-to-close drawdown of Rs 0.00 on the same day. **E13 BASIS:** one NET basis, one population; gross profit/loss/PF are net sums over net populations (`winners x avg profit == gross profit` is now an identity, asserted); before-costs totals on ONE labelled line; the definitions block added above section 7a. **Also closed:** C1 (capital -> `config.yaml initial_capital`, loader accessor, NO default in src/, money tripwire WIDENED to ast-scan every module in src/acumen with the magnitudes derived from config), Q3 (`_first_recovery` resolves the level before the walk, so a peak that IS the opening capital keeps its recovery date), Q5 (run-up giveback implemented symmetrically), C2 (both capital divisions guarded), C4 (`REASON_BIAS_UNRESOLVED` written into the refusal-order chain). **Recorded as duties, no code:** C5 (chunk 13 takes non-standard sessions from the published NSE calendar; the store scan stays the backtest cross-check) and Q8 (9B's pack owes a DISCRIMINATING E2 witness). **PILOT PACK regenerated byte-reproducibly** (twice to scratch, diff empty, sha256 9e8a77c7...): profit factor **1.1432 -> 0.9803** (it no longer reads "profitable" above a negative net PnL), gross profit Rs 101,099.20 -> Rs 96,489.60, gross loss -Rs 88,434.15 -> -Rs 98,424.55, outliers NOT COMPUTED -> **none of 146**, fences [-Rs 4,243.30, Rs 4,141.50], intra-trade drawdown Rs 18,844.65 (provisional) -> **Rs 14,231.00 (12.98%)** and run-up Rs 17,956.90 -> **Rs 17,515.10 (19.00%)**, plus 3 new invariants (146 paths / 903 marks reconcile; 649 path observations; no mark outside the session). The RUN LEDGER is untouched -- sha256 still c3363f6f...c1e318. Seven REVIEW_9A probe bodies FLIPPED to assert the rules, each citing its ruling (six had pinned a DEFECT -- Q1/Q2, Q4, Q3, Q5, C2 -- and one pinned the Q-16 STOP); an eighth is kept unchanged with its rationale re-pointed to the ruling's disclosed limit; no test weakened or deleted; no fixture byte changed; B193-B201. **RE-REVIEW OWED (REVIEW_9A_2, fresh session, both personas) over 4fa85fb..HEAD.**
# superseded 2026-07-30 by the fix session: reviewed-PASS  # QC (both personas, fresh session) over dfa3ff8..998ac1d -- the architect's plan amendment, the runner + ledger + portfolio build, the pilot pack and the ledger commit. **1689/0 from clean** (1675 build + 14 kept reviewer probes; the build's own 1675/0 reproduced first, no skips, no xfails). CONTEXT 3.2 REIMPLEMENTED from the spec text by the reviewer, importing nothing from src/acumen for the arithmetic. **RECONCILIATION 17 of 17** re-counted from the ledger against the COMMITTED chunk-8 pack; the real-vs-empty factor equality re-verified against the RAW CA cache (5 in-window events, all ordinary dividends, worst 1.6270% of its own cum-date close, ZERO share-count events -> every k=1); the pack regenerates BYTE-IDENTICALLY (sha 3e56f26c). **CA WALKS**: RELIANCE 2024-10-28 bonus k=1/2 reproduces digit by digit and the Rule-2 bearish verdict really does turn on **TEN PAISE** (132,210 vs 132,200 -- at -11 paise the day becomes an outside bar and falls to Rule 3); HDFCBANK 2019-09-19 split REVERSES the bias (adjusted Rule-2 bullish vs unadjusted Rule-1 bearish), both half-paise roundings landing half-even exactly; the 2023-07-20 demerger blocks E+1/E+2 and resumes at E+3; the Muhurat 2024-11-01 has a bhavcopy row but 60 candles stamped 18:00-18:59, so E2's own detection removes it and 2024-11-04's pair reaches back to (10-30, 10-31). **THE 47 AUDITED**: an independent from-scratch scan reproduces **204 symbols / 112 events / 107 pairs / 47 changed** EXACTLY, the pack's first-15 table matching row for row; four changed pairs hand-walked (GRASIM 1/5, BEL 1/10, CONCOR bonus 1:4 -> **4/5**, ICICIBANK bonus 1:10 -> **10/11** with a non-terminating rounding decided by 100 paise) -- the INDIAN bonus convention is correct. **DETERMINISM**: byte-identical resume at THREE kill points the builder never used (after HDFCBANK, after ICICIBANK, and a DOUBLE interruption), ledger sha `c3363f6f...` = the published one, 0 duplicate keys, all four stable manifest digests equal; moved-SHA resume REFUSED; no clock leak in rows or manifest. **PARTITION** proved on 492 damaged IOC/TATASTEEL symbol-days: 4 reasons summing to 492, usable+refused==walked, **75 rows fail TWO gates and each still carries exactly ONE reason**, zero exceptions. **PURITY**: portfolio.py has ZERO float literals, zero float()/round(), zero clock reads, no I/O import at all; all 15 divisions are Fraction/Decimal; Sharpe/Sortino are Decimal DISPLAY-only and no ledger row carries a float. B178-B192 all 15 APPROVED. **14 findings, none blocking**: 2 MEDIUM quant -- the E13 **BASIS** and **MEMBERSHIP** mixing, where the Rs 4,609.60 gap between 45 x avg profit and gross profit decomposes EXACTLY into Rs 4,500.00 winners' commission + Rs 109.60 from four gross-positive/net-negative trades (49 made money gross, 45 net; no number is wrong, the basis is unprinted) -- plus 1 MEDIUM quant Q3 (the drawdown recovery date is silently lost whenever the peak is the opening capital, which B185 makes a normal case; not exercised by the pilot, live for 9B) and 1 MEDIUM code C1 (CONTEXT 3.5's Rs 1,00,000 capital HARDCODED at portfolio.py:44 with no config key while risk and cost are both config-loaded; the chunk-8 tripwire scans simulate.py only). **SECTION 0 RECORDS A MATERIAL DISCREPANCY**: the review brief's directed check 6 presumes a Q-16 fix (Tukey fences, a 15-min intraday path, PROVISIONAL removed) that exists NOWHERE in this repo -- Q-16 is still class A OPEN STOP and a fix session is owed. Six conditions carried to 9B (REVIEW_9A section 12). No CONTEXT deviation, no look-ahead, no test weakened, no fixture byte changed, F9 untouched, no secrets, no AI attribution. docs/reviews/REVIEW_9A.md · tag chunk9A-pass
# superseded 2026-07-29: built  # CHUNK 9 SPLIT into 9A/9B by the architect (29-Jul-2026; ruling verbatim in QUESTIONS.md, one line appended to plan.md's chunk-9 card). **9A = the machine**: `src/acumen/backtest.py` (the runner: per settled symbol x trading day -- CONTEXT 4.6's gate battery RECOMPUTED per day incl. gate 1P, bias via chunk 4 WITH THE REAL CHUNK-3 FACTOR TABLE WIRED, POC via chunk 6, signals via chunk 7, money via chunk 8; ONE refusal reason per day in a fixed order, E2 non-standard sessions detected from the candles' own stamps and excluded from the calendar BEFORE any bias pair, demerger suppression and an unassemblable bias both COUNTED no-trades, never a crash) + the RUN LEDGER (one JSON row per walked symbol-day through atomic_io+fsync, per-symbol shards written only when a symbol is COMPLETE -> resume with zero duplicates; a completed re-run is BYTE-IDENTICAL, proved) + the MANIFEST (spec v1.4, code SHA, config digest, universe, span, per-symbol walked/usable/refused-by-reason, rare-shape counters at zero when absent, the residual-register acknowledgment and CONTEXT 4.6's IOC/TATASTEEL caveat VERBATIM) + `src/acumen/portfolio.py` (PURE over the ledger: daily net PnL, equity = capital + cumulative SUM, the full E13 list, take-all disclosures per Q40-d, and the capital-flag machinery that computes NOTHING while Q43 is pending -- `capital_reference`/`margin_basis` are optional config keys, both null, no default). **1675/0 from clean** (was 1552 at chunk8-pass: +42 runner, +41 portfolio, +28 evidence, +12 config). PILOT PACK `docs/evidence/chunk9a_pilot.{py,md}`, regenerated BYTE-IDENTICALLY from its own generator: (a) the chunk-8 window re-run THROUGH THE RUNNER with the real table reconciles on **17 of 17 figures** (290/146/88/56, 53,750 shares, -Rs 1,934.95) and the five in-window events are all ordinary dividends at k=1; (b) RELIANCE's 1:1 bonus ex 2024-10-28 walked digit by digit (P x 0.5, Rule 2 bearish decided by TEN PAISE) plus HDFCBANK's 2019 split where the adjustment REVERSES the bias, plus the 2023-07-20 demerger suppression window and NSE's Muhurat 2024-11-01 refused under E2 -- and a universe scan: **47 of 107** real share-count events change the bias, which is what the empty table would have cost; (c) resume proof -- killed after RELIANCE, resumed, ledger sha256 IDENTICAL, 0 duplicate keys. **Q-16 RAISED (class A, OPEN)**: E13 names "outliers" with no definition (NOT computed) and the intra-trade drawdown has no observable path under concurrency (computed but labelled PROVISIONAL everywhere). QC REVIEW OWED (fresh session, both personas).
chunk 9B: COMPLETE -- report final, prose-fixed (REVIEW_9B_FINAL + the 06-Aug-2026 rulings)  # **PROSE-FIX 2026-08-06**, under the architect's REVIEW WAIVER scoped to this session only (QUESTIONS.md, ARCHITECT'S RULINGS 06-Aug-2026 clause 3): the three one-line prose edits REVIEW_9B_FINAL left owed, plus its two cheap items, closed by the numeric freeze + the flipped reviewer probes + the architect's direct reading -- no fresh review session. **2174/0/0 from clean** in 307s (same count as REVIEW_9B_FINAL: 2 test functions renamed, 0 added, 0 removed, none weakened, skipped or deleted). **Q1** -- section 10's blockquote is BYTE-VERBATIM against QUESTIONS.md's record again: the ruling's own *(491.90% as generated)* and its trailing *Architect.* are restored and the inserted *QUESTIONS.md* pointer now sits OUTSIDE the quotation, in the sentence above it and in a new attribution line beneath it (the only remaining difference is the ASCII fold every docs/reports/ file carries). **Q2** -- *"What THE BENCHMARK applies"* is scoped to the benchmark's own members: **94 share-count factors across 61 of its 134 members**, with the universe-wide **125 across 86** kept on the page, stated separately and labelled as the figure the factor table reports; the difference is factors on symbols with no close on the first trade date, which E13 excludes and section 10 names. **Q3** -- the 77-row is labelled the **HALF-OPEN convention `entry <= T < exit`**, not *the 15-minute path's convention*; section 4's definition, section 11's paragraph and `concurrency_closing_first`'s own docstring all say outright that the 15-minute path marks a trade THROUGH its exit stamp and its own maximum is the pessimistic **90**. **C2** -- section 1's headline coverage is COMPUTED from the register (`ResidualEntry.usable_pass`, new and defaulted; `_usable_share` = **409,205 settled usable days / 435,641 whole-lake stored days = 93.9317%**), the literal retired; it renders identically, so the freeze needed no new exclusion for it. **C3** -- the freeze docstring reads 2,457. **PROBE FLIPS PROVEN**: both REVIEW_9B_FINAL probes that pinned Q1/Q2 as defects were run against the PRE-FIX source (HEAD 74b04cd's `src/`, copied to scratch) and were **RED** -- Q1 on `published == recorded`, Q2 on `AttributeError: 'BenchmarkPair' object has no attribute 'in_benchmark_share_count_events'` -- then green after the fix; they now assert the corrected properties in both directions and read them off the RENDERED page. C4's and C1's probes are byte-untouched. **REGENERATED, BYTE-REPRODUCIBLE x2**: the committed launcher wrote the artefact and a second run to scratch diffs **EMPTY**; sha256 **`65fbf6029b624c322d66f6396de5115233c390e424a52417a1ec09cee08d2a7a`**, **85,712 bytes, 897 lines** (was 09b485b8..., 84,700, 895 -- the two added lines are section 10's attribution line and its blank separator). **THE FREEZE HOLDS AND ITS EXCLUSION LIST GAINS NOTHING** -- still 5 lines, still 2,457 tokens, identical in order to the baseline from `git show 3cbfafa:`; the only two edits inside sections 1..9 fall on lines the existing markers already name ((1, `price-proven`) and (4, `**Concurrency, and which convention`)), and Q1/Q2/Q3's row label are in sections 10-11, outside its scope. Line-level diff of sections 1..9: **13 differing lines, 10 marker-covered, 3 blank, ZERO uncovered non-blank**. **ZERO STORE WRITES** -- run-dir 208 files, newest mtime still 2026-08-05 20:25:39, ledger sha256 still `c70a72b097879914...a4d134`, manifest `2594c6e81d404029...`, 0 files anywhere under data_root modified this session. No CONTEXT deviation, no fixture byte moved, `portfolio.py` and every engine module untouched, no secrets, no AI attribution. The architect's three rulings are recorded VERBATIM in QUESTIONS.md and executed; `personas/code_reviewer.md` gains checklist item 10 (a rendering fix is pinned at the RENDERED OUTPUT). tag chunk9b-prosefix
# superseded 2026-08-06 by REVIEW_9B_FINAL: chunk 9B: reviewed-PASS (REVIEW_9B_REPORT) + FINAL EDITS built on top (unreviewed) -- re-review owed over `3cbfafa..HEAD`  # **FINAL-EDITS SESSION 2026-08-06.** The architect's **Q-23 REFINEMENT** recorded verbatim and **PUBLISHED**, and REVIEW_9B_REPORT's six presentational findings closed. **THE BENCHMARK is now the SHARE-COUNT-ONLY construction** -- 125 bonus/split/rights factors across 86 symbols, **Rs 566,668.64 = 466.67%** -- with the mixed reading (Rs 591,899.89 = 491.90%) and the raw reading (Rs 398,922.17 = 298.92%) still printed and each labelled *NOT the benchmark*, the ruling quoted verbatim in section 10, and the stated line the refinement asks for: the mixed row additionally applies **308** special-dividend factors worth **+25.23 pp of opening capital, 5.13% of its own figure**. Cash distributions are excluded UNIFORMLY -- ordinary and special alike -- so the CA engine's 2% threshold no longer decides the benchmark. `BENCHMARK_STOP` is gone, replaced by `BENCHMARK_RULING`; **B275 is SUPERSEDED by B282**; section 13's *Q-23 open* line is closed. **THE SIX FIXES**: Q1 the fixed-R fence claim is now made PER COLUMN (All and Short's fences do sit outside the band; **Long's do NOT -- Q3 Rs 440.00, IQR Rs 1,539.60, upper fence Rs 2,749.40 BELOW its own largest win, so 13,243 of 89,345 trades are outliers, 76.48% of that column's gross profit**); Q3 section 5's `Best year` row carries its PARTIAL-year marker inline (2016, 61 walked days, and the best FULL year named beside it: **2018 at -Rs 1,178,849.98**); Q4 section 1's headline is restated as **ALL THREE GATES, 93.9317%**, with **gate 1P alone at 97.8444%** and the different denominators stated; Q5 the two %-of-notional rows become RANGES over the tied set (**5,383** trades tied at the largest win, **20,939** at the largest loss); Q6 the refusal-pricing rows print their measured base (**13 of 105**, **7 of 18**); Q7 the concurrency convention is defined in section 4 and BOTH readings print (**90** open-first at 2026-05-07 12:45, **77** closing-first at 2026-03-20 12:30). **2168/0/0 from clean** (was 2151 at `chunk9b-pass`; +13 report tests, +4 freeze tests). **BYTE-REPRODUCIBLE, PROVED TWICE** -- two independent full regenerations to different paths, diff EMPTY, both sha256 `09b485b8beb41d2c...`, 84,700 bytes. **NO NUMBER IN SECTIONS 1..9 MOVED, AS DATA**: `docs/evidence/chunk9b_report_numeric_baseline.py` freezes **2,457 numeric tokens** taken through `git show 3cbfafa:` (not the working tree, so a regeneration cannot move its own baseline), only the five corrected lines excluded and scoped per SECTION, and `tests/test_report_9b_numeric_freeze.py` also asserts each correction is PRESENT so a freeze that passed on an unedited report goes red. Fixtures untouched; stores READ-ONLY (nothing in the run directory newer than 2026-08-05 20:25; ledger sha256 still `c70a72b097879914...a4d134`). Q-23 **CLOSED** -- QUESTIONS.md has no open item touching chunk 9B. B282-B291. **RE-REVIEW OWED** (fresh session, both personas) over `3cbfafa..HEAD` -- the next review's span starts at tag `chunk9b-pass` per REVIEW_9B_REPORT finding C3.
# superseded 2026-08-06 by the FINAL-EDITS session: chunk 9B: reviewed-PASS -- COMPLETE  # QC of THE REPORT (both personas, fresh session) over `41f3e3e..796c39d`. **2140/0/0 from clean** (the build's claim reproduced exactly); **2151/0/0** with 11 kept reviewer probes. **VERDICT PASS**, 15 findings, none blocking, **not one of them a wrong number**. **PART 0 -- the architect's Q-23 RULING RECORDED VERBATIM** in QUESTIONS.md (the buy&hold benchmark is the SHARE-COUNT-ADJUSTED construction, 491.90% as generated; both figures stay printed; the adjusted one is THE benchmark) and Q-23 moved OPEN-STOP -> RULED. **THE GENERATOR DOES NOT YET PUBLISH IT** -- section 10 still reads *"neither published"*, still quotes `BENCHMARK_STOP`, and B275 still records *"NEITHER is published"*: that is THE ONE OPEN EDIT, owed to `report_9b.py`, and **no number moves**. **THE HEADLINE IS TRUE**, re-derived from the raw ledger importing NOTHING from src/acumen: net **-Rs 16,836,018.20** / gross profit Rs 113,514,085.92 / gross loss -Rs 130,350,104.12 / **PF 0.8708** / 188,345 trades / 59,385 winners / **31.53%** / avg win Rs 1,911.49 / avg loss -Rs 1,011.09 / ratio 1.8905 / expectancy -Rs 89.39 / largest pair +Rs 2,900.00 and -Rs 1,100.00 / commission Rs 18,834,500.00 / 256,816,544 shares -- **every figure to the paisa**, plus all ELEVEN year rows (summing to the net exactly, 11 of 11 negative) and the Long/Short split (-Rs 13,248,749.37 + -Rs 3,587,268.83, PF 0.7908 / 0.9465). Decomposition proved: net + 188,345 x Rs 100 = Rs 1,998,481.80 = the ledger's own gross; every executed row carries exactly Rs 100 and net == gross - cost on all 188,345. **BREAK-EVEN WIN RATE 34.60% against an actual 31.53%** -- a 3.07 pp shortfall, ~5,774 winners, which is the whole -Rs 16.8m in one line. **DRAWDOWNS**: close-to-close Rs 16,852,007.80 (14,528.90%), 2016-10-06 -> 2026-07-30, 2,424 observations, and NEVER RECOVERED proved directly (0 later observations at or above the Rs 115,989.60 peak; the trough IS the last day). **THE 15-MINUTE PATH RE-ASSEMBLED FROM THE RAW LAKE** with this session's own CONTEXT 7-E1/E2/E12 aggregation: **1,101,029 marks, 188,345 of 188,345 paths reconciling, 40,981 observations, 2,428 of 2,428 day-closes tying to the daily curve, drawdown Rs 16,864,934.22 (13,174.01%) over 40,909 observations and run-up Rs 424,810.90 over 795 -- identical field for field.** **THE REFUSALS FIRE FOR THE STATED REASON**: 2,390 of 2,428 daily observations sit at or below zero, 2,389 daily returns divide by a NEGATIVE denominator, and a naive Sharpe built from them prints **+0.5882** by this reviewer's own arithmetic -- the report prints `n/a` with its reason on Sharpe AND Sortino on all three columns, CAGR undefined on all three, and **no sign-pathological number appears anywhere on the page**. **RECONCILIATIONS RE-VERIFIED**: 32 of 32; pilot 290 IDENTICAL FIELD FOR FIELD (ledger sha `c3363f6f...`); crashed run 145 of 250,084 with the four classes re-derived (105/18/9/13, ZERO unexplained) and **a hand-classified sample of 20** read off the two rows themselves; the 59 byte-identical shards proved as arithmetic -- 44 touched, 27 by Q-21(b) (FIX-2's own prediction), 103-27 = **76**, 44-27 = **17**, 76-17 = **59**, and 59 confirmed independently by hashing the files. **WITNESSES**: TWO gap days hand-walked bias -> POC -> signal -> money from the spec text -- MUTHOOTFIN 2016-10-06 (Rule-1 bearish; POC Rs 343.50 from totalTicks 68 / tpr 3 / 23 rows with EXACT volume conservation; ARMED; gap since the entry candle's high Rs 343.30 < POC; SL = prior close Rs 343.60; risk Rs 0.70; qty 1,428 with both bounds tight; stop filled at the LEVEL; net -Rs 1,099.60) and TIINDIA 2026-07-27 (WAIT-ABOVE -> ARMED -> entry; **the gap predicate turns on TEN PAISE**, high Rs 2,848.80 < POC Rs 2,848.90; qty 114; net -Rs 1,091.80) -- **16 of 16 fields each, no divergence**; both qty-zero days' per-share risks (Rs 1,019.70, Rs 1,173.30) flooring to zero with no invented fill; the five Q-17 dates (123/0/55/1/148) and the 889 total; the 5 no-break carries with exactly 2 flagged. **ALL 204 PER-SYMBOL ROWS recomputed cell by cell -- 1,632 cells, 0 disagreements** -- and all 204 price-proven percentages re-derived from the register itself. **DISCLOSURES**: Q43 verbatim x3, Q44 x2, all five manifest disclosures verbatim, the B149 caveat verbatim, the Q-21(b) concentration (210/59/47.14%), coverage 409,205 = 93.9317% reproduced EXACTLY from the register's own `usable_pass` over the 204 settled, and R10's pricing traced to the crashed shards (13 trades -Rs 7,358.35; 7 trades -Rs 3,698.60; 2 rows that traded on both runs and differently). **BYTE-REPRODUCIBLE**: regenerated to scratch, **diff EMPTY**, sha256 `98c45129a8215d2a4e78a389dfa54db4ceccfabb458e5c3ee21a7fcd116b7487`, 77,799 bytes. **10-MUTANT MATRIX over the generator: the shipped suite catches 3.** Four of the five corrections commit `4eba3bf` ships can be reverted with all 2,140 tests green -- including **the Sharpe/Sortino refusal it headlines** and **the CALL SITE of the walked-day index fix** (the shipped test pins the helper, nothing pins that `build_everything` calls it) -- plus the path run-up's order and all three "computed rather than asserted" prose fixes. **All seven closed by kept probes, 9 mutants -> 9 red, engine restored sha256-identically each time.** B271-B281 judged: 9 APPROVED, B275 APPROVED-BUT-SUPERSEDED by the Q-23 ruling, B281 APPROVED-WITH-NOTE. **15 findings, none blocking**: quant 3 MEDIUM (section 6a's closing sentence is refuted by its own Long column -- **13,243 outliers worth 76.48% of Long gross profit**, because Long's upper fence Rs 2,749.40 falls BELOW the Rs 2,900 max win; **308 of the benchmark's 433 'share-count factors' are special DIVIDENDS**, worth +25.23 pp of the ruled 491.90%, so the ruling's *"dividends excluded and stated"* half is owed; section 5 names PARTIAL 2016 as the *best year* without section 8's caveat, where the least negative FULL year is 2018) + 4 LOW + 2 INFO; code 2 MEDIUM (the two test-coverage gaps above) + 3 LOW (four commits since `chunk9b-relaunch-pass` sit OUTSIDE the directed span and are reviewed by nobody -- audited here, nothing wrong; eight prose sites type the CONTEXT 3.5 money magnitudes as text; three section-12 sentences still ASSERT counts the ledger could supply) + 1 INFO. No CONTEXT deviation, no look-ahead, no test weakened, deleted or skipped, no fixture byte moved, 13 of 13 engine modules blob-identical to the run's own code SHA, no secrets, no AI attribution, **ZERO store writes**. docs/reviews/REVIEW_9B_REPORT.md - tag chunk9b-pass
# superseded 2026-08-06 by the REPORT REVIEW: report built (unreviewed)  # **THE REPORT SESSION 2026-08-06** over `41f3e3e..HEAD`. `docs/reports/chunk9b_backtest_report.md` (880 lines, sha256 `98c45129a8215d2a...`, **BYTE-REPRODUCIBILITY VERIFIED DIRECTLY** -- a second full run to a scratch path came back byte-identical, diff empty) + its generator `src/acumen/report_9b.py` + launcher, byte-reproducible and offline over the completed ledger. **2140/0/0 from clean** (was 2,111; +29). **RECONCILIATION 32 OF 32**: every manifest figure recounted from all 495,312 rows, the partition exact four ways (usable+refused==walked; the 9 reasons sum to 88,824; outcomes sum to the walk; 204 x 2,428 == walked), 0 duplicate keys, and all ELEVEN rare-shape counters recounted independently rather than copied. **PILOT WINDOW IDENTICAL FIELD FOR FIELD on all 290 rows** against the ledger whose sha256 `c3363f6f...` three reviews published. **CRASHED RUN: 145 of 250,084 rows differ, ZERO unexplained** -- Q-21(b) 105, Q-21(a) 18, Q-22(a) 9, downstream carry 13 -- and FIX-2's prediction lands to the symbol (76 under Q-21(b) alone, minus the 17 the later rulings touched, == the **59** byte-identical shards). **THE NUMBERS**: net **-Rs 16,836,018.20** on 188,345 trades, PF **0.8708**, win rate **31.53%** (59,385), avg win Rs 1,911.49 / avg loss -Rs 1,011.09, expected payoff -Rs 89.39, commission Rs 18,834,500.00, max drawdown **Rs 16,852,007.80** close-to-close and **Rs 16,864,934.22** on the 15-minute path, CAGR undefined (final equity negative), every one of the eleven years negative, longs -Rs 13.25m against shorts -Rs 3.59m. **TUKEY: 0 outliers of 188,345** -- a fixed-R trade is bounded [-Rs 1,100.00, +Rs 2,900.00] and both bounds are the largest loss and largest win exactly. **THE 15-MINUTE PATH: 188,345 paths / 1,101,029 marks, ALL 188,345 reconciling** to their ledger net (verified again by an independent script importing nothing from portfolio.py). **READING THE GENERATED REPORT FOUND THREE DEFECTS THE TESTS DID NOT**, each now pinned: (1) `pf.side_split` derives its index from the rows given, so the E13 columns were annualized over the 2,415 days that TRADED, dropping 13 flat days -- the exact failure `pf.walked_days` warns about; (2) **Sharpe printed +0.5833 for a strategy that lost 168x its capital**, because once equity crosses zero a loss divides by a negative base and reads as a positive return -- Sharpe, Sortino and every excursion percentage are now REFUSED with their reason where the base is not positive; (3) run-ups printed peak->trough and 12f claimed three zero counters while printing 30 for one. **R10 DISCHARGED**: refusals priced from the crashed shards -- Q-21(b) 13 trades worth -Rs 7,358.35, gate-2 completion 7 worth -Rs 3,698.60 -- and the seven classes with no counterfactual print "none" rather than an extrapolation. **RARE-SHAPE WITNESSES AT LAST**: 2,068 gap entries (REVIEW_7 Q3) with their year table and exit split, exactly **2** qty-zero days NAMED (BOSCHLTD 2021-05-20 at Rs 1,019.70, SHREECEM 2020-03-19 at Rs 1,173.30 -- REVIEW_8 Q1), the Q-17 five market-wide dates with the 2021-02-24 outage annotation, the 22 demerger suppressions listed per symbol (with the 8 further suppressed rows the refusal order counts elsewhere), and the 5 Rule-3 no-break carries of which exactly 2 are Q-22(a)'s. **HOUSEKEEPING**: the RUN RECEIPT recorded and the owed re-gate VERIFIED FROM THE REGISTER (0 stale rows, 409,205 / 93.9317%, 204 settled / 6 quarantined); the runbook-banner defect found and fixed -- the handed re-gate command opened with `acumen-universe-backfill`, which does not exist on this machine, corrected in the banner via a new `REGATE_LAUNCHER` constant, in runbook step 7, and in the test that pins it; REVIEW_9B_FIX4 **F3/F4/F5** closed; REVIEW_9B_PRESEAL **Q2** (2,428 vs 2,425) and **Q3** (the "EXACT" erratum) closed; **C6** closed DIRECTLY -- the chunk-9A pilot pack regenerated IN PLACE under the named write sanction, its **ledger byte-identical** at `c3363f6f...`, the pack differing only by the spec-version label, three new zero-valued rare-shape rows and two manifest digests, **no number moved**. **Q-23 RAISED (class A, OPEN, STOP)**: CONTEXT 7-E13 is silent on the benchmark's PRICE DOMAIN; both readings measured (raw 298.92%, share-count adjusted 491.90%), NEITHER published. B271-B281, including the disambiguation of the register's only two genuine ID collisions. **ZERO store writes outside the sanctioned pilot-pack run directories.** QC REVIEW OWED (fresh session, both personas).
# superseded 2026-08-06 by the REPORT session: chunk 9B: RELAUNCH AUTHORIZED (REVIEW_9B_FIX4)  # FIX-4 RE-REVIEW 2026-08-05, BOTH personas, focused QC over `6b6baaa..bb2ad60` (7 commits). **VERDICT PASS; the authorisation REVIEW_9B_FIXES withheld is GRANTED** -- its one condition (finding R1, the Rule-3 scan consuming out-of-session bars against CONTEXT 4.6's Q-17 law) is discharged. **THE R1 ADJUDICATION, by the reviewer's own two-engine walk of the shipped `BiasEngine` from the run's own span start 2016-10-03, each symbol's real factor table wired** (GODREJCP 50 factors incl. bonuses ex 2017-06-22 k=1/2 and ex 2018-09-12 k=2/3; LAURUSLABS 19 incl. the 2020-09-29 split k=1/5), the 8 CONTEXT 7-E2 sessions removed and 2021-02-24 asserted NOT among them: the bias carried into 2021-02-25 is **bearish on BOTH** -- GODREJCP 2021-02-24 `rule-1-breakout` bearish, LAURUSLABS 2021-02-24 `inside-bar-carry` bearish -- so **FIX-4's correction is UPHELD and R1's ">= 2 biases change" is REFUTED**. Over 1,086 walked days per symbol the two walks disagree on **1** day (2021-02-25, on the RULE: `rule-3-outside-bar` -> `rule-3-no-break-carry`, the ruling's own "-> carry") and on **0** biases. **11 of the 21 impact days across 10 of the 16 symbols re-derived, both halves, reviewer's arithmetic** -- every stray count, carry side and verdict matches the evidence table exactly; the 21/16 recounted; and the per-date counts (8 on 2021-02-24, 4 on 2017-04-28) AGREE with R1's independently sampled figures. **THE ORDERING IS LOAD-BEARING, measured on the real store**: `gate_day` is handed the WHOLE stored day (GODREJCP 1,054,365 shares vs 847,018 filtered; folds [67600,69890] vs [68000,68885]) and gating the FILTERED day would REFUSE both symbols on gate 1 (gap 23.089% / 22.421%) instead of re-answering them. Drop ledger counts **DAYS**: 77 strays x 2 consumers -> 2 rows, 1 occurrence each, manifest 2; clean window 0. **Tripwire behaviourally RED at 6b6baaa (224 candles, 77 out-of-session, `rule-3-outside-bar`) and GREEN at bb2ad60 (147, 0, `rule-3-no-break-carry`)** in the reviewer's own checkouts. All three claimed mutants reproduce exactly (unfiltered scan **6**, unflagged drop **3**, R3's exact mutant **1** -- R3 CLOSED); engine restored sha256-identical. **Q-22(b)**: own AST confirms `_minute_loader` GONE and `bt.gated_minute_loader(minute_store, pipeline)` wired, no cycle; and regenerations of the chunk-8 pack at 6b6baaa and bb2ad60 are **byte-identical to each other** -- the gating moves not a number, not a byte, so NOT regenerating it is right. Pilot ledger digest still `c3363f6f...` and all 290 committed rows re-serialise byte-identically through the shipped code. No "gate gate" anywhere; the import guard NAMES a fifth subclass the reviewer defined (and one two levels down); R4's corrected command dry-run resolves **210** vs the bare command's **208** (missing exactly EXIDEIND, NUVAMA) and the bare `--report-path` default IS the committed sealed report. **CONTEXT v1.7** touches CONTEXT.md alone in **five** hunks = the five listed edits; population 48+2+1=**51** verified bar by bar against the store with exactly one (APLAPOLLO 2017-10-05 15:28, C outside) already sealed-refused; `SPEC_VERSION` v1.7 on the CODE commit and `GATE_DEFINITION` correctly UNBUMPED (no gate definition moved; `quality_gates.py` byte-identical). **B263-B270 judged: 6 APPROVED, B267 APPROVED-WITH-CHALLENGE, B270 APPROVED-WITH-NOTE.** **2111/0/0 from clean reproduced; 2115/0/0 with this review's 4 kept probes**, no test weakened (1,422 -> 1,439 names, exactly one RENAMED -- the promised tripwire flip), fixtures frozen, 12 pure engines byte-identical, commit hygiene clean, no AI attribution, no secrets, `HEAD == origin/main`. **5 findings, NONE blocking**: F1 MEDIUM -- B267's filter in `minute_backfill.minute_loader` is pinned by NOTHING (reverting it leaves all 2,111 tests green; it reaches no production caller, CLOSED by kept probe); F2/F3/F4/F5 LOW (a vacuous assertion that cannot fail, one stale docstring, "four edits, no fifth" on a five-edit commit, one comment naming the wrong test file). **ZERO store writes.** docs/reviews/REVIEW_9B_FIX4.md · tag chunk9b-relaunch-pass
# superseded 2026-08-05 by REVIEW_9B_FIX4: fixes reviewed-PASS + Q-22 executed (unreviewed) -- RELAUNCH UNBLOCKED, re-review owed over 6b6baaa..HEAD  # FIX-4 SESSION 2026-08-03 over `6b6baaa..HEAD`. **PART A**: the architect's **Q-22(a)/Q-22(b) ruling recorded VERBATIM** in QUESTIONS.md -- *"Q-17's candle-level drop binds EVERY consumer of stored minute bars, the Rule-3 first-break scan included ... where the only break lives in stray bars the scan finds NO break and the day carries"*, and *"the Q-21(b) battery precondition binds trade_evidence too ... FIX-2's measurement must be ASSERTED when gating it"*. **PART B EXECUTED** at the one boundary both loaders share: `backtest.candles_for` calls `aggregate.in_session_bars` BEFORE it builds a candle and returns `(candles, dropped)`; the GATES still see the whole stored day (`gate_day` is handed the unfiltered bars, CONTEXT 4.6's own other half); the drop travels loader -> runner through a shared `Rule3SessionDrops` and lands on the trade day's row under the EXISTING `FLAG_OUT_OF_SESSION_DROPPED`, which finally gains its manifest counter (R9). `minute_backfill.minute_loader` gets the same filter -- the ruling binds every consumer. **THE FULL-SPAN IMPACT, MEASURED read-only over all 204 settled symbols and the whole span** (`docs/evidence/chunk9b_q22_session_filter.{py,md}`): of **7,031** Rule-3 scan days, 30 have no minutes, **210** are battery-refused first, and **21 across 16 symbols** read a D-1 carrying strays; of those the scan's RULE changes on **2** and the BIAS on **0**, and **no walked day in the ten-year span ends with a different bias**. The 2 are exactly the days REVIEW_9B_FIXES named -- GODREJCP and LAURUSLABS, trade day 2021-02-25 -- both becoming `rule-3-no-break-carry`, the ruling's own *"-> carry"*. **ONE REVIEW FIGURE DOES NOT REPRODUCE and is recorded**: R1's ">= 2 biases change" rests on a probe that passed the literal "BULLISH" as the carried bias; walked from the span's start -- and re-derived through the shipped `BiasEngine` -- the carry into 2021-02-25 is already bearish on both, so the carry lands on the same answer the 15:44 print produced. **PART C**: `trade_evidence` wires the RUN's `gated_minute_loader` (shared, not copied; its private `_minute_loader` deleted), and the ruling's condition is ASSERTED -- store-backed tests show ZERO refusals in the committed chunk-8 window and the pack's 290/146/146, 53,750 shares, gross Rs 12,665.05, costs Rs 14,600.00, net -Rs 1,934.95 unchanged to the paisa; the pack is NOT regenerated. **PART D, the punch list CLOSED**: R3 (the malformed-bar refusal asserted end to end through `walk_symbol` -- its exact mutant passed all 2,083 tests and now fails one), R9 all four (stable refusal-reason KEYS with the specifics in a new conditionally-serialized `detail`, so no ordinary row's bytes and no committed ledger digest move; the doubled "gate gate 2" wording fixed before ~210 rows froze it; the flag lookup guarded and a fifth case now failing LOUDLY at IMPORT; `FLAG_OUT_OF_SESSION_DROPPED` given its counter), R4 (the owed re-gate documented in full as runbook step 7 with both mandatory flags, and the staleness banner's direction corrected from "understated" to OVERSTATED for the bump actually in force). **PART E**: CONTEXT **v1.7** applied as supplied and committed ALONE -- five edits, no sixth (corrected 05-Aug-2026 per REVIEW_9B_FIX4 F4: the phrase "four edits, no fifth" was carried forward from v1.6 without re-counting; `d66d23e` has five hunks and enumerates five edits, and its commit message stands as history). `SPEC_VERSION` v1.6 -> v1.7 (B268); `GATE_DEFINITION` deliberately NOT bumped, since no gate definition moved. **2111/0/0 from clean** (was 2094), +17 net, no test weakened, no fixture byte moved, three mutants verified with the engine restored sha256-identical each time. **ZERO store writes.** B263-B270. **QC RE-REVIEW OWED** (fresh session, both personas) over `6b6baaa..HEAD`.
# superseded 2026-08-03 by FIX-4: fixes reviewed-PASS (Q-21 + Q-21(b) + Q-21(a)) -- RELAUNCH AUTHORISATION WITHHELD pending Q-22  # QC REVIEW 2026-08-03 over `20b45c9..a707615`, BOTH personas, all 8 directed checks plus both checklists in full; B244-B262 all judged (18 APPROVED, B255 APPROVED-WITH-CHALLENGE). **THE FIX ARC PASSES**: all three architect rulings executed faithfully and narrowly; every published figure re-derived from the machine (JUBLFOOD 2023-03-03 09:15 O 44210 H 44440 L 44295 C 44295 V 12909 with the bhavcopy open matching to the paisa; 47 flips on ONE date; 409,252 -> 409,205; 93.9425% -> 93.9317%; Q-21(b) radius 210 = 121/88/1; 11 moved-POC days; the sealed side reproducing the store's ledger on a 10-symbol spot-check with zero mismatches); the crash pair verified end-to-end on the READ-ONLY store to refuse ONCE under gate 2's own name via case 4, the row moving counters rather than duplicating (re-run against 3a15a38 to prove it); all counted-once and crash-restored mutants caught; NO look-ahead (gate_day reads only D-1's own minutes and D-1's own raw daily row); the battery adds ~75s to the whole run; fixtures frozen, pure engines and trade_evidence byte-identical, the two ruling-driven test flips audited line by line and both are STRENGTHENINGS, zero skips, no AI attribution, no secrets, `HEAD == origin/main`. **2,094 passed / 0 failed / 0 skipped** from clean (2,083 + 11 kept probes, each mutation-verified). **RELAUNCH WITHHELD ON ONE PRE-EXISTING DEFECT, NOT ON THE ARC** (REVIEW_9B_FIXES R1 -> QUESTIONS.md Q-22): the Rule-3 first-break scan consumes OUT-OF-SESSION bars, which CONTEXT 4.6's Q-17 law says are dropped at the CANDLE level -- measured on the store, a 15:44 bar on 2021-02-24 (the NSE outage day, NOT one of the 8 removed non-standard sessions) FLIPS the bias on GODREJCP and LAURUSLABS, both settled, from carried BULLISH to bearish. The arc NARROWED this (2020-12-08's pre-open garbage now fails gate 1) and did not cause it; the remedy is one line and one architect ruling. **ALSO RECORDED**: the lake's malformed population is 51, not 48 or 50 (APLAPOLLO 2017-10-05 15:28, a close-outside bar the flip-derived count cannot see -- no figure moves); the owed `--regate` needs `--universe-snapshot docs/recovery/sealed_universe_210.json --report-path <scratch>` or it re-gates 208 of 210 rows and overwrites the committed sealed report (R4) -- but the relaunch does NOT depend on the re-gate: `load_residual_register` reads only status/gate1p fields, none of which the completion moves. ZERO store writes this session. tag chunk9b-fixes-pass
# superseded 2026-08-03 by FIX-3: Q-21 + Q-21(b) fixes built (unreviewed) -- RELAUNCH owed  # FIX-2 SESSION 2026-08-03 over `74fd2fc..HEAD`. **PART A**: the architect's **Q-21(b) ruling recorded VERBATIM** in QUESTIONS.md with the execution beneath it -- *"a day's minutes may serve a Rule-3 first-break scan ONLY if that day passes the CONTEXT 4.5/4.6 gate battery ... a battery-failing D-1 makes the bias UNRESOLVABLE: fourth counted case (minutes-ungated), same machinery as Q-21's third, detail naming which gate failed"*. **PART B EXECUTED** at the same narrow boundary Q-21 used: `backtest.gated_minute_loader` runs the battery BEFORE a single `bias.Candle` is built and raises `bias_engine.UngatedMinuteDay` carrying the refusing gate and ITS OWN reason (`DayGates.refusal_detail`, new); `BiasEngine._bias_for` catches it exactly as it catches `MalformedMinuteBar` (both are now `UnusableMinuteEvidence`), returns rule `minutes-ungated`, `tradeable=False`, and **leaves the carried bias UNCHANGED**; `walk_symbol` emits ONE refused row under `REASON_BIAS_UNRESOLVED` flagged `FLAG_UNGATED_MINUTE_DAY`, and the manifest gains a rare-shape counter DERIVED from that flag (resume-identical). The verdict is memoised per `(symbol, D-1)` -- the ruling's "computes (or reuses)" -- never persisted, because CONTEXT 4.6 forbids a per-day exclusion file. Rules 1 and 2 read no minutes and are provably untouched. **THE SEALED BATTERY IS NOT TOUCHED**: `quality_gates.py` is byte-identical to its committed blob, as are `bias`, `poc`, `signals`, `simulate`, `aggregate`. **BLAST RADIUS, MEASURED read-only over the sealed era** (`docs/evidence/chunk9b_q21b_blast_radius.{py,md}`, span 2016-10-03..2026-07-30, 204 settled symbols, Rule-3 membership decided by the REAL `bias.evaluate_pair` with a probe provider): of **7,031** trade days that reach the Rule-3 1-minute scan, **209 have a battery-failing D-1 and become counted refusals** (2.97%) across **59 symbols** -- **gate 1: 121, gate 1P: 88, gate 2: 0** -- **189 of 209 (90%) with a D-1 in 2016-2021** (IOC 33, GRASIM 16, TATASTEEL 15, OIL 14, TORNTPHARM 11, NMDC 10). The figure is stamped on every run manifest as a disclosure that names its own limits. **THE PREVIOUS SESSION'S HANDOVER IS CORRECTED**: Q-21 cost zero completed symbols a refusal, but Q-21(b) costs **27 of the 103** (ADANIPOWER..IOC), so the crashed run's shards cross-check **76** symbols, not 103; the relaunch re-walks everything regardless (the spec digest covers the code SHA, which moved). **NO COLLATERAL DAMAGE**: the committed Q-21 evidence walk re-runs identically under the gated loader (41 symbol-days, 1 flagged, same bar, counters 1 and 0), and no refusal falls inside the chunk-9A pilot window, so 290/146/88/56 is unmoved. **PART C -- Q-21(a) MEASURED, NOTHING DECIDED** (`docs/evidence/chunk9b_q21a_poc_impact.{py,md}`, the architect's deferral): for all **48** malformed-bar days as TRADE days the CONTEXT 3.3 POC is computed twice, bar as stored vs low clamped to `min(low, open)` -- the clamp changes the bar on **29**, **moves the POC on 11**, **changes the row count on 8** (RECLTD 31->22, POLYCAB 27->20, MAZDOCK 26->24, RBLBANK 20->25, SUPREMEIND 25->24, BANKBARODA 22->23, BANKINDIA 24->25, UNIONBANK 24->25), and one day (JIOFIN 2023-08-21, high<low) has no computable profile either way. **2073/0/0 from clean** (was 2056: +12 runner, +4 engine, +1 disclosure), no test weakened, no fixture byte moved, no CONTEXT/plan.md edit, **zero store writes**. **OPERATOR STILL OWES**: rename (never delete) `<data_root>/backtests/chunk9b_full` to `chunk9b_full_crashed_0803`, then run step 6. **QC REVIEW OWED** (fresh session) over `20b45c9..HEAD`, now covering both fixes.
# superseded 2026-08-03 by FIX-2: run attempted 03-Aug-2026 and CRASHED at symbol 104/204; Q-21 fix built (unreviewed) -- RELAUNCH owed  # RUN-CRASH FIX SESSION 2026-08-03 over `20b45c9..HEAD`. The authorized full-history run died inside **JUBLFOOD (symbol 104 of 204)** on a vendor-corrupt 1-minute bar -- **2023-03-03 09:15, O 44210 H 44440 L 44295 C 44295 V 12909**, an open 85 paise BELOW the low -- read by a genuine CONTEXT 3.2 Rule-3 outside-bar scan (D 2023-03-06, pair D-1 2023-03-03 / D-2 2023-03-02). `bias.Candle`'s CONTEXT 7-E11 invariant refused it and the uncaught `BiasError` killed the run at 103 completed shards (verified from `progress.json` and the 103 whole shards on disk, which are the settled universe's own leading prefix -- the ruling's "symbol 103" is the last one FINISHED). **PART A**: the architect's **Q-21 ruling recorded VERBATIM** in QUESTIONS.md with the execution beneath it. **PART B EXECUTED** at the narrowest correct scope, the RUN's Rule-3 loader: `backtest.minute_loader` re-raises `BiasError` as `bias_engine.MalformedMinuteBar` carrying the RAW bar (the only site that still holds it); `BiasEngine._bias_for` turns that into a `DailyBias` with rule `minutes-malformed`, `tradeable=False` and the bar in `unresolved_detail`, leaving the carried bias UNCHANGED; `walk_symbol` emits ONE refused row under `REASON_BIAS_UNRESOLVED` (its third case) with the bar's stamp and OHLCV in the reason, flagged `FLAG_MALFORMED_MINUTE_BAR`, and the manifest gains a rare-shape counter DERIVED from that flag. `bias.py` untouched; no bar dropped (a scan minus the corrupt bar flips this very day BULLISH -> BEARISH, proved on the fixture). **PART C**: the progress reporter never fed -- the runner walks THEN reports, so a symbol's cost is the interval ENDING at its `progress` call, while the reporter stamped the clock at the END of `progress` and closed it in `after_symbol` two prints later; on this box `time.monotonic` resolves to 15.625 ms (`GetTickCount64`) so the accumulator was EXACTLY 0.0 and the `<= 0` guard printed "no symbol walked yet" over 101 walked symbols. Interval now spans the walk, committed only when `after_symbol` confirms the symbol was walked (resumed shards still excluded); clock moved to `perf_counter` (display only, never a ledger). All 6 reporter tests FAIL on the pre-fix reporter. **MEASURED FROM THE STORE** (`docs/evidence/chunk9b_q21_malformed_bar.{py,md}`, offline, read-only, ZERO store writes): the lake carries **48 malformed bars, not one** -- 47 on the SAME minute **2023-03-03 09:15** across 47 symbols plus **JIOFIN 2023-08-21 09:15** (H<L, O and C both outside) -- and **exactly ONE is read by a Rule-3 scan**: JUBLFOOD 2023-03-06. **19 of the 48 affected symbols had already COMPLETED**, so the ruling's premise ("they would have crashed") is wrong as stated -- they never READ those dates -- while its CONCLUSION holds: **zero completed symbols gain a refusal**, every finished shard reproduces. **TWO THINGS FOR THE ARCHITECT'S EYE, recorded not decided**: JUBLFOOD 2023-03-03 **PASSES all three gates** (gate 2 enumerates only `high<low` and a CLOSE outside `[low,high]`; CONTEXT 4.5 deliberately does NOT test the open), so 46 of the 48 bars are invisible to it and the Q-21 refusal is the ONLY backstop; and a day's gate verdicts never gated its use as bias EVIDENCE at all -- `BiasEngine` reads D-1's minutes straight from the store, consulting no gate. **2056/0 from clean** (was 2044 at `q18-reseal-pass`: +9 Q-21, +3 net reporter), no test weakened, no fixture byte moved, no CONTEXT/plan.md edit. **OPERATOR OWES ONE ACTION BEFORE RELAUNCH**: rename (never delete -- layer 2) `<data_root>/backtests/chunk9b_full` to `chunk9b_full_crashed_0803`, retaining its 103 shards as a cross-check. **QC REVIEW OWED** (fresh session) over `20b45c9..HEAD`.
# superseded 2026-08-03 by the run-crash fix: staged, reviewed-PASS -- RUN AUTHORIZED  # Q-18 RE-SEAL REVIEW (QC, both personas, fresh session) over the FULL unreviewed arc `6b0436d..f4c019a` -- 41 commits, five sessions (9B prep incl. the Q-17 fix and the run layer; Q-18 recovery, verification and triage; CONTEXT v1.5; RESUME-1 law/migration/guard; RESUME-2 pin/pilot/projection). **1927/0/0 from clean** (the build's claim reproduced first, no skips, no xfails), **2044/0/0** with 117 kept reviewer probes. **VERDICT PASS -- THE FULL-HISTORY RUN IS AUTHORIZED**, 12 findings, none blocking. Everything re-derived importing nothing from src/acumen for the arithmetic. **THE PIN**: CONTEXT 3.3 reimplemented from the spec text, ticks read RAW from the pinned master -- all 25 frozen F7 `poc_prorata` values reproduce EXACTLY, and so do HDFCBANK 2026-06-10 (739.80 / 182 / 8 / 23 rows), RELIANCE 2026-05-05 (1465.85 / 224 / 9 / 25) and the chunk-6 gate day BHARTIARTL **2026-07-17 (1914.60 in 26 rows)**; ICICIBANK 2026-05-21 reproduces 1245.70 / 82 / 4 with **ceil(82/4)=21 rows**, confirming the architect's transcription-slip ruling. The pin's sha256 `ce198be4...b29f5ab` hashed independently and matching in four places; six malformed pins REFUSED; my own broader AST sweep finds the master glob at ONE site in `src/acumen` (`instrument_master.cached_masters`) and none on the run path. **THE PILOT CAME BACK BYTE-IDENTICAL**: ledger sha256 `c3363f6f...c1e318` on THREE run directories = the pre-incident published digest, with every headline figure re-derived from those 290 rows (290/146/88/56, 53,750 shares, gross Rs 12,665.05, costs Rs 14,600.00, net **-Rs 1,934.95**, exits 86/36/24, pots Rs 96,489.60 / -Rs 98,424.55, **PF 0.9803**, Tukey fences [-Rs 4,243.30, Rs 4,141.50], **0 outliers of 146**). The five moved pins are DISCRIMINATING: run against the pre-regeneration artefact, exactly those five FAIL and the other thirteen pack-claim pins pass. **RESUMABILITY AT FOUR KILL POINTS OF THE REVIEWER'S OWN**, incl. a MID-SYMBOL crash the builder never ran -- byte-identical ledgers, 0 duplicate keys, no partial shard -- and all three refusal shapes fire (moved code SHA, moved `master_file`, moved `master_sha256`). **THE Q-17 DROP: a 10-MUTANT MATRIX, 8 caught including the DATE-LEVEL mutant**; the 2 survivors PROVED EQUIVALENT (the POC window opens at 09:15; E10's fallback is unreachable while a profile exists) and both premises now PINNED by kept probes. Flagged-not-fatal confirmed on the smoke's 3 days incl. RELIANCE 2017-04-28, the bar that killed the pre-fix run. **THE RECOVERY, SPOT-RE-DERIVED**: T1 re-run on TEN symbols of the reviewer's choosing -- **10/10** boundaries inside the sealed fetch window; T3 hand-recomputed on APLAPOLLO (x10.0 / x10.0 / x2.0 on ALL FOUR price fields, gate 1 -90.16%/-90.15%/-50.05%, gate 1P FAIL) and POWERGRID (4/3), each with a CLEAN control day outside the block; T5's BSE split verified on two of the five flipped days -- gate 1 **PASS** at 0.18%/0.15% while gate 1P **FAILS** at 3.0x price, so "+84 is not coverage" stands; and `q18_reconciliation.md` regenerates **BYTE-IDENTICALLY** (`0f99291b...3b3995`) from a throwaway COPY with **ZERO unexplained** (414/5/3/0). **Q-19**: 23/23, the boundary swept as a PARTITION from -365 to +365 (day 7 exact stays PENDING, day 8 seals), across a leap day and a year end, an ERROR never sealing at any age, and the 2026-07-31 phantom shape irreproducible -- the calendar REFUSES rather than gaining a holiday. **v1.5 + CLAUDE.md**: `b895739` touches CONTEXT.md alone with exactly three edits (header, §4.6, §10 row) and `b3d6760` touches CLAUDE.md alone (+9 lines = the three layers); the loader attacked with ELEVEN malformed configs incl. a `..` round trip back into the repo and the repo root itself -- all refused; zero literal store paths in `src/` or `scripts/` and **no sixth caller** of the retired path key. **THE Q-17 LAKE SCAN REPRODUCES EXACTLY** -- 3,100 symbol-days / 526 dates / 8 Muhurat / 518 mixed / 1,747 covered, and all five market-wide dates with both counts incl. **2021-02-24 at 168 of 169**. **B202-B243 ALL JUDGED, ALL APPROVED.** **12 findings: 1 MEDIUM + 5 LOW + 6 INFO.** The MEDIUM is **Q1 -- the published throughput does not reproduce**: an independent re-run of the identical smoke (RELIANCE + MARUTI, same span, same HEAD, same stores, same machine, same day, idle box) measured **13.59 symbol-days/s against the published 25.56**, putting the full run near **10h 50m** rather than 6h 04m. Not blocking -- the evidence's own §4 discloses exactly this risk, the STOP threshold is 36 hours and resume is free -- but the 6h 04m headline is a FLOOR, not a duration. The LOWs: `walked` counts three non-trading Muhurat dates per symbol that step 6 prints without explaining (the 204x2,428 projection basis is nonetheless RIGHT); T3's word "EXACT" overstates two of six hand-verified days (the reciprocal signature, which is what the verdict rests on, holds); the Q-17 equivalence premises were unpinned (CLOSED by kept probes); commit `f4c019a` touches `tests/` without its `(unreviewed)` suffix; and **B214-B219 are used TWICE inside this arc** for six different decisions. No CONTEXT deviation, no look-ahead, no test weakened, no fixture byte changed, no secrets, no AI attribution, and **zero store writes by this review**. docs/reviews/REVIEW_9B_PRESEAL.md · tag q18-reseal-pass
# superseded 2026-08-02 by the re-seal review: resume-2 done -- staged for re-seal review (unreviewed)  # RESUME-2 session 2026-08-02 over `8e67081..HEAD`. **1927/0 from clean, ZERO skipped** -- the 7 skips are GONE (they awaited the chunk-9A pilot ledger the incident destroyed; the pilot re-run restored it and all 19 REVIEW_9A_2 probes RUN). Collection 1900 -> 1927; test functions 1,351 -> 1,370, no file losing one. **PART A: the Q-20 RULING recorded VERBATIM** with its execution beneath, plus the architect's APPROVALS of B220, B221 and B232 recorded beside the questions they answer. **PART B: Q-20 EXECUTED.** The pin is a REQUIRED `config.yaml` key (`instrument_master: OpenAPIScripMaster_2026-07-31.json`, sha256 `ce198be4...b29f5ab`), validated as a bare filename so it cannot resolve outside the cache; `backtest.latest_cached_master` is DELETED and replaced by `pinned_master`, with newest-by-filename moved off the run path into `instrument_master.newest_cached_master` and an ast tripwire asserting that is the only site; the manifest records the pin by filename AND sha256, closing Q-20's own complaint that a finished ledger could not be traced back to its ticks; both fields sit inside the spec digest so **a resume under any other master REFUSES** exactly as a moved code SHA does, tested on BOTH shapes (pin re-pointed, file overwritten in place); `build_runner` refuses a master that is not the configured pin. **CALIBRATION RE-VERIFIED UNDER THE PIN: 38 of 39 figures reproduce EXACTLY** -- every F7 tick (Q-2's frozen fixture) and every sealed-walk tick, all 25 frozen `poc_prorata` values recomputed with the PIN's ticks, the chunk-6 gate day (BHARTIARTL 2026-07-17: POC 1914.60 in 26 rows), HDFCBANK 2026-06-10 (739.80) and RELIANCE 2026-05-05 (1465.85) complete -- so the card's STOP condition does not fire. The 39th is NOT a tick move: REVIEW_7's ICICIBANK 2026-05-21 walk publishes "23 rows" where CONTEXT 3.3 gives ceil(82/4) = **21**, and the day recomputes IDENTICALLY under the other cached snapshot; a transcription slip (the HDFCBANK walk above it is genuinely 23), and no POC, entry, stop, target or money figure depends on a printed row count. **PART C: THE PILOT LEDGER CAME BACK BYTE-IDENTICAL** -- sha256 `c3363f6f...c1e318`, the same bytes REVIEW_9A verified at four kill points before the Q-18 incident destroyed it. 290 walked / 146 entered / 88 armed-no-cross / 56 never-armed, 53,750 shares, gross Rs 12,665.05, costs Rs 14,600.00, **net -Rs 1,934.95**, exits 86/36/24: the directed continuity check passes on "no byte moved", not merely "the figures agree". Pack regenerated, byte-identical across two consecutive runs, 20 invariants PASS. The two annotated pins (Q1, Q2) are FLIPPED to assert their rules; three further pack-claim pins were RE-POINTED at the regenerated artefact (IOC 41.9% -> **42.0%**, since B232 made the caveat computed and the frozen figure was WRONG not stale; "the only before-costs figures in this pack" -> "in section 7"; the outlier zero-branch now printing all four quantities per the GO ruling's condition (3)). One INVARIANT was itself defective and is fixed: "the manifest's residual caveat quotes its own register entries" reconstructed from `per_symbol` -- the universe the run WALKED -- so it could never pass on a five-symbol pilot that contains neither B149 symbol; the manifest now carries `caveat_basis`. **ONE NUMBER MOVED and it is not a pilot figure**: the universe-wide bias-materiality count reads 45 where the sealed era read 47 (population identical: 204 symbols, 112 events, 107 pairs, same first-15 table). The pin is refuted structurally -- the scan takes no master, no tick, no row size, now asserted by test -- leaving the rebuilt daily store, its derived calendar and the minute lake, i.e. exactly what CONTEXT 4.6 v1.5 re-sealed. The two individual pairs CANNOT be named: the sealed run published only its first 15 and the store that produced the rest is gone. **PART D: MEASURED 25.56 symbol-days/second** (4,856 walked in 3m10s over the FULL era, RELIANCE 95s vs MARUTI 94s), projecting **6h 04m** for the full run -- walk 495,312/25.558 = 5h23m, plus a 39m session scan and a 2m CA-table term, both measured separately because they scale with SYMBOLS and the smoke saw 2 of 204. Far under the 36-hour STOP threshold. Q-17's machinery earned its keep again: 3 symbol-days carried an out-of-session stamp, flagged rather than killing the run. **PART E:** preflight **GO 10 of 10** (204 settled symbols, span **2016-10-03 -> 2026-07-30** -- step 1's re-ingest cleared the 178-day store lag, so the oracle clamp now costs ONE day), `chunk9b_out_of_session.md` written at last (3,100 symbol-days, 526 dates, 8 Muhurat), and the runbook gains **step 6**: the exact command, the four-item pre-run checklist and both manifest stamps. **THE COMMAND IS STAGED, NOT HANDED OVER.** Q-17 gains a measured addendum: its open market-wide-dates reading has FIVE candidates, not three -- 2020-12-08 (143/167) and **2021-02-24 (168 of 169)** are new. No CONTEXT/plan.md edit, no fixture byte moved, no engine module touched, no test weakened, no secrets, no AI attribution.
# superseded 2026-08-02 by RESUME-2: resume-1 done -- law + migration + guard (unreviewed)  # RESUME-1 session 2026-08-02 over `601f3bf..HEAD` (7 commits). **1893/0 from clean, 7 skipped**; collection 1863 -> 1900, test functions 1,331 -> 1,351, no file losing one. **PART A: CONTEXT v1.5 IS LAW**, architect-authored, committed ALONE, exactly three edits (header 1.4->1.5 with its date; section 4.6 REPLACED in full with the re-sealed era -- 435,641 stored / 409,252 passing = 93.9425%, 204 settled / 6 quarantined with APLAPOLLO in and NESTLEIND out, the four divergence classes incl. the BSE volume-only caveat, Q-17 and Q-19 made law, the E5 rebuild-universe clarification; section 10's new top row). **PART B: the three Q-18 data-safety layers** added to CLAUDE.md, architect-authored, committed alone. **PART C: THE STORES NOW LIVE OUTSIDE THE REPO** at `C:/Users/chinm/acumen-data` (+ `/cache`) -- moved by the operator, every path config-routed through `paths.data_root`/`cache_root`, the old in-repo `data/` and `cache/` GONE, `.gitignore` keeping both as a belt with the rule written beside them. Layer 1 is STRUCTURAL: the loader REFUSES a store root that is missing, relative or inside the repo tree. Layer 3's freshness stamps print both roots' last-changed times and never touch the manifest. VERIFIED FROM THE MACHINE, offline and read-only, **8 of 8 PASS** (`docs/evidence/chunk9b_store_migration.{py,md}`): 21,837 files / 2.82 GiB; daily ledger 6,610 file-present / error 0 / never-attempted 0 / pending 0; minute lake **435,641 stored symbol-days** re-counted from parquet; register 204/6; CA day-cache 41,372 rows with 8 network entry points patched to raise and none escaping; instrument master resolving token AND tick. **PART D: THE Q-19 GUARD** -- a confirmed-404 seals only past 7 CALENDAR days; younger 404s record a fourth persisted outcome `pending` and are retried; an ERROR is still never a holiday; the exact 2026-07-31 phantom shape is pinned as a regression; the runbook records that the belt now has structure behind it. **SPEC_VERSION moved to v1.5** so a ledger names the law it ran under, and CONTEXT 4.6 (v1.5)'s residual caveat is now DERIVED from the register's own current figures -- the frozen string claimed IOC 41.9% where the rebuilt register says **42.0%**, i.e. it was wrong, not stale. **NEW Q-20 (class A, OPEN, STOP)**: the vendor's instrument master gives 11 of the sealed 210 -- ALL SETTLED, all walked -- a DIFFERENT tick_size two days apart, one NSE-EQ row each, so CONTEXT 3.3's row grid and every POC on those symbols depends on which snapshot the run reads; the manifest neither pins NOR records it. Nothing decided, four options to the architect; blocks RESUME-2's tick input only. This session also audited its own diff adversarially (21 candidates, 16 refuted, 5 confirmed and fixed) -- diligence, NOT the review, which still runs FRESH. No fixture byte changed, no engine behaviour changed, no test weakened, no secrets, no AI attribution.
# superseded 2026-08-02 by RESUME-1: Q-18 rebuild RECONCILED -- zero unexplained; awaiting CONTEXT 4.6 v1.5 (unreviewed)  # VERIFICATION + TRIAGE session 2026-08-02 over `8f96c78..HEAD`. **1856/0 from clean, 7 skipped** (collection 1838 -> 1863 = exactly the +25 added, all to test_recovery_reconcile.py; skips 8 -> 7 because the minute-store probes RUN again, the 7 left await `data/backtests/`). **PART 0 -- the operator's five-step runbook CERTIFIED FROM THE MACHINE, 18/18** under CLAUDE.md's new architect rule (*operator-executed runbooks are relayed, not certified*): ledger 6,610 file-present = sealed 6,606 + Jul 27/28/29/30 with error 0 / never-attempted 0 and the two settled outcomes partitioning all 9,708 dates; 2026-07-31 ABSENT (no Q-19 phantom holiday); 32 weekend sessions all excluded; four TCS witness rows matched to the paisa against values committed elsewhere in this repo; CA cache 41,371 rows offline with every network entry point monkeypatched to raise and **0 requests fired**; master 2,433 NSE-EQ with all 210 SEALED symbols resolving token AND tick; no temp or truncated artefact under `data/`; the independent leg agreeing on 432,512 stored symbol-days; and the reconciler **AS COMMITTED AT HEAD**, re-run from a throwaway COPY of `src/` (never a junction), reproducing the operator's `q18_reconciliation.md` **byte for byte**. **PART B -- the architect's T1-T5 executed.** T1 sealed-fetch-horizon: **210/210** boundaries land inside the sealed fetch window, histogram 168 at B=2026-07-27 / 37 at 07-24 / 5 at 07-28 -- the sealed store's per-symbol horizon really was earlier than its report label; the gate leg is a two-sided interval bound (B215) attributing the move to the window, and it discriminates exactly: 204 attributable, the 6 that are not are precisely T3's four regressions and T5's two improvements. T2 emptied `new-CA-explained` to **0** -- all 85 candidate ex-dates are FUTURE (2026-08-03 on). T3 forensics re-ran CONTEXT 4.5's own battery over all four regressions and reproduced the ruling's deltas EXACTLY (-467 / -60 / -23 / -21); each is one contiguous era-keyed block whose 1-minute fold sits at an EXACT corporate-action factor from the raw bhavcopy [ERRATUM 05-Aug-2026, chunk-9B REPORT session, REVIEW_9B_PRESEAL Q3: **"EXACT" is withdrawn.** It holds on POWERGRID 2021-07-29 (4/3 on the high) but not on 2021-08-16 (1.333424) or 2021-09-06 (1.333428) -- a fold's extremes are the max/min of per-minute prices ALREADY rounded to the paisa, so exact equality with a rational factor cannot be expected of them and is not what the verdict rests on. The verdict rests on the RECIPROCAL price x volume signature, which holds at 0.999794..0.999979 on those three days and 0.984..0.999 on APLAPOLLO's larger factors. No verdict, class or number moves; the same erratum is recorded in `docs/recovery/q18_t3_forensics.py`, and the generated pack never carried the word] (APLAPOLLO x10.0 then x2.0, GAIL 0.28125 = 0.75x0.75x0.5, POWERGRID 4/3, LODHA x2.0) with the volume reciprocal conserved to ~1.0000 and every hand-verified day carrying a SOUND raw row -> all four `vendor-snapshot-drift`, none escalated. **APLAPOLLO's quarantine STANDS** -- explained, not cleared, gate 1 is genuinely 77.9%. T4 re-fetched EXIDEIND (2,434d, 2,419/2,433) and NUVAMA (695d, 684/694) on the SEALED 210-symbol universe, both settled, both showing the +4/+4/+4 horizon shape; the regenerated report adds exactly those two and changes **zero** pre-existing rows. T5 hand-verified five flipped days each: NESTLEIND 998+4=+1,002 at price ratio 1.0 on both gates -> **repair BELIEVED**; **BSE +84 is VOLUME-ONLY** -- 80+4=84 arithmetic closes and gate 1 passes, but all five days FAIL gate 1P at exactly **3.0x** the exchange's own prices (one 2:1 bonus un-applied, price and volume in DIFFERENT domains), so usable days moved 2,115 -> 2,117 and **no coverage is recovered**. **VERDICT: ZERO UNEXPLAINED** -- 414 sealed-fetch-horizon, 5 vendor-snapshot-drift, 3 vendor-repair-explained, 0 new-CA-explained, **0 unexplained**. FINAL ERA: **435,641 stored symbol-days, 409,252 pass all three gates = 93.9425%, 204 settled / 6 quarantined** (vs sealed 411,690/434,769 = 94.6917%, 204/6). The deltas are the amendment payload; **CONTEXT 4.6 v1.5 is the ARCHITECT's to write** and the chunk-9B RUN stays blocked until it exists. Operator owes a `data/`+`cache/` snapshot -- T4 changed the store. No CONTEXT/plan.md edit, no fixture byte moved, no engine module touched, no gate loosened, no secrets, no AI attribution.
# superseded 2026-07-31: prep built -- run NOT handed to operator, BLOCKED on Q-18 (unreviewed)  # PREP session 2026-07-31 over `6b0436d..HEAD`. **1794/0 from clean while the stores existed; 1783/0 + 8 skips after the incident below.** DONE: the architect's **GO RULING** (31-Jul-2026) recorded VERBATIM in QUESTIONS.md -- 9B proceeds without the trader's Q43/Q44 under four conditions -- and all **SIX REVIEW_9A_2 conditions CLOSED**, one commit each: Q1 (the third before-costs row labelled, the stated exception widened to three), Q2 ("given back" on both run-up rows), Q3 (the MFE/MAE pair and section 8's excursion invariant now state their BEFORE-COSTS basis beside the numbers, with the net-inside count printed), C1 ("the first TWO are decided by walk_symbol", probe FLIPPED), **C3 the architect's ruling** (offline serves the frozen day-cache at ANY age; missing still raises; five docstrings corrected; the pinned refusal probe FLIPPED and two build tests replaced by same-scope tests asserting the retirement) and C4 (the tripwire's literal-scan limit written and DEMONSTRATED beside the tripwire), plus the GO ruling's condition (3), Q4 (one outlier format on both branches). **C3 VERIFIED THE WAY IT WAS ASKED**: the pre-fix tree `6b0436d` plus ONLY the one-line cache change regenerated the committed pilot pack BYTE-IDENTICALLY on 2026-07-31, two days after the cache's `fetched_on`, sha256 `9e8a77c78644d0ffabb082987f2e4f5c07a0f0dbc19fdd7569d6b9d1b724df41`, **with no harness** -- the thing REVIEW_9A_2 could only do under one. **PART C BUILT**: `src/acumen/run_backtest.py` (a 10-check PREFLIGHT that refuses to emit the run command if any check fails; the universe read from the disclosed-residual register and the span measured from the minute store's own parquet files, neither typed; the CONTEXT 7-E2 session scan CACHED beside the ledger per decision B181, keyed on universe+span+code SHA; per-symbol progress with elapsed/rate/ETA; the GO ruling's disclosures stamped on the run manifest and on nothing else, so no chunk-9A digest moves) + `scripts/run_backtest.py` + `docs/evidence/chunk9b_preflight.py` + 43 new tests. The preflight PASSED before the incident: 204 settled symbols, span 2016-10-03 -> 2026-07-24 (clamped to the raw daily oracle and DISCLOSED), 2,421 trading days. **THE SMOKE EARNED ITS KEEP AND THEN THE SESSION BROKE THE MACHINE.** The smoke (real runner, full era, RELIANCE + MARUTI) died on RELIANCE 2017-04-28: one vendor bar stamped 09:14 that gate 2 deliberately ADMITS ("CONTEXT 7-E2 excludes an out-of-session candle at the CANDLE level, not at the day level") and `aggregate_15min` REFUSES -- two reviewed layers disagreeing, and a full-history run dying on its first symbol. Raised as **Q-17**, MEASURED over the whole store first (3,099 of ~493,900 symbol-days across 526 dates; the competing date-level reading would have excluded ~105,000), and FIXED by applying gate 2's own written reading at the two consumption points, with the drop counted on the ledger row as a FLAG so no clean row's bytes move. **THEN: `git worktree remove --force` over a worktree carrying NTFS junctions to `data/` and `cache/` followed them and DESTROYED both trees -- the whole chunk-5B minute lake, the daily store, the CA day-cache, the residual register, the adjustment maps and every chunk-9A run ledger. Unrecoverable: no Recycle Bin entry, no shadow copy, no other copy on the machine.** The tracked repo is UNDAMAGED (git fsck clean, all 44 fixture files intact, `docs/evidence/chunk9a_pilot.md` NOT overwritten). **QUESTIONS.md Q-18 (class A, OPEN, STOP)** records the incident in full and puts the restore-or-rebuild decision to the architect, because CONTEXT 4.6 seals that era with exact counts and a re-backfill may not reproduce them. **STILL OWED, all blocked on Q-18:** the pilot-pack regeneration and the Q1/Q2 probe flips (renderers already fixed; the two pins are green, stale and now annotated with exactly why), the smoke re-run and therefore the MEASURED throughput and the projected full-run duration, `docs/evidence/chunk9b_preflight.md`, `docs/evidence/chunk9b_out_of_session.md`, and THE RUN ITSELF -- not handed to the operator, because the preflight refuses it and the preflight is right.
# superseded 2026-07-31: todo  # the full-history run + report. BLOCKED on the trader's Round-4 answers (Q43 capital figure, Q44 confirmation) per the architect's split. -- LIFTED for the RUN by the architect's GO ruling of 31-Jul-2026 (both questions stay open with the trader); now blocked instead on Q-18, the data-store loss.
chunk 10: ABSORBED into chunk 9B  # ARCHITECT'S PLAN AMENDMENT (class C, 06-Aug-2026), recorded VERBATIM in QUESTIONS.md by the chunk-12 VALIDATION-PACK session: *"chunk 10's report scope was completed inside chunk 9B"*. plan.md's chunk-10 card is the CONTEXT 7-E13 metric layer, and `src/acumen/portfolio.py` implements the complete E13 list -- built in chunk 9A, reviewed TWICE (REVIEW_9A, then REVIEW_9A_2 over the Q-16 fix, where the reviewer recomputed the whole set by hand importing nothing from src/acumen) and consumed by `docs/reports/chunk9b_backtest_report.md`, whose own QC review (REVIEW_9B_REPORT) independently re-derived the headline, all ELEVEN year rows and all 204 per-symbol rows (1,632 cells, 0 disagreements) from the raw ledger. The chunk-10 card's own definition of done -- "the review session independently recomputes all of them by hand" -- is therefore met by REVIEW_9A_2 and REVIEW_9B_REPORT together. NO separate chunk-10 deliverable and NO separate chunk-10 review is owed. No metric was left uncomputed and none was re-derived by the amendment.
chunk 11: todo  # The 07-Aug-2026 architect's rulings (operator-confirmed, recorded VERBATIM in QUESTIONS.md by the chunk-13 build session) LIFT the 06-Aug hold: *"the operator rules the complete tool is the deliverable and the trader decides its use; build order re-sequenced 13 -> 14 -> 11 -> 15"*. The card is UNCHANGED -- the ruling rescopes nothing -- and every DEPENDENCY rule in plan.md section 3 still binds. Chunk 11 (report UI) now builds FOURTH. Its own dependency, chunk 10, was absorbed into 9B; DESIGN.md now exists and PART II's design intent binds it as it binds chunk 13's dashboard.
# superseded 2026-08-08 (the hold is LIFTED): **ARCHITECT'S RULING (06-Aug-2026)**, recorded VERBATIM in QUESTIONS.md by the chunk-12 QC REVIEW session: *"chunk 11 (web report UI) joins chunks 13-15 ON HOLD pending the trader's decision; its content scope is satisfied by the twice-reviewed static report for the chunk-12 gate. plan.md's chunk-12 dependency on 11 is met by that reading."* This CLOSES the class-C item the chunk-12 build session raised and left open (plan.md §3 lists chunk 12's dependencies as 9 AND 11; the 06-Aug plan amendments named chunk 10 and were silent on 11; that session did not read the silence either way). NOT cancelled and NOT failed: the card stands exactly as plan.md v1.1 writes it and the hold is a state, not a verdict. What is true on the machine, unchanged: chunk 11's card is the getdesign/DESIGN.md **web report UI** and none of it exists here -- no DESIGN.md, no served report, no UI code; what DOES exist is the static markdown report `docs/reports/chunk9b_backtest_report.md`, carrying the equity, drawdown, per-year, per-symbol and disclosure content that card lists, reviewed under REVIEW_9B_REPORT + REVIEW_9B_FINAL and prose-fixed since -- and that is what the ruling reads as discharging the content scope for the chunk-12 gate. No chunk-11 deliverable and no chunk-11 review is owed for that gate.
chunk 12: reviewed-PASS -- PACK CLEARED FOR THE TRADER  # RE-REVIEW of the REVIEW_12_2 FIX (QC, both personas, fresh session) over `adaedbc..d893500` -- 10 commits, `ad48ff1` audited as part of the span. **PART 0 (architect-directed RECORDING, own commits, before the review):** CONTEXT section 10's erratum row replaced by the supplied THREE-cell line `| 1.9e | 07-Aug-2026 | OPEN-6 aligned with the Round-4 flags retirement |`, committed ALONE (3415d75) -- **Q-27 answered, B317 DISCHARGED**; and the architect's three 07-Aug rulings recorded WORD FOR WORD in QUESTIONS.md (ef114fb) -- **Q-26 CLOSED** (the report stays FROZEN, the committed evidence file is the technical record and page 5's clause is its pointer, so B324 stands and no freeze exclusion is owed), **B318 RATIFIED**, and the new **OPERATOR RULE** (no session starts while another is alive -- terminal closed AND no surviving process, checked before every launch). **VERDICT PASS. THE BLOCK ON THE PACK IS LIFTED.** **PAGE 5 RE-DERIVED LINE BY LINE by an independent streamer importing nothing from src/acumen:** 495,312 - 1,632 = **493,680**; all ten rule rows reproduce to the day; **406,488 + 74,081 (73,841 + 210 + 30) + 13,111 = 493,680**, the sum closing exactly; the OVERLAP is **exactly 2** (`no-data` x `evaluated` = 2, `minutes-ungated` 0, `suppressed` 0) and both are **FORCEMOT 2024-02-14/15, bias bearish CARRIED, status evaluated, 360/368 minutes, neither traded**; the **13,113** population confirmed (and its by-rule split -- 9,033 + 2,139 + 1,858 + 60 + 23 -- is REVIEW_12_2's own, which summed to 13,113 while that review's prose said 13,111; the fix read the parts and its page is right). From the STORES: FORCEMOT's daily store holds **1,648 EQ rows, 2019-08-19..2026-07-30, exactly ONE gap over five days -- 2023-10-25 -> 2024-02-14, 112 calendar days**, and February 2024 resumes on the 14th, so the bias pairs of the 14th and the 15th are the only two that miss a daily candle. Every other sentence of the paragraph checked: all **1,632** ruleless rows carry `CONTEXT 7-E2 non-standard session` on 8 dates x 204 symbols (all Muhurat); **ZERO** evaluated rows carry a null bias, so *'a bias was settled'* is exactly true; 88,824 = 87,192 + 1,632. The clean partition B318 declined is real -- 406,486 + 74,081 + 13,113 -- and moves a four-page headline. **Q2:** `bias_engine.py:310` emits `no-data` from `store.daily(...)` on `pair.current`/`pair.previous`, so the new DAILY/bias-pair label names the right store, resolution and days where the old named none of the three. **Q3 recounted: 988 flats across 171 of 204 stocks, 61,406 of 188,345 positive = 32.60%**, and page 1's own 40 money-flats re-derive beside them. **C1:** the config diff touches COMMENTS ONLY -- both keys still valueless, both now `# null -- retired by trader, Round 4`, and **exactly one `pending` survives**, explaining itself as the frozen manifest's own wording (B305). **C4 PROVEN BOTH WAYS:** against `git archive adaedbc` both `python -m` invocations **exit 0 having written nothing**; at HEAD both **exit 1 naming the missing ledger path**. **C7:** all seven signs re-derived against their own rows (|avg| 0.000054..0.004975 rupees), no eighth row rounds away, no stock's total is exactly zero, `| 0.00 |` occurs zero times, no magnitude moved. **C8** pinned at all three cases. **C5's lesson HOLDS:** both flipped probes run against the COMMITTED pre-fix artefacts are RED and **each fails on its own page-text assertion**. **PAGE 6 recomputed in half paise: 2,068 = 850 long + 1,218 short, ZERO violations**, and the losing branch is driven at the rendered page. **TOKEN ACCOUNTING RE-DERIVED: pack departures = exactly {`87,192`}**, and all 34 arrivals classified with none unclassified -- 25 extra occurrences of already-published figures, 9 the fix's own new disclosures, every one of which was re-derived here from the ledger. **Pack, companion and points table BYTE-REPRODUCED once each from a clean clone, diffs EMPTY** (`9419ccb5...`, `14ce2cc0...`, `773cf2a1...`). **The report was NOT regenerated, by instruction, and is verified by HASH and blob identity: unchanged across the whole span, 87,756 B, `529a04a887d2b69a...`** (B325). **And the whole 204-row companion re-derived rather than sampled: 204 x 11 = 2,244 published figures, ZERO mismatches**, ranking order identical. **THE CONCURRENCY INCIDENT VERIFIED, NOT ACCEPTED:** over STATUS/PROGRESS/REVIEW_12_2, `ad48ff1..HEAD` removes **exactly ONE line** (the chunk-12 STATUS line), re-added under the supersession prefix with its 7,756 bytes **carried byte for byte** including `ad48ff1`'s own correction; PROGRESS.md loses **zero** lines and REVIEW_12_2.md **zero**. Both sessions behaved correctly and the process did not; the architect's OPERATOR RULE now closes it. **SUITE 2279/0/0 FROM CLEAN** (425.12s, fresh clone -- the claim to the test, no skips, no xfails); **2283/0/0** with 4 kept probes. AST audit **1,547 -> 1,556 = 9 new, 1 renamed, ZERO removed**, no test weakened; fixtures untouched; no engine module touched; no AI attribution; no secret in the span; `(unreviewed)` correct on every code commit; linear SHA chain; **ZERO store writes -- 22,186 files, digest `7ea13480266ebd04...` before and after everything**. **B317-B324 ALL APPROVED** (B317 discharged, B318 and B324 now ratified by the architect). **5 findings, none blocking.** The one worth the architect's eye is **M1**: three of the four page-text fixes are pinned at their HELPER and not at the rendered page, and Q3's is revertible at BOTH levels -- proved by mutation (revert Q2's label in the page: 104 green; delete Q3's sentence from both documents: 108 green; delete Q3's two `emit.add` blocks from the generator: 104 green; strip C7's seven signs: 108 green). That is the FOURTH occurrence of the pattern `personas/code_reviewer.md` item 10 exists for. **This review's 4 kept probes close it at the rendered output**, each proved RED against its own mutation on its own page-text assertion. Then **C1** (both `__main__` guards claim a subprocess test that does not exist), **C2** (a traceback where `run_backtest.main` returns 1), **Q1** (page 6's zero violations is guaranteed by CONTEXT 3.4's ARMED path -- a tripwire, not evidence) and **Q2** (page 5's overlap parenthetical reads as the hole's span) and **C3** (`d893500` says append-only of an in-place rewrite; nothing lost, no rule broken). docs/reviews/REVIEW_12C.md + tests/test_review12c_probes.py. tag chunk12-round4-pass. **THE PACK IS CLEARED FOR THE TRADER**; the chunk-12 gate itself closes on his confirmation of the rules on page 2 and his row count on day 3f, which is what the pack asks him for.
# superseded 2026-08-07 by REVIEW_12C (PASS): chunk 12: fixed -- REVIEW_12_2's Q1, Q2, Q3, C1 and C2-C8 all executed (unreviewed) -- RE-REVIEW OWED over `adaedbc..HEAD`; TRADER GATE STAYS OPEN  # FIX SESSION 2026-08-07 over REVIEW_12_2's FAIL. **NOT ONE PUBLISHED FIGURE MOVED AND NO RE-RUN WAS NEEDED**, exactly as the review predicted: a numeric-token diff of the whole pack against its pre-fix self shows **exactly ONE token LEFT -- `87,192`, the defective sentence's own figure** -- and every token that arrived is either a figure already published elsewhere in the pack or one of the new disclosures the fixes require. **THE REPORT BYTE-REPRODUCES AND IS UNCHANGED**: `python -m acumen.report_9b` ran 11:08:44 -> 12:45:11 (96 minutes) to a scratch path and the output is byte-IDENTICAL to the committed file -- sha256 `529a04a887d2b69a...`, 87,756 bytes, 899 lines, diff EMPTY. **This is the SECOND independent reproduction, not the discharge**: the REVIEW_12_2 session was still alive, finished its own regeneration and committed that correction itself in `ad48ff1` at 11:45, before these fix commits. This one ran through the `python -m` entry point finding C4 created rather than by importing `main()` by hand, and the two agree byte for byte. **Process deviation recorded, not judged:** plan.md section 2 says one session runs at a time and two ran; `ad48ff1` touched STATUS.md, PROGRESS.md and REVIEW_12_2.md, and nothing was clobbered -- `git diff ad48ff1..HEAD` over those three files removes exactly one line, the chunk-12 STATUS line, re-added under the standard supersession prefix with `ad48ff1`'s own correction carried through byte for byte. **Q1 (the FAIL):** page 5's reconciliation now closes. The defective *'87,192 more had a bias but were refused afterwards'* is GONE; the table splits three ways and prints its own sum -- **406,488 judged + 74,081 not judged (73,841 + 210 + 30, stated once) + 13,111 = 493,680** -- and every figure is COMPUTED (the not-judged rows are selected by the words the page itself prints, so the split and the sentence cannot drift). **The two buckets OVERLAP by 2 days and the page says so in its own clause**: a day with no daily candle for the bias pair is still tradeable when a bias is being carried, so those 2 days are counted as judged AND as a not-judged row, which is why the third figure reads 13,111 where the population it describes is 13,113. A reconciliation that closed by quietly absorbing that would have been the same defect in a new sentence. **Q2:** the 73,841-row label becomes *'no DAILY candle for the bias pair (the two days before) -- not judged'* (it is emitted on `store.daily`, not the minute lake, and on D-1/D-2, not the trade day), and the boundary case is the overlap clause above -- FORCEMOT 2024-02-14/15, at the near edge of a 112-calendar-day hole in that symbol's daily store, neither day traded. **Q3:** page 7 AND the 204-row companion now state **988 trades ended exactly level in points across 171 of the 204 stocks**, and page 7 names the denominator of its 32.60% out loud (61,406 of all 188,345, 988 of them level) exactly as REVIEW_12 Q5 made page 1 do for its 40. **C1 (the second FAIL): `config.yaml` IS NOW EDITED** -- both keys read `# null -- retired by trader, Round 4`, the block comment is retitled to the retirement and rewritten so no sentence says an answer is awaited, and the `initial_capital` clause loses *'the trader's pending Q43 answer'*. VALUES untouched, both still null. One `pending` remains ON PURPOSE and says why: it names the RUN's own frozen manifest (B305). Corrections appended (append-only) to QUESTIONS.md's EXECUTED list and here, acknowledging that the 06-Aug claim was premature. **C2:** `SPEC_VERSION` v1.8 -> **v1.9** with its two pinning tests. **C3:** CONTEXT **v1.9 erratum** committed ALONE (`adaedbc`), two edits: section 9's OPEN-6 row no longer resolves the question on machinery the trader retired, and section 10 carries the architect's row verbatim. **C4:** both generators gain a `__main__` guard, a `[project.scripts]` entry and a `confirm_written` read-back. PROVED both ways against `git archive f88d125`: pre-fix `python -m acumen.trader_pack --out ... --run <nonexistent>` exits **0** having written nothing and said nothing; post-fix it exits **1** naming the missing ledger. Accuracy note on the finding: the committed launchers `docs/validation/trader_pack.py` and `docs/reports/chunk9b_backtest_report.py` DID already work, so 'regenerable only by importing the module by hand' overstates it; the exit-0 defect and the missing script entries were real and are fixed. **C5:** the probe-claim wording corrected in `tests/test_review12_probes.py`'s own docstring -- 2 of the 5 failed on a substantive assertion, 3 died earlier on KeyError/AttributeError. **And the lesson is APPLIED, not only recorded**: both probes flipped today assert the PAGE before touching any new companion key, and were proved RED against the committed pre-fix artefacts on exactly those assertions (*'the defective sentence is gone from the document the trader receives'* / *'the sentence REVIEW_12_2 Q1 found to be wrong is GONE from the trader's page'*), then green after regeneration. **C6:** the 'no number moved' scope note added to the freeze test's own docstring -- it is true of METRICS, which is what the freeze covers. **C7:** the sign is now read off the value handed in, not the quantized one, so the 7 companion rows that printed a bare `0.00` beside a signed non-zero *Points* column now print `+0.00` / `-0.00`, each matching its own row's direction. No magnitude moved: the token diff shows 4 unsigned zeros out, 4 signed zeros in. **C8: PINNED, not fixed** (it is latent): a test asserts the two safe orderings and the unreachable `TypeError`, with CONTEXT 3.4's one-trade-per-stock-per-day named as the only reason it is unreachable. **PAGE 6 (architect-directed):** *'Your Round-4 gap rule was checked against every gap trade the run took -- 2,068 of them, 850 long and 1,218 short -- and the stop sat on the correct side of the POC in every single one.'* COMPUTED from a widened census (long `stop <= POC`, short `stop >= POC`, compared in HALF PAISE so an off-grid POC is never rounded), **with a losing branch that the tests drive** -- a verdict with only one reachable outcome asserts nothing. **ARTEFACTS, all four byte-reproduced by independent second runs, diffs EMPTY**: pack `9419ccb53eee6225...` (53,377 bytes, 651 lines), companion `14ce2cc02f4d1f5d...`, points table `773cf2a1d51618e6...` (24,017 bytes, 220 lines), report `529a04a887d2b69a...` UNCHANGED. **EVIDENCE**: every store-derived figure this session states is re-measured by `docs/evidence/chunk12_fix_review12_2_measurements.{py,json}` and all reproduce exactly (493,680 / 406,488 / 74,081 / 87,192 / 13,111 / overlap 2 / 2,068 = 850 + 1,218 with ZERO violations / 988 flats across 171 stocks). **STORES READ-ONLY** -- no session write; the ledger and both stores were opened for reading only. **TWO NON-BLOCKING ITEMS RAISED: Q-26** (the Q2 boundary DETAIL is on the trader's page but NOT in the technical report -- writing it there would move numeric tokens inside the frozen region of sections 1..9, force new freeze exclusions and contradict the same instruction's 'no number moves' plus the byte-reproduction; a report edit needs the architect's explicit direction) and **Q-27** (the supplied section-10 erratum row has two cells in a three-column table; written VERBATIM rather than reworded, since a session may not reword supplied text). B317-B324. **RE-REVIEW OWED** over `adaedbc..HEAD`; the trader gate still closes on his confirmation of the rules and the row count.
# superseded 2026-08-07 by the REVIEW_12_2 FIX session: chunk 12: reviewed-FAIL -- fix session owed (Q1, Q2, Q3, C1); TRADER GATE STAYS OPEN, pack NOT cleared to go to him  # QC RE-REVIEW (both personas, fresh session) over `453ba5e..6504fbf`, the ROUND-4 EXECUTION span. **PART 0 (architect-directed RECORDING, own commits, distinct from the review):** the trader's two Round-4 texts recorded VERBATIM in QUESTIONS.md (7b04079) -- both were already pure ASCII so no fold was needed, the `[flow diagram:]` label is marked as OURS, and **B304 is DISCHARGED**; **CONTEXT v1.9** committed ALONE (35b533e), three edits and no fourth (header; 3.5's Q40-d flags clause replaced by the supplied Round-4 supersession sentence; 10's row verbatim). Section 3.4 untouched for the third consecutive version. **VERDICT FAIL on two findings, neither in the engine, the ledger, the money or the points arithmetic -- no re-run is needed and not one published figure moves.** **Q1 (MEDIUM):** page 5's reconciliation, the one paragraph that exists to be checked by the trader, does NOT close -- 493,680 - 74,081 (its own three 'not judged' rows) - 87,192 = 332,407, against the 406,488 it states; **the error is exactly 74,081, the not-judged population subtracted twice**, because the 87,192 (`ruled - usable`) CONTAINS those rows rather than standing beside them, and 74,081 of them had no bias at all. The figure that genuinely 'had a bias but was refused afterwards' is **13,111** and is on no page. Aggravating: REVIEW_12's own flipped probe asserts the defective sentence verbatim, so it will stay green. **C1 (MEDIUM):** the architect's ruling says `config.yaml`'s two keys are 'labelled retired by trader, Round 4'; **config.yaml was never touched in the span** and still says *pending* in four places, while QUESTIONS.md's EXECUTED list AND STATUS.md both state the edit was made. Values are right (null, no flag computed anywhere) and every PUBLISHED output does carry the retirement wording, so no trader-facing document is wrong -- only the config and the record of itself. Corrected append-only in QUESTIONS.md; the edit is owed. **Also Q2 (MEDIUM):** `bias_rule == 'no-data'` (73,841 rows, the page's SECOND-LARGEST) is emitted when a **DAILY** candle is missing for D-1/D-2 (`bias_engine.py:310`, `_candle` reads `store.daily`), yet the page calls it *'no stored one-minute data for the stock that day -- not judged'* -- wrong cause, and **2 of the 73,841 were judged** (FORCEMOT 2024-02-14/15, bias bearish carried, 360/368 minutes, status evaluated), the boundary of a **112-calendar-day hole** in FORCEMOT's daily store (2023-10-25 -> 2024-02-14). **Q3 (MEDIUM):** page 7 never states its **988** flat trades across 171 of 204 stocks, though REVIEW_12 Q5 required page 1 to state its 40 -- 25x the population, unstated on the newer page. Plus C2-C8 (SPEC_VERSION now one version behind CONTEXT v1.9; section 9's OPEN-6 row still names the retired flags; **neither `trader_pack` nor `report_9b` has a `__main__` guard or a `[project.scripts]` entry, so `python -m` exits 0 having written nothing**; 'failed on its own assertion' is true of 2 of the 5 flipped probes, the other 3 dying on KeyError/AttributeError; 'no number' is true of METRICS but 19 numeric tokens arrive and 3 leave, all Round-4 references; 7 companion rows print an unsigned `0.00`; `_ordered`'s sort key can mix date and datetime, unreachable today). **WHAT REPRODUCED EXACTLY, importing nothing from src/acumen:** **2218/0/0 from clean** (243.22s, the claimed count to the test; **2270/0/0** with this review's 52 kept probes). **The six F1/F2 renames have BYTE-IDENTICAL bodies, proved by AST** -- 'no expected value re-measured' is TRUE mechanically, not by eye; the other 8 renames and all 13 body-changed tests inspected assertion by assertion, **none weakened** (the money tripwire that looked weakened is STRENGTHENED -- both asserts moved inside a loop over the points module too). **All four gap numbers hand-derived from 3.4** (SL 2025/2027, risk 12/10, TP 2073/2067), matching CONTEXT v1.8 section 8 to the paisa. **THE POINTS VIEW: 204 stocks / 188,345 trades / +72,096.43 points / +0.38 a trade / 32.60% positive all reproduce; BOSCHLTD and DIXON recomputed in full; and rather than five spot-checks, ALL 204 ROWS x 9 FIELDS = 1,836 published companion figures reproduce EXACTLY with zero mismatches**, ranking order identical, plus page 7's 40 x 7 = 280 figures and non-overlapping slices. Max-DD-in-points re-implemented from the PAGE'S OWN WORDS (peak seeded at zero) and it matches the module on all 204. **SIZE-INDEPENDENCE PROVED BY AST**: the only row attributes read anywhere are day/entry_close_stamp/entry_paise/exit_paise/side/symbol -- qty, notional, gross, net and cost are read from NO row. Caveat byte-exact on BOTH artifacts with `by_chance(204)=int(204/20)=10` self-computing; cost-honesty line on both. **THE TRADER'S OWN Q44 CONSTRAINT ('at or below 2030') HOLDS ON ALL 2,068 GAP TRADES -- 850 long (stop <= POC), 1,218 short (stop >= POC), ZERO violations**, compared in half-paise. His own three figures (entry 2037, entry-candle low 2032, POC 2030) put his own example on the gap branch, corroborating CONTEXT v1.8 on HIS authority. Numeric freeze exact (2,457->2,449, 8 out / 0 in, all Q43/Q44 digits, 3 new exclusions = the 3 Round-4 sentences, frozen region unchanged at 558 lines); independently, **every numeric token in the WHOLE report** compared 3,511->3,527 with only Round-4 references moving -- **not one metric moved anywhere**. Red-then-green verified independently against `git archive 15a72b6`: **all 5 flipped probes RED there, green here**. **ALL FOUR ARTEFACTS BYTE-REPRODUCED in this session, diffs EMPTY** (pack, companion, points table, and -- completing after the review was first committed, folded in as a stated correction to REVIEW_12_2 rather than dropped -- the report, 87,756 chars, sha256 `529a04a887d2b69a`). **ZERO store writes: 22,186 files under data_root fingerprinted (mtime+size) before and after, identical**, across two full generator runs. No secrets, no AI attribution (only permitted CLAUDE.md/CONTEXT.md citations), `(unreviewed)` suffix correct on every commit touching src/ or tests/, no engine module touched, no fixture byte moved, frozen run manifest still byte-exact at v1.7 with its PENDING disclosures (B305's whole purpose). **B304-B311 all APPROVED**, B304 discharged and **B306 CLOSED by this review** -- it honestly recorded that CONTEXT 8's F1/F2 were left with no PnL-level golden, and a reviewer may compute one: F1 qty 83, square-off 2053, net **+Rs 1,228.00**; F2 qty 100, square-off 2054, net **+Rs 1,600.00**, both hand-computed from 3.5. **Q4 (measured, then closed):** CONTEXT 3.2 bounds no age on a carried bias and FORCEMOT shows one can survive 112 days -- but over all 188,345 executed trades, 84.538% fire on a bias set for that day, 10.848% carry one day, 3.667% two-to-three, 0.946% four-to-seven, **2 trades** carry 8-30 days and **ZERO** carry more than 30. Mechanism exists; this run does not exercise it. **Q5 / Q-25:** F2's 'gap' is not UNTRADED on the trader's own definition (its arming candle runs 2026-2038, straight through 2030-2032) -- the engine and section 8 are right because 3.4's predicate is about the ENTRY candle, but one of only two goldens he may read teaches a 'gap' his own sentence would not call one. **Q-24 and Q-25 opened** (his text also asks for a Risk-to-Reward Ratio and a percentage focus page 7 does not carry, and names a one-share size basis and a Nifty 200 universe where CONTEXT 3.5 sizes by Rs 1,000 of risk over 3.1's ~210 F&O underlyings). docs/reviews/REVIEW_12_2.md + tests/test_review12_2_probes.py (52 kept probes). NO tag (FAIL). **FIX SESSION OWED**, then re-review over the fix commits. #
# superseded 2026-08-07 by REVIEW_12_2 (FAIL): chunk 12: Round-4 executed, pack repolished (unreviewed) -- QC RE-REVIEW OWED over `453ba5e..HEAD`  # ROUND-4 EXECUTION SESSION 2026-08-07 (07-Aug commit-time date; the work ran 06/07-Aug). **PART A**: the architect's two Round-4 rulings recorded WORD FOR WORD in QUESTIONS.md before any edit -- Q44 CONFIRMS the implemented CONTEXT 3.4 gap rule and OVERTURNS Q-15's example parametrization; Q43 is SUPERSEDED by its own asker (capital flags RETIRED, per-stock POINTS view adopted). **The trader's OWN two texts were NOT supplied to this session and are nowhere on this machine**, so under the 06-Aug quotation-fidelity ruling they are NOT written: both slots are reserved, each is described outside quotation marks with a citation, and the operator's paste is recorded as OWED (B304). Nothing executed depends on their wording. **PART B**: **CONTEXT v1.8** applied and committed ALONE -- three edits, no fourth: header; section 8's F1/F2 rows re-parametrized to the GAP day at POC 2030 (entry 2037, SL = the previous 15-min candle's close -- 2025/risk 12 on F1's candles, 2027/risk 10 on F2's) with the POC-2032 pair KEPT as a labelled low == POC boundary ILLUSTRATION; section 10's log row verbatim as supplied. CONTEXT 3.4 untouched, again. **PART C**: the F1/F2 GOLDEN/ILLUSTRATION designations FLIPPED (four renames at signal level, two relabels at PnL level, **no expected value re-measured** -- B164's construction; B306 records that CONTEXT 8's F1/F2 therefore have no PnL-level golden of their own and that F4 remains the gap branch's); the **Q44 stamp path CLOSES as CONFIRMED** (`Q44_CONFIRMED_STAMP`, escalation branch closed UNUSED, no re-run, not one figure moved); every *flags pending Q43* sentence becomes the **RETIREMENT wording** in the report, the pack and the manifest constant, with `CAPITAL_FLAGS_PENDING_NOTE` / `Q44_PENDING_STAMP` / `Q44_ESCALATION` kept BYTE-EXACT as historical constants so the frozen run manifest and the committed chunk-9A pilot pack are still quoted correctly (B305); `SPEC_VERSION` v1.7 -> **v1.8** (B307). **PART D**: `src/acumen/points_view.py` -- PURE over the ledger, size-independent by construction (points = exit - entry per share, signed by side; no qty, notional, gross or net is read anywhere), hand-computed fixture FIRST (11 tests). Rendered as the pack's **page 7** (top and bottom 20) plus the full 204-row `docs/reports/points_by_symbol.md` and every stock in the JSON companion, with the rupee-cost honesty line, the MANDATORY multiple-comparisons caveat as a self-computing template (B309) and a two-way cross-reference to page 1. **204 stocks, 188,345 trades, +72,096.43 points, +0.38 a trade, 32.60% positive** -- positive in points while page 1 is -Rs 16,836,018.20 in money, and the page says why in its own sentence. The B301 no-typed-figure tripwire is EXTENDED to points in both halves. **PART E**: REVIEW_12's fifteen sentence fixes executed (Q1 two gap entries with INDUSINDBK at 12:45 named, Q2 the population reconciled 493,680/495,312/1,632/87,192, Q3 the symbol-name last resort stated both ways, Q4 210/204/six named, Q5 one denominator + 40 flats, Q6 both decisive observables printed -- the gap day's Rs 0.10 margin and the Rule-3 first break at 09:15 Rs 741.90 vs P's high Rs 741.50, Q7 the E7 sentence on page 2 and beside every entry-candle stop, Q8/C5 409,205 of 435,641 printed with the two measurements separated, Q9 'no NEW rule fired', Q10 'at most Rs 1,000.00, as whole shares allow', Q12 balanced closings, Q13 the write-in row count, Q14 the tie counted and ZYDUSLIFE named with the executed key, C2 the stop-out pool widened -- B308, same day chosen, C3 the companion's sort artefact split into two honest fields). **THE FIVE PINNING PROBES ARE FLIPPED, RED-then-green PROVEN**: each was run against the pre-fix source in a scratch tree (`git archive 15a72b6`) and FAILED on its own assertion, then passes here. **PART F**: **2218/0/0 from clean** in 376.78s (was 2,195; +23; 17 new test functions, 14 RENAMED, ZERO removed, none weakened). **ALL FOUR ARTEFACTS BYTE-REPRODUCIBLE, second independent runs, diffs EMPTY**: pack `8cbabed9e2f29922...` (51,627 bytes, 639 lines), companion `208b142e79dcb93c...`, points table `312bc9e0d49683f8...` (218 lines), report `529a04a887d2b69a...` (87,756 bytes, 899 lines). **THE REPORT MOVES FIVE LINES AND NO NUMBER** (whole-file diff 8+/6-), and **the numeric freeze holds with exactly three stated new exclusions**: 2,457 -> 2,449 tokens, and the eight that left are exactly `43`,`43`,`44`,`44`,`2032`,`3.4`,`31`,`-2026` -- every one a Q43/Q44 digit, none a metric, proved by the kept REVIEW_9B_FINAL probe re-extracting under the ORIGINAL five markers (B310). **ZERO store writes** (0 files under data_root modified). No CONTEXT deviation, no fixture byte moved, no engine module (bias/poc/signals/simulate) touched, no secrets, no AI attribution. B304-B311. **QC RE-REVIEW OWED** (fresh session, both personas) over `453ba5e..HEAD`. **THE TRADER GATE STAYS OPEN**: it closes on his confirmation of the rules and the row count, which is what the pack asks for.
# superseded 2026-08-07 by the ROUND-4 EXECUTION session: chunk 12: reviewed-PASS -- pack cleared for the trader  # QC REVIEW (both personas, fresh session) over `db6c686..d1c9d72`. **2195/0/0 from clean** in 413.34s -- the build's claim reproduced exactly, no skips, no xfails; **2201/0/0** with 6 kept reviewer probes. **VERDICT PASS, 19 findings (14 quant + 5 code), none blocking, NOT ONE OF THEM A WRONG NUMBER.** **ALL SIX DAYS RE-DERIVED FROM THE SPEC TEXT** (CONTEXT 3.2/3.3/3.4/3.5 reimplemented from its own sentences over the raw parquet, importing nothing from src/acumen) -- HDFCBANK 2026-06-10, HINDZINC 2026-07-30, TIINDIA 2026-07-27, POLYCAB 2026-07-29, SBIN 2026-07-30 and BAJFINANCE 2026-04-10, **ZERO divergences** against both the page and the committed shard row (all six rows carry exactly 38 keys, so "38 of 38" is the WHOLE row): incl. TIINDIA's gap predicate turning on **TEN PAISE** (entry-candle high Rs 2,848.80 vs POC Rs 2,848.90 -> risk 8.70 not 6.70, qty 114 not 149), HDFCBANK's Rule-3 first break (HIGH at 09:15, Rs 741.90 vs P's high Rs 741.50), HINDZINC's genuine HALF-PAISE POC Rs 537.525, POLYCAB's reference EXACTLY on the POC (Q41-A side-unset), SBIN's carry walked back to the Rule-1 day that set it, and every one of the 15/10/5 transition rows. **THE SIX SELECTION RULES RE-EXECUTED against the ledger: each selects exactly the printed day**, then attacked -- the 3b median re-derived at 236 paise (HINDZINC uniquely nearest at 14; the stop-outs-only and mean readings DO move the day and are both refuted by the printed wording). **BAJFINANCE BOTH WAYS**: the 130.5-tick residual, half-even 130/tpr 5/**26 rows**/Rs 918.55 against half-up 131/tpr 6/**22 rows**/Rs 918.60, both volume-conserving through the engine's own spreader, and **the trade IDENTICAL in every field under both** -- the page's claim is true and correctly hedged. **COUNTS ALL RECOUNTED from the raw ledger**: page 1 to the paisa (199,848,180 - 1,883,450,000 = -1,683,601,820 paise), break-even 34.60% recomputed by hand against a delivered 31.53%, all eleven year rows (summing to the net exactly), drawdown Rs 16,852,007.80 from a from-scratch equity curve with **0 later observations at or above the peak**, 62 ties **62 bullish / 0 bearish** (proved STRUCTURALLY over 1,401 routed pairs, not quoted), 4,151 wait-rule days / 2,246 traded over an exact 406,488 denominator, and **51 rounding candidates / 13 POC-moving reproduced twice** under two independent I/O strategies. **THE REPORT EDIT re-derived independently**: 369 = 94 + 275 on the 134 members against a table-wide 433 = 125 + 308, and **the freeze holds at 2,457 tokens with ZERO new exclusions** (sections 1..9 byte-identical, 566 lines both sides, whole-file diff exactly one line). **BYTE-REPRODUCIBILITY RE-PROVEN IN THIS SESSION**, all three diffs EMPTY: pack `00503d64...`, companion `237d3f98...`, report `9ca0ed52...`. **7-mutant matrix over the no-typed-figure tripwire: 7 caught, 0 survivors**, both halves; 170 of 170 rupee tokens on the page accounted for by the companion under this session's own renderer; the nine Round-3 quotes **byte-equal, 9 of 9**, to QUESTIONS.md's receipts. B297-B303: **5 APPROVED, 2 APPROVED-WITH-CHALLENGE** (B299, B303). No CONTEXT deviation, no look-ahead, no test weakened/deleted/skipped (1,502 -> 1,520 functions, ZERO removed), no fixture byte moved, no engine module touched, no secrets, no AI attribution, **ZERO store writes**. The three findings recommended for the architect BEFORE handover: **Q1** (3c's *"the LAST gap entry of the whole ten years"* -- INDUSINDBK gapped the same day at 12:45 against TIINDIA's 12:30, and the qualification test checks only membership), **Q4** (the six quarantined F&O stocks APLAPOLLO/ASTRAL/IEX/NTPC/UPL/VBL, absent from the run and unmentioned on the page whose job is to name what the machine did not do, which also asserts 204 IS the list) and **Q7** (page 2's stop reads as already touched on its own entry candle -- CONTEXT 7-E7 is nowhere on the page and day 3a is a live instance). Two more for his judgement: **Q13** (the row-count question is asked as a binary and the trader's only previous row count in this repo, quoted on page 2 of this very pack, landed between the two answers) and **Q14** (TWO days tie at the maximum row-count separation with a moving POC -- ZYDUSLIFE 2026-03-11 beside BAJFINANCE -- and the key that chose BAJFINANCE lives only in a docstring). docs/reviews/REVIEW_12.md - tag chunk12-pass. **THE TRADER GATE STAYS OPEN**: it closes on his confirmation, which is what the pack asks for.
# superseded 2026-08-06 by REVIEW_12: built (unreviewed) -- QC REVIEW OWED  # VALIDATION-PACK SESSION 2026-08-06. **PART A**: the architect's three plan amendments recorded VERBATIM in QUESTIONS.md (chunk 10 absorbed / chunk 13+ ON HOLD / chunk 12 now) and his assigned report edit executed -- section 10's mixed-row sentence is now SCOPED to the members the mixed row is built from (**369** non-unit factors on the 134 members = **94** share-count + **275** dividend) with the table-wide **433** (125 + 308) stated beside it and labelled, exactly like the share-count pair above it. `BenchmarkPair` gains `in_benchmark_events_applied` / `in_benchmark_excluded_by_kind`, both DEFAULTED so the eleven kept probes that build a pair by hand are unchanged. **THE REPORT REGENERATED AND BYTE-REPRODUCIBLE x2** (two independent full runs to different paths, diff EMPTY): sha256 `9ca0ed52cc0899c1c51ccdbe80afef6513b417abced6fe7b7690b648153f7d19`, 86,145 bytes, 897 lines. **THE FREEZE HOLDS AND NEEDED NO NEW EXCLUSION** -- sections 1..9 are 566 lines on both sides with **ZERO differing lines**, not one of them even marker-covered, and the 2,457 numeric tokens are identical; the whole-file diff is exactly **2 lines**, the one edited sentence. REVIEW_9B_FINAL's kept Q2 probe is EXTENDED (never weakened: every original assertion stands, a special dividend is added to each fixture symbol so the two scopes can differ at all, and the new assertions read the mixed row's line off the RENDERED page). **PART B/C**: `docs/validation/trader_pack.md` + `docs/validation/trader_pack.json` + `src/acumen/trader_pack.py` + launcher -- the trader-facing pack, plain English, no verdict language, **BYTE-REPRODUCIBLE x2 in BOTH files** (see the PROGRESS entry for the digests). Every figure is PULLED: the ten-year totals through `report_9b.read_run` (the same reader the report uses) and `portfolio`, the six days REPLAYED through the run's own `BacktestRunner` with each replay compared FIELD BY FIELD against the committed ledger row -- **38 of 38 on all six, zero divergences**. **THE SIX DAYS, each chosen by a printed rule**: HDFCBANK 2026-06-10 (Rule 3, LONG, target, +Rs 2,892.50 -- the architect's named day, qualification CHECKED), HINDZINC 2026-07-30 (Rule 1, LONG, stop, -Rs 1,100.00 -- the last walked day's stop-out nearest the run's MEDIAN per-share risk of Rs 2.36), TIINDIA 2026-07-27 (Rule 1, SHORT, GAP, -Rs 1,091.80 -- the architect's named day and the span's last gap entry), POLYCAB 2026-07-29 (Rule 1, LONG, square-off, +Rs 950.00 -- the most recent ==POC wait-rule day that traded), SBIN 2026-07-30 (inside-bar carry, BEARISH, no trade), BAJFINANCE 2026-04-10 (inside-bar carry, LONG, stop, -Rs 1,098.20 -- the Q-13 rounding probe). **THE COUNTS**: Rule-3 TIE days **62** in ten years (62 bullish / 0 bearish, and the zero is the bullish-precedence predicate showing through, stated on the page); ==POC wait-rule days **4,151** of 406,488 judged stock-days, **2,246** of them traded. **THE PINNED-INTERIM VERIFICATION THE SPEC OWES IS NOW ASKABLE**: over the span's last calendar year **51** days have a half-tick residual where half-even and half-up draw a DIFFERENT row count, **13** of them move the POC; BAJFINANCE 2026-04-10 is the widest (**26 rows** half-even vs **22** half-up, POC Rs 918.55 vs Rs 918.60) and the pack asks him to count the rows. Tests: `tests/test_trader_pack.py` -- the no-hand-typed-money tripwire in BOTH halves (source: no rupee string, no grouped digit run outside the trader's own verbatim quotes, no CONTEXT 3.5 magnitude; RENDERED PAGE: every rupee token on the committed page is a value the JSON companion carries). Stores READ-ONLY. **QC REVIEW OWED (fresh session, both personas).** **TRADER GATE OPEN** -- the pack asks for the two confirmations and states the three paths with no recommendation.
chunk 13: reviewed-PASS -- the live screener stands  # **QC RE-REVIEW (both personas, fresh session) over the FIX-2 span `d7b43a4..4e51848`, against REVIEW_13's FAIL, its ten BLOCKING findings and its PART 3 list. VERDICT PASS -- chunk 14 is UNBLOCKED and a live morning may be run.** All ten blocking findings closed, each verified by an attack built HERE rather than by re-reading the fix session's evidence. **The coverage gap that caused the FAIL is closed twice over**: `run_day()`, `close_day()` and `restore()` all have live callers in `src/` (run_screener.main:179 and :206, live_screener.py:730), and the shipped CLI was driven end to end through the **REAL vendor client** -- the real `SmartConnect` constructor, `SmartApiClient.login()`, `_call_with_retry`, `parse_candles` and `SmartApiBarSource`, with only dummy credentials, the two network methods and the clock substituted -- for **18 sweeps, 375 bars, 09:15..15:29, one POC pinned, 3 alerts 3/3 disclosed, NTPC named excluded and NEVER FETCHED**, reproducing the golden numbers to the paisa (POC 739.80, entry 740.95, SL 738.10, TP 749.50, qty 350, exit target-hit 749.50). **B1/B355 re-derived independently**: 495,312 rows re-streamed, the strata table and the whole reach distribution reproduce to the digit, worst case 112 days (FORCEMOT 2024-02-15) under THREE definitions of "reach", and a too-short seed provably REFUSES rather than mis-carries (`_carry(None)` -> `bias=None` -> `PHASE_REFUSED`); the 9-row stratified sample matches the ledger field-for-field. **B2**: a twin inside one reply refuses the day with **ZERO alerts delivered** against a control that delivers three, survives a restart, and the morning after names it; a twin split across sweeps is a revision, recorded, and `revisions()` reaches `verification.json`. **B3**: on a day where the short window really bites (HDFCBANK 2026-06-08, complete 738.90 vs pinned 739.70) the short answer governs all day and across `restore()`'s four-field rebuild, and a second full morning over the complete day does not move it; measured at **16.67% of 216 real symbol-days** and **0 false positives**. **B4/B8**: a real `os._exit(9)` INSIDE the sink delivering the TRIGGER, then a restart -- nothing re-sent, 4,774 JSONL lines with **zero malformed**, recording whole to 15:29. **B5**: the guard survives the real constructor (level 50, **no FileHandler**, where the review measured 40 + RotatingFileHandler) and a live-posture session writes zero credential-shaped bytes. **B6/B7/B9/B10, M8/M9**: verified; every master fence holds including a poisoned recording manifest; Q-20's headline reproduces at exactly 11 symbols of the sealed 210. **CONTEXT 4.7 verified BYTE against QUESTIONS.md's recorded ruling** per the architect's 12-Aug ruling -- the disclosed sentence is the ruling's own **68 characters** in the ruling, the law and `LIVE_DISCLOSURE`, all **eleven** operative clauses present including the section-6 parity clause, zero transliteration; the check REVIEW_13 declared undischargeable is DISCHARGED. Tripwire: **31/31** on the shipped corpus (5 AST-only, 1 literal-only) over 170 files, and **7 of 10 NEW shapes** put to it here. **2431/0/2 from a clean clone** (the fix session's claim, reproduced) and **2440/0/0** with 7 kept probes; fixtures byte-frozen; report, pack and points table the SAME git blobs; no AI attribution; no secret in the span; linear chain. **B350-B373 all judged** -- 22 approved, B363 challenged in one clause, B372 approved with one substitution unnamed. **4 findings, none blocking** (Q1 a frozen-but-answering feed delivers a wrong exit alert with no banner and no staleness marker ON THE ALERT; Q2 the live calendar falls through a mid-history gap without refusing, contradicting B363; Q3 the shipped live path REWRITES 22 corporate-action year files inside `data_root` on any `--allow-network` morning and nothing discloses it -- this review caused it, attributed it (19 symbols, one ordinary dividend each, every ex-date AFTER the frozen run's end, no walked row moved) and owes the operator a snapshot; Q4 two ordinary string idioms still evade the tripwire) plus ten carried REVIEW_13 MAJORs handed to chunk 14 with fresh measurements (M22 at 15 of 17 boundaries; M21's population at 2 stock-days in 495,312). **OPERATOR DUTIES**: rotate the six `logs/` files (97 `X-PrivateKey` + 86 `Bearer` lines, recounted here) and SNAPSHOT `data/` and `cache/` before chunk 14. docs/reviews/REVIEW_13B.md · tag chunk13-pass
# superseded 2026-08-13 by REVIEW_13B (PASS): chunk 13: FIXED under REVIEW_13 (FIX-2, unreviewed) -- the live half runs end to end; QC RE-REVIEW OWED, both personas  # **ALL TEN BLOCKING FINDINGS FIXED, in the reviewer's order, plus M1, M2, M3, M5, M6, M7, M8, M9, M10, M11, M17, M20, M23 and M24.** PART A: the architect's 08-Aug-2026 rulings on B3/Q-31, Q-30 and M24 recorded VERBATIM in QUESTIONS.md in their own commit before a line of code moved, reconstructed and byte-checked at **562 characters** with both em dashes and the section sign PRESERVED. PART B: **CONTEXT v2.1**, committed ALONE (`e638918`) -- section 4.7 gains the section-6 parity clause the architect's own ruling contained and 4.7 omitted (Q-31(1)), gains the settled-universe rule (Q-30), and its quoted disclosed line is trimmed to the ruling's **68 characters**, now byte-identical to `LIVE_DISCLOSURE` (Q-31(2)). PART C, in order: **B1** the bias series is seeded 180 days back -- a value MEASURED over all 495,312 ledger rows (reach to the last rule-firing day: 84.23% zero, 99.03% within 3 days, 99.98% within 7, **112 at the very worst**), so all 62,692 carried stock-days are reachable; **B2** the duplicate twins the merge resolves away are carried beside the merged day and handed to gate 2, in the screener AND in the next morning's verification, and `revisions()` has a caller in src/ at last; **B3** the POC is pinned at 11:15 through a new engine seam and is immutable for the day, with a short window FLAGGED `POC provisional / incomplete window` and never re-fixed; **B4+B8** `run_screener.main` CALLS `run_day()`, so `close_day()` runs (the recording is a whole 375-minute session, last stamp 15:29) and `restore()` is reachable (a restart re-sends NOTHING and says so); **B5** the credential guard runs on BOTH sides of the vendor constructor and DETACHES its file handler, proved at `logs/` across a full live-posture session rather than at `repr()`; **B6** the live calendar takes today from the PUBLISHED NSE master and its history from the store's own scan (the C5 division of labour), so a today-dated `--mode live` run now STARTS -- exit 0 where it exited 1; **B7/B9/M3/M6/M11** alerts are STATE-DERIVED and idempotent, the dedup key re-cut to (symbol, kind, identity) so a corrected trigger is delivered AND recorded naming what it supersedes, the phase machine monotonic, a second outage loud; **B10/M20** every row renders its bar count and last stamp and a stale row is visibly distinct, and an EMPTY vendor answer is a not-answer that raises the banner; **M8/M9** `named_master` refuses anything but a bare filename and `master_file` is fenced to the day's own dump in live mode; **M1/M2** the operator banner carries B341's BRACKET (0.5229%-2.6808%) and the settled-universe rule, and the preflight NAMES every quarantined symbol excluded. **THE COVERAGE GAP THAT CAUSED THE FAIL IS CLOSED**: `tests/test_review13_fix.py` and `docs/evidence/chunk13_fix2_live_morning.{py,md}` build and DRIVE the shipped live path end to end -- 375 bars 09:15..15:29, 18 sweeps closed, one POC pinned at 11:15, NTPC named excluded, three alerts each carrying CONTEXT 4.7's disclosed line, and the chunk-7/8 golden's own numbers (POC 739.80, entry 740.95, SL 738.10, TP 749.50, qty 350). **Stratified replay invariant: 9 of 9 `bias_rule` strata match the chunk-9B ledger field for field, 5 of them on a CARRIED bias**, including REVIEW_13's own witness ITC 2026-06-10 (`docs/evidence/chunk13_fix2_bias_stratified.{py,md,json}`, re-run by the suite). Order-endpoint tripwire widened from 46 top-level modules to **170 files** recursively across src/, tests/, scripts/ and docs/evidence/, catching **31/31** of a 31-variant evasion corpus against the shipped scan's 20, with 5 AST-only catches and 1 literal-only. ****2431 passed / 0 failed / 2 skipped from a clean git clone** (1,099.27s); the 2 skips are the two probes whose inputs are GITIGNORED and therefore absent from any clone (`logs/` and `.env`), exactly as at the review head.** The five defect pins FLIPPED, each keeping its own measurement; F3 deliberately NOT flipped and stated. Fixtures byte-frozen; report, pack and points table untouched; **ZERO store writes** (22,186 files, newest mtime 2026-08-05, zero files carrying today's date). Class-B decisions **B355-B373**. **OPERATOR DUTY OUTSTANDING**: rotate the six files under `logs/` (the key is already rotated). NO tag -- the re-review owes it.
# superseded 2026-08-12 by the FIX-2 session above: chunk 13: reviewed-FAIL -- the ENGINE half holds, the LIVE half does not; fix session owed, chunk 14 BLOCKED, NO live morning  # QC REVIEW (both personas, fresh session) over the whole chunk-13 span `7f22f27..7459ab5`, plus the architect-directed PART 0 recording (CONTEXT **2.0e** table of contents, `6789079`, committed alone; the **B340 ruling** verbatim, `5418a63`, byte-checked at 253 chars). **VERDICT FAIL on TEN BLOCKING findings, EIGHT of them SILENT** (only B6's refusal-to-start and B8's re-sent alerts are visible). What HOLDS, measured: CONTEXT 4.6's settled battery did not move -- **2,530 ledger symbol-days recomputed 2,530/2,530 exact**, and the same days under the span base, the pre-change tree and HEAD give **byte-identical DayGates**; a 1,800-window differential fuzz of `integrity_gate` shows the new default reproduces the old field-for-field and the live reading never tightens; **GATE_DEFINITION correctly unbumped** (a bump costs a store-WRITING re-gate of 435,641 days for identical verdicts); **B340's identity is unconditional** over the whole posture space; CONTEXT 4.7's residual re-streamed independently to the row -- **2,187/418,275 = 0.5229%** and B341's bracket **10,516 = 2.5141%**, with all five manifest cross-checks agreeing; the oracle-free battery's bar-level triggers are **identical in both postures over 30 corrupt shapes**; **B330 verified on a real day** (TCS 2026-06-10 in-trade 12:45->15:00, square-off at 215350 at 15:15, no exit alert before it); the replay invariant **holds on five of six NEW real symbol-days** incl. a gap entry and two ==POC days, with a rebuilt LedgerRow byte-identical to the chunk-9B line; **2392/0/0 from a clean clone** (2,405/0/2 with this review's probes from a clone, the 2 skips being the probes whose inputs are gitignored; **2,407/0/0** in the operator's tree); fixtures byte-frozen; report, pack JSON and points table byte-identical since chunk 12 (pack MD one prose line, numeric tokens 1,107 in / 1,107 out); **ZERO store writes** (22,186 files, digest `933cfffbcc487c65...` before and after). What FAILS: **B1** the live bias series is seeded at the trade day, so every CARRIED bias is lost -- **29,121 of 188,345 executed trades (15.46%)** invisible to the live half, witness ITC 2026-06-10; **B2** `merge_bars` launders duplicate stamps before gate 2 can see them and the CORRUPT TWIN WINS; **B3/B9** the POC and the entry price move after publication (2.76%/14.48% of symbol-days, 53 arming flips) and the correction is neither sent nor recorded; **B4/B8** `run_screener.main` re-implements `run_day`, so `close_day()`, `restore()` and `run_day()` have NO caller in src/ -- every recording is 15 minutes short (22% of clean days flip to REFUSED) and a restart re-sends every alert; **B5** the credential logging guard is undone by the vendor constructor, **97 X-PrivateKey and 86 Bearer lines already on disk**; **B6** `mode="live"` cannot start on any real morning (CalendarError); **B7** the TRIGGER alert is lost if its boundary is missed; **B10** a blind screener renders as a calm morning. 25 MAJOR, ~35 minor/note. `docs/reviews/REVIEW_13.md` + `tests/test_review13_probes.py` (15 kept probes, 5 pinning defects). **NO tag.**
# superseded 2026-08-08 by REVIEW_13 (FAIL): chunk 13: built + FIXED under CONTEXT v2.0 section 4.7 (unreviewed) -- QC REVIEW OWED over the whole chunk-13 span  # **THE LIVE MODE IS UNBLOCKED.** The architect's 08-Aug-2026 rulings on **Q-28 and Q-29** are recorded VERBATIM in QUESTIONS.md (own commit, before any code moved), the residual is MEASURED from the ten-year ledger before a line of spec was written, and **CONTEXT v2.0 section 4.7 LIVE-MODE VALIDITY** is committed ALONE (`a72a17a`) in the architect's own template with the measured figures inserted. **MEASURED RESIDUAL: 0.5229% of settled symbol-days (2,187 / 418,275 over the chunk-9B ten-year ledger) failed gate 1 ALONE** -- the frequency a live morning accepts and discloses; the stream cross-checks against the run's own manifest and agrees TO THE DAY on all five readings they share (495,312 rows walked, 6,056 gate-1 refusals, 47 gate-2-alone, 5,157 gate-1P-alone, 407 auction reliefs), and the evidence states its own limit (a LOWER bound; the measurable upper end of the bracket is 10,516 days = 2.5141% refused only by an ORACLE gate) -- docs/evidence/chunk13_q28_residual.{py,md}. **EXECUTED:** the ORACLE-FREE battery per sweep as ONE argument on the SHARED `quality_gates.integrity_gate` (`completeness_measurable`, default = today's settled behaviour, so no stored symbol-day moves) with the live battery's whole body proved by AST to be one call and one construction; gates 1/1P recorded as UNRUN (`None`) and never as failed, with a settled `DayGates` carrying no gate-1P verdict RAISING rather than reading as a pass; the disclosed line on EVERY live alert payload and on the dashboard header in live and dry-run, and on NO replay alert; the day's own instrument master fetched pre-open, named+hashed into the recording, with a live morning REFUSING to start without it (Q-29) and the pin-vs-day divergence reported every morning as information; the next pre-open running the FULL battery over yesterday's recording under the master THAT recording names, writing `verification.json` back into the day it judges, and **naming the live-alerted-oracle-refused case LOUDLY -- driven by test in both directions** (a refused day nobody alerted on is reported and deliberately not shouted); the replay invariant extended so a recording carrying a different master than the config pin replays under the RECORDING's (proved RED against the pre-fix resolution first). The Q-20 pin still governs the historical ledger and `build_runner` still refuses any other master for a backtest. `SPEC_VERSION` -> **v2.0** (a ledger names the law it was produced under; section 4.7 governs days whose bhavcopy does not exist yet, so no walked row moves). Safety tripwires UNTOUCHED and re-asserted against the path that can now actually run: an AST walk proves opening the feed added exactly `login` and `get_candles`, `live_source` still touches the client once, and `--preflight-only` opens no broker session at all. **2392 / 0 / 0 from a clean clone** (182.26s) vs 2,352 at the build -- +40 collected, +38 test functions, **ZERO tests removed**, exactly TWO rewritten (the Q-28 STOP-rule pair, replaced by stricter assertions). Fixtures untouched; **ZERO store writes** (0 files under `data_root` modified, verified from the machine). Evidence: docs/evidence/chunk13_context47_walk.{py,md} -- the same three real symbol-days walked under BOTH batteries with the whole `SymbolState` identical on all three, every live alert disclosed, and the loud case reached on a real recording against a wrong oracle built in memory. NO tag (review owed).
# superseded 2026-08-08 by the CONTEXT v2.0 fix above: chunk 13: built (unreviewed)  # THE LIVE SCREENER. **The 07-Aug-2026 rulings LIFT the hold** (recorded VERBATIM in QUESTIONS.md by this session): the operator rules the complete tool is the deliverable, build order re-sequenced 13 -> 14 -> 11 -> 15, and every UI surface from here on is bound to the getdesign `cohere` system plus deliberate design judgment. **2352/0/0 from clean** in 227s (was 2283 at chunk12-pass with its kept probes: +2 Part A, +67 chunk 13). NEW `src/acumen/{live_recording,live_source,live_screener,live_dashboard,live_refresh,run_screener}.py` and six test files. **THE ONE-ENGINE LAW IS A SPLIT, NOT A CLAIM**: `SignalPipeline.stock_day` now loads its minutes and DELEGATES to a new `evaluate(symbol, day, bias=, minutes=, gates=)` which is the whole of its old body, and the screener calls THAT -- same battery, same POC engine, same aggregation, same `signals.evaluate_day`, same order. A source-level test forbids the live layer from calling any of them directly, and `stock_day == evaluate == evaluate(gates=)` is asserted as object equality. **REPLAY INVARIANT: 3 of 3 real symbol-days IDENTICAL** (docs/evidence/chunk13_replay_walk.{py,md}) -- HDFCBANK 2026-06-10 (the chunk-7 Rule-3 outside-bar golden: POC 739.80, ref 738.20, ARMED, entry 740.95, SL 738.10, TP 749.50, qty 350, target on the 13:15 candle, all reproduced through the LIVE pipeline boundary by boundary on a virtual clock), ICICIBANK 2026-06-10 (wait-below all day, NO trade) and RELIANCE 2026-06-11 (armed, never crossed, NO trade). The whole `StockDay` compared -- profile, aggregated bars, signal, every transition note -- plus `bars collected == stored` and `bias == BacktestRunner.bias_map`, all three, zero vendor revisions. **REPLAY CONTRACT** per day: manifest (spec v1.9, code SHA, config digest, the Q-20 master by filename AND sha256, Row Size, money, factor digest, the CALENDAR READING with its governing source named), 4,455 candle lines, 18 fetch rows, the bias table, every alert, 36 lifecycle events, crash-safe state, and a sha256 over the whole directory. **C5 EXECUTED**: the PUBLISHED NSE calendar governs live, the store scan is recorded beside it as the backtest cross-check, and a disagreement is reported and never resolved by the screener. **Q-19's guard applied**: the live top-up ends at the last COMPLETED trading day and REFUSES a window reaching today. **SAFETY**: no order endpoint anywhere in src/acumen (literal scan of 15 endpoint names + an AST scan of every attribute and getattr string; the client calls exactly generateSession, terminateSession, getCandleData), credentials absent from both reprs, and a skipped symbol produces NO alert at all. **TWO CLASS-A QUESTIONS RAISED, NOT GUESSED. Q-28 BLOCKS THE LIVE MODE** -- CONTEXT 3.3's POC needs gate 1 to have PASSED, gate 1 needs THAT DAY's bhavcopy, and today's does not exist during today; measured (the battery over a 60-minute prefix of a day that passes it whole is refused by gate 1), four options laid out, none taken, and `build_live_screener(mode="live")` refuses with the question's own text. **Q-29** binds the first live morning: which instrument master governs it, the Q-20 pin or the day's dump (Q-20 measured 11 of 210 disagreeing across two snapshots). Replay is unaffected by both. **PART A also done**: the architect's rulings recorded verbatim; `DESIGN.md` established (`npx getdesign@latest add cohere`) with a page of design intent for this tool; REVIEW_12C's C1/C2/Q2 closed with the pack regenerated -- ONE line moved, numeric-token diff EMPTY (1,107 in, 1,107 out) -- and M1 needs no fix, its four rendered-output probes ARE the standing guard. ZERO store writes: 0 files under data_root modified. **QC REVIEW OWED** (fresh session, both personas) over 7f22f27..HEAD.
# superseded 2026-08-08 (the hold is LIFTED): ARCHITECT'S PLAN AMENDMENT (class C, 06-Aug-2026), verbatim in QUESTIONS.md: *"chunk 13+ live screener ON HOLD pending the trader's decision"* -- the decision the chunk-12 pack exists to inform. NOT cancelled and NOT failed: the card stands exactly as plan.md v1.1 writes it, and the hold is a state, not a verdict. Nothing in the chunk-12 session touches live-screener code; the CONTEXT 6 replay invariant this card targets is unaffected.
chunk 14: reviewed-PASS -- live equals backtest, proven  # **QC RE-REVIEW (both personas, fresh session) over the FIX-2 span `993d57a..fd2e2d4`, against REVIEW_14's FAIL and its nine-item closing list. VERDICT PASS -- the FAIL is LIFTED and chunk 14 is SEALED.** Every finding was verified by an attack built HERE, not by re-reading the fix session's evidence. **STORE INTEGRITY FIRST AND LAST**: the operator's restored 13-Aug state confirmed from the machine before this session's first command (22,186 files, 4,109,782,853 bytes, content `d97ba419...`, newest file 2026-08-12 -- so REVIEW_14's 11 contaminating rows are GONE rather than buried) and **byte-identical after its last**, across two full suite runs, a scratch-copy `--refresh --allow-network`, a 15-day parity re-run and every probe. **B1/B2 (Q3)**: driven through `morning_refresh` on a COPY of BOTH roots with the network stubbed at four layers -- the fence DOWNGRADES (`may_network=False`), a **40-day-old** cached window planted by this review is SERVED (`events_total=1`, step `ok`), the CA endpoint is **never attempted**, `<cache_root>/ca/` is untouched, and **0 files moved of 66 under data_root and 0 of 3 under cache_root** measured by path+size+mtime_ns+sha256. On the operator's machine `<cache_root>/` still holds nothing but two instrument masters, so the directory B1 measured a real morning CREATING does not exist. **B3**: the runbook's own 08:45 command through `run_screener.main` -- exit 0, READY, no "cannot start", preflight printed, and the manifest still names `published-nse-holiday-master` with `is_trading_day=True`, so REVIEW_13 M17's C5 duty survives the fix. **B408/B409** verified (6 steps, NOT RUN by name, `report.ok` False; and the real `backfill_daily.main --store <scratch>` moved **0 files under the real daily_store**). **H1**: the review's own command `--day 2026-06-10 --telegram --live-alerts` (mode defaulting to replay) through `main()` -> 3 alerts produced, **0 messages on the transport**; the five-case table plus a sixth case nobody wrote down; a replayed 2020 trade now reads `[2020-03-19 11:30] ... / [DRY RUN ...] / [REPLAY of a PAST day ...]`; all five three-act claims consistent; the no-order tripwire proved to cover `telegram_sink.py` (planting `placeOrder` turns 2 tests RED). **M19**: ten symbols, ONE poisoned this review's own way -> **18/18 sweeps, 9 evaluated at every boundary, dashboard.html written, the banner NAMING the tenth, 18 per-boundary reasons on disk, restart recovers**; a raising sink costs only its own delivery. **H3/M21**: BOSCHLTD 2021-05-20's **eight** phase-regression events re-derived BY HAND from the raw minute store before the screener was asked (entry 1556990, stop-without-late-bars 1458000, true low 1455020, risk 98990 -> qty 1 vs 101970 -> qty 0, 8 boundaries after 13:30) and measured at exactly 8; the ordinary qty-0 day compared to the byte across the span -- decision identical, one added field (`"mode": "settled"`, B411's deliberate stamp). **H4/Q1**: a refused state reports its REAL age (**211**), a FALSE fresh stamp is refused by the sink, a marked stale price still travels, an age exactly at the clamp is not called a lie. **H2**: a resumed process reports the day's real alerts (`armed 11:15, trigger 11:30, exit 11:45`) and an actually-empty morning still says so. **M15/M16** distinct on BOTH dashboards (TEXT `!!`, HTML `role="alert"` vs `row quiet`). **H5**: a live-posture day judged, `transitions_equal` real, matched. **PARITY UNCHANGED, re-run independently: 15 days / 14 judged / 14 matched / 0 mismatched / 1 disclosed**, report byte-identical to the committed one apart from its own timestamp; ITC 2026-06-10, ADANIENSOL 2026-05-08 and BOSCHLTD 2021-05-20 all MATCH, ADANIPORTS 2026-02-06 correctly DISCLOSED. Engine modules `bias`/`poc`/`signals`/`simulate` are the SAME git blobs at `db45998` and HEAD; `backtest.py` byte-unchanged across the FIX-2 span. **The 5 flipped pins FAIL 5 of 5 on the reviewed tip `993d57a`** (HEAD's probes run in a clone checked out there) and 13/13 pass at HEAD; census by AST **1,760 -> 1,778**, 6 names gone and all six proved renames; B393/B398/B401's corrections read right. **B406-B415 all judged: ten of ten APPROVED** (B407 against the architect's own 15-Aug ruling), with four LOW notes. **2,510/0/1 from a clean clone and 2,511/0/0 in the operator's tree**; fixtures byte-frozen; trader pack, report and points table the same git blobs since `db45998`; REVIEW_7 C1 perfect 8/8; no AI attribution (all five hits are the filename CLAUDE.md, which the git rule permits); no secret anywhere; linear 8-commit chain, `main == origin/main`. **ONE NEW FINDING, R1 (MAJOR, chunk-15 scope, non-blocking)**: `_poll`'s guard wraps only `source.fetch(...)`, so the `merge_bars` / `duplicate_stamps` / `record_bars` / `record_fetch` block after it is outside both guards -- **3 of 7 malformed-bar shapes still end the morning** (tz-aware stamp -> `merge_bars`; `close_paise=None` and `volume=None` -> `record_bars`). Out of reach of the shipped vendor source (`parse_candles` drops the tzinfo and refuses a non-paise price from inside the guarded `fetch`), reachable from another `BarSource` or a per-symbol file I/O failure. 14 kept reviewer probes, store-free, one of them the R1 DEFECT pin chunk 15 flips. Carried to chunk 15: R1, M4, M12, M13, M14, M25 and REVIEW_14 PART 3's harness-fidelity residue. **No new snapshot owed.** docs/reviews/REVIEW_14B.md · tag chunk14-pass
# superseded 2026-08-15 by the QC RE-REVIEW above: chunk 14: built + FIXED TWICE (unreviewed) -- QC RE-REVIEW OWED over `db45998..HEAD`  # **FIX-2 SESSION over REVIEW_14's FAIL (15-Aug-2026): every finding in the reviewer's order, plus the three carried majors the architect's 15-Aug ruling moved into scope.** **BLOCKING, closed:** (B1) `fence_ca_cache` is now ASKED inside `live_refresh.refresh_corporate_actions`, judged against the roots the JOB runs under, so `--refresh --allow-network` no longer creates `<cache_root>/ca/` -- a fenced pull that finds no cached window downgrades to the cache LOUDLY and does not refuse the morning (**B407**); (B2) the certifying test now drives `morning_refresh` END TO END against the real data_root AND cache_root, both fingerprinted whole before and after, with the network replaced by a recorder that raises and the CA endpoint asserted never asked -- plus a second test proving the downgrade serves a 40-day-old cache without moving its bytes, and an AST pin on the call itself; (B3) a supplied PUBLISHED calendar is COMPOSED through `live_trading_calendar` instead of handed raw to a runner that refuses it, so the runbook's own 08:45 command exits 0 with the preflight printed -- proved by the FIRST test in this repository to drive `--refresh` through `run_screener.main`. **BLOCKING the dry-run week, closed:** **M19** per-symbol isolation at the evaluation site AND the sink loop (measured: 10 symbols, one poisoned with a previous-day-stamped candle -> 18/18 sweeps, 9 evaluated at every one, dashboard.html written, the banner NAMING the tenth, the reason on disk per boundary, and a restart that recovers -- against 0 of 18 and an exception escaping `main` before); **M15** an alerted symbol with no candle file is a counted NOT-VERIFIED verdict instead of disappearing under a `0 alerted` headline; **M16** a gate-1P `no-raw-daily-row` failure is THE ORACLE HAS NOT SPOKEN, not the oracle refusing, so correct alerts are no longer loudly withdrawn. **HIGH, closed:** H1 the Telegram gate gains its third act (`telegram_is_live` = `--mode live` AND `--telegram` AND `--live-alerts`; a replay with both flags now sends NOTHING, proved through `main()`) and every message carries its trade date plus a DRY-RUN / REPLAY marker; H2 the end-of-day summary is built from `recording.alerts()`, so a RESUMED morning reports the day's real alerts instead of 'nothing fired'; H3 `_consumed_unsizable` returns through `_monotonic` (BOSCHLTD 2021-05-20, two late candles: the delivered qty-1 trade survives with 8 regression events, and the ordinary qty-0 day is unchanged); H4 `data_age` no longer stamps a refused state FALSE-fresh (211 minutes measured) and `unvouched_price` rejects a stamp that contradicts itself; H5 `parity.run_live` gains a live-posture path so the trail comparison is real. **Three neighbours found by driving the real path:** the daily top-up ignored the store it was handed (**B409**), `refresh_calendar` was the one unguarded step (**B408**), and the preflight's fence line asked the wrong roots. **B393/B398/B401 corrected in place** with the originals preserved; **5 defect pins FLIPPED**, 3 deliberately not. **2,510/0/1 from a clean clone and 2,511/0/0 in the operator's tree**; census by AST 1,760 -> 1,778 with all 6 vanished names proved renames. **ZERO store writes, bracketed by whole-store fingerprint across BOTH runs** -- 22,186 files, content digest `d97ba419...` unmoved, matching the operator's restored 13-Aug state (verified from the machine as this session's first duty). M4/M12/M13/M14/M25 remain chunk-15 scope. docs/evidence/chunk14_fix2_store_bracket.md · NO tag (re-review owed)
# superseded 2026-08-15 by the FIX-2 session: chunk 14: reviewed **FAIL** -- a fix session is owed  # **QC REVIEW (both personas, fresh session) over `db45998..e3c43fa`. VERDICT FAIL on THREE chunk-14-scope findings.** **The parity claim itself is TRUE and could not be falsified**: the ITC 2026-06-10 carried-bias day and the ADANIENSOL 2026-05-08 gap-entry day were re-derived BY HAND from the raw Parquet importing nothing from `src/acumen` -- bias (incl. walking the carry chain back to its `rule-1-breakout` seed on 2026-06-09), POC (56655/2 paise, half-paise because rows span an odd 25 paise; winning row a 2.27x margin), reference, every 15-min close, and the gap-entry stop that is the PREVIOUS candle's close (risk 370/qty 270, not 310/322) -- and the result is a **THREE-WAY MATCH across all 36 boundary rows, 7 fields, 5 day fields** with `project_boundaries()` and the live screener. **Zero decision drift in 96 parity runs**; the ADANIPORTS 52.135% gate-1 refusal re-derived from the ten-year ledger and correctly DISCLOSED not failed; both negative controls REAL (red on 4 of 4 rubber-stamp mutations, naming boundary AND field); M21 and M22 genuinely closed; no gate moved (895 symbol-days) so `GATE_DEFINITION` correctly unbumped; credentials clean incl. B392's dropped `__cause__`; Telegram attaches with ZERO screener edits (AST); fixtures frozen; no AI attribution; five test removals all opposite-assertion flips; census 1,709 -> 1,742 -> 1,748 re-derived by AST. **THE FAIL:** (B1) REVIEW_13B's **Q3 is NOT closed** -- `fence_ca_cache` works on all four layouts and is a genuine downgrade-to-cache, but `live_refresh.refresh_corporate_actions` never asks it, so `--refresh --allow-network` still writes `cache_root/ca/`, `cache_root/instrument_master/` and `data_root/daily_store/ledger.parquet`; (B2) `test_Q3_a_LIVE_REFRESH_writes_ZERO_bytes_under_the_stores` **never enters `morning_refresh`** and fingerprints only `data_root/nse` -- it asserts its own name, the REVIEW_9B M1/M6 shape; (B3) with `--refresh` the PUBLISHED calendar (`trading_days=None`) reaches `build_runner`, which refuses it, so **the runbook's own 08:45 command mutates the stores and then exits 1** -- and NO test drives `--refresh` through the CLI. Plus 8 HIGH: the three-act Telegram gate is TWO acts in code (a replay of a 2020 day with `--telegram --live-alerts` really sends undated alerts to the phone, contradicting five places incl. B393); the end-of-day summary reports **'nothing fired' on a resumed morning** whose `alerts.jsonl` holds three alerts, defeating B402 exactly; the M21 fix made `_consumed_unsizable` the only transition that walks BACKWARDS along PHASE_RANK, leaving an alerted position unmanaged with no ALERT_FAILURE; `data_age` short-circuits PHASE_REFUSED to a FALSE zero so a 211-minute-old price passes the Q1 vouch; the harness **cannot judge the LIVE posture at all** (`transitions_equal` structurally False -- 8 of 8 days falsely mismatched; fails safe); M15/M16 (a false GREEN over an unverified morning; correct alerts loudly withdrawn); M4 now reaches the PHONE after the close. Harness fidelity: `bias`/`bias_rule` and the boundary count are **self-comparisons**. B379-B405 judged -- 24 approved, **B393/B398/B401 CHALLENGED** (B401 cites REVIEW_13 M23 for M23's inverse). **Suite 2,478/1/1 from a clean clone run ALONE; the one failure is THIS REVIEW's own store contamination**, proved read-only -- 11 `error` rows written into `daily_store/ledger.parquet` by a review probe that ran `--refresh` against the real store (itself corroboration of B1). No candle data, no network, no verdict moves. **OPERATOR OWES: restore that one file from the 13-Aug snapshot** (content digest returns to `d97ba419...`); until then no session reproduces a green suite. Carried to chunk 15 as **BLOCKING the dry-run week: M19** (one poisoned symbol = 0 of 18 sweeps, exception escapes `run_screener.main`, restart cannot recover), **M15**, **M16**. 13 kept reviewer probes, store-free, all green. docs/reviews/REVIEW_14.md · NO tag (FAIL)
# superseded 2026-08-14 by the chunk-14 FIX session above: chunk 14: built (unreviewed)  # **THE PARITY HARNESS AND TELEGRAM.** `src/acumen/parity.py` compares the shipped live screener against the backtester CANDLE FOR CANDLE: the live sequence is the LiveScreener driven boundary by boundary with its own SymbolState read after every sweep, the backtest sequence is the whole-day answer priced through `simulate` and PROJECTED onto the same boundaries by a SECOND reading written in the harness (never imported from the live layer), and the comparison covers bias + rule, POC, reference, and then per boundary the phase, entry, stop, target, qty, exit kind and exit price, plus the alert sequence and CONTEXT 3.4's whole transition trail. Judged per CONTEXT 4.7: a day the full battery refuses is DISCLOSED, not counted. **RESULT over a sample stratified by `bias_rule` from the chunk-9B ten-year ledger: 15 symbol-days, 14 judged, 14 MATCHED, 0 MISMATCHED, 1 disclosed** (docs/evidence/chunk14_parity_report.md) -- every bias_rule stratum incl. rule-3-tie and both rare carries, REVIEW_13's own carried-bias witness ITC 2026-06-10, a gap-entry day, a reference-exactly-ON-POC day, both qty-zero days in the whole ledger, a recording round trip, and ADANIPORTS 2026-02-06 as the oracle-refused day (live posture delivered armed / trigger / square-off; the full battery then refuses it at gap 52.135%). Two NEGATIVE CONTROLS prove the harness can fail. **TELEGRAM** attaches as the AlertSink chunk 13 reserved -- zero screener edits, asserted by AST -- sends only under `--telegram --live-alerts`, refuses any alert whose price the screener cannot vouch for, degrades a send failure to silence plus a visible failure, never duplicates on resume, and never puts a credential in any string it emits. **CARRIED DEFECTS CLOSED, each with its test**: Q1 (staleness + provisional-POC as first-class alert STATES on the payload, and `unvouched_price` as the forwarding rule), M21 (qty==0 is no trade, consumed + logged -- proved on BOSCHLTD 2021-05-20 and SHREECEM 2020-03-19), M22 (a batteryless day is refused BY NAME and a recording can supply the whole-day battery), Q2 (the live calendar refuses a gap inside the history), Q3 (`fence_ca_cache`: a live refresh writes ZERO bytes under the stores, tripwired against the real store), Q4 (the tripwire folds ast.Mod and str.format). REVIEW_13B's four defect pins FLIPPED, and the log-leak probe's skip-vs-red one-liner made a real RED before being flipped to the standing guarantee. **2473 passed / 0 failed / 1 skipped from a clean clone** (488.76s); test census 1,709 -> 1,742, zero tests deleted or weakened (exactly five names gone, all five deliberate flips). ZERO store writes -- the 13-Aug snapshot verified byte-faithful first (docs/evidence/chunk14_store_verification.md) and the same content digest at the end. Q-32 raised (class B, blocks nothing). **QC REVIEW OWED over `db45998..HEAD`, both personas, fresh session.**
# superseded 2026-08-14 by the chunk-14 build: chunk 14: todo  # The 07-Aug-2026 architect's rulings (operator-confirmed, recorded VERBATIM in QUESTIONS.md by the chunk-13 build session) LIFT the 06-Aug hold: *"the operator rules the complete tool is the deliverable and the trader decides its use; build order re-sequenced 13 -> 14 -> 11 -> 15"*. The card is UNCHANGED -- the ruling rescopes nothing -- and every DEPENDENCY rule in plan.md section 3 still binds. Chunk 14 (alerts: dashboard + Telegram) is NEXT, and chunk 13's replay contract was built to serve its parity harness: one recording directory per session holding the manifest, every fetched candle, every fetch attempt, the bias table, every alert and the lifecycle events. Its own manual prerequisite is unchanged (the operator creates the Telegram bot via @BotFather and puts the token + chat id in .env). Chunk 13's `AlertSink` protocol is where the Telegram channel attaches -- no edit to the screener is owed for it.
# superseded 2026-08-08 (the hold is LIFTED): same amendment (chunk 13+).
chunk 15: reviewed-PASS (R1 fix reviewed-PASS, REVIEW_R1); tagged acumen-v1.0.1 -- **PROJECT COMPLETE -- every chunk sealed; the tool is done**  # POST-REVIEW HOUSEKEEPING (18-Aug-2026), DOCUMENTS ONLY: no `src/`, no `tests/`, no fixture, no CONTEXT and no plan byte changed, no engine behaviour touched, no test run or needed, and neither store read, written or named -- so NO REVIEW IS OWED on this commit. **Closes REVIEW_R1's F1** on the architect's ruling of 18-Aug-2026, recorded VERBATIM in QUESTIONS.md (one quoted line each, 542 and 349 characters, round-trip byte-checked, three em dashes preserved as U+2014): an incidental OS temp path embedding the build tool's product name inside a committed evidence transcript is a MACHINE PATH and never was AI attribution -- so nothing that passed REVIEW_15 or REVIEW_15B is retroactively a defect -- but, the repository now being public, it is REDACTED anyway. Both known instances scrubbed to the literal `<SCRATCH>`: `docs/evidence/chunk15_flip.before.txt:1` and `docs/evidence/r1_fix_bare_clone.md:48`, **ONE line changed in each and nothing else** -- proved at the byte level, every other line reconstructing identical and the new bytes equal to `raw.replace(fragment, '<SCRATCH>', 1)`; not a digest, count, command, exit code or verdict in either transcript moved. The whole repository was grepped for the same fragment before and after: two hits before, **ZERO after**. The frozen pre-C1 transcript `docs/evidence/chunk15_readiness_gate.out.txt` does NOT carry the fragment and was correctly left untouched, as were CONTEXT.md, `docs/acumen_poc.md` and the review documents, whose 'Claude' mentions are prose and not host paths. **TAGGING ruled the same day**: no existing tag is moved -- `acumen-v1.0` and `chunk15-final` both stay on `bd1be58` as historical markers of the release as it was cut -- and the reviewed R1 fix plus this housekeeping are marked by a NEW ANNOTATED tag **`acumen-v1.0.1`** at this commit; post-release fixes take semver patch tags from here. **One divergence FLAGGED, not resolved**: ruling 1 states the repository is now public while CLAUDE.md's git rules still read 'The repo stays private, zero collaborators' -- CLAUDE.md is architect-owned, outside this session's scope, and not one byte of it was touched. REVIEW_R1's other five findings (F2-F6) are records rather than defects and stand untouched, as do REVIEW_15B's carried R2-R6 -- **R6** (the committed parity SAMPLE no longer reproducing on the disclosed day ADANIPORTS 2026-02-06) is still the one open item worth the architect's eye. Files: QUESTIONS.md, STATUS.md, PROGRESS.md and the two scrubbed transcripts, nothing else.
# superseded 2026-08-18 by the POST-REVIEW HOUSEKEEPING session above: chunk 15: reviewed-PASS (R1 fix re-checked) -- **PROJECT COMPLETE -- every chunk sealed; the tool is done**  # SCOPED ADVERSARIAL RE-CHECK of the R1 fix (code_reviewer persona, fresh session) over `bd1be58..2073c29` -- 2 commits, linear, 0 merges, closing the one carried MEDIUM of REVIEW_15B. **VERDICT PASS.** **2,621 passed / 0 failed / 0 skipped** in the operator's tree (1148.14s) -- exactly the fix's claim and exactly this review's own `--collect-only` census (2,613 -> 2,621, delta +8, one node ID gone and it is the FLIP's rename, no test lost, no skip or xfail added). **BOTH STORES BYTE-UNMOVED across the whole re-check** -- 22,186 files, 4,109,782,853 bytes, metadata `dbea5660...`, **content `d97ba419...`**, newest_mtime 2026-08-12T23:19:20, open and close, read by a fingerprint program this review wrote from the frozen recipe importing nothing from src/. **That closes decision B438's own disclosed gap**: the fix's bracket was metadata-only and said so; the content half is now measured and agrees to the digit. **THE STRINGS WERE RUN, NOT READ**: a bare clone of 2073c29 in an OS temp dir with `PYTHONPATH` unset and no editable install anywhere on `sys.path` (measured), each of the three code paths RAISED, the argv EXTRACTED from what it raised, and every extracted command run -- zero `No module named`, zero traceback -- while all three DEAD `-m acumen.*` forms still answer `No module named 'acumen'` in that same shell and `import acumen` exits 1. **THE ARGV IS UNCHANGED, STRUCTURALLY**: each launcher run in its OWN fresh process, `acumen` proved absent first, and `_load_main()` proved to return the SAME `main` OBJECT the `-m` form would have run (`main is acumen.<x>.main -> True`, three for three), with every printed flag parsed through that module's own parser and resolved to the attribute it sets. The two remedies whose real flags WRITE were never executed (B437's shape, and it is right). **THE SWEEP WENT WIDER THAN THE FIX**: all of `src/` re-swept by three independent lenses (AST non-docstring constants, AST docstrings, `tokenize` COMMENT tokens) plus a raw byte count -- **15 occurrences across 8 files, 15 of 15 accounted for as comment or docstring, ZERO printable carriers**; and, wider still, **no printable string anywhere in `src/` or `scripts/` names a `python -m` invocation at all**, which also forecloses runtime assembly; and every one of the seven `scripts/*.py` named in a printable string exists on disk. The only doc hit is `morning_runbook.md:94`, which DISCLOSES that the `-m` form needs `pip install -e .` -- correct and correctly worded. **THE FLIP RE-DERIVED** on a pristine `bd1be58` with only the two test files transplanted: **9 failed, 9 x AssertionError, ZERO AttributeError / ImportError / collection error** -- every probe on its OWN subject, the pre-fix carriers measured at backtest.py 1586+1612 and live_screener.py 2372 (REVIEW_15B's three) -- and **9 passed** in the 2073c29 clone. The flipped pin is strictly STRONGER (`carriers == {}` subsumes both assertions it replaced) and nothing was dropped. **B436's DRIFT PIN BROKEN THREE WAYS**, restoring after each: renaming `dry_run_readiness.MASTER_LAUNCHER` alone -> 4 red; `backtest.MASTER_LAUNCHER` alone -> 5 red; `live_screener.SCREENER_LAUNCHER` alone -> 3 red. It fires in both directions and the `is_file()` half catches a renamed launcher FILE too. **SCOPE PROVED TWICE**: at the AST, every top-level name in both modules compared across the span -- 3 constants added, `__all__` gains one entry, and the ONLY other deltas are two string literals becoming f-strings plus docstring provenance, no other function or constant differing by a single node; and at the RENDERED CHARACTER STREAM -- each message raised by the pre-fix AND post-fix code from identical inputs, `bd1be58`'s text with ONLY the command token swapped == `2073c29`'s text, **True for all three, not one other character moved**. **PURITY UNMOVED**: `bias`, `poc`, `signals`, `simulate`, `bias_engine`, `signal_engine` and `parity.py` the SAME git blobs; all six published artefacts byte-identical; fixtures frozen; CONTEXT.md, plan.md, CLAUDE.md, pyproject.toml, config.yaml and DESIGN.md untouched. `(unreviewed)` correct on both commits (a1764a4 carries it; 2073c29 touches zero src/ or tests/ files). No CONTEXT deviation, no test weakened, deleted or skipped, no secret, no AI attribution in any commit message, author or trailer, `.env` untracked and unread. **B436, B437, B438 all 3 APPROVED**, each on evidence. 6 findings, **all INFO, none blocking, none a defect in the fix**: F1 an incidental host path in a committed transcript names a tool product (precedented since b762d74; architect's call); F2 the master launcher's literal now exists twice and only the pin keeps them equal (the dependency direction forbids de-duplicating it); F3 the printed remedy is repo-root-relative and the string does not say so (C1's already-passed shape; the runbook states it at line 57); F4 the evidence script clones current HEAD rather than a pinned commit; F5 the src/-wide pin has no non-vacuity guard; F6 B438's limit closed by this review. docs/reviews/REVIEW_R1.md - no tag created or moved (the architect's)
# superseded 2026-08-18 by REVIEW_R1 above: chunk 15: reviewed-PASS (cleanup re-checked), then R1 CLOSED (unreviewed) -- a SCOPED re-check of the R1 fix is owed  # R1 FIX SESSION (build, fresh session), closing the one carried MEDIUM of REVIEW_15B PART 10. **Three printable, operator-facing strings still told the operator to run `python -m acumen.<x>`**, which answers `No module named 'acumen'` in a hand-typed shell: pyproject's `pythonpath = ["src"]` is PYTEST's path and not a subprocess's, which is how the form stayed invisible to a green suite (REVIEW_14 **B3** -> **B429** -> REVIEW_15 **C1**, which fixed the readiness gate and missed these three). **All three now name the working launcher**: `live_screener._require_day_master` -- **THE LIVE MORNING'S OWN 09:00 REFUSAL**, which named BOTH `-m acumen.run_screener --refresh` and `-m acumen.instrument_master` -- prints `scripts/run_screener.py` and `scripts/fetch_instrument_master.py`; `backtest.named_master` prints `scripts/fetch_instrument_master.py`; `backtest.CA_REFRESH_FENCED`, printed on EVERY preflight, prints `scripts/ca_report.py`. **The flags are UNCHANGED** -- each launcher forwards to the same `main`, and every printed flag is parsed through that launcher's own parser in a kept probe rather than assumed equivalent. Paths are module constants (`backtest.MASTER_LAUNCHER`, `backtest.CA_REPORT_LAUNCHER`, `live_screener.SCREENER_LAUNCHER`) in **B435**'s shape, pinned equal to `dry_run_readiness.MASTER_LAUNCHER` so the four sites cannot drift; the dead form survives only in provenance comments and docstrings. **VERIFIED AS THE OPERATOR, NOT AS PYTEST**: in a bare clone with `PYTHONPATH` unset, `import acumen` FAILS and all three DEAD forms answer `No module named 'acumen'`, while all three NEW commands run with no such line -- `scripts/fetch_instrument_master.py` exiting 1 on an empty scratch cache is REVIEW_15B **R4**'s disclosed conditional, and it printed *'Nothing was fetched and nothing was written.'* (`docs/evidence/r1_fix_bare_clone.md`, generated by the committed `r1_fix_bare_clone.sh`). 8 KEPT PROBES in `tests/test_review15b_fix.py`, one per string plus its drive, each extracting the command FROM the string; REVIEW_15B's MEASURED pin FLIPPED on its own instruction to `test_FLIPPED_R15B_R1_the_dead_form_is_GONE_from_every_printable_string_in_src`. **All 9 FAIL on `bd1be58`, each on its OWN subject** (the dead-form assertion fires first, not a missing-constant AttributeError) and all 9 pass at HEAD -- `docs/evidence/r1_fix_probe_flip.md`. Suite: **2,621 passed / 0 failed / 0 skipped** in the operator's tree (871.53s) = REVIEW_15B's 2,613 plus this session's 8, to the test. **NOT ONE BYTE OF EITHER STORE MOVED**: metadata digest `dbea5660b7734f6a71edd5e99eac0159e53174ec431a1a7fb17c2bad5bf61423`, 22,186 files, 4,109,782,853 bytes, newest 2026-08-12T23:19:20 -- read before the first edit and after the last command, identical to each other and to REVIEW_15B PART 0 (`docs/evidence/r1_fix_store_bracket.md`). No mutating CLI, no `--allow-network`, no network. No engine module, no fixture, no published artefact, no CONTEXT and no plan byte touched; no AI attribution; no secret. **A FRESH ADVERSARIAL REVIEW IS OWED -- nothing here is reviewed and nothing was re-tagged.**
# superseded 2026-08-18 by the R1 FIX session above: chunk 15: reviewed-PASS (cleanup re-checked) -- **PROJECT COMPLETE -- every chunk sealed; the tool is done**  # SCOPED RE-CHECK (both personas, fresh session) over `ee412a6..29cd748` -- 3 commits, linear, 0 merges, closing REVIEW_15's nine findings. **VERDICT PASS.** **2,598 / 0 / 1 skipped from a clean clone** (1187.36s, exactly the build's claim; the skip is the `.env` probe whose own guard skips it when `.env` is absent) and **2,613 / 0 / 0 in the operator's tree** = the build's 2,599 plus this review's 14 kept probes. **THE STORE IS BYTE-UNMOVED across the whole re-check** -- 22,186 files, 4,109,782,853 bytes, content `d97ba419...`, metadata `dbea5660...`, newest_mtime 2026-08-12T23:19:20, bracketed open and close around two full suite runs, three parity re-derivations and every probe. The reading was **RE-DERIVED by an independently written implementation** (`docs/evidence/review15b_store_fingerprint.py`): the committed fingerprint script could not run on this machine (`MemoryError` at its per-read 1 MB allocation, the box at 95-98% memory load) and was correctly left untouched as frozen evidence, so a second program reading the same disk now agrees to the digit. **Q2 ATTACKED FOR OVER-FIRING**, which is what a fail-closed change actually risks: all three fallback outcomes driven (manifest wins where readable; the NAME dates a corrupt, an empty AND a trade_date-less manifest, each blocking a Monday; nothing-can-date is refused and named in `figures['unknown_day']`); a legitimate week NEVER reaches the new code (`unreadable == ()` across five mornings); a NON-prior broken day is named and does not block; and -- the property no build test covers -- **a DATED blocker stops exactly ONE morning and ages out by itself** (same artefact untouched on disk: Monday `ok=False`, Tuesday `ok=True`), so no deletion and no escalation is needed for the week to continue. **B431 proved by AST over BOTH loops** of `verify_prior_recordings` (the blocking loop calls `recording_day_or_name` and neither `verify_prior_recording` nor `write_verification`; the pending loop calls `recording_day` and never the fallback), by a single-call-site sweep of `src/`, and on disk -- **no `verification.json` is ever written for a day a directory name chose**. **Q4**: orphan surfaced + queued + reasoned, cannot over-block a dated entry, runbook section 8 states the remedy (*'Tell the architect; do not delete anything'*); `<data_root>/live` does not exist -- read from the machine -- so nothing existing can be refused. **C1 RUN, not read**: the command extracted from the refusal string and executed as a real subprocess in a bare clone with `PYTHONPATH` stripped -- exit 0, *'Nothing was fetched and nothing was written.'*, no *'No module named'*, while `import acumen` fails in that same environment; AST-confirmed no printable string in the gate names the dead form. **Q3** driven at the mixed 1-sent/1-refused/1-failed boundary the build's single-counter matrix cannot distinguish (line correctly ABSENT), and the kind-filter spurious-fire vector closed by pinning the sink's default kinds to `ls.ALERT_KINDS`. **Q1/C2/C5 re-confirmed FROM SOURCE**: the handover's three parsed out of `trader_pack.md` in the pack's order with the delivered path under the third and the repaired test opening the pack; `NOT_READY_LINE` quoted verbatim from the constant; Q-30's heading matching Q-28's and Q-29's. **All 4 flipped pins + the repaired handover test FAIL on `ee412a6`, each on its own subject** -- 24 failed / 3 passed there (the 3 are this review's deliberate invariant probes), all passing at HEAD. **PURITY UNMOVED**: `bias`, `poc`, `signals`, `simulate`, `bias_engine`, `signal_engine`, `backtest.py`, `parity.py` the SAME git blobs across the span; all six published artefacts byte-identical; fixtures frozen; CONTEXT.md and plan.md untouched. Parity re-derived to scratch: **15 days / 14 judged / 14 matched / 0 mismatched / 1 disclosed**, ITC 2026-06-10 and ADANIENSOL 2026-05-08 spot-checked field for field and identical to the committed artefact. **B431-B435 all 5 APPROVED.** 6 findings, none blocking and **none created by this span**: 1 MEDIUM code (**R1** -- C1's lesson has not reached three more printable operator-facing strings: `live_screener.py:2373-2375`, the LIVE MORNING's own refusal, plus `backtest.py:1589` and `CA_REFRESH_FENCED`, all naming `-m acumen.*` forms verified failing in a bare clone) and 5 INFO (**R2** two unstated behaviour changes from Q4's surfacing; **R3** an undateable entry blocks every morning and clearing it is an escalation; **R4** the launcher's exit code is conditional on a seeded cache; **R5** the committed fingerprint script cannot run under memory pressure; **R6** the committed parity SAMPLE no longer reproduces on the DISCLOSED day ADANIPORTS 2026-02-06 -- not this span's doing, proved by `ee412a6` and HEAD producing byte-identical day payloads and by none of the three touched modules being reachable from the parity path). No CONTEXT deviation, no look-ahead, no test weakened or deleted, no fixture byte moved, no secret, no AI attribution. docs/reviews/REVIEW_15B.md - tags chunk15-final, acumen-v1.0
# superseded 2026-08-16 by the chunk-15 RE-CHECK above: chunk 15: reviewed-PASS, then CLEANED (the nine REVIEW_15 findings, unreviewed) -- a SCOPED re-check is owed; on its PASS the tool is DONE  # FINAL CLEANUP SESSION (16-Aug-2026) over `ee412a6..HEAD`, 3 commits, linear, 0 merges. REVIEW_15 passed the chunk and recorded nine findings, none blocking and not one touching a decision, a price, a gate or a rupee. Seven are FIXED, each with its test flipped or driven; two (C3, C4) are matters of record. **Q1** -- `docs/handover.md` section 4 claimed the validation pack "put three paths in front of you" and then listed three the pack never offered: it DROPPED *Change it* (an option the trader still has and which the document nowhere told him about) and INVENTED *Automation* (plan.md section 8's v2 backlog item, as its own next sentence said). It now carries the pack's three VERBATIM -- **Retire it / Change it / Take it live knowing the arithmetic** -- in the pack's order, with the delivered path named under the third and automation stated as the v2 item it is; and the defective test now OPENS `docs/validation/trader_pack.md` and reads the pack's own headings out of it (the SIXTH pinned-at-the-wrong-place occurrence, now pinned at the source). **Q2** -- B420's "stops the morning only when it is the prior trading day's" was true of the intent and false of the code for the one entry it mattered on: the test asked `recording_day(...) == prior` and that reads the MANIFEST, so the entry whose manifest could not be read answered `None` and did NOT block. The day now comes from the manifest, or failing that from the directory NAME (`recording_day_or_name`, for this blocking test alone -- no day is ever verified or re-judged under a name), and an entry neither can date is treated as possibly-prior and REFUSED. **Q4** -- a recording directory with no `manifest.json` at all is surfaced by the queue instead of skipped silently (absence from that list reads as "passed", M15's shape). **C1** -- `check_master`'s refusal named `python -m acumen.instrument_master`, which answers `No module named 'acumen'` on this machine at 08:00; it now names `scripts/fetch_instrument_master.py`, and a test takes the command OUT OF THE REFUSAL and RUNS it with `PYTHONPATH` stripped. **Q3** -- L1's clarifying line fires when the counters' TOTAL falls short of the day's alert count, so a PARTIAL resume carries it too. **C2** -- the runbook quotes `NOT_READY_LINE` verbatim. **C5** -- Q-30's heading reads `CLOSED 08-Aug-2026`, matching Q-28 and Q-29 (ruling text unmoved; a dry-run session would have STOPPED on a question answered eight days earlier). **2,598 / 0 / 1 skipped from a clean clone** (508.08s) = REVIEW_15's own 2,593 at `ee412a6` plus this session's 5 new tests, exactly; **2,599 / 0 / 0 in the operator's tree**; three names gone and all three are FLIPS of the review's own pins. **The stores are byte-unmoved** -- `content d97ba419... metadata dbea5660... files 22186`, bracketed open and close (`docs/evidence/chunk15_cleanup_store_bracket.md`); no engine module, no parity artefact, no published report and no fixture touched. Decisions **B431-B435**.
# superseded 2026-08-16 by the chunk-15 CLEANUP session above: chunk 15: reviewed-PASS -- THE TOOL IS COMPLETE  # QC REVIEW (both personas, fresh session) over `d4d90fd..feba31d` -- 8 commits, linear, 0 merges. **2,572 / 0 / 1 skipped from a clean clone** (506.37s, exactly the build's claim; the skip is the `.env` probe a clone must skip) and **2,594 / 0 / 0 in the operator's tree** = the build's 2,573 plus this review's 21 kept probes. **VERDICT PASS**, 9 findings, none blocking. **THE STORES ARE BYTE-UNMOVED across the whole review** -- 22,186 files, 4,109,782,853 bytes, content `d97ba419...`, metadata `dbea5660...`, newest_mtime 2026-08-12T23:19:20, every digit as at REVIEW_14B PART 6, bracketed before the first store-touching command and after the last (two full suite runs, two flip runs, a 15-day parity re-run, 21 probes, a full readiness assessment with a real pytest subprocess inside it). **R1 RE-FLIPPED BY THIS SESSION**: `chunk15_flip.py` run unchanged on `cf53cfd` and on HEAD, both outputs byte-identical to the committed before/after files -- 3 of 7 malformed shapes ESCAPE `run_day` before / all 7 CONTAINED after, and the locked candle file goes from 0 sweeps ("the whole morning") to 18 sweeps losing ITC's 11:30 alone. **B416 PROVED, not accepted**: `duplicate_bars` keys on `(sweep, stamp)`, so the retry the guard refuses would duplicate the WHOLE 136-bar reply under one label -- a day gate 2 must refuse -- while the next boundary's label duplicates none; and no-retry loses no data (stamp set and closing phase identical to a clean control). **B417 DRIVEN** at the statement now preceding the accumulation: the failed boundary contributes ZERO twins where the pre-fix ordering contributes its share; also pinned by AST as the final statement of the guarded block. An EIGHTH malformed shape and a reviewer-defined exception from `record_fetch` (a different statement and type from the build's probe) are both CONTAINED, and B418's worst case -- both best-effort recordings refused -- still completes 18 sweeps. **M4**: an ordinary day's alerts ALL still deliver with zero withheld events; the starved day's 15:30 TRIGGER + EXIT reach 0 transports and are recorded as `post-session-alert-withheld`; no 15:30 row exists in `alerts.jsonl`, which is why the end-of-day summary (built from `recorded_alerts`) structurally cannot see one -- and that summary itself still goes. **M13/M14** verified on two stacked days, the newer holding two recordings: all three pending oldest-first and all three NAMED; today's own, a future day, a replay and a judged day all correctly excluded; blocking follows the prior TRADING day across a weekend. **THE GATE**: all seven checks driven to both a pass and a refusal through `assess`, one failure never hiding the next; a missing or re-ordered check can never read READY; an unrunnable suite is a refusal four ways; the test message is opt-in AND required; and the credential hunt run through the REAL `post_message` with `requests` raising an exception carrying the whole request URL leaked NOTHING -- not the token, the chat id, the token's secret half or even `api.telegram.org` -- across `render()`, every `figures`, every `detail`, the `repr` and every refusal. Its no-write claim was FINGERPRINTED over both roots around a full assess including a real pytest subprocess: identical. **THE RUNBOOK** read as the operator: all 6 commands parse, and in a bare clone `import acumen` and `python -m acumen.run_screener` both fail while `python scripts/run_screener.py` exits 0 -- B429 is real; every quoted sentence pinned to its CONSTANT; the "refresh FENCED = correct" line present; exactly one command rings the phone with an alert and exactly one other sends a labelled test message. **THE HANDOVER** read as the trader: no-order sentence at line 19 of 170, the 0.52%-2.68% bracket unflattered, success defined as alerts-match not PnL, and zero commands, flags or module paths anywhere in it. **PARITY + PURITY UNCHANGED**: 15 days / 14 judged / **14 matched** / 0 mismatched / 1 disclosed re-derived independently, report byte-identical bar its run stamp, ITC 2026-06-10 + ADANIENSOL 2026-05-08 (entry 137790 / SL 138160 / TP 136680 / qty 270 identical both halves at every boundary) + BOSCHLTD 2021-05-20 spot-checked at 0 mismatches; `bias`, `poc`, `signals`, `simulate`, `bias_engine`, `signal_engine`, **`backtest.py` and `parity.py` the SAME git blobs** at `d4d90fd` and HEAD; all six published artefacts byte-identical; fixtures frozen; CONTEXT.md and plan.md untouched. Census: 1,792 -> 1,834 tests in 96 -> 99 files, 3 names gone and all three RENAMES, no file losing a test, the `test_FLIPPED_R1_*` probe failing on pre-fix on its OWN subject. **B416-B430 all 15 APPROVED.** 9 findings, none blocking: 2 MEDIUM quant (**Q1** `docs/handover.md` attributes three paths to the chunk-12 pack that are NOT the pack's three -- it drops *Change it* and invents *Automation*, which is plan.md's v2 backlog item, and the build's own test checks the handover against itself rather than the pack; **Q2** B420's stated "only the prior trading day's" clause does not cover a prior-day recording whose manifest cannot say which day it is), 1 MEDIUM code (**C1** the gate's master refusal prints `python -m acumen.instrument_master`, which answers `No module named 'acumen'` on this machine, while `scripts/fetch_instrument_master.py` exists and works), 2 LOW and 4 INFO. No CONTEXT deviation, no look-ahead, no test weakened, no secrets, no AI attribution, `main == origin/main`. docs/reviews/REVIEW_15.md - tag chunk15-pass
# superseded 2026-08-15 by the chunk-15 BUILD session: chunk 15: todo  # The 07-Aug-2026 architect's rulings (operator-confirmed, recorded VERBATIM in QUESTIONS.md by the chunk-13 build session) LIFT the 06-Aug hold: *"the operator rules the complete tool is the deliverable and the trader decides its use; build order re-sequenced 13 -> 14 -> 11 -> 15"*. The card is UNCHANGED -- the ruling rescopes nothing -- and every DEPENDENCY rule in plan.md section 3 still binds. plan.md section 2's one blocking deadline is UNTOUCHED and still binds chunk 15: the chunk-12 trader gate closes before it. That gate is `pending` -- the pack is CLEARED and awaiting the trader's confirmation. **QUESTIONS.md Q-28 also stands between chunk 15 and a real dry-run week**: a dry-run week is five LIVE sessions, and the live mode refuses to start until Q-28 is ruled.
# superseded 2026-08-08 (the hold is LIFTED): same amendment (chunk 13+). plan.md §2's deadline "chunk-12 gate before chunk 15" is untouched and is trivially satisfied while 13-15 are held.
